# ESP-IDF sdkconfig Options

Target: `esp32s3` — IDF `idf`

Total symbols: 3211

---

## CONFIG_ADC_CALI_EFUSE_TP_ENABLE

**Use Two Point Values**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration > ADC Calibration Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:22`

Some ESP32s have Two Point calibration values burned into eFuse BLOCK3.
This option will allow the ADC calibration component to characterize the
ADC-Voltage curve using Two Point values if they are available.

---

## CONFIG_ADC_CALI_EFUSE_VREF_ENABLE

**Use eFuse Vref**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration > ADC Calibration Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:30`

Some ESP32s have Vref burned into eFuse BLOCK0. This option will allow
the ADC calibration component to characterize the ADC-Voltage curve using
eFuse Vref if it is available.

---

## CONFIG_ADC_CALI_LUT_ENABLE

**Use Lookup Tables**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration > ADC Calibration Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:38`

This option will allow the ADC calibration component to use Lookup Tables
to correct for non-linear behavior in 11db attenuation. Other attenuations
do not exhibit non-linear behavior hence will not be affected by this option.

---

## CONFIG_ADC_CALI_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration > Legacy ADC Calibration Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:59`

Whether to suppress the deprecation warnings when using legacy adc calibration
driver (esp_adc_cal.h).
If you want to continue using the legacy driver, and don't want to see related
deprecation warnings, you can enable this option.

---

## CONFIG_ADC_CAL_EFUSE_TP_ENABLE

**Use Two Point Values**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration > Legacy ADC Calibration Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:32`

Some ESP32s have Two Point calibration values burned into eFuse BLOCK3.
This option will allow the ADC calibration component to characterize the
ADC-Voltage curve using Two Point values if they are available.

---

## CONFIG_ADC_CAL_EFUSE_VREF_ENABLE

**Use eFuse Vref**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration > Legacy ADC Calibration Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:41`

Some ESP32s have Vref burned into eFuse BLOCK0. This option will allow
the ADC calibration component to characterize the ADC-Voltage curve using
eFuse Vref if it is available.

---

## CONFIG_ADC_CAL_LUT_ENABLE

**Use Lookup Tables**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration > Legacy ADC Calibration Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:50`

This option will allow the ADC calibration component to use Lookup Tables
to correct for non-linear behavior in 11db attenuation. Other attenuations
do not exhibit non-linear behavior hence will not be affected by this option.

---

## CONFIG_ADC_CONTINUOUS_FORCE_USE_ADC2_ON_C3_S3

**Force use ADC2 continuous mode on ESP32S3 or ESP32C3**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:59`

On ESP32C3 and ESP32S3, ADC2 Digital Controller is not stable. Therefore,
ADC2 continuous mode is not suggested on ESP32S3 and ESP32C3

If you stick to this, you can enable this option to force use ADC2 under above conditions.
For more details, you can search for errata on espressif website.

---

## CONFIG_ADC_CONTINUOUS_ISR_IRAM_SAFE

**ADC continuous mode driver ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:10`

Ensure the ADC continuous mode ISR is IRAM-Safe. When enabled, the ISR handler
will be available when the cache is disabled.

---

## CONFIG_ADC_DISABLE_DAC

**Disable DAC when ADC2 is used on GPIO 25 and 26**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:7`

If this is set, the ADC2 driver will disable the output of the DAC corresponding to the specified
channel. This is the default value.

For testing, disable this option so that we can measure the output of DAC by internal ADC.

---

## CONFIG_ADC_DISABLE_DAC_OUTPUT

**Disable DAC when ADC2 is in use**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:47`

By default, this is set. The ADC oneshot driver will disable the output of the
corresponding DAC channels:
ESP32:   IO25 and IO26
ESP32S2: IO17 and IO18

Disable this option so as to measure the output of DAC by internal ADC, for test usage.

---

## CONFIG_ADC_ENABLE_DEBUG_LOG

**Enable ADC debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:81`

whether to enable the debug log message for ADC driver.
Note that this option only controls the ADC driver log, will not affect other drivers.

note: This cannot be used in the ADC legacy driver.

---

## CONFIG_ADC_ONESHOT_CTRL_FUNC_IN_IRAM

**Place ISR version ADC oneshot mode read function into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:3`

Place ISR version ADC oneshot mode read function into IRAM.

---

## CONFIG_ADC_ONESHOT_FORCE_USE_ADC2_ON_C3

**Force use ADC2 oneshot mode on ESP32C3**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ADC and ADC Calibration
- **Kconfig:** `/opt/esp/idf/components/esp_adc/Kconfig:70`

On ESP32C3, ADC2 Digital Controller is not stable. Therefore,
ADC2 oneshot mode is not suggested on ESP32C3

If you stick to this, you can enable this option to force use ADC2 under above conditions.
For more details, you can search for errata on espressif website.

---

## CONFIG_ADC_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:25`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_ADC_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy ADC Driver Configuration
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:17`

Whether to suppress the deprecation warnings when using legacy adc driver (driver/adc.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_ANA_CMPR_CTRL_FUNC_IN_IRAM

**Place Analog Comparator control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Analog Comparator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_ana_cmpr/Kconfig:11`

Place Analog Comparator control functions (like ana_cmpr_set_internal_reference) into IRAM,
so that these functions can be IRAM-safe and able to be called in an IRAM interrupt context.
Enabling this option can improve driver performance as well.

---

## CONFIG_ANA_CMPR_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Analog Comparator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_ana_cmpr/Kconfig:36`

If enabled, the driver component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_ANA_CMPR_ISR_CACHE_SAFE

**Allow Analog Comparator ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Analog Comparator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_ana_cmpr/Kconfig:20`

Enable this option to allow the Analog Comparator Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the comparator functionality is still required to operate correctly.

---

## CONFIG_ANA_CMPR_ISR_HANDLER_IN_IRAM

**Place Analog Comparator ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Analog Comparator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_ana_cmpr/Kconfig:4`

Place Analog Comparator ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_ANA_CMPR_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the driver object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_APPTRACE_BUF_SIZE

**Size of the apptrace buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:187`

Size of the memory buffer for trace data in bytes.

---

## CONFIG_APPTRACE_DEST_JTAG

**JTAG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > Data Destination 1
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:9`

---

## CONFIG_APPTRACE_DEST_NONE

**None**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Application Level Tracing > Data Destination 1
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:15`

---

## CONFIG_APPTRACE_DEST_TRAX

- **Type:** `string`
- **Default:** `n`

Enables/disable TRAX tracing HW.

---

## CONFIG_APPTRACE_DEST_UART

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_APPTRACE_DEST_UART0

**UART0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > Data Destination 2
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:32`

---

## CONFIG_APPTRACE_DEST_UART1

**UART1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > Data Destination 2
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:39`

---

## CONFIG_APPTRACE_DEST_UART2

**UART2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > Data Destination 2
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:46`

---

## CONFIG_APPTRACE_DEST_UART_NONE

**None**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Application Level Tracing > Data Destination 2
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:59`

---

## CONFIG_APPTRACE_DEST_UART_NOUSB

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_APPTRACE_DEST_USB_CDC

**USB_CDC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > Data Destination 2
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:53`

---

## CONFIG_APPTRACE_ENABLE

- **Type:** `string`
- **Default:** `n`

Enables/disable application tracing module.

---

## CONFIG_APPTRACE_GCOV_DUMP_TASK_STACK_SIZE

**Gcov dump task stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing > GCOV to Host Enable
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:393`

Configures stack size of Gcov dump task

---

## CONFIG_APPTRACE_GCOV_ENABLE

**GCOV to Host Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:385`

Enables support for GCOV data transfer to host.

---

## CONFIG_APPTRACE_LOCK_ENABLE

- **Type:** `string`
- **Default:** `y`

Enables/disable application tracing module internal sync lock.

---

## CONFIG_APPTRACE_MEMBUFS_APPTRACE_PROTO_ENABLE

- **Type:** `string`
- **Default:** `n`

Enables/disable swapping memory buffers tracing protocol.

---

## CONFIG_APPTRACE_ONPANIC_HOST_FLUSH_TMO

**Timeout for flushing last trace data to host on panic**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:169`

Timeout for flushing last trace data to host in case of panic. In ms.
Use -1 to disable timeout and wait forever.

---

## CONFIG_APPTRACE_PENDING_DATA_SIZE_MAX

**Size of the pending data buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:194`

Size of the buffer for events in bytes. It is useful for buffering events from
the time critical code (scheduler, ISRs etc). If this parameter is 0 then
events will be discarded when main HW buffer is full.

---

## CONFIG_APPTRACE_POSTMORTEM_FLUSH_THRESH

**Threshold for flushing last trace data to host on panic**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:178`

Threshold for flushing last trace data to host on panic in post-mortem mode.
This is minimal amount of data needed to perform flush. In bytes.

---

## CONFIG_APPTRACE_SV_BUF_WAIT_TMO

**Trace buffer wait timeout**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:284`

Configures timeout (in us) to wait for free space in trace buffer.
Set to -1 to wait forever and avoid lost events.

---

## CONFIG_APPTRACE_SV_DEST_CPU_0

**CPU0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > CPU to trace
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:240`

Send SEGGER SystemView events for Pro CPU.

---

## CONFIG_APPTRACE_SV_DEST_CPU_1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > CPU to trace
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:245`

Send SEGGER SystemView events for App CPU.

---

## CONFIG_APPTRACE_SV_DEST_JTAG

**Data destination JTAG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > SystemView Tracing Enable > SystemView destination
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:219`

Send SEGGER SystemView events through JTAG interface.

---

## CONFIG_APPTRACE_SV_DEST_UART

**Data destination UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > SystemView Tracing Enable > SystemView destination
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:225`

Send SEGGER SystemView events through UART interface.

---

## CONFIG_APPTRACE_SV_ENABLE

**SystemView Tracing Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:205`

Enables support for SEGGER SystemView tracing functionality.

---

## CONFIG_APPTRACE_SV_EVT_IDLE_ENABLE

**System Idle Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:362`

Enables "System Idle" event.

---

## CONFIG_APPTRACE_SV_EVT_ISR_ENTER_ENABLE

**ISR Enter Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:299`

Enables "ISR Enter" event.

---

## CONFIG_APPTRACE_SV_EVT_ISR_EXIT_ENABLE

**ISR Exit Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:306`

Enables "ISR Exit" event.

---

## CONFIG_APPTRACE_SV_EVT_ISR_TO_SCHED_ENABLE

**ISR Exit to Scheduler Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:313`

Enables "ISR to Scheduler" event.

---

## CONFIG_APPTRACE_SV_EVT_OVERFLOW_ENABLE

**Trace Buffer Overflow Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:292`

Enables "Trace Buffer Overflow" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_CREATE_ENABLE

**Task Create Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:348`

Enables "Task Create" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_START_EXEC_ENABLE

**Task Start Execution Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:320`

Enables "Task Start Execution" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_START_READY_ENABLE

**Task Start Ready State Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:334`

Enables "Task Start Ready State" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_STOP_EXEC_ENABLE

**Task Stop Execution Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:327`

Enables "Task Stop Execution" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_STOP_READY_ENABLE

**Task Stop Ready State Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:341`

Enables "Task Stop Ready State" event.

---

## CONFIG_APPTRACE_SV_EVT_TASK_TERMINATE_ENABLE

**Task Terminate Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:355`

Enables "Task Terminate" event.

---

## CONFIG_APPTRACE_SV_EVT_TIMER_ENTER_ENABLE

**Timer Enter Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:369`

Enables "Timer Enter" event.

---

## CONFIG_APPTRACE_SV_EVT_TIMER_EXIT_ENABLE

**Timer Exit Event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:376`

Enables "Timer Exit" event.

---

## CONFIG_APPTRACE_SV_MAX_TASKS

**Maximum supported tasks**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:276`

Configures maximum supported tasks in sysview debug

---

## CONFIG_APPTRACE_SV_TS_SOURCE_CCOUNT

**CPU cycle counter (CCOUNT)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > Timer to use as timestamp source
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:263`

---

## CONFIG_APPTRACE_SV_TS_SOURCE_ESP_TIMER

**esp_timer high resolution timer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > Timer to use as timestamp source
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:271`

---

## CONFIG_APPTRACE_SV_TS_SOURCE_GPTIMER

**General Purpose Timer (Timer Group)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Application Level Tracing > FreeRTOS SystemView Tracing > Timer to use as timestamp source
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:267`

---

## CONFIG_APPTRACE_UART_BAUDRATE

**UART baud rate**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:83`

This baud rate is used for UART.

The app's maximum baud rate depends on the UART clock source. If Power Management is disabled,
the UART clock source is the APB clock and all baud rates in the available range will be sufficiently
accurate. If Power Management is enabled, REF_TICK clock source is used so the baud rate is divided
from 1MHz. Baud rates above 1Mbps are not possible and values between 500Kbps and 1Mbps may not be
accurate.

---

## CONFIG_APPTRACE_UART_RX_BUFF_SIZE

**UART RX ring buffer size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:99`

Size of the UART input ring buffer.
This size related to the baudrate, system tick frequency and amount of data to transfer.
The data placed to this buffer before sent out to the interface.

---

## CONFIG_APPTRACE_UART_RX_GPIO

**UART RX on GPIO<num>**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:73`

This GPIO is used for UART RX pin.

---

## CONFIG_APPTRACE_UART_TASK_PRIO

**UART Task Priority**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:129`

UART task priority. In case of high events rate,
this parameter could be changed up to (configMAX_PRIORITIES-1).

---

## CONFIG_APPTRACE_UART_TX_BUFF_SIZE

**UART TX ring buffer size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:110`

Size of the UART output ring buffer.
This size related to the baudrate, system tick frequency and amount of data to transfer.

---

## CONFIG_APPTRACE_UART_TX_GPIO

**UART TX on GPIO<num>**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:63`

This GPIO is used for UART TX pin.

---

## CONFIG_APPTRACE_UART_TX_MSG_SIZE

**UART TX message size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Application Level Tracing
- **Kconfig:** `/opt/esp/idf/components/app_trace/Kconfig:120`

Maximum size of the single message to transfer.

---

## CONFIG_APP_BUILD_BOOTLOADER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_APP_BUILD_GENERATE_BINARIES

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_APP_BUILD_TYPE_APP_2NDBOOT

**Default (binary application + 2nd stage bootloader)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Build type > Application build type
- **Kconfig:** `/opt/esp/idf/Kconfig:242`

---

## CONFIG_APP_BUILD_TYPE_PURE_RAM_APP

**Build app without SPI_FLASH/PSRAM support (saves ram)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type
- **Kconfig:** `/opt/esp/idf/Kconfig:264`

If this option is enabled, external memory and related peripherals, such as Cache, MMU,
Flash and PSRAM, won't be initialized. Corresponding drivers won't be introduced either.
Components that depend on the spi_flash component will also be unavailable, such as
app_update, etc. When this option is enabled, about 26KB of RAM space can be saved.

---

## CONFIG_APP_BUILD_TYPE_RAM

**Build app runs entirely in RAM (EXPERIMENTAL)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type > Application build type
- **Kconfig:** `/opt/esp/idf/Kconfig:250`

---

## CONFIG_APP_BUILD_USE_FLASH_SECTIONS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_APP_COMPATIBLE_PRE_V2_1_BOOTLOADERS

**App compatible with bootloaders before ESP-IDF v2.1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type
- **Kconfig:** `/opt/esp/idf/Kconfig:292`

Bootloaders before ESP-IDF v2.1 did less initialisation of the
system clock. This setting needs to be enabled to build an app
which can be booted by these older bootloaders.

If this setting is enabled, the app can be booted by any bootloader
from IDF v1.0 up to the current version.

If this setting is disabled, the app can only be booted by bootloaders
from IDF v2.1 or newer.

Enabling this setting adds approximately 1KB to the app's IRAM usage.

---

## CONFIG_APP_COMPATIBLE_PRE_V3_1_BOOTLOADERS

**App compatible with bootloader and partition table before ESP-IDF v3.1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type
- **Kconfig:** `/opt/esp/idf/Kconfig:310`

Partition tables before ESP-IDF V3.1 do not contain an MD5 checksum
field, and the bootloader before ESP-IDF v3.1 cannot read a partition
table that contains an MD5 checksum field.

Enable this option only if your app needs to boot on a bootloader and/or
partition table that was generated from a version *before* ESP-IDF v3.1.

If this option and Flash Encryption are enabled at the same time, and any
data partitions in the partition table are marked Encrypted, then the
partition encrypted flag should be manually verified in the app before accessing
the partition (see CVE-2021-27926).

---

## CONFIG_APP_COMPILE_TIME_DATE

**Use time/date stamp for app**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Application manager
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:3`

If set, then the app will be built with the current time/date stamp. It is stored in the app description
structure. If not set, time/date stamp will be excluded from app image. This can be useful for getting the
same binary image files made from the same source, but at different times.

---

## CONFIG_APP_EXCLUDE_PROJECT_NAME_VAR

**Exclude PROJECT_NAME from firmware image**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Application manager
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:19`

The PROJECT_NAME variable from the build system will not affect the firmware image.
This value will not be contained in the esp_app_desc structure.

---

## CONFIG_APP_EXCLUDE_PROJECT_VER_VAR

**Exclude PROJECT_VER from firmware image**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Application manager
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:12`

The PROJECT_VER variable from the build system will not affect the firmware image.
This value will not be contained in the esp_app_desc structure.

---

## CONFIG_APP_INIT_CLK

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_APP_NO_BLOBS

**No Binary Blobs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type
- **Kconfig:** `/opt/esp/idf/Kconfig:285`

If enabled, this disables the linking of binary libraries in the application build. Note
that after enabling this Wi-Fi/Bluetooth will not work.

---

## CONFIG_APP_PROJECT_VER

**Project version**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Application manager > Get the project version from Kconfig
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:33`

Project version

---

## CONFIG_APP_PROJECT_VER_FROM_CONFIG

**Get the project version from Kconfig**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Application manager
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:26`

If this is enabled, then config item APP_PROJECT_VER will be used for the variable PROJECT_VER.
Other ways to set PROJECT_VER will be ignored.

---

## CONFIG_APP_REPRODUCIBLE_BUILD

**Enable reproducible build**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Build type
- **Kconfig:** `/opt/esp/idf/Kconfig:277`

If enabled, all date, time, and path information would be eliminated. A .gdbinit file would be create
automatically. (or will be append if you have one already)

---

## CONFIG_APP_RETRIEVE_LEN_ELF_SHA

**The length of APP ELF SHA is stored in RAM(chars)**

- **Type:** `unknown`
- **Default:** `9`
- **Menu:** Application manager
- **Kconfig:** `/opt/esp/idf/components/esp_app_format/Kconfig.projbuild:40`

At startup, the app will read the embedded APP ELF SHA-256 hash value from flash
and convert it into a string and store it in a RAM buffer.
This ensures the panic handler and core dump will be able to print this string
even when cache is disabled.
The size of the buffer is APP_RETRIEVE_LEN_ELF_SHA plus the null terminator.
Changing this value will change the size of this buffer, in bytes.

---

## CONFIG_AUDIO_BOARD_CUSTOM

**Custom audio board**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:27`

---

## CONFIG_BITSCRAMBLER_CTRL_FUNC_IN_IRAM

**Place BitScrambler control functions in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > BitScrambler Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_bitscrambler/Kconfig:3`

Place BitScrambler control functions into IRAM for better performance and fewer cache misses.

---

## CONFIG_BITSCRAMBLER_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the BitScrambler object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_BLE_MESH

**ESP BLE Mesh Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:114`

This option enables ESP BLE Mesh support. The specific features that are
available may depend on other features that have been enabled in the
stack, such as Bluetooth Support, Bluedroid Support & GATT support.

---

## CONFIG_BLE_MESH_ACTIVE_SCAN

**Support Active Scan in BLE Mesh**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:108`

Enable this option to allow using BLE Active Scan for BLE Mesh.

---

## CONFIG_BLE_MESH_ADV_BUF_COUNT

**Number of advertising buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:739`

Number of advertising buffers available. The transport layer reserves
ADV_BUF_COUNT - 3 buffers for outgoing segments. The maximum outgoing
SDU size is 12 times this value (out of which 4 or 8 bytes are used
for the Transport Layer MIC). For example, 5 segments means the maximum
SDU size is 60 bytes, which leaves 56 bytes for application layer data
using a 4-byte MIC, or 52 bytes using an 8-byte MIC.

---

## CONFIG_BLE_MESH_ADV_INST_ID

**Extended adv instance for Mesh normal packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:36`

Extended ADV instance used by Mesh normal advertising packets.

---

## CONFIG_BLE_MESH_AGG_CLI

**Opcodes Aggregator Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1264`

Enable support for Opcodes Aggregator Client model.

---

## CONFIG_BLE_MESH_AGG_SRV

**Opcodes Aggregator Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1269`

Enable support for Opcodes Aggregator Server model.

---

## CONFIG_BLE_MESH_APP_KEY_COUNT

**Maximum number of application keys per network**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:665`

This option specifies how many application keys the device can store per network.
Indeed, this value decides the number of the application keys which can be owned by a node.

---

## CONFIG_BLE_MESH_BLE_ADV_BUF_COUNT

**Number of advertising buffers for BLE advertising packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh and BLE coexistence support > Support sending normal BLE advertising packets
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:214`

Number of advertising buffers for BLE packets available.

---

## CONFIG_BLE_MESH_BLE_ADV_INST_ID

**Extended adv instance for normal BLE packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh > Support using multiple adv instance for BLE Mesh > Use separate extended adv instance for BLE normal packets
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:85`

Extended ADV instance used by normal BLE advertising packets.

---

## CONFIG_BLE_MESH_BQB_TEST

**Enable BLE Mesh specific internal test**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1610`

This option is used to enable some internal functions for auto-pts test.

---

## CONFIG_BLE_MESH_BQB_TEST_LOG

- **Type:** `string`
- **Default:** `n`

This option is used to enable the log of auto-pts test.

---

## CONFIG_BLE_MESH_BRC_CLI

**Bridge Configuration Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1195`

Enable support for Bridge Configuration Client model.

---

## CONFIG_BLE_MESH_BRC_SRV

**Bridge Configuration Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1200`

Enable support for Bridge Configuration Server model.

---

## CONFIG_BLE_MESH_BRIDGE_CRPL

**Maximum capacity of bridge replay protection list**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Bridge Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1215`

This option specifies the maximum capacity of the bridge replay
protection list. The bridge replay protection list is used to
prevent a bridged subnet from replay attack, which will store the
source address and sequence number of the received bridge messages.

---

## CONFIG_BLE_MESH_CERT_BASED_PROV

**Support Certificate-based provisioning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh Provisioning support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:367`

Enable this option to support BLE Mesh Certificate-Based Provisioning.

---

## CONFIG_BLE_MESH_CFG_CLI

**Configuration Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1177`

Enable support for Configuration Client model.

---

## CONFIG_BLE_MESH_CLIENT_MSG_TIMEOUT

**Timeout(ms) for client message response**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1163`

Timeout value used by the node to get response of the acknowledged
message which is sent by the client model.
This value indicates the maximum time that a client model waits for
the response of the sent acknowledged messages. If a client model
uses 0 as the timeout value when sending acknowledged messages, then
the default value will be used which is four seconds.

---

## CONFIG_BLE_MESH_COMP_DATA_1

**Support Composition Data Page 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1284`

Composition Data Page 1 contains information about the relationships
among models.
Each model either can be a root model or can extend other models.

---

## CONFIG_BLE_MESH_COMP_DATA_128

**Support Composition Data Page 128**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1291`

Composition Data Page 128 is used to indicate the structure of
elements, features, and models of a node after the successful
execution of the Node Address Refresh procedure or the Node
Composition Refresh procedure, or after the execution of the
Node Removal procedure followed by the provisioning process.
Composition Data Page 128 shall be present if the node supports
the Remote Provisioning Server model; otherwise it is optional.

---

## CONFIG_BLE_MESH_CRPL

**Maximum capacity of the replay protection list**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:697`

This option specifies the maximum capacity of the replay protection list.
It is similar to Network message cache size, but has a different purpose.
The replay protection list is used to prevent a node from replay attack,
which will store the source address and sequence number of the received
mesh messages.
For Provisioner, the replay protection list size should not be smaller than
the maximum number of nodes whose information can be stored. And the element
number of each node should also be taken into consideration. For example, if
Provisioner can provision up to 20 nodes and each node contains two elements,
then the replay protection list size of Provisioner should be at least 40.

---

## CONFIG_BLE_MESH_DEBUG

**Enable BLE Mesh debug logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1650`

Enable debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_ACCESS

**Access layer debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1683`

Enable Access layer debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_ADV

**Advertising debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1693`

Enable advertising debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_BEACON

**Beacon debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1668`

Enable Beacon-related debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_CRYPTO

**Crypto debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1673`

Enable cryptographic debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_FRIEND

**Friend debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1703`

Enable Friend debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_LOW_POWER

**Low Power debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1698`

Enable Low Power debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_MODEL

**Foundation model debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1688`

Enable Foundation Models debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_NET

**Network layer debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1658`

Enable Network layer debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_PROV

**Provisioning debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1678`

Enable Provisioning debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_PROXY

**Proxy debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1708`

Enable Proxy protocol debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEBUG_TRANS

**Transport layer debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option > Enable BLE Mesh debug logs
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1663`

Enable Transport layer debug logs for the BLE Mesh functionality.

---

## CONFIG_BLE_MESH_DEINIT

**Support de-initialize BLE Mesh stack**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:196`

If enabled, users can use the function esp_ble_mesh_deinit() to de-initialize
the whole BLE Mesh stack.

---

## CONFIG_BLE_MESH_DF_CLI

**Directed Forwarding Configuration Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1391`

Enable support for Directed Forwarding Configuration Client model.

---

## CONFIG_BLE_MESH_DF_SRV

**Directed Forwarding Configuration Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1396`

Enable support for Directed Forwarding Configuration Server model.

---

## CONFIG_BLE_MESH_DISCARD_OLD_SEQ_AUTH

- **Type:** `string`
- **Default:** `n`

This option is used to decide whether discarding the old SeqAuth when
receiving a segmented message.

---

## CONFIG_BLE_MESH_EXPERIMENTAL

**Make BLE Mesh experimental features visible**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1718`

Make BLE Mesh Experimental features visible.
Experimental features list:
- CONFIG_BLE_MESH_NOT_RELAY_REPLAY_MSG
- CONFIG_BLE_MESH_USE_BLE_50
- CONFIG_BLE_MESH_PROXY_CLI_SRV_COEXIST

---

## CONFIG_BLE_MESH_FAST_PROV

**Enable BLE Mesh Fast Provisioning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:232`

Enable this option to allow BLE Mesh fast provisioning solution to be used.
When there are multiple unprovisioned devices around, fast provisioning can
greatly reduce the time consumption of the whole provisioning process.
When this option is enabled, and after an unprovisioned device is provisioned
into a node successfully, it can be changed to a temporary Provisioner.

---

## CONFIG_BLE_MESH_FREERTOS_STATIC_ALLOC

**Enable FreeRTOS static allocation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:160`

Enable this option to use FreeRTOS static allocation APIs for BLE Mesh,
which provides the ability to use different dynamic memory (i.e. SPIRAM
or IRAM) for FreeRTOS objects.
If this option is disabled, the FreeRTOS static allocation APIs will not
be used, and internal DRAM will be allocated for FreeRTOS objects.

---

## CONFIG_BLE_MESH_FREERTOS_STATIC_ALLOC_EXTERNAL

**External SPIRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Enable FreeRTOS static allocation > Memory allocation for FreeRTOS objects
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:177`

If enabled, BLE Mesh allocates dynamic memory from external SPIRAM for
FreeRTOS objects, i.e. mutex, queue, and task stack. External SPIRAM
can only be used for task stack when SPIRAM_ALLOW_STACK_EXTERNAL_MEMORY
is enabled. See the SPIRAM options for more details.

---

## CONFIG_BLE_MESH_FREERTOS_STATIC_ALLOC_IRAM_8BIT

**Internal IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Enable FreeRTOS static allocation > Memory allocation for FreeRTOS objects
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:186`

If enabled, BLE Mesh allocates dynamic memory from internal IRAM for
FreeRTOS objects, i.e. mutex, queue. Note: IRAM region cannot be used
as task stack.

---

## CONFIG_BLE_MESH_FRIEND

**Support for Friend feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1027`

Enable this option to be able to act as a Friend Node.

---

## CONFIG_BLE_MESH_FRIEND_LPN_COUNT

**Number of supported LPN nodes**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Friend feature
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1060`

Number of Low Power Nodes with which a Friend can have Friendship simultaneously.
A Friend node can have friendship with multiple Low Power nodes at the same time,
while a Low Power node can only establish friendship with only one Friend node at
the same time.

---

## CONFIG_BLE_MESH_FRIEND_QUEUE_SIZE

**Minimum number of buffers supported per Friend Queue**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Friend feature
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1041`

Minimum number of buffers available to be stored for each local Friend Queue.
This option decides the size of each buffer which can be used by a Friend node
to store messages for each Low Power node.

---

## CONFIG_BLE_MESH_FRIEND_RECV_WIN

**Friend Receive Window**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Friend feature
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1034`

Receive Window in milliseconds supported by the Friend node.

---

## CONFIG_BLE_MESH_FRIEND_SEG_RX

**Number of incomplete segment lists per LPN**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Friend feature
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1070`

Number of incomplete segment lists tracked for each Friends' LPN.
In other words, this determines from how many elements can segmented
messages destined for the Friend queue be received simultaneously.

---

## CONFIG_BLE_MESH_FRIEND_SUB_LIST_SIZE

**Friend Subscription List Size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Friend feature
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1050`

Size of the Subscription List that can be supported by a Friend node for a
Low Power node. And Low Power node can send Friend Subscription List Add or
Friend Subscription List Remove messages to the Friend node to add or remove
subscription addresses.

---

## CONFIG_BLE_MESH_GATT_PROXY_CLIENT

**BLE Mesh GATT Proxy Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:494`

This option enables support for Mesh GATT Proxy Client. The Proxy Client
can use the GATT bearer to send mesh messages to a node that supports the
advertising bearer.

---

## CONFIG_BLE_MESH_GATT_PROXY_SERVER

**BLE Mesh GATT Proxy Server**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:423`

This option enables support for Mesh GATT Proxy Service, i.e. the
ability to act as a proxy between a Mesh GATT Client and a Mesh network.
This option should be enabled if a node is going to be a Proxy Server.

---

## CONFIG_BLE_MESH_GENERIC_BATTERY_CLI

**Generic Battery Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1474`

Enable support for Generic Battery Client model.

---

## CONFIG_BLE_MESH_GENERIC_DEF_TRANS_TIME_CLI

**Generic Default Transition Time Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1459`

Enable support for Generic Default Transition Time Client model.

---

## CONFIG_BLE_MESH_GENERIC_LEVEL_CLI

**Generic Level Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1454`

Enable support for Generic Level Client model.

---

## CONFIG_BLE_MESH_GENERIC_LOCATION_CLI

**Generic Location Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1479`

Enable support for Generic Location Client model.

---

## CONFIG_BLE_MESH_GENERIC_ONOFF_CLI

**Generic OnOff Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1449`

Enable support for Generic OnOff Client model.

---

## CONFIG_BLE_MESH_GENERIC_POWER_LEVEL_CLI

**Generic Power Level Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1469`

Enable support for Generic Power Level Client model.

---

## CONFIG_BLE_MESH_GENERIC_POWER_ONOFF_CLI

**Generic Power OnOff Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1464`

Enable support for Generic Power OnOff Client model.

---

## CONFIG_BLE_MESH_GENERIC_PROPERTY_CLI

**Generic Property Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1484`

Enable support for Generic Property Client model.

---

## CONFIG_BLE_MESH_GENERIC_SERVER

**Generic server models**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1534`

Enable support for Generic server models.

---

## CONFIG_BLE_MESH_HCI_5_0

**Support sending 20ms non-connectable adv packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:3`

It is a temporary solution and needs further modifications.

---

## CONFIG_BLE_MESH_HEALTH_CLI

**Health Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1182`

Enable support for Health Client model.

---

## CONFIG_BLE_MESH_HEALTH_SRV

**Health Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1187`

Enable support for Health Server model.

---

## CONFIG_BLE_MESH_IVU_DIVIDER

**Divider for IV Update state refresh timer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:751`

When the IV Update state enters Normal operation or IV Update
in Progress, we need to keep track of how many hours has passed
in the state, since the specification requires us to remain in
the state at least for 96 hours (Update in Progress has an
additional upper limit of 144 hours).

In order to fulfill the above requirement, even if the node might
be powered off once in a while, we need to store persistently
how many hours the node has been in the state. This doesn't
necessarily need to happen every hour (thanks to the flexible
duration range). The exact cadence will depend a lot on the
ways that the node will be used and what kind of power source it
has.

Since there is no single optimal answer, this configuration
option allows specifying a divider, i.e. how many intervals
the 96 hour minimum gets split into. After each interval the
duration that the node has been in the current state gets
stored to flash. E.g. the default value of 4 means that the
state is saved every 24 hours (96 / 4).

---

## CONFIG_BLE_MESH_IVU_RECOVERY_IVI

**Recovery the IV index when the latest whole IV update procedure is missed**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:777`

According to Section 3.10.5 of Mesh Specification v1.0.1.
If a node in Normal Operation receives a Secure Network beacon with an IV index
equal to the last known IV index+1 and the IV Update Flag set to 0, the node may
update its IV without going to the IV Update in Progress state, or it may initiate
an IV Index Recovery procedure (Section 3.10.6), or it may ignore the Secure
Network beacon. The node makes the choice depending on the time since last IV
update and the likelihood that the node has missed the Secure Network beacons
with the IV update Flag.
When the above situation is encountered, this option can be used to decide whether
to perform the IV index recovery procedure.

---

## CONFIG_BLE_MESH_IV_UPDATE_TEST

**Test the IV Update Procedure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1586`

This option removes the 96 hour limit of the IV Update Procedure and
lets the state to be changed at any time.
If IV Update test mode is going to be used, this option should be enabled.

---

## CONFIG_BLE_MESH_LABEL_COUNT

**Maximum number of Label UUIDs used for Virtual Addresses**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:689`

This option specifies how many Label UUIDs can be stored.
Indeed, this value decides the number of the Virtual Addresses can be supported by a node.

---

## CONFIG_BLE_MESH_LCD_CLI

**Large Composition Data Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1327`

Enable support for Large Composition Data Client model.

---

## CONFIG_BLE_MESH_LCD_SRV

**Large Composition Data Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1332`

Enable support for Large Composition Data Server model.

---

## CONFIG_BLE_MESH_LIGHTING_SERVER

**Lighting server models**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1552`

Enable support for Lighting server models.

---

## CONFIG_BLE_MESH_LIGHT_CTL_CLI

**Light CTL Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1514`

Enable support for Light CTL Client model.

---

## CONFIG_BLE_MESH_LIGHT_HSL_CLI

**Light HSL Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1519`

Enable support for Light HSL Client model.

---

## CONFIG_BLE_MESH_LIGHT_LC_CLI

**Light LC Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1529`

Enable support for Light LC Client model.

---

## CONFIG_BLE_MESH_LIGHT_LIGHTNESS_CLI

**Light Lightness Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1509`

Enable support for Light Lightness Client model.

---

## CONFIG_BLE_MESH_LIGHT_XYL_CLI

**Light XYL Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1524`

Enable support for Light XYL Client model.

---

## CONFIG_BLE_MESH_LOW_POWER

**Support for Low Power features**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:888`

Enable this option to operate as a Low Power Node. If low power consumption
is required by a node, this option should be enabled. And once the node
enters the mesh network, it will try to find a Friend node and establish a
friendship.

---

## CONFIG_BLE_MESH_LPN_AUTO

**Automatically start looking for Friend nodes once provisioned**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:911`

Once provisioned, automatically enable LPN functionality and start looking
for Friend nodes. If this option is disabled LPN mode needs to be manually
enabled by calling bt_mesh_lpn_set(true).
When an unprovisioned device is provisioned successfully and becomes a node,
enabling this option will trigger the node starts to send Friend Request at
a certain period until it finds a proper Friend node.

---

## CONFIG_BLE_MESH_LPN_AUTO_TIMEOUT

**Time from last received message before going to LPN mode**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features > Automatically start looking for Friend nodes once provisioned
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:922`

Time in seconds from the last received message, that the node waits out
before starting to look for Friend nodes.

---

## CONFIG_BLE_MESH_LPN_ESTABLISHMENT

**Perform Friendship establishment using low power**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:899`

Perform the Friendship establishment using low power with the help of a
reduced scan duty cycle. The downside of this is that the node may miss
out on messages intended for it until it has successfully set up Friendship
with a Friend node.
When this option is enabled, the node will stop scanning for a period of
time after a Friend Request or Friend Poll is sent, so as to reduce more
power consumption.

---

## CONFIG_BLE_MESH_LPN_GROUPS

**Number of groups the LPN can subscribe to**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1011`

Maximum number of groups to which the LPN can subscribe.

---

## CONFIG_BLE_MESH_LPN_INIT_POLL_TIMEOUT

**The starting value of the PollTimeout timer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:991`

The initial value of the PollTimeout timer when Friendship is to be
established for the first time. After this, the timeout gradually
grows toward the actual PollTimeout, doubling in value for each iteration.
The value is in units of 100 milliseconds, so e.g. a value of 300 means
30 seconds.

---

## CONFIG_BLE_MESH_LPN_MIN_QUEUE_SIZE

**Minimum size of the acceptable friend queue (MinQueueSizeLog)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:959`

The MinQueueSizeLog field is defined as log_2(N), where N is the minimum
number of maximum size Lower Transport PDUs that the Friend node can store
in its Friend Queue. As an example, MinQueueSizeLog value 1 gives N = 2,
and value 7 gives N = 128.

---

## CONFIG_BLE_MESH_LPN_POLL_TIMEOUT

**The value of the PollTimeout timer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:978`

PollTimeout timer is used to measure time between two consecutive
requests sent by a Low Power node. If no requests are received
the Friend node before the PollTimeout timer expires, then the
friendship is considered terminated. The value is in units of 100
milliseconds, so e.g. a value of 300 means 30 seconds.
The smaller the value, the faster the Low Power node tries to get
messages from corresponding Friend node and vice versa.

---

## CONFIG_BLE_MESH_LPN_RECV_DELAY

**Receive delay requested by the local node**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:969`

The ReceiveDelay is the time between the Low Power node sending a
request and listening for a response. This delay allows the Friend
node time to prepare the response. The value is in units of milliseconds.

---

## CONFIG_BLE_MESH_LPN_RECV_WIN_FACTOR

**ReceiveWindowFactor, used in Friend Offer Delay calculation**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:949`

The contribution of the supported Receive Window used in Friend Offer
Delay calculations. 0 = 1, 1 = 1.5, 2 = 2, 3 = 2.5.
ReceiveWindowFactor, one of the parameters carried by Friend Request sent by
Low Power node, which is used to calculate the Friend Offer Delay.

---

## CONFIG_BLE_MESH_LPN_RETRY_TIMEOUT

**Retry timeout for Friend requests**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:931`

Time in seconds between Friend Requests, if a previous Friend Request did
not yield any acceptable Friend Offers.

---

## CONFIG_BLE_MESH_LPN_RSSI_FACTOR

**RSSIFactor, used in Friend Offer Delay calculation**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:939`

The contribution of the RSSI, measured by the Friend node, used in Friend
Offer Delay calculations. 0 = 1, 1 = 1.5, 2 = 2, 3 = 2.5.
RSSIFactor, one of the parameters carried by Friend Request sent by Low Power
node, which is used to calculate the Friend Offer Delay.

---

## CONFIG_BLE_MESH_LPN_SCAN_LATENCY

**Latency for enabling scanning**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1002`

Latency (in milliseconds) is the time it takes to enable scanning. In
practice, it means how much time in advance of the Receive Window, the
request to enable scanning is made.

---

## CONFIG_BLE_MESH_LPN_SUB_ALL_NODES_ADDR

**Automatically subscribe all nodes address**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for Low Power features
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1018`

Automatically subscribe all nodes address when friendship
established.

---

## CONFIG_BLE_MESH_MAX_BLOB_RECEIVERS

**Maximum number of simultaneous blob receivers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models > BLOB Transfer Client model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1567`

Maximum number of BLOB Transfer Server models that can participating
in the BLOB transfer with a BLOB Transfer Client model.

---

## CONFIG_BLE_MESH_MAX_BRIDGING_TABLE_ENTRY_COUNT

**Maximum number of Bridging Table entries**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Bridge Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1208`

Maximum number of Bridging Table entries that the Bridge Configuration Server can support.

---

## CONFIG_BLE_MESH_MAX_DEPS_NODES_PER_PATH

**Maximum number of dependent nodes per path**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Directed Forwarding Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1417`

Maximum size of dependent nodes list supported by each forward table entry.

---

## CONFIG_BLE_MESH_MAX_DISC_TABLE_ENTRY_COUNT

**Maximum number of discovery table entries in a given subnet**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Directed Forwarding Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1403`

Maximum number of Discovery Table entries supported by the node in a given subnet.

---

## CONFIG_BLE_MESH_MAX_FORWARD_TABLE_ENTRY_COUNT

**Maximum number of forward table entries in a given subnet**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Directed Forwarding Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1410`

Maximum number of Forward Table entries supported by the node in a given subnet.

---

## CONFIG_BLE_MESH_MAX_NVS_NAMESPACE

**Maximum number of NVS namespaces**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently > Support using multiple NVS namespaces by Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:647`

This option specifies the maximum NVS namespaces supported by Provisioner.

---

## CONFIG_BLE_MESH_MAX_PROV_NODES

**Maximum number of devices that can be provisioned by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:271`

This option specifies how many devices can be provisioned by a Provisioner.
This value indicates the maximum number of unprovisioned devices which can be
provisioned by a Provisioner. For instance, if the value is 6, it means the
Provisioner can provision up to 6 unprovisioned devices.
Theoretically a Provisioner without the limitation of its memory can provision
up to 32766 unprovisioned devices, here we limit the maximum number to 100
just to limit the memory used by a Provisioner. The bigger the value is, the
more memory it will cost by a Provisioner to store the information of nodes.

---

## CONFIG_BLE_MESH_MBT_CLI

**BLOB Transfer Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1558`

Enable support for BLOB Transfer Client model.

---

## CONFIG_BLE_MESH_MBT_SRV

**BLOB Transfer Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1577`

Enable support for BLOB Transfer Server model.

---

## CONFIG_BLE_MESH_MEM_ALLOC_MODE_DEFAULT

**Default alloc mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:144`

Enable this option to use the default memory allocation strategy when
external SPIRAM is enabled. See the SPIRAM options for more details.

---

## CONFIG_BLE_MESH_MEM_ALLOC_MODE_EXTERNAL

**External SPIRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:140`

---

## CONFIG_BLE_MESH_MEM_ALLOC_MODE_INTERNAL

**Internal DRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:137`

---

## CONFIG_BLE_MESH_MEM_ALLOC_MODE_IRAM_8BIT

**Internal IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:150`

Allows to use IRAM memory region as 8bit accessible region. Every
unaligned (8bit or 16bit) access will result in an exception and
incur penalty of certain clock cycles per unaligned read/write.

---

## CONFIG_BLE_MESH_MODELS_METADATA_0

**Support Models Metadata Page 0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1302`

The Models Metadata state contains metadata of a node’s models.
The Models Metadata state is composed of a number of pages of
information.
Models Metadata Page 0 shall be present if the node supports
the Large Composition Data Server model.

---

## CONFIG_BLE_MESH_MODELS_METADATA_128

**Support Models Metadata Page 128**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Support Models Metadata Page 0
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1311`

The Models Metadata state contains metadata of a node’s models.
The Models Metadata state is composed of a number of pages of
information.
Models Metadata Page 128 contains metadata for the node’s models
after the successful execution of the Node Address Refresh
procedure or the Node Composition Refresh procedure, or after
the execution of the Node Removal procedure followed by the
provisioning process.
Models Metadata Page 128 shall be present if the node supports
the Remote Provisioning Server model and the node supports the
Large Composition Data Server model.

---

## CONFIG_BLE_MESH_MODEL_GROUP_COUNT

**Maximum number of group address subscriptions per model**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:681`

This option specifies the maximum number of addresses to which each model can
be subscribed.

---

## CONFIG_BLE_MESH_MODEL_KEY_COUNT

**Maximum number of application keys per model**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:673`

This option specifies the maximum number of application keys to which each model
can be bound.

---

## CONFIG_BLE_MESH_MSG_CACHE_SIZE

**Network message cache size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:728`

Number of messages that are cached for the network. This helps prevent
unnecessary decryption operations and unnecessary relays. This option
is similar to Replay protection list, but has a different purpose.
A node is not required to cache the entire Network PDU and may cache
only part of it for tracking, such as values for SRC/SEQ or others.

---

## CONFIG_BLE_MESH_NET_BUF_POOL_USAGE

- **Type:** `string`
- **Default:** `n`

Enable BLE Mesh net buffer pool tracking. This option is used to introduce another
variable in the bottom layer to record the usage of advertising buffers of BLE Mesh
devices. Recommend to enable this option as default.

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1144`

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1138`

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_INFO

**INFO**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1142`

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1136`

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1146`

---

## CONFIG_BLE_MESH_NET_BUF_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh NET BUF DEBUG LOG LEVEL > BLE_MESH_NET_BUF
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1140`

---

## CONFIG_BLE_MESH_NODE

**Support for BLE Mesh Node**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:245`

Enable the device to be provisioned into a node. This option should be
enabled when an unprovisioned device is going to be provisioned into a
node and communicate with other nodes in the BLE Mesh network.

---

## CONFIG_BLE_MESH_NODE_ID_TIMEOUT

**Node Identity advertising timeout**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Server
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:433`

This option determines for how long the local node advertises using
Node Identity. The given value is in seconds. The specification limits
this to 60 seconds and lists it as the recommended value as well.
So leaving the default value is the safest option.
When an unprovisioned device is provisioned successfully and becomes a
node, it will start to advertise using Node Identity during the time
set by this option. And after that, Network ID will be advertised.

---

## CONFIG_BLE_MESH_NOT_RELAY_REPLAY_MSG

**Not relay replayed messages in a mesh network**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:713`

There may be many expired messages in a complex mesh network that would be
considered replayed messages.
Enable this option will refuse to relay such messages, which could help to
reduce invalid packets in the mesh network.
However, it should be noted that enabling this option may result in packet
loss in certain environments.
Therefore, users need to decide whether to enable this option according to
the actual usage situation.

---

## CONFIG_BLE_MESH_NO_LOG

**Disable BLE Mesh debug logs (minimize bin size)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1081`

Select this to save the BLE Mesh related rodata code size. Enabling this option
will disable the output of BLE Mesh debug log.

---

## CONFIG_BLE_MESH_ODP_CLI

**On-Demand Private Proxy Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1237`

Enable support for On-Demand Private Proxy Client model.

---

## CONFIG_BLE_MESH_ODP_SRV

**On-Demand Private Proxy Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1242`

Enable support for On-Demand Private Proxy Server model.

---

## CONFIG_BLE_MESH_PARTITION_NAME

**Name of the NVS partition for BLE Mesh**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently > Use a specific NVS partition for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:622`

This value defines the name of the specified NVS partition used by the
mesh stack.

---

## CONFIG_BLE_MESH_PATH_MONITOR_TEST

**Enable Path Monitoring test mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Directed Forwarding Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1424`

The option only removes the Path Use timer; all other behavior of the
device is not changed.
If Path Monitoring test mode is going to be used, this option should
be enabled.

---

## CONFIG_BLE_MESH_PBA_SAME_TIME

**Maximum number of PB-ADV running at the same time by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:286`

This option specifies how many devices can be provisioned at the same time
using PB-ADV. For examples, if the value is 2, it means a Provisioner can
provision two unprovisioned devices with PB-ADV at the same time.

---

## CONFIG_BLE_MESH_PBG_SAME_TIME

**Maximum number of PB-GATT running at the same time by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:298`

This option specifies how many devices can be provisioned at the same
time using PB-GATT. For example, if the value is 2, it means a Provisioner
can provision two unprovisioned devices with PB-GATT at the same time.

---

## CONFIG_BLE_MESH_PB_ADV

**Provisioning support using the advertising bearer (PB-ADV)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:384`

Enable this option to allow the device to be provisioned over the
advertising bearer. This option should be enabled if PB-ADV is
going to be used during provisioning procedure.

---

## CONFIG_BLE_MESH_PB_GATT

**Provisioning support using GATT (PB-GATT)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:406`

Enable this option to allow the device to be provisioned over GATT.
This option should be enabled if PB-GATT is going to be used during
provisioning procedure.

# Virtual option enabled whenever any Proxy protocol is needed

---

## CONFIG_BLE_MESH_PRB_CLI

**Mesh Private Beacon Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1227`

Enable support for Mesh Private Beacon Client model.

---

## CONFIG_BLE_MESH_PRB_SRV

**Mesh Private Beacon Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1232`

Enable support for Mesh Private Beacon Server model.

---

## CONFIG_BLE_MESH_PROV

**BLE Mesh Provisioning support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:350`

Enable this option to support BLE Mesh Provisioning functionality. For
BLE Mesh, this option should be always enabled.

---

## CONFIG_BLE_MESH_PROVISIONER

**Support for BLE Mesh Provisioner**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:252`

Enable the device to be a Provisioner. The option should be enabled when
a device is going to act as a Provisioner and provision unprovisioned
devices into the BLE Mesh network.

---

## CONFIG_BLE_MESH_PROVISIONER_APP_KEY_COUNT

**Maximum number of application keys that can be owned by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:317`

This option specifies how many application keys the Provisioner can have.
Indeed, this value decides the number of the application keys which can be added by a Provisioner.

---

## CONFIG_BLE_MESH_PROVISIONER_RECV_HB

**Support receiving Heartbeat messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:325`

When this option is enabled, Provisioner can call specific functions to enable
or disable receiving Heartbeat messages and notify them to the application layer.

---

## CONFIG_BLE_MESH_PROVISIONER_RECV_HB_FILTER_SIZE

**Maximum number of filter entries for receiving Heartbeat messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner > Support receiving Heartbeat messages
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:334`

This option specifies how many heartbeat filter entries Provisioner supports.
The heartbeat filter (acceptlist or rejectlist) entries are used to store a
list of SRC and DST which can be used to decide if a heartbeat message will
be processed and notified to the application layer by Provisioner.
Note: The filter is an empty rejectlist by default.

---

## CONFIG_BLE_MESH_PROVISIONER_SUBNET_COUNT

**Maximum number of mesh subnets that can be created by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:309`

This option specifies how many subnets per network a Provisioner can create.
Indeed, this value decides the number of network keys which can be added by a Provisioner.

---

## CONFIG_BLE_MESH_PROV_EPA

**BLE Mesh enhanced provisioning authentication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh Provisioning support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:357`

Enable this option to support BLE Mesh enhanced provisioning authentication
functionality. This option can increase the security level of provisioning.
It is recommended to enable this option.

---

## CONFIG_BLE_MESH_PROXY

**BLE Mesh Proxy protocol support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:416`

Enable this option to support BLE Mesh Proxy protocol used by PB-GATT
and other proxy pdu transmission.

---

## CONFIG_BLE_MESH_PROXY_ADV_INST_ID

**Extended adv instance for Mesh proxy packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh > Support using multiple adv instance for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:51`

Extended ADV instance used by Mesh proxy advertising packets.

---

## CONFIG_BLE_MESH_PROXY_CLI_SRV_COEXIST

**Support Proxy Client and Proxy Server coexistence**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:487`

Enable this option to support the coexistence of proxy client and proxy server.

---

## CONFIG_BLE_MESH_PROXY_FILTER_SIZE

**Maximum number of filter entries per Proxy Client**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Server
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:447`

This option specifies how many Proxy Filter entries the local node supports.
The entries of Proxy filter (whitelist or blacklist) are used to store a
list of addresses which can be used to decide which messages will be forwarded
to the Proxy Client by the Proxy Server.

---

## CONFIG_BLE_MESH_PROXY_PRIVACY

**Support Proxy Privacy**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Server
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:458`

The Proxy Privacy parameter controls the privacy of the Proxy Server
over the connection. The value of the Proxy Privacy parameter is
controlled by the type of proxy connection, which is dependent on the
bearer used by the proxy connection.

---

## CONFIG_BLE_MESH_PROXY_SOLIC_PDU_RX

**Support receiving Proxy Solicitation PDU**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Server
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:468`

Enable this option to support receiving Proxy Solicitation PDU.

---

## CONFIG_BLE_MESH_PROXY_SOLIC_PDU_TX

**Support sending Proxy Solicitation PDU**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Client
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:503`

Enable this option to support sending Proxy Solicitation PDU.

---

## CONFIG_BLE_MESH_PROXY_SOLIC_RX_CRPL

**Maximum capacity of solicitation replay protection list**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Server > Support receiving Proxy Solicitation PDU
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:475`

This option specifies the maximum capacity of the solicitation replay
protection list. The solicitation replay protection list is used to
reject Solicitation PDUs that were already processed by a node, which
will store the solicitation src and solicitation sequence number of
the received Solicitation PDU message.

---

## CONFIG_BLE_MESH_PROXY_SOLIC_TX_SRC_COUNT

**Maximum number of SSRC that can be used by Proxy Client**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh GATT Proxy Client > Support sending Proxy Solicitation PDU
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:510`

This option specifies the maximum number of Solicitation Source (SSRC)
that can be used by Proxy Client for sending a Solicitation PDU.
A Proxy Client may use the primary address or any of the secondary
addresses as the SSRC for a Solicitation PDU.
So for a Proxy Client, it's better to choose the value based on its
own element count.

---

## CONFIG_BLE_MESH_RANDOM_ADV_INTERVAL

**Support using random adv interval for mesh packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:15`

Enable this option to allow using random advertising interval
for mesh packets. And this could help avoid collision of
advertising packets.

---

## CONFIG_BLE_MESH_RECORD_FRAG_MAX_SIZE

**Maximum size of the provisioning record fragment that Provisioner can receive**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh Provisioning support > Support Certificate-based provisioning
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:375`

This option sets the maximum size of the provisioning record fragment that the
Provisioner can receive. The range depends on provisioning bearer.

---

## CONFIG_BLE_MESH_RELAY

**Relay support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:856`

Support for acting as a Mesh Relay Node. Enabling this option will allow
a node to support the Relay feature, and the Relay feature can still
be enabled or disabled by proper configuration messages. Disabling this
option will let a node not support the Relay feature.

---

## CONFIG_BLE_MESH_RELAY_ADV_BUF

**Use separate advertising buffers for relay packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Relay support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:868`

When selected, self-send packets will be put in a high-priority
queue and relay packets will be put in a low-priority queue.

---

## CONFIG_BLE_MESH_RELAY_ADV_BUF_COUNT

**Number of advertising buffers for relay packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Relay support > Use separate advertising buffers for relay packets
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:877`

Number of advertising buffers for relay packets available.

---

## CONFIG_BLE_MESH_RELAY_ADV_INST_ID

**Extended adv instance for Mesh relay packets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh > Support using multiple adv instance for BLE Mesh > Use separate extended adv instance for Mesh relay packets
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:69`

Extended ADV instance used by Mesh relay advertising packets.

---

## CONFIG_BLE_MESH_RPL_STORE_TIMEOUT

**Minimum frequency that the RPL gets updated in storage**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:572`

This value defines in seconds how soon the RPL (Replay Protection List)
gets written to persistent storage after a change occurs. If the node
receives messages frequently, then a large value is recommended. If the
node receives messages rarely, then the value can be as low as 0 (which
means the RPL is written into the storage immediately).
Note that if the node operates in a security-sensitive case, and there is
a risk of sudden power-off, then a value of 0 is strongly recommended.
Otherwise, a power loss before RPL being written into the storage may
introduce message replay attacks and system security will be in a
vulnerable state.

---

## CONFIG_BLE_MESH_RPR_CLI

**Remote Provisioning Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1338`

Enable support for Remote Provisioning Client model

---

## CONFIG_BLE_MESH_RPR_CLI_PROV_SAME_TIME

**Maximum number of PB-Remote running at the same time by Provisioner**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Remote Provisioning Client model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1347`

This option specifies how many devices can be provisioned at the same time
using PB-REMOTE. For example, if the value is 2, it means a Provisioner can
provision two unprovisioned devices with PB-REMOTE at the same time.

---

## CONFIG_BLE_MESH_RPR_SRV

**Remote Provisioning Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1358`

Enable support for Remote Provisioning Server model

---

## CONFIG_BLE_MESH_RPR_SRV_ACTIVE_SCAN

**Support Active Scan for remote provisioning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Remote Provisioning Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1375`

Enable this option to support Active Scan for remote provisioning.

---

## CONFIG_BLE_MESH_RPR_SRV_MAX_EXT_SCAN

**Maximum number of extended scan procedures**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Remote Provisioning Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1381`

This option specifies how many extended scan procedures can be
started by the Remote Provisioning Server.

---

## CONFIG_BLE_MESH_RPR_SRV_MAX_SCANNED_ITEMS

**Maximum number of device information can be scanned**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Remote Provisioning Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1367`

This option specifies how many device information can a Remote
Provisioning Server store each time while scanning.

---

## CONFIG_BLE_MESH_RX_SDU_MAX

**Maximum incoming Upper Transport Access PDU length**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:824`

Maximum incoming Upper Transport Access PDU length. Leave this to the default
value, unless you really need to optimize memory usage.

---

## CONFIG_BLE_MESH_RX_SEG_MSG_COUNT

**Maximum number of simultaneous incoming segmented messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:812`

Maximum number of simultaneous incoming multi-segment and/or reliable messages.
The default value is 1, which means the device can only receive one segmented
message at a time. And if another segmented message is going to be received,
it should wait for the completion of the previous one.
If users are going to receive multiple segmented messages at the same time, this
value should be configured properly.

---

## CONFIG_BLE_MESH_SAR_CLI

**SAR Configuration Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1274`

Enable support for SAR Configuration Client model.

---

## CONFIG_BLE_MESH_SAR_ENHANCEMENT

**Segmentation and reassembly enhancement**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:792`

Enable this option to use the enhanced segmentation and reassembly
mechanism introduced in Bluetooth Mesh Protocol 1.1.

---

## CONFIG_BLE_MESH_SAR_SRV

**SAR Configuration Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1279`

Enable support for SAR Configuration Server model.

---

## CONFIG_BLE_MESH_SCENE_CLI

**Scene Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1499`

Enable support for Scene Client model.

---

## CONFIG_BLE_MESH_SCHEDULER_CLI

**Scheduler Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1504`

Enable support for Scheduler Client model.

---

## CONFIG_BLE_MESH_SELF_TEST

**Perform BLE Mesh self-tests**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1603`

This option adds extra self-tests which are run every time BLE Mesh
networking is initialized.

---

## CONFIG_BLE_MESH_SENSOR_CLI

**Sensor Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1489`

Enable support for Sensor Client model.

---

## CONFIG_BLE_MESH_SENSOR_SERVER

**Sensor server models**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1540`

Enable support for Sensor server models.

---

## CONFIG_BLE_MESH_SEPARATE_BLE_ADV_INSTANCE

**Use separate extended adv instance for BLE normal packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh > Support using multiple adv instance for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:77`

Enable this option to support using a separate extended ADV instance for normal BLE advertising packets.

---

## CONFIG_BLE_MESH_SEPARATE_RELAY_ADV_INSTANCE

**Use separate extended adv instance for Mesh relay packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh > Support using multiple adv instance for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:61`

Enable this option to support using a separate extended ADV instance for Mesh relay packets.

---

## CONFIG_BLE_MESH_SEQ_STORE_RATE

**How often the sequence number gets updated in storage**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:557`

This value defines how often the local sequence number gets updated in
persistent storage (i.e. flash). e.g. a value of 100 means that the
sequence number will be stored to flash on every 100th increment.
If the node sends messages very frequently a higher value makes more
sense, whereas if the node sends infrequently a value as low as 0
(update storage for every increment) can make sense. When the stack
gets initialized it will add sequence number to the last stored one,
so that it starts off with a value that's guaranteed to be larger than
the last one used before power off.

---

## CONFIG_BLE_MESH_SETTINGS

**Store BLE Mesh configuration persistently**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:531`

When selected, the BLE Mesh stack will take care of storing/restoring the BLE
Mesh configuration persistently in flash.
If the device is a BLE Mesh node, when this option is enabled, the configuration
of the device will be stored persistently, including unicast address, NetKey,
AppKey, etc.
And if the device is a BLE Mesh Provisioner, the information of the device will
be stored persistently, including the information of provisioned nodes, NetKey,
AppKey, etc.

---

## CONFIG_BLE_MESH_SETTINGS_BACKWARD_COMPATIBILITY

**A specific option for settings backward compatibility**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:588`

This option is created to solve the issue of failure in recovering
node information after mesh stack updates. In the old version mesh
stack, there is no key of "mesh/role" in nvs. In the new version
mesh stack, key of "mesh/role" is added in nvs, recovering node
information needs to check "mesh/role" key in nvs and implements
selective recovery of mesh node information. Therefore, there may
be failure in recovering node information during node restarting
after OTA.

The new version mesh stack adds the option of "mesh/role" because
we have added the support of storing Provisioner information, while
the old version only supports storing node information.

If users are updating their nodes from old version to new version,
we recommend enabling this option, so that system could set the flag
in advance before recovering node information and make sure the node
information recovering could work as expected.

---

## CONFIG_BLE_MESH_SHELL

**Enable BLE Mesh shell**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1644`

Activate shell module that provides BLE Mesh commands to the console.

---

## CONFIG_BLE_MESH_SPECIFIC_PARTITION

**Use a specific NVS partition for BLE Mesh**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:611`

When selected, the mesh stack will use a specified NVS partition instead of
default NVS partition. Note that the specified partition must be registered
with NVS using nvs_flash_init_partition() API, and the partition must exists
in the csv file.
When Provisioner needs to store a large amount of nodes' information in the
flash (e.g. more than 20), this option is recommended to be enabled.

---

## CONFIG_BLE_MESH_SRPL_CLI

**Solicitation PDU RPL Configuration Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1249`

Enable support for Solicitation PDU RPL Configuration Client model.

---

## CONFIG_BLE_MESH_SRPL_SRV

**Solicitation PDU RPL Configuration Server model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1254`

Enable support for Solicitation PDU RPL Configuration Server model.
Note:
This option depends on the functionality of receiving Solicitation
PDU. If the device doesn't support receiving Solicitation PDU, then
there is no need to enable this server model.

---

## CONFIG_BLE_MESH_STACK_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BLE_MESH_STORE_TIMEOUT

**Delay (in seconds) before storing anything persistently**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:546`

This value defines in seconds how soon any pending changes are actually
written into persistent storage (flash) after a change occurs.
The option allows nodes to delay a certain period of time to save proper
information to flash. The default value is 0, which means information
will be stored immediately once there are updates.

---

## CONFIG_BLE_MESH_SUBNET_COUNT

**Maximum number of mesh subnets per network**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:657`

This option specifies how many subnets a Mesh network can have at the same time.
Indeed, this value decides the number of the network keys which can be owned by a node.

---

## CONFIG_BLE_MESH_SUPPORT_BLE_ADV

**Support sending normal BLE advertising packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh and BLE coexistence support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:205`

When selected, users can send normal BLE advertising packets
with specific API.

---

## CONFIG_BLE_MESH_SUPPORT_BLE_SCAN

**Support scanning normal BLE advertising packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh and BLE coexistence support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:223`

When selected, users can register a callback and receive normal BLE
advertising packets in the application layer.

---

## CONFIG_BLE_MESH_SUPPORT_DIRECTED_PROXY

**Enable Directed Proxy functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Foundation models > Directed Forwarding Configuration Server model
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1434`

Support Directed Proxy functionality.

---

## CONFIG_BLE_MESH_SUPPORT_MULTI_ADV

**Support using multiple adv instance for BLE Mesh**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support using BLE 5.0 APIs for BLE Mesh
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:44`

Enable this option to support using multiple adv instance while running BLE Mesh.

---

## CONFIG_BLE_MESH_TEST_AUTO_ENTER_NETWORK

**Unprovisioned device enters mesh network automatically**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1626`

With this option enabled, an unprovisioned device can automatically
enters mesh network using a specific test function without the pro-
visioning procedure. And on the Provisioner side, a test function
needs to be invoked to add the node information into the mesh stack.

---

## CONFIG_BLE_MESH_TEST_USE_WHITE_LIST

**Use white list to filter mesh advertising packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh specific test option
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1635`

With this option enabled, users can use white list to filter mesh
advertising packets while scanning.

---

## CONFIG_BLE_MESH_TIME_CLI

**Time Client model**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1494`

Enable support for Time Client model.

---

## CONFIG_BLE_MESH_TIME_SCENE_SERVER

**Time and Scenes server models**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Client/Server models
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1546`

Enable support for Time and Scenes server models.

---

## CONFIG_BLE_MESH_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1107`

---

## CONFIG_BLE_MESH_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1101`

---

## CONFIG_BLE_MESH_TRACE_LEVEL_INFO

**INFO**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1105`

---

## CONFIG_BLE_MESH_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1099`

---

## CONFIG_BLE_MESH_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1109`

---

## CONFIG_BLE_MESH_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > BLE Mesh STACK DEBUG LOG LEVEL > BLE_MESH_STACK
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:1103`

---

## CONFIG_BLE_MESH_TX_SEG_MAX

**Maximum number of segments in outgoing messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:832`

Maximum number of segments supported for outgoing messages.
This value should typically be fine-tuned based on what
models the local node supports, i.e. what's the largest
message payload that the node needs to be able to send.
This value affects memory and call stack consumption, which
is why the default is lower than the maximum that the
specification would allow (32 segments).

The maximum outgoing SDU size is 12 times this number (out of
which 4 or 8 bytes is used for the Transport Layer MIC). For
example, 5 segments means the maximum SDU size is 60 bytes,
which leaves 56 bytes for application layer data using a
4-byte MIC and 52 bytes using an 8-byte MIC.

Be sure to specify a sufficient number of advertising buffers
when setting this option to a higher value. There must be at
least three more advertising buffers (BLE_MESH_ADV_BUF_COUNT)
as there are outgoing segments.

---

## CONFIG_BLE_MESH_TX_SEG_MSG_COUNT

**Maximum number of simultaneous outgoing segmented messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:800`

Maximum number of simultaneous outgoing multi-segment and/or reliable messages.
The default value is 1, which means the device can only send one segmented
message at a time. And if another segmented message is going to be sent, it
should wait for the completion of the previous one.
If users are going to send multiple segmented messages at the same time, this
value should be configured properly.

---

## CONFIG_BLE_MESH_UNPROVISIONED_BEACON_INTERVAL

**Interval between two consecutive Unprovisioned Device Beacon**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Provisioning support using the advertising bearer (PB-ADV)
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:393`

This option specifies the interval of sending two consecutive unprovisioned
device beacon, users can use this option to change the frequency of sending
unprovisioned device beacon. For example, if the value is 5, it means the
unprovisioned device beacon will send every 5 seconds. When the option of
BLE_MESH_FAST_PROV is selected, the value is better to be 3 seconds, or less.

---

## CONFIG_BLE_MESH_USE_BLE_50

**Support using BLE 5.0 APIs for BLE Mesh**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:25`

This option to enable BLE Mesh using some BLE 5.0 APIs.

---

## CONFIG_BLE_MESH_USE_DUPLICATE_SCAN

**Support Duplicate Scan in BLE Mesh**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:93`

Enable this option to allow using specific duplicate scan filter
in BLE Mesh, and Scan Duplicate Type must be set by choosing the
option in the Bluetooth Controller section in menuconfig, which is
"Scan Duplicate By Device Address and Advertising Data".

---

## CONFIG_BLE_MESH_USE_MULTIPLE_NAMESPACE

**Support using multiple NVS namespaces by Provisioner**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support > Store BLE Mesh configuration persistently
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:630`

When selected, Provisioner can use different NVS namespaces to store
different instances of mesh information.
For example, if in the first room, Provisioner uses NetKey A, AppKey
A and provisions three devices, these information will be treated as
mesh information instance A. When the Provisioner moves to the second
room, it uses NetKey B, AppKey B and provisions two devices, then the
information will be treated as mesh information instance B.
Here instance A and instance B will be stored in different namespaces.
With this option enabled, Provisioner needs to use specific functions
to open the corresponding NVS namespace, restore the mesh information,
release the mesh information or erase the mesh information.

---

## CONFIG_BLE_MESH_V11_SUPPORT

**Support ESP BLE Mesh v1.1 features (Preview)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP BLE Mesh Support
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:9`

Support BLE Mesh v1.1 features

---

## CONFIG_BLE_MESH_WAIT_FOR_PROV_MAX_DEV_NUM

**Maximum number of unprovisioned devices that can be added to device queue**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP BLE Mesh Support > Support for BLE Mesh Provisioner
- **Kconfig:** `/opt/esp/idf/components/bt/esp_ble_mesh/Kconfig.in:261`

This option specifies how many unprovisioned devices can be added to device
queue for provisioning. Users can use this option to define the size of the
queue in the bottom layer which is used to store unprovisioned device
information (e.g. Device UUID, address).

---

## CONFIG_BOOTLOADER_ANTI_ROLLBACK_ENABLE

**Enable bootloader rollback support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Recovery Bootloader and Rollback > Enable Recovery Bootloader
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.bootloader_rollback:32`

This option prevents rollback to previous bootloader image with lower security version.

---

## CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK

**Enable app anti-rollback support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Application Rollback > Enable app rollback support
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.app_rollback:15`

This option prevents rollback to previous firmware/application image with lower security version.

---

## CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE

**Enable app rollback support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Application Rollback
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.app_rollback:3`

After updating the app, the bootloader runs a new app with the "ESP_OTA_IMG_PENDING_VERIFY" state set.
This state prevents the re-run of this app. After the first boot of the new app in the user code, the
function should be called to confirm the operability of the app or vice versa about its non-operability.
If the app is working, then it is marked as valid. Otherwise, it is marked as not valid and rolls back to
the previous working app. A reboot is performed, and the app is booted before the software update.
Note: If during the first boot a new app the power goes out or the WDT works, then roll back will happen.
Rollback is possible only between the apps with the same security versions.

---

## CONFIG_BOOTLOADER_APP_SECURE_VERSION

**eFuse secure version of app**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Application Rollback > Enable app rollback support > Enable app anti-rollback support
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.app_rollback:22`

The secure version is the sequence number stored in the header of each firmware.
The security version is set in the bootloader, version is recorded in the eFuse field
as the number of set ones. The allocated number of bits in the efuse field
for storing the security version is limited (see BOOTLOADER_APP_SEC_VER_SIZE_EFUSE_FIELD option).

Bootloader: When bootloader selects an app to boot, an app is selected that has
a security version greater or equal that recorded in eFuse field.
The app is booted with a higher (or equal) secure version.

The security version is worth increasing if in previous versions there is
a significant vulnerability and their use is not acceptable.

Your partition table should has a scheme with ota_0 + ota_1 (without factory).

---

## CONFIG_BOOTLOADER_APP_SEC_VER_SIZE_EFUSE_FIELD

**Size of the efuse secure version field**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Application Rollback > Enable app rollback support > Enable app anti-rollback support
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.app_rollback:41`

The size of the efuse secure version field.
Its length is limited to 32 bits for ESP32 and 16 bits for ESP32-S2.
This determines how many times the security version can be increased.

---

## CONFIG_BOOTLOADER_APP_TEST

**GPIO triggers boot from test app partition**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:213`

Allows to run the test app from "TEST" partition.
A boot from "test" partition will occur if there is a GPIO input pulled low while device starts up.
See settings below.

---

## CONFIG_BOOTLOADER_APP_TEST_PIN_HIGH

**Enter test app on GPIO high**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > GPIO triggers boot from test app partition > App test GPIO level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:255`

---

## CONFIG_BOOTLOADER_APP_TEST_PIN_LOW

**Enter test app on GPIO low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > GPIO triggers boot from test app partition > App test GPIO level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:252`

---

## CONFIG_BOOTLOADER_CACHE_32BIT_ADDR_OCTAL_FLASH

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_BOOTLOADER_CACHE_32BIT_ADDR_QUAD_FLASH

**Enable cache access to 32-bit-address (over 16MB) range of SPI Flash (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Serial Flash Configurations
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:111`

Enabling this option allows the CPU to access 32-bit-address flash beyond 16M range.
1. This option only valid for 4-line flash. Octal flash doesn't need this.
2. This option is experimental, which means it can’t use on all flash chips stable, for more
information, please contact Espressif Business support.

---

## CONFIG_BOOTLOADER_COMPILER_OPTIMIZATION_DEBUG

**Debug (-Og)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Bootloader optimization Level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:33`

---

## CONFIG_BOOTLOADER_COMPILER_OPTIMIZATION_NONE

**Debug without optimization (-O0) (Deprecated, will be removed in IDF v6.0)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Bootloader optimization Level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:37`

---

## CONFIG_BOOTLOADER_COMPILER_OPTIMIZATION_PERF

**Optimize for performance (-O2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Bootloader optimization Level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:35`

---

## CONFIG_BOOTLOADER_COMPILER_OPTIMIZATION_SIZE

**Size (-Os with GCC, -Oz with Clang)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Bootloader optimization Level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:31`

---

## CONFIG_BOOTLOADER_COMPILE_TIME_DATE

**Use time/date stamp for bootloader**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Bootloader manager
- **Kconfig:** `/opt/esp/idf/components/bootloader/../esp_bootloader_format/Kconfig.bootloader:3`

If set, then the bootloader will be built with the current time/date stamp.
It is stored in the bootloader description
structure. If not set, time/date stamp will be excluded from bootloader image.
This can be useful for getting the
same binary image files made from the same source, but at different times.

---

## CONFIG_BOOTLOADER_CUSTOM_RESERVE_RTC

**Reserve RTC FAST memory for custom purposes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:390`

This option allows the customer to place data in the RTC FAST memory,
this area remains valid when rebooted, except for power loss.
This memory is located at a fixed address and is available
for both the bootloader and the application.
(The application and bootloader must be compiled with the same option).
The RTC FAST memory has access only through PRO_CPU.

---

## CONFIG_BOOTLOADER_CUSTOM_RESERVE_RTC_IN_CRC

**Include custom memory in the CRC calculation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Reserve RTC FAST memory for custom purposes
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:403`

This option allows the customer to use the legacy bootloader behavior when the
RTC FAST memory CRC calculation takes place. When this option is enabled, the
allocated user custom data will be taken into account in the CRC calculation.
This means that any change to the custom data would need a CRC update to prevent
the bootloader from marking this data as corrupted.
If this option is disabled, the custom data will not be taken into account when
calculating the RTC FAST memory CRC. The user custom data can be changed freely,
without the need to update the CRC.
THIS OPTION MUST BE THE SAME FOR BOTH THE BOOTLOADER AND THE APPLICATION BUILDS.

---

## CONFIG_BOOTLOADER_CUSTOM_RESERVE_RTC_SIZE

**Size in bytes for custom purposes**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Reserve RTC FAST memory for custom purposes
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:418`

This option reserves in RTC FAST memory the area for custom purposes.
If you want to create your own bootloader and save more information
in this area of memory, you can increase it. It must be a multiple of 4 bytes.
This area (rtc_retain_mem_t) is reserved and has access from the bootloader and an application.

---

## CONFIG_BOOTLOADER_DATA_FACTORY_RESET

**Comma-separated names of partitions to clear on factory reset**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > GPIO triggers factory reset
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:201`

Allows customers to select which data partitions will be erased while factory reset.

Specify the names of partitions as a comma-delimited with optional spaces for readability. (Like this:
"nvs, phy_init, ...")
Make sure that the name specified in the partition table and here are the same.
Partitions of type "app" cannot be specified here.

---

## CONFIG_BOOTLOADER_EFUSE_SECURE_VERSION_EMULATE

**Emulate operations with efuse secure version(only test)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Application Rollback > Enable app rollback support > Enable app anti-rollback support
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.app_rollback:55`

This option allows to emulate read/write operations with all eFuses and efuse secure version.
It allows to test anti-rollback implementation without permanent write eFuse bits.
There should be an entry in partition table with following details: `emul_efuse, data, efuse, , 0x2000`.

This option enables: EFUSE_VIRTUAL and EFUSE_VIRTUAL_KEEP_IN_FLASH.

---

## CONFIG_BOOTLOADER_FACTORY_RESET

**GPIO triggers factory reset**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:148`

Allows to reset the device to factory settings:
- clear one or more data partitions;
- boot from "factory" partition.
The factory reset will occur if there is a GPIO input held at the configured level while
device starts up. See settings below.

---

## CONFIG_BOOTLOADER_FACTORY_RESET_PIN_HIGH

**Reset on GPIO high**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > GPIO triggers factory reset > Factory reset GPIO level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:190`

---

## CONFIG_BOOTLOADER_FACTORY_RESET_PIN_LOW

**Reset on GPIO low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > GPIO triggers factory reset > Factory reset GPIO level
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:187`

---

## CONFIG_BOOTLOADER_FLASH_32BIT_ADDR

- **Type:** `string`
- **Default:** `n`

This is a helper config for 32bits address flash. Invisible for users.

---

## CONFIG_BOOTLOADER_FLASH_DC_AWARE

**Allow app adjust Dummy Cycle bits in SPI Flash for higher frequency (READ HELP FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Serial Flash Configurations
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:75`

This will force 2nd bootloader to be loaded by DOUT mode, and will restore Dummy Cycle setting by
resetting the Flash

---

## CONFIG_BOOTLOADER_FLASH_NEEDS_32BIT_ADDR_QUAD_FLASH

- **Type:** `string`
- **Default:** `n`

This is a helper config for 32bits address quad flash. Invisible for users.

---

## CONFIG_BOOTLOADER_FLASH_NEEDS_32BIT_FEAT

- **Type:** `string`
- **Default:** `n`

This is a helper config for 32bits address flash. Invisible for users.

---

## CONFIG_BOOTLOADER_FLASH_XMC_SUPPORT

**Enable the support for flash chips of XMC (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Serial Flash Configurations
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:81`

Perform the startup flow recommended by XMC. Please consult XMC for the details of this flow.
XMC chips will be forbidden to be used, when this option is disabled.

DON'T DISABLE THIS UNLESS YOU KNOW WHAT YOU ARE DOING.

comment "Features below require specific hardware (READ DOCS FIRST!)"

---

## CONFIG_BOOTLOADER_HOLD_TIME_GPIO

**Hold time of GPIO for reset/test mode (seconds)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:259`

The GPIO must be held low continuously for this period of time after reset
before a factory reset or test partition boot (as applicable) is performed.

---

## CONFIG_BOOTLOADER_LOG_COLORS

**Color**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.format:3`

Enable ANSI terminal color codes. Logs (info, errors, warnings) will contain color codes.
In order to view these, your terminal program must support ANSI color codes.

---

## CONFIG_BOOTLOADER_LOG_COLORS_SUPPORT

**Allow enabling color output at run time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.format:11`

Enables support for color codes in the esp_log() function. If CONFIG_LOG_COLORS is enabled, this option
is always active. If CONFIG_LOG_COLORS is disabled, this option allows you to still handle color codes
in specific files by defining ESP_LOG_COLOR_DISABLED as 0 before including esp_log.h.

Note that enabling this option may slightly increase RAM/FLASH usage due to additional color handling
functionality. It provides flexibility to manage color output even when CONFIG_LOG_COLORS is turned off.

---

## CONFIG_BOOTLOADER_LOG_LEVEL

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:38`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_ERROR

**Error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:32`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:36`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_NONE

**No output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:30`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:40`

---

## CONFIG_BOOTLOADER_LOG_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Bootloader log verbosity
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:34`

---

## CONFIG_BOOTLOADER_LOG_MODE_BINARY

**Binary Log Mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Settings > Log Mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.settings:21`

Enables binary logging with host-side format string expansion. In this mode, the
format argument of ESP_LOGx, ESP_EARLY_LOG, and ESP_DRAM_LOG macros is stored in a
NOLOAD section, not included in the final binary file. This reduces flash usage by
approximately 10% - 35%. The esp_log() function uses the binary log handler to output
messages. Instead of sending the full log string, the chip transmits only the
addresses of these strings (if present in the ELF file). If the format string
cannot be found in the ELF file, the chip sends the entire string. The host-side
monitor tool, which has access to the ELF file, reconstructs the log message using
the format string.
This reduces firmware size by eliminating format strings from
flash memory and removing the usage of printf-like functions, potentially freeing up
a few kilobytes of space. To further reduce firmware size, wrap string data with ESP_LOG_ATTR_STR.

---

## CONFIG_BOOTLOADER_LOG_MODE_BINARY_EN

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_BOOTLOADER_LOG_MODE_TEXT

**Text Log Mode**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Log > Settings > Log Mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.settings:13`

Enables text-based logging, where log messages are stored in a human-readable format.
This mode is useful for development and debugging, as it allows logs to be easily
read and interpreted without additional processing.

---

## CONFIG_BOOTLOADER_LOG_MODE_TEXT_EN

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_BOOTLOADER_LOG_TIMESTAMP_SOURCE_CPU_TICKS

**Milliseconds Since Boot**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.format:45`

---

## CONFIG_BOOTLOADER_LOG_TIMESTAMP_SOURCE_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.format:41`

---

## CONFIG_BOOTLOADER_LOG_TIMESTAMP_SUPPORT

**Allow enabling timestamp output at run time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log.format:51`

Enables support for timestamp in the esp_log() function.
If CONFIG_LOG_TIMESTAMP_SOURCE_NONE, this option allows you to still handle timestamp
in specific files by defining ESP_LOG_TIMESTAMP_DISABLED as 0 before including esp_log.h.

Note that enabling this option may slightly increase RAM/FLASH usage due to additional timestamp handling
functionality. It provides flexibility to manage timestamp output even when
CONFIG_LOG_TIMESTAMP_SOURCE_NONE.

---

## CONFIG_BOOTLOADER_LOG_VERSION

- **Type:** `unknown`
- **Default:** `1`

This configuration sets the log version number based on the chosen log version.

---

## CONFIG_BOOTLOADER_LOG_VERSION_1

**V1**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > Log > Log version
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:11`

---

## CONFIG_BOOTLOADER_LOG_VERSION_2

**V2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Log > Log version
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.log:13`

---

## CONFIG_BOOTLOADER_NUM_PIN_APP_TEST

**Number of the GPIO input to boot TEST partition**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > GPIO triggers boot from test app partition
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:222`

The selected GPIO will be configured as an input with internal pull-up enabled.
To trigger a test app, this GPIO must be pulled low on reset.
After the GPIO input is deactivated and the device reboots, the old application will boot.
(factory or OTA[x]).

Note that on some SoCs not all pins have an internal pull-up and certain pins are already
used by ROM bootloader as bootstrapping. Refer to the technical reference manual for further
details on the selected SoC.

---

## CONFIG_BOOTLOADER_NUM_PIN_FACTORY_RESET

**Number of the GPIO input for factory reset**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > GPIO triggers factory reset
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:159`

The selected GPIO will be configured as an input with internal pull-up enabled. To trigger a factory
reset, this GPIO must be held high or low (as configured) on startup.

Note that on some SoCs not all pins have an internal pull-up and certain pins are already
used by ROM bootloader as bootstrapping. Refer to the technical reference manual for further
details on the selected SoC.

---

## CONFIG_BOOTLOADER_OFFSET_IN_FLASH

- **Type:** `unknown`
- **Default:** `0x0`

Offset address that 2nd bootloader will be flashed to.
The value is determined by the ROM bootloader.
It's not configurable in ESP-IDF.

---

## CONFIG_BOOTLOADER_OTA_DATA_ERASE

**Clear OTA data on factory reset (select factory partition)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > GPIO triggers factory reset
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:194`

The device will boot from "factory" partition (or OTA slot 0 if no factory partition is present) after a
factory reset.

---

## CONFIG_BOOTLOADER_PROJECT_VER

**Project version**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Bootloader config > Bootloader manager
- **Kconfig:** `/opt/esp/idf/components/bootloader/../esp_bootloader_format/Kconfig.bootloader:14`

Project version. It is placed in "version" field of the esp_bootloader_desc structure.
The type of this field is "uint32_t".

---

## CONFIG_BOOTLOADER_RECOVERY_ENABLE

**Enable Recovery Bootloader**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Recovery Bootloader and Rollback
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.bootloader_rollback:3`

The recovery bootloader feature is implemented in the ROM bootloader. It is required for safe OTA
updates of the bootloader. The feature is activated when the eFuse field
(ESP_EFUSE_RECOVERY_BOOTLOADER_FLASH_SECTOR) is set, which defines the flash address of the
recovery bootloader. If activated and the primary bootloader fails to load, the ROM bootloader
will attempt to load the recovery bootloader from the address specified in eFuse.

---

## CONFIG_BOOTLOADER_RECOVERY_OFFSET

**Recovery Bootloader Flash Offset**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Recovery Bootloader and Rollback > Enable Recovery Bootloader
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.bootloader_rollback:14`

Flash address where the recovery bootloader is stored.
This value must be written to the eFuse field (ESP_EFUSE_RECOVERY_BOOTLOADER_FLASH_SECTOR)
to activate the recovery bootloader in the ROM bootloader. The eFuse can be programmed
using espefuse.py or in the user application with the API esp_efuse_set_recovery_bootloader_offset().
Setting this value in the config allows parttool.py to verify that it does not overlap with existing
partitions in the partition table.

The address must be a multiple of the flash sector size (0x1000 bytes).
The eFuse field stores the offset in sectors.
If the feature is no longer needed or unused, you can burn the 0xFFF value to disable this feature in
the ROM bootloader.

---

## CONFIG_BOOTLOADER_REGION_PROTECTION_ENABLE

**Enable protection for unmapped memory regions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:267`

Protects the unmapped memory regions of the entire address space from unintended accesses.
This will ensure that an exception will be triggered whenever the CPU performs a memory
operation on unmapped regions of the address space.
NOTE: Disabling this config on some targets (ESP32-C6, ESP32-H2, ESP32-C5) would not generate
an exception when reading from or writing to 0x0.

---

## CONFIG_BOOTLOADER_RESERVE_RTC_MEM

- **Type:** `string`
- **Default:** `n`

This option reserves an area in RTC FAST memory for the following features:
- "Skip image validation when exiting deep sleep"
- "Reserve RTC FAST memory for custom purposes"
- "GPIO triggers factory reset"

---

## CONFIG_BOOTLOADER_RESERVE_RTC_SIZE

- **Type:** `unknown`
- **Default:** `0`

Reserve RTC FAST memory for Skip image validation. This option in bytes.
This option reserves an area in the RTC FAST memory (access only PRO_CPU).
Used to save the addresses of the selected application.
When a wakeup occurs (from Deep sleep), the bootloader retrieves it and
loads the application without validation.

---

## CONFIG_BOOTLOADER_SECURE_VERSION

**Secure version of bootloader**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Recovery Bootloader and Rollback > Enable Recovery Bootloader > Enable bootloader rollback support
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.bootloader_rollback:39`

The secure version is the sequence number stored in the header of each bootloader.

The ROM Bootloader which runs the 2nd stage bootloader (PRIMARY or RECOVERY) checks that
the security version is greater or equal that recorded in the eFuse field.
Bootloaders that have a secure version in the image < secure version in efuse will not boot.

The security version is worth increasing if in previous versions there is
a significant vulnerability and their use is not acceptable.

---

## CONFIG_BOOTLOADER_SKIP_VALIDATE_ALWAYS

**Skip image validation always (READ HELP FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:362`

Selecting this option prevents the bootloader from ever validating the app image before
booting it. Any flash corruption of the selected app partition will make the entire SoC
unbootable.

Although flash corruption is a very rare case, it is not recommended to select this option.
Consider selecting "Skip image validation from power on reset" instead. However, if boot time
is the only important factor then it can be enabled.

---

## CONFIG_BOOTLOADER_SKIP_VALIDATE_IN_DEEP_SLEEP

**Skip image validation when exiting deep sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:317`

This option disables the normal validation of an image coming out of
deep sleep (checksums, SHA256, and signature). This is a trade-off
between wakeup performance from deep sleep, and image integrity checks.

Only enable this if you know what you are doing. It should not be used
in conjunction with using deep_sleep() entry and changing the active OTA
partition as this would skip the validation upon first load of the new
OTA partition.

It is possible to enable this option with Secure Boot if "allow insecure
options" is enabled, however it's strongly recommended to NOT enable it as
it may allow a Secure Boot bypass.

---

## CONFIG_BOOTLOADER_SKIP_VALIDATE_ON_POWER_ON

**Skip image validation from power on reset (READ HELP FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:340`

Some applications need to boot very quickly from power on. By default, the entire app binary
is read from flash and verified which takes up a significant portion of the boot time.

Enabling this option will skip validation of the app when the SoC boots from power on.
Note that in this case it's not possible for the bootloader to detect if an app image is
corrupted in the flash, therefore it's not possible to safely fall back to a different app
partition. Flash corruption of this kind is unlikely but can happen if there is a serious
firmware bug or physical damage.

Following other reset types, the bootloader will still validate the app image. This increases
the chances that flash corruption resulting in a crash can be detected following soft reset, and
the bootloader will fall back to a valid app image. To increase the chances of successfully recovering
from a flash corruption event, keep the option BOOTLOADER_WDT_ENABLE enabled and consider also enabling
BOOTLOADER_WDT_DISABLE_IN_USER_CODE - then manually disable the RTC Watchdog once the app is running.
In addition, enable both the Task and Interrupt watchdog timers with reset options set.

---

## CONFIG_BOOTLOADER_SPI_CUSTOM_WP_PIN

**Use custom SPI Flash WP Pin when flash pins set in eFuse (read help)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Serial Flash Configurations
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:46`

This setting is only used if the SPI flash pins have been overridden by setting the eFuses
SPI_PAD_CONFIG_xxx, and the SPI flash mode is QIO or QOUT.

When this is the case, the eFuse config only defines 3 of the 4 Quad I/O data pins. The WP pin (aka
ESP32 pin "SD_DATA_3" or SPI flash pin "IO2") is not specified in eFuse. The same pin is also used
for external SPIRAM if it is enabled.

If this config item is set to N (default), the correct WP pin will be automatically used for any
Espressif chip or module with integrated flash. If a custom setting is needed, set this config item to
Y and specify the GPIO number connected to the WP.

---

## CONFIG_BOOTLOADER_SPI_WP_PIN

**Custom SPI Flash WP Pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Bootloader config > Serial Flash Configurations
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:63`

The option "Use custom SPI Flash WP Pin" must be set or this value is ignored

If burning a customized set of SPI flash pins in eFuse and using QIO or QOUT mode for flash, set this
value to the GPIO number of the SPI flash WP pin.

---

## CONFIG_BOOTLOADER_VDDSDIO_BOOST_1_8V

**1.8V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > VDDSDIO LDO voltage
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:141`

---

## CONFIG_BOOTLOADER_VDDSDIO_BOOST_1_9V

**1.9V**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config > VDDSDIO LDO voltage
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:144`

---

## CONFIG_BOOTLOADER_WDT_DISABLE_IN_USER_CODE

**Allows RTC watchdog disable in user code**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Bootloader config > Use RTC watchdog in start code
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:290`

If this option is set, the ESP-IDF app must explicitly reset, feed, or disable the rtc_wdt in
the app's own code.
If this option is not set (default), then rtc_wdt will be disabled by ESP-IDF before calling
the app_main() function.

Use function wdt_hal_feed() for resetting counter of RTC_WDT.
For esp32/s2 you can also use rtc_wdt_feed().

Use function wdt_hal_disable() for disabling RTC_WDT.
For esp32/s2 you can also use rtc_wdt_disable().

---

## CONFIG_BOOTLOADER_WDT_ENABLE

**Use RTC watchdog in start code**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Bootloader config
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:277`

Tracks the execution time of startup code.
If the execution time is exceeded, the RTC_WDT will restart system.
It is also useful to prevent a lock up in start code caused by an unstable power source.
NOTE: Tracks the execution time starts from the bootloader code - re-set timeout, while selecting the
source for slow_clk - and ends calling app_main.
Re-set timeout is needed due to WDT uses a SLOW_CLK clock source. After changing a frequency slow_clk a
time of WDT needs to re-set for new frequency.
slow_clk depends on RTC_CLK_SRC (INTERNAL_RC or EXTERNAL_CRYSTAL).

---

## CONFIG_BOOTLOADER_WDT_TIME_MS

**Timeout for RTC watchdog (ms)**

- **Type:** `unknown`
- **Default:** `9000`
- **Menu:** Bootloader config > Use RTC watchdog in start code
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:306`

Verify that this parameter is correct and more then the execution time.
Pay attention to options such as reset to factory, trigger test partition and encryption on boot
- these options can increase the execution time.
Note: RTC_WDT will reset while encryption operations will be performed.

---

## CONFIG_BOOT_ROM_LOG_ALWAYS_OFF

**Permanently disable logging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Boot ROM Behavior > Permanently change Boot ROM output
- **Kconfig:** `/opt/esp/idf/components/esp_rom/Kconfig.projbuild:19`

Don't print ROM logs.

---

## CONFIG_BOOT_ROM_LOG_ALWAYS_ON

**Always Log**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Boot ROM Behavior > Permanently change Boot ROM output
- **Kconfig:** `/opt/esp/idf/components/esp_rom/Kconfig.projbuild:15`

Always print ROM logs, this is the default behavior.

---

## CONFIG_BOOT_ROM_LOG_ON_GPIO_HIGH

**Log on GPIO High**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Boot ROM Behavior > Permanently change Boot ROM output
- **Kconfig:** `/opt/esp/idf/components/esp_rom/Kconfig.projbuild:23`

Print ROM logs when GPIO level is high during start up.
The GPIO number is chip dependent,
e.g. on ESP32-S2, the control GPIO is GPIO46.

---

## CONFIG_BOOT_ROM_LOG_ON_GPIO_LOW

**Log on GPIO Low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Boot ROM Behavior > Permanently change Boot ROM output
- **Kconfig:** `/opt/esp/idf/components/esp_rom/Kconfig.projbuild:29`

Print ROM logs when GPIO level is low during start up.
The GPIO number is chip dependent,
e.g. on ESP32-S2, the control GPIO is GPIO46.

---

## CONFIG_BT_A2DP_ENABLE

**A2DP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:84`

Advanced Audio Distribution Profile

---

## CONFIG_BT_A2DP_USE_EXTERNAL_CODEC

**Use External Codec for A2DP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > A2DP
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:92`

If enable, user shall register audio codec capability to A2DP and encode/decode
audio data in application layer. The internal codec in A2DP will be remove in
the future, it is recommend to use external codec for new design.

---

## CONFIG_BT_ABORT_WHEN_ALLOCATION_FAILS

**Abort when memory allocation fails in BT/BLE stack**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1510`

This enables abort when memory allocation fails

---

## CONFIG_BT_ACL_CONNECTIONS

**BT/BLE MAX ACL CONNECTIONS(1~9)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1212`

Maximum BT/BLE connection count. The ESP32-C3/S3 chip supports a maximum of 10 instances,
including ADV, SCAN and connections. The ESP32-C3/S3 chip can connect up to 9 devices if
ADV or SCAN uses only one. If ADV and SCAN are both used, The ESP32-C3/S3 chip is connected
to a maximum of 8 devices. Because Bluetooth cannot reclaim used instances once ADV or SCAN
is used.

---

## CONFIG_BT_ALARM_MAX_NUM

**Maximum number of Bluetooth alarms**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:1`

This option decides the maximum number of alarms which
could be used by Bluetooth host.

---

## CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST

**BT/BLE will first malloc the memory from the PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1231`

This select can save the internal RAM if there have the PSRAM

---

## CONFIG_BT_AVRCP_CT_COVER_ART_ENABLED

**AVRCP CT Cover Art**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > A2DP > AVRCP Features
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:112`

This enable Cover Art feature of AVRCP CT role

---

## CONFIG_BT_AVRCP_ENABLED

- **Type:** `string`
- **Default:** `n`

Audio/Video Remote Control Profile, AVRCP and A2DP are coupled in Bluedroid,
AVRCP still controlled by A2DP option, this is a dummy option currently

---

## CONFIG_BT_BLE_42_ADV_EN

**Enable BLE 4.2 advertising**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 4.2 features(please disable BLE 5.0 if enable BLE 4.2)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1370`

This enables BLE v4.2 advertising

---

## CONFIG_BT_BLE_42_DTM_TEST_EN

**Enable BLE 4.2 DTM test**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 4.2 features(please disable BLE 5.0 if enable BLE 4.2)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1363`

This enables BLE 4.2 direct test mode

---

## CONFIG_BT_BLE_42_FEATURES_SUPPORTED

**Enable BLE 4.2 features(please disable BLE 5.0 if enable BLE 4.2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1353`

This enables BLE 4.2 features.
This option is universally supported by all ESP chips with BLE capabilities.
BLE 4.2 and BLE 5.0 cannot be used simultaneously.

---

## CONFIG_BT_BLE_42_SCAN_EN

**Enable BLE 4.2 scan**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 4.2 features(please disable BLE 5.0 if enable BLE 4.2)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1377`

This enables BLE v4.2 scan

---

## CONFIG_BT_BLE_50_DTM_TEST_EN

**Enable BLE 5.0 DTM test**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1325`

This enables BLE 5.0 direct test mode

---

## CONFIG_BT_BLE_50_EXTEND_ADV_EN

**Enable BLE extend advertising**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1297`

This enables BLE extend advertising

---

## CONFIG_BT_BLE_50_EXTEND_SCAN_EN

**Enable BLE extend scan**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1311`

This enables BLE extend scan

---

## CONFIG_BT_BLE_50_EXTEND_SYNC_EN

**Enable BLE periodic advertising sync**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0) > Enable BLE extend scan
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1318`

This enables BLE periodic advertising sync

---

## CONFIG_BT_BLE_50_FEATURES_SUPPORTED

**Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1288`

Enabling this option activates BLE 5.0 features.
This option is universally supported in chips that support BLE, except for ESP32.
BLE 4.2 and BLE 5.0 cannot be used simultaneously.

---

## CONFIG_BT_BLE_50_PERIODIC_ADV_EN

**Enable BLE periodic advertising**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.0 features(please disable BLE 4.2 if enable BLE 5.0) > Enable BLE extend advertising
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1304`

This enables BLE periodic advertising

---

## CONFIG_BT_BLE_ACT_SCAN_REP_ADV_SCAN

**Report adv data and scan response individually when BLE active scan**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1258`

Originally, when doing BLE active scan, Bluedroid will not report adv to application layer
until receive scan response. This option is used to disable the behavior. When enable this option,
Bluedroid will report adv data or scan response to application layer immediately.

# Memory reserved at start of DRAM for Bluetooth stack

---

## CONFIG_BT_BLE_ADV_DATA_LENGTH_ZERO_AUX

**Enable aux packet when ext adv data length is zero**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:477`

When this option is enabled, auxiliary packets will be present in the events of
'Non-Connectable and Non-Scannable' regardless of whether the advertising length is 0.
If this option is not enabled, auxiliary packets will only be present when the advertising length is not 0.

---

## CONFIG_BT_BLE_BLUFI_ENABLE

**Include blufi function**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:294`

This option can be close when the app does not require blufi function.

---

## CONFIG_BT_BLE_CCA_MODE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_BLE_CCA_MODE_HW

**Hardware**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE CCA mode
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:92`

---

## CONFIG_BT_BLE_CCA_MODE_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE CCA mode
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:90`

---

## CONFIG_BT_BLE_CCA_MODE_SW

**Software (experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE CCA mode
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:94`

---

## CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY

**Use dynamic memory allocation in BT/BLE stack**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1238`

This select can make the allocation of memory will become more flexible

---

## CONFIG_BT_BLE_ENABLED

**Bluetooth Low Energy**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:271`

This enables Bluetooth Low Energy

---

## CONFIG_BT_BLE_ESTAB_LINK_CONN_TOUT

**Timeout of BLE connection establishment**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT client module(GATTC)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:406`

Bluetooth Connection establishment maximum time, if connection time exceeds this value, the connection
establishment fails, ESP_GATTC_OPEN_EVT or ESP_GATTS_OPEN_EVT is triggered.

---

## CONFIG_BT_BLE_FEAT_CONN_SUBRATING

**Enable BLE connection subrating feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1489`

Enable BLE connection subrating feature

---

## CONFIG_BT_BLE_FEAT_CREATE_SYNC_ENH

**Enable create sync enhancements(reporting disable and duplicate filtering enable support)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1346`

Enable the create sync enhancements

---

## CONFIG_BT_BLE_FEAT_CTE_CONNECTIONLESS_EN

**Enable BLE CTE connectionless feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE CTE feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1468`

Transmission of CTE in periodic advertising

---

## CONFIG_BT_BLE_FEAT_CTE_CONNECTION_EN

**Enable BLE CTE connection feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE CTE feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1475`

Transmission of CTE by ACL connection

---

## CONFIG_BT_BLE_FEAT_CTE_EN

**Enable BLE CTE feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1461`

Enable BLE 5.1 CTE

---

## CONFIG_BT_BLE_FEAT_ISO_60_EN

**Enable ISO v6.0 feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1391`

Enable iso 6.0 feature

---

## CONFIG_BT_BLE_FEAT_ISO_BIG_BROCASTER

**Enable BLE iso BIG brocaster**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1398`

Enable BLE 5.2 BIG brocaster

---

## CONFIG_BT_BLE_FEAT_ISO_BIG_SYNCER

**Enable BLE iso BIG syncer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1405`

Enable BLE 5.2 BIG syncer

---

## CONFIG_BT_BLE_FEAT_ISO_CIG_CENTRAL

**Enable BLE iso CIG central**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1420`

Enable BLE 5.2 CIG central

---

## CONFIG_BT_BLE_FEAT_ISO_CIG_PERIPHERAL

**Enable BLE iso CIG peripheral**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1427`

Enable BLE 5.2 CIG peripheral

---

## CONFIG_BT_BLE_FEAT_ISO_EN

**Enable BLE 5.2 iso feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1384`

Enable BLE 5.2 iso

---

## CONFIG_BT_BLE_FEAT_PERIODIC_ADV_ENH

**Enable periodic adv enhancements(adi support)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1339`

Enable the periodic advertising enhancements

---

## CONFIG_BT_BLE_FEAT_PERIODIC_ADV_SYNC_TRANSFER

**Enable BLE periodic advertising sync transfer feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1332`

This enables BLE periodic advertising sync transfer feature

---

## CONFIG_BT_BLE_FEAT_POWER_CONTROL

**Enable BLE power control feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1482`

Enable BLE power control feature

---

## CONFIG_BT_BLE_HIGH_DUTY_ADV_INTERVAL

**Enable BLE high duty advertising interval feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1503`

This enable BLE high duty advertising interval feature

---

## CONFIG_BT_BLE_ISO_BIS_MAX_COUNT

**Maximum bis count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1412`

Enable BLE 5.2 CIG peripheral

---

## CONFIG_BT_BLE_ISO_CIS_MAX_COUNT

**Maximum cis count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1434`

Enable BLE 5.2 CIG peripheral

---

## CONFIG_BT_BLE_ISO_NON_STD_FLOW_CTRL

**ISO non-standard flow control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature > Select ISO flow control type
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1454`

Enable ISO non-standard flow control

---

## CONFIG_BT_BLE_ISO_STD_FLOW_CTRL

**ISO standard flow control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Enable BLE 5.2 iso feature > Select ISO flow control type
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1449`

Enable ISO standard flow control

---

## CONFIG_BT_BLE_LOG_SPI_OUT_CS_IO_NUM

**GPIO number of SPI CS**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:114`

GPIO number of SPI CS

---

## CONFIG_BT_BLE_LOG_SPI_OUT_ENABLED

**Output ble logs to SPI bus (Experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:9`

Output ble logs to SPI bus

---

## CONFIG_BT_BLE_LOG_SPI_OUT_FLUSH_TIMEOUT

**Buffer flush out period in unit of ms**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable periodic buffer flush out
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:143`

Buffer flush out period in unit of ms

---

## CONFIG_BT_BLE_LOG_SPI_OUT_FLUSH_TIMER_ENABLED

**Enable periodic buffer flush out**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:135`

Enable periodic buffer flush out
Not recommended when SPI receiver is unavailable

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HCI_BUF_SIZE

**SPI transaction buffer size for HCI logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable HCI log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:30`

SPI transaction buffer size for HCI logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HCI_ENABLED

**Enable HCI log output to SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:23`

Enable logging of HCI packets to the SPI bus when BLE SPI log output is enabled.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HCI_TASK_CNT

**HCI task count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable HCI log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:38`

HCI task count

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HOST_BUF_SIZE

**SPI transaction buffer size for host logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable Host log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:53`

SPI transaction buffer size for host logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HOST_ENABLED

**Enable Host log output to SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:45`

This configuration applies to the logs of both Bluedroid Host and NimBLE Host.
When BLE SPI log output is enabled, this option allows host logs to be transmitted via SPI.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_HOST_TASK_CNT

**Host task count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable Host log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:61`

Host task count.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LE_AUDIO_BUF_SIZE

**SPI transaction buffer size for LE Audio logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable LE Audio log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:157`

SPI transaction buffer size for LE Audio logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LE_AUDIO_ENABLED

**Enable LE Audio log output to SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:150`

Enable LE Audio log output to SPI

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LE_AUDIO_TASK_CNT

**LE audio task count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable LE Audio log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:165`

LE audio task count

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LL_ENABLED

**Enable Controller log output to SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:68`

Enable controller log output to SPI bus.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LL_HCI_BUF_SIZE

**SPI transaction buffer size for lower layer HCI logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable Controller log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:92`

SPI transaction buffer size for upper layer HCI logs.
There will be 2 SPI DMA buffers with the same size

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LL_ISR_BUF_SIZE

**SPI transaction buffer size for lower layer ISR logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable Controller log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:84`

SPI transaction buffer size for lower layer ISR logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_LL_TASK_BUF_SIZE

**SPI transaction buffer size for lower layer task logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable Controller log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:76`

SPI transaction buffer size for lower layer task logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_MESH_BUF_SIZE

**SPI transaction buffer size for BLE mesh logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable BLE mesh log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:179`

SPI transaction buffer size for BLE mesh logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_SPI_OUT_MESH_ENABLED

**Enable BLE mesh log output to SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:172`

Enable BLE mesh log output to SPI

---

## CONFIG_BT_BLE_LOG_SPI_OUT_MESH_TASK_CNT

**Mesh task count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable BLE mesh log output to SPI
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:187`

Mesh task count

---

## CONFIG_BT_BLE_LOG_SPI_OUT_MOSI_IO_NUM

**GPIO number of SPI MOSI**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:100`

GPIO number of SPI MOSI

---

## CONFIG_BT_BLE_LOG_SPI_OUT_SCLK_IO_NUM

**GPIO number of SPI SCLK**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:107`

GPIO number of SPI SCLK

---

## CONFIG_BT_BLE_LOG_SPI_OUT_SYNC_IO_NUM

**GPIO number of SYNC IO**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental) > Enable ble log & logic analyzer log time sync
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:128`

GPIO number of SYNC IO

---

## CONFIG_BT_BLE_LOG_SPI_OUT_TS_SYNC_ENABLED

**Enable ble log & logic analyzer log time sync**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:121`

Enable ble log & logic analyzer log time sync

---

## CONFIG_BT_BLE_LOG_SPI_OUT_UL_TASK_BUF_SIZE

**SPI transaction buffer size for upper layer task logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs to SPI bus (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:15`

SPI transaction buffer size for upper layer task logs.
There will be 2 SPI DMA buffers with the same size.

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_ENABLED

**Output ble logs via UHCI (UART DMA) driver (Experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:194`

Output ble logs via UHCI (UART DMA) driver
On enable, BT_BLE_LOG_UHCI_OUT_UART_PORT would be reinited with
BT_BLE_LOG_UHCI_OUT_UART_BAUD_RATE as new baud rate and
BT_BLE_LOG_UHCI_OUT_UART_IO_NUM_TX as new UART Tx IO

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_LL_HCI_BUF_SIZE

**UHCI transaction buffer size for lower layer HCI logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:227`

UHCI transaction buffer size for lower layer HCI logs

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_LL_ISR_BUF_SIZE

**UHCI transaction buffer size for lower layer ISR logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:220`

UHCI transaction buffer size for lower layer ISR logs

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_LL_TASK_BUF_SIZE

**UHCI transaction buffer size for lower layer task logs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:213`

UHCI transaction buffer size for lower layer task logs

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_UART_BAUD_RATE

**Baud rate for BT_BLE_LOG_UHCI_OUT_UART_PORT**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental) > Enable to init UART port
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:241`

Baud rate for BT_BLE_LOG_UHCI_OUT_UART_PORT

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_UART_IO_NUM_TX

**IO number for UART TX port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental) > Enable to init UART port
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:248`

IO number for UART TX port

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_UART_NEED_INIT

**Enable to init UART port**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:234`

Enable to init UART port

---

## CONFIG_BT_BLE_LOG_UHCI_OUT_UART_PORT

**UART port connected to UHCI controller**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Common Options > Output ble logs via UHCI (UART DMA) driver (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/common/Kconfig.in:203`

UART port connected to UHCI controller
If UART port 0 is selected, UART VFS Driver, UART ROM Driver
and UART Driver output would be redirected to BLE Log UHCI Out
to solve UART Tx FIFO multi-task access issue

---

## CONFIG_BT_BLE_RPA_SUPPORTED

**Update RPA to Controller**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include BLE security module(SMP)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:448`

This enables controller RPA list function.
For ESP32, ESP32 only support network privacy mode. If this option is enabled, ESP32 will only accept
advertising packets from peer devices that contain private address, HW will not receive the advertising
packets contain identity address after IRK changed. If this option is disabled, address resolution will
be performed in the host, so the functions that require controller to resolve address in the white list
cannot be used. This option is disabled by default on ESP32, please enable or disable this option according
to your own needs.

For other BLE chips, devices support network privacy mode and device privacy mode,
users can switch the two modes according to their own needs. So this option is enabled by default.

---

## CONFIG_BT_BLE_RPA_TIMEOUT

**Timeout of resolvable private address**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1279`

This set RPA timeout of Controller and Host.
Default is 900 s (15 minutes). Range is 1 s to 1 hour (3600 s).

---

## CONFIG_BT_BLE_SMP_BOND_NVS_FLASH

**Save SMP bonding keys to nvs flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include BLE security module(SMP)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:441`

This select can save SMP bonding keys to nvs flash

---

## CONFIG_BT_BLE_SMP_ENABLE

**Include BLE security module(SMP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:415`

This option can be close when the app not used the ble security connect.

---

## CONFIG_BT_BLE_SMP_ID_RESET_ENABLE

**Reset device identity when all bonding records are deleted**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include BLE security module(SMP)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:430`

There are tracking risks associated with using a fixed or static IRK.
If enabled this option, Bluedroid will assign a new randomly-generated IRK
when all pairing and bonding records are deleted. This would decrease the ability
of a previously paired peer to be used to determine whether a device
with which it previously shared an IRK is within range.

---

## CONFIG_BT_BLE_VENDOR_HCI_EN

**Enable BLE Vendor HCI command and event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1496`

This enables BLE vendor HCI command and event

---

## CONFIG_BT_BLUEDROID_ENABLED

**Bluedroid - Dual-mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluetooth > Host
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:16`

This option is recommended for classic Bluetooth or for dual-mode
usecases

---

## CONFIG_BT_BLUEDROID_ESP_COEX_VSC

**Enable Espressif Vendor-specific HCI commands for coexist status configuration**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:43`

Enable Espressif Vendor-specific HCI commands for coexist status configuration

---

## CONFIG_BT_BLUEDROID_MEM_DEBUG

**Bluedroid memory debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:36`

Bluedroid memory debug

---

## CONFIG_BT_BLUEDROID_PINNED_TO_CORE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_BLUEDROID_PINNED_TO_CORE_0

**Core 0 (PRO CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > The cpu core which Bluedroid run
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:15`

---

## CONFIG_BT_BLUEDROID_PINNED_TO_CORE_1

**Core 1 (APP CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > The cpu core which Bluedroid run
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:17`

---

## CONFIG_BT_BTC_TASK_STACK_SIZE

**Bluetooth event (callback to application) task stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1`

This select btc task stack size

---

## CONFIG_BT_BTU_TASK_STACK_SIZE

**Bluetooth Bluedroid Host Stack task stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:29`

This select btu task stack size

---

## CONFIG_BT_CLASSIC_BQB_ENABLED

**Host Qualitifcation support for Classic Bluetooth**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:77`

This enables functionalities of Host qualification for Classic Bluetooth.

---

## CONFIG_BT_CLASSIC_ENABLED

**Classic Bluetooth**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:51`

For now this option needs "SMP_ENABLE" to be set to yes

---

## CONFIG_BT_CONTROLLER_DISABLED

**Disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluetooth > Controller
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:50`

This option is recommended for Bluetooth Host only usecases

---

## CONFIG_BT_CONTROLLER_ENABLED

**Enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluetooth > Controller
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:44`

This option is recommended for Bluetooth controller usecases

---

## CONFIG_BT_CONTROLLER_ONLY

**Disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluetooth > Host
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:27`

This option is recommended when you want to communicate directly with the
controller (without any host) or when you are using any other host stack
not supported by Espressif (not mentioned here).

---

## CONFIG_BT_CTRL_ADV_DUP_FILT_MAX

**The maximum number of 5.0 extend duplicate scan filter**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:74`

The maximum number of 5.0 extend duplicate

---

## CONFIG_BT_CTRL_AGC_RECORRECT_EN

**Enable HW AGC recorrect**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:456`

Enable uncoded phy AGC recorrect

---

## CONFIG_BT_CTRL_BLE_ADV

**Enable BLE ADV feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:554`

---

## CONFIG_BT_CTRL_BLE_ADV_REPORT_DISCARD_THRSHOLD

**BLE adv lost event threshold value**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > BLE adv report flow control supported
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:255`

When adv report flow control is enabled, The ADV lost event will be generated when the number
of ADV packets lost in the controller reaches this threshold. It is better to set a larger value.
If you set `BT_CTRL_BLE_ADV_REPORT_DISCARD_THRSHOLD` to a small value or printf every adv lost event, it
may cause adv packets lost more.

---

## CONFIG_BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM

**BLE adv report flow control number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > BLE adv report flow control supported
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:241`

The number of unprocessed advertising report that bluetooth host can save.If you set
`BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM` to a small value, this may cause adv packets lost.
If you set `BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM` to a large value, bluetooth host may cache a
lot of adv packets and this may cause system memory run out. For example, if you set
it to 50, the maximum memory consumed by host is 35 * 50 bytes. Please set
`BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM` according to your system free memory and handle adv
packets as fast as possible, otherwise it will cause adv packets lost.

---

## CONFIG_BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_SUPP

**BLE adv report flow control supported**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:232`

The function is mainly used to enable flow control for advertising reports. When it is enabled,
advertising reports will be discarded by the controller if the number of unprocessed advertising
reports exceeds the size of BLE adv report flow control.

---

## CONFIG_BT_CTRL_BLE_LLCP_CHAN_MAP_UPDATE

**BLE ACL channel map update procedure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE disconnects when Instant Passed (0x28) occurs
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:507`

If this option is enabled, Controller will terminate the connection
when Instant Passed (0x28) error occurs in channel map update procedure.

---

## CONFIG_BT_CTRL_BLE_LLCP_CONN_UPDATE

**BLE ACL connection update procedure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE disconnects when Instant Passed (0x28) occurs
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:500`

If this option is enabled, Controller will terminate the connection
when Instant Passed (0x28) error occurs during connection update procedure.

---

## CONFIG_BT_CTRL_BLE_LLCP_PHY_UPDATE

**BLE ACL PHY update procedure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE disconnects when Instant Passed (0x28) occurs
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:514`

If this option is enabled, Controller will terminate the connection
when Instant Passed (0x28) error occurs in PHY update procedure.

---

## CONFIG_BT_CTRL_BLE_MASTER

**Enable BLE connection feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:536`

If this option is disabled, it is not recommended to use connectable ADV.

---

## CONFIG_BT_CTRL_BLE_MAX_ACT

**BLE Max Instances**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:5`

BLE maximum activities of bluetooth controller，both of connections,
scan , sync and adv(periodic adv, multi-adv). Each instance needs to
consume 828 bytes, you can save RAM by modifying the instance value
according to actual needs.

---

## CONFIG_BT_CTRL_BLE_MAX_ACT_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_BLE_MESH_SCAN_DUPL_EN

**Special duplicate scan mechanism for BLE Mesh scan**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:336`

This enables the BLE scan duplicate for special BLE Mesh scan.

---

## CONFIG_BT_CTRL_BLE_SCAN

**Enable BLE scan feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:546`

---

## CONFIG_BT_CTRL_BLE_SCAN_DUPL

**BLE Scan Duplicate Options**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:266`

This select enables parameters setting of BLE scan duplicate.

---

## CONFIG_BT_CTRL_BLE_SECURITY_ENABLE

**Enable BLE security feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:550`

---

## CONFIG_BT_CTRL_BLE_STATIC_ACL_TX_BUF_NB

**BLE static ACL TX buffer numbers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:20`

BLE ACL buffer have two methods to be allocated. One is persistent allocating
(allocate when controller initialise, never free until controller de-initialise)
another is dynamically allocating (allocate before TX and free after TX).

---

## CONFIG_BT_CTRL_BLE_TEST

**Enable BLE QA test feature (Not Used)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:542`

---

## CONFIG_BT_CTRL_CE_LENGTH_TYPE_CE

**Use CE parameter for HCI command**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Connection event length determination method
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:128`

---

## CONFIG_BT_CTRL_CE_LENGTH_TYPE_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_CE_LENGTH_TYPE_ORIG

**ORIGINAL**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Connection event length determination method
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:126`

---

## CONFIG_BT_CTRL_CE_LENGTH_TYPE_SD

**Use Espressif self-defined method**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Connection event length determination method
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:130`

---

## CONFIG_BT_CTRL_CHAN_ASS_EN

**Enable channel assessment**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:485`

If this option is enabled, The Controller will records the communication quality
for each channel and then start a timer to check and update the channel map every 4 seconds.

---

## CONFIG_BT_CTRL_CHECK_CONNECT_IND_ACCESS_ADDRESS

**Enable enhanced Access Address check in CONNECT_IND**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:558`

Enabling this option will add stricter verification of the Access Address in the CONNECT_IND PDU.
This improves security by ensuring that only connection requests with valid Access Addresses are accepted.
If disabled, only basic checks are applied, improving compatibility.

---

## CONFIG_BT_CTRL_CODED_AGC_RECORRECT_EN

**Enable coded phy AGC recorrect**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Enable HW AGC recorrect
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:462`

Enable coded phy AGC recorrect

---

## CONFIG_BT_CTRL_COEX_PHY_CODED_TX_RX_TLIM_DIS

**Force Disable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Coexistence: limit on MAX Tx/Rx time for coded-PHY connection
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:365`

Disable the limitation on max tx/rx time for Coded-PHY connection

---

## CONFIG_BT_CTRL_COEX_PHY_CODED_TX_RX_TLIM_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_COEX_PHY_CODED_TX_RX_TLIM_EN

**Force Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Coexistence: limit on MAX Tx/Rx time for coded-PHY connection
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:360`

Always enable the limitation on max tx/rx time for Coded-PHY connection

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N0

**0dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:194`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N12

**-12dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:186`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N15

**-15dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:184`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N18

**-18dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:182`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N21

**-21dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:180`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N24

**-24dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:178`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N3

**-3dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:192`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N6

**-6dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:190`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_N9

**-9dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:188`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P12

**+12dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:202`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P15

**+15dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:204`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P18

**+18dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:206`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P20

**+20dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:208`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P3

**+3dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:196`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P6

**+6dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:198`

---

## CONFIG_BT_CTRL_DFT_TX_POWER_LEVEL_P9

**+9dBm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE default Tx power level
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:200`

---

## CONFIG_BT_CTRL_DTM_ENABLE

**Enable direct test mode feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:532`

---

## CONFIG_BT_CTRL_DUPL_SCAN_CACHE_REFRESH_PERIOD

**Duplicate scan list refresh period (seconds)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:320`

If the period value is non-zero, the controller will periodically clear the device information
stored in the scan duuplicate filter. If it is 0, the scan duuplicate filter will not be cleared
until the scanning is disabled. Duplicate advertisements for this period should not be sent to the
Host in advertising report events.
There are two scenarios where the ADV packet will be repeatedly reported:
1. The duplicate scan cache is full, the controller will delete the oldest device information and
add new device information.
2. When the refresh period is up, the controller will clear all device information and start filtering
again.

---

## CONFIG_BT_CTRL_HCI_MODE_UART_H4

**UART(H4)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > HCI mode
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:59`

If use external bluetooth host which run on other hardware and use UART as the HCI interface,
choose this option.

---

## CONFIG_BT_CTRL_HCI_MODE_VHCI

**VHCI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > HCI mode
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:54`

Normal option. Mostly, choose this VHCI when bluetooth host run on ESP32S3 or ESP32C3.

---

## CONFIG_BT_CTRL_HCI_TL

- **Type:** `unknown`
- **Default:** ``

HCI mode as VHCI or UART(H4)

---

## CONFIG_BT_CTRL_HCI_TL_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_HW_CCA_EFF

- **Type:** `unknown`
- **Default:** ``

If other devices are sending packets in the air and the signal is strong,
the packet hw to be sent this time is cancelled.

---

## CONFIG_BT_CTRL_HW_CCA_VAL

**CCA threshold value**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:104`

It is the threshold value of HW CCA, if the value is 30, it means CCA threshold is -30 dBm.
If the channel assessment result exceeds the CCA threshold (e.g. -75 dBm), indicating the channel is busy,
the hardware will not transmit packets on that channel.

---

## CONFIG_BT_CTRL_LE_HCI_LOG_EN

**Enable BLE HCI log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:572`

---

## CONFIG_BT_CTRL_LE_LOG_BUF1_SIZE

**The size of BLE log buffer1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:619`

---

## CONFIG_BT_CTRL_LE_LOG_BUF2_SIZE

**The size of BLE log buffer2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:629`

---

## CONFIG_BT_CTRL_LE_LOG_DUMP_ONLY

**Enable BLE log dump only**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:577`

---

## CONFIG_BT_CTRL_LE_LOG_EN

**Enable BLE debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental)
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:567`

---

## CONFIG_BT_CTRL_LE_LOG_HCI_BUF_SIZE

**The size of BLE log HCI buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:624`

---

## CONFIG_BT_CTRL_LE_LOG_LEVEL

**The level of BLE log**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:613`

---

## CONFIG_BT_CTRL_LE_LOG_MODE_EN

**Enable log for specified BLE mode**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:607`

---

## CONFIG_BT_CTRL_LE_LOG_PARTITION_SIZE

**The size of ble controller log partition(Multiples of 4K)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log > Enable BLE log storage to flash
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:587`

The size of ble controller log partition shall be a multiples of 4K.
The name of log partition shall be "bt_ctrl_log".
The partition type shall be ESP_PARTITION_TYPE_DATA.
The partition sub_type shall be ESP_PARTITION_SUBTYPE_ANY.

---

## CONFIG_BT_CTRL_LE_LOG_SPI_OUT_EN

**Output ble controller logs to SPI bus**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:597`

Output ble controller logs to SPI bus

---

## CONFIG_BT_CTRL_LE_LOG_STORAGE_EN

**Enable BLE log storage to flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > Controller debug log Options (Experimental) > Enable BLE debug log
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:582`

---

## CONFIG_BT_CTRL_LE_PING_EN

**Enable LE Ping procedure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:492`

If this option is disabled, The Controller will not start the LE authenticated payload timer.
This option is used for some compatibility problems related to LE ping procedure.

---

## CONFIG_BT_CTRL_LPCLK_SEL_EXT_32K_XTAL

**External 32kHz crystal/oscillator**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options > Bluetooth modem sleep > Bluetooth Modem sleep Mode 1 > Bluetooth low power clock
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:410`

External 32kHz crystal/oscillator has a nominal frequency of 32.768kHz and provides good frequency
stability. If used as Bluetooth low power clock, External 32kHz can support Bluetooth
modem sleep to be used with both DFS and light sleep.

---

## CONFIG_BT_CTRL_LPCLK_SEL_MAIN_XTAL

**Main crystal**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options > Bluetooth modem sleep > Bluetooth Modem sleep Mode 1 > Bluetooth low power clock
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:403`

Main crystal can be used as low power clock for bluetooth modem sleep. If this option is
selected, bluetooth modem sleep can work under Dynamic Frequency Scaling(DFS) enabled, and
bluetooth can work under light sleep enabled. Main crystal has a relatively better performance
than other bluetooth low power clock sources.

---

## CONFIG_BT_CTRL_LPCLK_SEL_RTC_SLOW

**Internal 136kHz RC oscillator**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options > Bluetooth modem sleep > Bluetooth Modem sleep Mode 1 > Bluetooth low power clock
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:418`

Internal 136kHz RC oscillator. The accuracy of this clock is a lot larger than 500ppm which is required
in Bluetooth communication, so don't select this option in scenarios such as BLE connection state.

---

## CONFIG_BT_CTRL_MAIN_XTAL_PU_DURING_LIGHT_SLEEP

**power up main XTAL during light sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:426`

If this option is selected, the main crystal will power up during light sleep when the low power clock
selects an external 32kHz crystal but the external 32kHz crystal does not exist or the low power clock
selects the main crystal.

---

## CONFIG_BT_CTRL_MESH_DUPL_SCAN_CACHE_SIZE

**Maximum number of Mesh adv packets in scan duplicate filter**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options > Special duplicate scan mechanism for BLE Mesh scan
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:343`

Maximum number of adv packets which can be recorded in duplicate scan cache for BLE Mesh.
When the maximum amount of device in the filter is reached, the cache will be refreshed.

---

## CONFIG_BT_CTRL_MODEM_SLEEP

**Bluetooth modem sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:380`

Enable/disable bluetooth controller low power mode.
Modem sleep is not supported to be used with UART HCI.

---

## CONFIG_BT_CTRL_MODEM_SLEEP_MODE_1

**Bluetooth Modem sleep Mode 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > MODEM SLEEP Options > Bluetooth modem sleep
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:388`

Mode 1 is the currently supported sleep mode. In this mode,
bluetooth controller sleeps between and BLE events. A low
power clock is used to maintain bluetooth reference clock.

---

## CONFIG_BT_CTRL_MODE_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_PINNED_TO_CORE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_PINNED_TO_CORE_0

**Core 0 (PRO CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > The cpu core which bluetooth controller run
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:36`

---

## CONFIG_BT_CTRL_PINNED_TO_CORE_1

**Core 1 (APP CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > The cpu core which bluetooth controller run
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:38`

---

## CONFIG_BT_CTRL_RUN_IN_FLASH_ONLY

**Put all BLE Controller code in flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:521`

If this option is enabled, all code for the Bluetooth controller will be moved from ROM and IRAM
to flash, saving over 20K bytes of memory. However, it will require more flash resources and the
performance of Bluetooth will decrease If this option is enabled, Bluetooth may not work properly
during erasing flash. It is recommended to turn on the auto suspend function of flash. After auto
suspend is turned on, Bluetooth interrupts can be executed normally during erasing flash, with less
impact on Bluetooth performance.

---

## CONFIG_BT_CTRL_RX_ANTENNA_INDEX_0

**Antenna 0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > default Rx anntena used
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:161`

---

## CONFIG_BT_CTRL_RX_ANTENNA_INDEX_1

**Antenna 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > default Rx anntena used
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:163`

---

## CONFIG_BT_CTRL_RX_ANTENNA_INDEX_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_SCAN_BACKOFF_UPPERLIMITMAX

**Disable active scan backoff**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:469`

Disable active scan backoff. The bluetooth spec requires that scanners should run a backoff procedure to
minimize collision of scan request PDUs from multiple scanners. If scan backoff is disabled, in active
scanning, scan request PDU will be sent every time when HW receives scannable ADV PDU.

---

## CONFIG_BT_CTRL_SCAN_DUPL_CACHE_SIZE

**Maximum number of devices in scan duplicate filter**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:311`

Maximum number of devices which can be recorded in scan duplicate filter.
When the maximum amount of device in the filter is reached, the cache will be refreshed.

---

## CONFIG_BT_CTRL_SCAN_DUPL_TYPE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_SCAN_DUPL_TYPE_DATA

**Scan Duplicate By Advertising Data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options > Scan Duplicate Type
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:290`

Advertising packets with identical advertising data, address type, and advertising type
are reported only once, even if they originate from different devices.

---

## CONFIG_BT_CTRL_SCAN_DUPL_TYPE_DATA_DEVICE

**Scan Duplicate By Device Address And Advertising Data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options > Scan Duplicate Type
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:296`

Advertising packets with the same address, advertising data, address type,
and advertising type are reported only once.

---

## CONFIG_BT_CTRL_SCAN_DUPL_TYPE_DEVICE

**Scan Duplicate By Device Address**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > BLE Scan Duplicate Options > Scan Duplicate Type
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:285`

Advertising packets with the same address, address type, and advertising type are reported once.

---

## CONFIG_BT_CTRL_SLEEP_CLOCK_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_SLEEP_MODE_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_CTRL_TX_ANTENNA_INDEX_0

**Antenna 0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > default Tx anntena used
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:145`

---

## CONFIG_BT_CTRL_TX_ANTENNA_INDEX_1

**Antenna 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Controller Options > default Tx anntena used
- **Kconfig:** `/opt/esp/idf/components/bt/controller/esp32c3/Kconfig.in:147`

---

## CONFIG_BT_CTRL_TX_ANTENNA_INDEX_EFF

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_ENABLED

**Bluetooth**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:3`

Select this option to enable Bluetooth and show the submenu with Bluetooth configuration choices.

---

## CONFIG_BT_ENC_KEY_SIZE_CTRL_NONE

**Not supported**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > configure encryption key size
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:73`

---

## CONFIG_BT_ENC_KEY_SIZE_CTRL_STD

**Supported by standard HCI command**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > configure encryption key size
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:65`

---

## CONFIG_BT_ENC_KEY_SIZE_CTRL_VSC

**Supported by Vendor-specific HCI command**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > configure encryption key size
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:69`

---

## CONFIG_BT_GATTC_CACHE_NVS_FLASH

**Save gattc cache data to nvs flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT client module(GATTC)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:391`

This select can save gattc cache data to nvs flash

---

## CONFIG_BT_GATTC_CONNECT_RETRY_COUNT

**The number of attempts to reconnect if the connection establishment failed**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT client module(GATTC)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:398`

The number of attempts to reconnect if the connection establishment failed

---

## CONFIG_BT_GATTC_ENABLE

**Include GATT client module(GATTC)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:368`

This option can be close when the app work only on gatt server mode

---

## CONFIG_BT_GATTC_MAX_CACHE_CHAR

**Max gattc cache characteristic for discover**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT client module(GATTC)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:375`

Maximum GATTC cache characteristic count

---

## CONFIG_BT_GATTC_NOTIF_REG_MAX

**Max gattc notify(indication) register number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT client module(GATTC)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:383`

Maximum GATTC notify(indication) register number

---

## CONFIG_BT_GATTS_APPEARANCE_WRITABLE

**Allow to write appearance by GATT clients**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:361`

Enabling this option allows remote GATT clients to write appearance

---

## CONFIG_BT_GATTS_DEVICE_NAME_WRITABLE

**Allow to write device name by GATT clients**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:354`

Enabling this option allows remote GATT clients to write device name

---

## CONFIG_BT_GATTS_ENABLE

**Include GATT server module(GATTS)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:278`

This option can be disabled when the app work only on gatt client mode

---

## CONFIG_BT_GATTS_PPCP_CHAR_GAP

**Enable Peripheral Preferred Connection Parameters characteristic in GAP service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:285`

This enables "Peripheral Preferred Connection Parameters" characteristic (UUID: 0x2A04)
in GAP service that has connection parameters like min/max connection interval, slave
latency and supervision timeout multiplier

---

## CONFIG_BT_GATTS_ROBUST_CACHING_ENABLED

**Enable Robust Caching on Server Side**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:344`

This option enables the GATT robust caching feature on the server.
if turned on, the Client Supported Features characteristic, Database Hash characteristic,
and Server Supported Features characteristic will be included in the GAP SERVICE.

---

## CONFIG_BT_GATTS_SEND_SERVICE_CHANGE_AUTO

**GATTS automatically send service change indication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS) > GATTS Service Change Mode
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:330`

Let Bluedroid handle the service change indication internally

---

## CONFIG_BT_GATTS_SEND_SERVICE_CHANGE_MANUAL

**GATTS manually send service change indication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS) > GATTS Service Change Mode
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:325`

Manually send service change indication through API esp_ble_gatts_send_service_change_indication()

---

## CONFIG_BT_GATTS_SEND_SERVICE_CHANGE_MODE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_GATT_MAX_SR_ATTRIBUTES

**Max GATT Service Attributes**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:309`

Maximum GATT Service Attributes Count

---

## CONFIG_BT_GATT_MAX_SR_PROFILES

**Max GATT Server Profiles**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include GATT server module(GATTS)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:301`

Maximum GATT Server Profiles Count

---

## CONFIG_BT_GOEPC_ENABLED

- **Type:** `string`
- **Default:** `n`

This enables the BT GOEP Profile Client role

---

## CONFIG_BT_HCI_LOG_ADV_BUFFER_SIZE

**Size of the cache used for adv report in Bluetooth HCI debug mode (N*1024 bytes)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Enable Bluetooth HCI debug mode
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:103`

This option is to configure the buffer size of the hci adv report cache in hci debug mode.
This is a ring buffer, the new data will overwrite the oldest data if the buffer is full.

---

## CONFIG_BT_HCI_LOG_DATA_BUFFER_SIZE

**Size of the cache used for HCI data in Bluetooth HCI debug mode (N*1024 bytes)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Enable Bluetooth HCI debug mode
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:94`

This option is to configure the buffer size of the hci data steam cache in hci debug mode.
This is a ring buffer, the new data will overwrite the oldest data if the buffer is full.

---

## CONFIG_BT_HCI_LOG_DEBUG_EN

**Enable Bluetooth HCI debug mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:87`

This option is used to enable bluetooth debug mode, which saves the hci layer data stream.

---

## CONFIG_BT_HFP_AG_ENABLE

**Audio Gateway**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:177`

---

## CONFIG_BT_HFP_AUDIO_DATA_PATH_HCI

**HCI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile > audio(SCO) data path
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:192`

---

## CONFIG_BT_HFP_AUDIO_DATA_PATH_PCM

**PCM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile > audio(SCO) data path
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:190`

---

## CONFIG_BT_HFP_CLIENT_ENABLE

**Hands Free Unit**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:172`

---

## CONFIG_BT_HFP_ENABLE

**Hands Free/Handset Profile**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:164`

Hands Free Unit and Audio Gateway can be included simultaneously
but they cannot run simultaneously due to internal limitations.

---

## CONFIG_BT_HFP_USE_EXTERNAL_CODEC

**Use External Codec for HFP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:196`

If enable, user shall encode/decode audio data in application layer. The internal
codec in HFP will be remove in the future, it is recommend to use external codec
for new design.

---

## CONFIG_BT_HFP_WBS_ENABLE

**Wide Band Speech**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Hands Free/Handset Profile
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:205`

This enables Wide Band Speech. Should disable it when SCO data path is PCM.
Otherwise there will be no data transmitted via GPIOs.

---

## CONFIG_BT_HID_DEVICE_ENABLED

**Classic BT HID Device**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Classic BT HID
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:227`

This enables the BT HID Device

---

## CONFIG_BT_HID_ENABLED

**Classic BT HID**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:213`

This enables the BT HID functionalities

---

## CONFIG_BT_HID_HOST_ENABLED

**Classic BT HID Host**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Classic BT HID
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:220`

This enables the BT HID Host

---

## CONFIG_BT_HID_REMOVE_DEVICE_BONDING_ENABLED

**Remove Device Bonding Information when HID Virtual Cable Unplugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > Classic BT HID
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:233`

This enables the BT HID to remove device bonding information when virtual cable unplugging,
removing device bonding information is optional in HID 1.0 but mandatory in HID 1.1

---

## CONFIG_BT_L2CAP_ENABLED

**BT L2CAP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:128`

This enables the Logical Link Control and Adaptation Layer Protocol.
Only supported classic bluetooth.

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:768`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:772`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:764`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:770`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:762`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:774`

---

## CONFIG_BT_LOG_A2D_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > A2D layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:766`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:978`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:982`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:974`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:980`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:972`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:984`

---

## CONFIG_BT_LOG_APPL_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > APPL layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:976`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:838`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:842`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:834`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:840`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:832`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:844`

---

## CONFIG_BT_LOG_AVCT_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVCT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:836`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:803`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:807`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:799`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:805`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:797`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:809`

---

## CONFIG_BT_LOG_AVDT_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVDT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:801`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:873`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:877`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:869`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:875`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:867`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:879`

---

## CONFIG_BT_LOG_AVRC_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > AVRC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:871`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1188`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1192`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1184`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1190`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1182`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1194`

---

## CONFIG_BT_LOG_BLUFI_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BLUFI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1186`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:698`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:702`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:694`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:700`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:692`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:704`

---

## CONFIG_BT_LOG_BNEP_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BNEP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:696`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1118`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1122`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1114`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1120`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1112`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1124`

---

## CONFIG_BT_LOG_BTC_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTC layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1116`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1083`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1087`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1079`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1085`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1077`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1089`

---

## CONFIG_BT_LOG_BTIF_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTIF layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1081`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:523`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:527`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:519`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:525`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:517`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:529`

---

## CONFIG_BT_LOG_BTM_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > BTM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:521`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:663`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:667`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:659`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:665`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:657`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:669`

---

## CONFIG_BT_LOG_GAP_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:661`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1013`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1017`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1009`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1015`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1007`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1019`

---

## CONFIG_BT_LOG_GATT_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > GATT layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1011`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:488`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:492`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:484`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:490`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:482`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:494`

---

## CONFIG_BT_LOG_HCI_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HCI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:486`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:943`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:947`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:939`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:945`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:937`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:949`

---

## CONFIG_BT_LOG_HID_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > HID layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:941`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:558`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:562`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:554`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:560`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:552`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:564`

---

## CONFIG_BT_LOG_L2CAP_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > L2CAP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:556`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:908`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:912`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:904`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:910`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:902`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:914`

---

## CONFIG_BT_LOG_MCA_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > MCA layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:906`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1153`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1157`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1149`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1155`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1147`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1159`

---

## CONFIG_BT_LOG_OSI_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > OSI layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1151`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:733`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:737`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:729`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:735`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:727`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:739`

---

## CONFIG_BT_LOG_PAN_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > PAN layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:731`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:593`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:597`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:589`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:595`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:587`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:599`

---

## CONFIG_BT_LOG_RFCOMM_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > RFCOMM layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:591`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:628`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:632`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:624`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:630`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:622`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:634`

---

## CONFIG_BT_LOG_SDP_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SDP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:626`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_API

**API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1048`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_DEBUG

**DEBUG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1052`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_ERROR

**ERROR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1044`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_EVENT

**EVENT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1050`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_NONE

**NONE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1042`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_VERBOSE

**VERBOSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1054`

---

## CONFIG_BT_LOG_SMP_TRACE_LEVEL_WARNING

**WARNING**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > BT DEBUG LOG LEVEL > SMP layer
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1046`

---

## CONFIG_BT_MAX_DEVICE_NAME_LEN

**length of bluetooth device name**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1269`

Bluetooth Device name length shall be no larger than 248 octets, If the broadcast data cannot contain
the complete device name, then only the shortname will be displayed, the rest parts that can't fit in
will be truncated.

---

## CONFIG_BT_MULTI_CONNECTION_ENBALE

**Enable BLE multi-connections**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1224`

Enable this option if there are multiple connections

---

## CONFIG_BT_NIMBLE_50_FEATURE_SUPPORT

**Enable BLE 5 feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:600`

Enable BLE 5 feature

---

## CONFIG_BT_NIMBLE_ANS_SERVICE

**Alert Notification service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:849`

Enable Alert Notification Service support

---

## CONFIG_BT_NIMBLE_AOA_AOD

**Direction Finding**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:732`

Enable support for Connectionless and Connection Oriented Direction Finding

---

## CONFIG_BT_NIMBLE_ATT_MAX_PREP_ENTRIES

**Max Prepare write entries**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:298`

This is the default value of ATT Maximum prepare entries

---

## CONFIG_BT_NIMBLE_ATT_PREFERRED_MTU

**Preferred MTU size in octets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:290`

This is the default value of ATT MTU indicated by the device during an ATT MTU exchange.
This value can be changed using API ble_att_set_preferred_mtu()

---

## CONFIG_BT_NIMBLE_BAS_SERVICE

**Battery service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:923`

Enable Battery service support

---

## CONFIG_BT_NIMBLE_BLE_GATT_BLOB_TRANSFER

**Blob transfer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:834`

This option is used when data to be sent is more than 512 bytes. For peripheral role,
BT_NIMBLE_MSYS_1_BLOCK_COUNT needs to be increased according to the need.

---

## CONFIG_BT_NIMBLE_BLE_POWER_CONTROL

**Enable support for BLE Power Control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:725`

Set this option to enable the Power Control feature

---

## CONFIG_BT_NIMBLE_BLUFI_ENABLE

**Enable blufi functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:814`

Set this option to enable blufi functionality.

---

## CONFIG_BT_NIMBLE_CRYPTO_STACK_MBEDTLS

**Override TinyCrypt with mbedTLS for crypto computations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:550`

Enable this option to choose mbedTLS instead of TinyCrypt for crypto
computations.

---

## CONFIG_BT_NIMBLE_CTS_SERVICE

**Current Time Service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:855`

Enable Current Time Service support

---

## CONFIG_BT_NIMBLE_DEBUG

**Enable extra runtime asserts and host debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:261`

This enables extra runtime asserts and host debugging

---

## CONFIG_BT_NIMBLE_DIS_SERVICE

**Device Information Service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:935`

Enable DIS service support

---

## CONFIG_BT_NIMBLE_DYNAMIC_SERVICE

**Enable dynamic services**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:268`

This enables user to add/remove Gatt services at runtime

---

## CONFIG_BT_NIMBLE_EATT_CHAN_NUM

**Maximum number of EATT channels**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1363`

Defines the number of channels EATT bearers can use

---

## CONFIG_BT_NIMBLE_ENABLED

**NimBLE - BLE only**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluetooth > Host
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:22`

This option is recommended for BLE only usecases to save on memory

---

## CONFIG_BT_NIMBLE_ENABLE_CONN_REATTEMPT

**Enable connection reattempts on connection establishment error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:574`

Enable to make the NimBLE host to reattempt GAP connection on connection
establishment failure.

---

## CONFIG_BT_NIMBLE_ENABLE_PERIODIC_ADV

**Enable periodic advertisement.**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:662`

Enable this option to start periodic advertisement.

---

## CONFIG_BT_NIMBLE_ENABLE_PERIODIC_SYNC

**Enable periodic sync**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended scanning
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:697`

Enable this option to receive periodic advertisement.

---

## CONFIG_BT_NIMBLE_ENC_ADV_DATA

**Encrypted Advertising Data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1195`

This option is used to enable encrypted advertising data.

---

## CONFIG_BT_NIMBLE_EXT_ADV

**Enable extended advertising**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:622`

Enable this option to do extended advertising. Extended advertising
will be supported from BLE 5.0 onwards.

---

## CONFIG_BT_NIMBLE_EXT_ADV_MAX_SIZE

**Maximum length of the advertising data.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:652`

Defines the length of the extended adv data. The value should not
exceed 1650.

---

## CONFIG_BT_NIMBLE_EXT_ADV_V2

**Enable support for extended adv v2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:631`

Enable this option to use Extended Adv V2 command instead of V1.

---

## CONFIG_BT_NIMBLE_EXT_SCAN

**Enable extended scanning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:690`

Enable this option to do extended scanning.

---

## CONFIG_BT_NIMBLE_GAP_DEVICE_NAME_MAX_LEN

**Maximum length of BLE device name in octets**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:283`

Device Name characteristic value shall be 0 to 248 octets in length

---

## CONFIG_BT_NIMBLE_GAP_SERVICE

**GAP Service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:997`

Enable GAP Service support

---

## CONFIG_BT_NIMBLE_GATTC_AUTO_PAIR

**Automatically pair upon receiving service request failure**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1237`

If enabled, when a service request (e.g. read, write) to a server fails, and the ATT
error suggests insufficient security, then the central will initiate pairing and retry
the service request.

---

## CONFIG_BT_NIMBLE_GATTC_PROC_PREEMPTION_PROTECT

**Gatt-proc preemption protect check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1228`

When BLE and Wireless protocol/IEEE 802.15.4 operate in coexistence, BLE preemption
can disrupt the GATT context,causing the service discovery callback to not be invoked.
A temporary list is maintained to preserve the GATT context and use it in case of preemption.

---

## CONFIG_BT_NIMBLE_GATT_CACHING

**Enable GATT caching**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:740`

Enable GATT caching

---

## CONFIG_BT_NIMBLE_GATT_CACHING_DISABLE_AUTO

**Do not start discovery procedure automatically upon receiving Out of Sync**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:791`

When client receives ATT out-of-sync error message, it will not automatically start the discovery procedure
to correct the invalid cache.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_INCLUDE_SERVICES

**Include services in GATT caching**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable GATT caching
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:746`

Enable this option to include *included services* (e.g., services referenced by other services)
in the GATT database cache. Disabling this will skip caching of included service entries.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_MAX_CHRS

**Maximum number of characteristics per connection**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:779`

Set this option to set the upper limit on number of characteristics per connection to be cached.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_MAX_CONNS

**Maximum connections to be cached**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:761`

Set this option to set the upper limit on number of connections to be cached.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_MAX_DSCS

**Maximum number of descriptors per connection**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:785`

Set this option to set the upper limit on number of descriptors per connection to be cached.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_MAX_INCL_SVCS

**Maximum number of included services per connection**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:773`

Set this option to set the upper limit on number of included services per connection to be cached.

---

## CONFIG_BT_NIMBLE_GATT_CACHING_MAX_SVCS

**Maximum number of services per connection**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:767`

Set this option to set the upper limit on number of services per connection to be cached.

---

## CONFIG_BT_NIMBLE_GATT_CLIENT

**Enable BLE GATT Client support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:168`

Enables support for GATT Client

---

## CONFIG_BT_NIMBLE_GATT_MAX_PROCS

**Maximum number of GATT client procedures**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:406`

Maximum number of GATT client procedures that can be executed.

---

## CONFIG_BT_NIMBLE_GATT_SERVER

**Enable BLE GATT Server support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:175`

Enables support for GATT Server

---

## CONFIG_BT_NIMBLE_HANDLE_REPEAT_PAIRING_DELETION

**Enable stack handling of repeat pairing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:590`

Use this option to let stack internally handle the request for repeat pairing.
Enabling this option will delete the pairing of the device and stack will NOT post any event
to application. If this option is disabled, application will get BLE_GAP_EVENT_REPEAT_PAIRING
event.

---

## CONFIG_BT_NIMBLE_HCI_UART_BAUDRATE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_HCI_UART_CTS_PIN

**UART Cts Pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1356`

UART HCI CTS pin

---

## CONFIG_BT_NIMBLE_HCI_UART_FLOW_CTRL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_HCI_UART_RTS_PIN

**UART Rts Pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1350`

UART HCI RTS pin

---

## CONFIG_BT_NIMBLE_HID_SERVICE

**Human Interface Device service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:903`

Enable HID service support

---

## CONFIG_BT_NIMBLE_HIGH_DUTY_ADV_ITVL

**Enable BLE high duty advertising interval feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1207`

This enable BLE high duty advertising interval feature

---

## CONFIG_BT_NIMBLE_HOST_ALLOW_CONNECT_WITH_SCAN

**Allow Connections with scanning in progress**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1213`

This enables support for user to initiate a new connection with scan in progress

---

## CONFIG_BT_NIMBLE_HOST_BASED_PRIVACY

**Enable host based privacy for random address.**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:566`

Use this option to do host based Random Private Address resolution.
If this option is disabled then controller based privacy is used.

---

## CONFIG_BT_NIMBLE_HOST_QUEUE_CONG_CHECK

**BLE queue congestion check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1219`

When scanning and scan duplicate is not enabled, if there are a lot of adv packets around
or application layer handling adv packets is slow, it will cause the controller memory
to run out. if enabled, adv packets will be lost when host queue is congested.

---

## CONFIG_BT_NIMBLE_HOST_TASK_STACK_SIZE

**NimBLE Host task stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:132`

This configures stack size of NimBLE host task

---

## CONFIG_BT_NIMBLE_HR_SERVICE

**Heart Rate service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:897`

Enable HeartRate Service support

---

## CONFIG_BT_NIMBLE_HS_FLOW_CTRL

**Enable Host Flow control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:413`

Enable Host Flow control

---

## CONFIG_BT_NIMBLE_HS_FLOW_CTRL_ITVL

**Host Flow control interval**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable Host Flow control
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:421`

Host flow control interval in msecs

---

## CONFIG_BT_NIMBLE_HS_FLOW_CTRL_THRESH

**Host Flow control threshold**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable Host Flow control
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:428`

Host flow control threshold, if the number of free buffers are at or
below this threshold, send an immediate number-of-completed-packets
event

---

## CONFIG_BT_NIMBLE_HS_FLOW_CTRL_TX_ON_DISCONNECT

**Host Flow control on disconnect**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable Host Flow control
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:437`

Enable this option to send number-of-completed-packets event to
controller after disconnection

---

## CONFIG_BT_NIMBLE_HS_STOP_TIMEOUT_MS

**BLE host stop timeout in msec**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:559`

BLE Host stop procedure timeout in milliseconds.

---

## CONFIG_BT_NIMBLE_HTP_SERVICE

**Health Thermometer service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:861`

Enable Health Thermometer Service support

---

## CONFIG_BT_NIMBLE_IAS_SERVICE

**Immediate Alert service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:879`

Enable Immediate Alert Service support

---

## CONFIG_BT_NIMBLE_INCL_SVC_DISCOVERY

**Enable Included service discovery**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:753`

Enable this option to start discovery for included service.

---

## CONFIG_BT_NIMBLE_IPSS_SERVICE

**Internet Protocol Support service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:867`

Enable Internet Protocol Service support

---

## CONFIG_BT_NIMBLE_L2CAP_COC_MAX_NUM

**Maximum number of connection oriented channels**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:95`

Defines maximum number of BLE Connection Oriented Channels. When set to (0), BLE COC is not compiled in

---

## CONFIG_BT_NIMBLE_L2CAP_COC_SDU_BUFF_COUNT

**L2cap coc Service Data Unit Buffer count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:397`

This is the service data unit buffer count for l2cap coc.

---

## CONFIG_BT_NIMBLE_L2CAP_ENHANCED_COC

**L2CAP Enhanced Connection Oriented Channel**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:103`

Enable Enhanced Credit Based Flow Control Mode

---

## CONFIG_BT_NIMBLE_LEGACY_VHCI_ENABLE

- **Type:** `string`
- **Default:** `n`

This option is used to distinguish whether a previous version of VHCI is being used

---

## CONFIG_BT_NIMBLE_LLS_SERVICE

**Link Loss service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:885`

Enable Link Loss Service support

---

## CONFIG_BT_NIMBLE_LL_CFG_FEAT_LE_2M_PHY

**Enable 2M Phy**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:608`

Enable 2M-PHY

---

## CONFIG_BT_NIMBLE_LL_CFG_FEAT_LE_CODED_PHY

**Enable coded Phy**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:615`

Enable coded-PHY

---

## CONFIG_BT_NIMBLE_LL_CFG_FEAT_LE_ENCRYPTION

**Enable LE encryption**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:229`

Enable encryption connection

---

## CONFIG_BT_NIMBLE_LOG_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_LOG_LEVEL_DEBUG

**Debug logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > NimBLE Host log verbosity
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:53`

---

## CONFIG_BT_NIMBLE_LOG_LEVEL_ERROR

**Error logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > NimBLE Host log verbosity
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:47`

---

## CONFIG_BT_NIMBLE_LOG_LEVEL_INFO

**Info logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > NimBLE Host log verbosity
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:51`

---

## CONFIG_BT_NIMBLE_LOG_LEVEL_NONE

**No logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > NimBLE Host log verbosity
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:45`

---

## CONFIG_BT_NIMBLE_LOG_LEVEL_WARNING

**Warning logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > NimBLE Host log verbosity
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:49`

---

## CONFIG_BT_NIMBLE_MAX_BONDS

**Maximum number of bonds to save across reboots**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:81`

Defines maximum number of bonds to save for peer security and our security

---

## CONFIG_BT_NIMBLE_MAX_CCCDS

**Maximum number of CCC descriptors to save across reboots**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:88`

Defines maximum number of CCC descriptors to save

---

## CONFIG_BT_NIMBLE_MAX_CONNECTIONS

**Maximum number of concurrent connections**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:65`

Defines maximum number of concurrent BLE connections. For ESP32, user
is expected to configure BTDM_CTRL_BLE_MAX_CONN from controller menu
along with this option. Similarly for ESP32-C3 or ESP32-S3, user is expected to
configure BT_CTRL_BLE_MAX_ACT from controller menu.
For ESP32C2, ESP32C6 and ESP32H2, each connection will take about 1k DRAM.

---

## CONFIG_BT_NIMBLE_MAX_CONN_REATTEMPT

**Maximum number connection reattempts**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable connection reattempts on connection establishment error
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:582`

Defines maximum number of connection reattempts.

---

## CONFIG_BT_NIMBLE_MAX_EADS

**Maximum number of EAD devices to save across reboots**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Encrypted Advertising Data
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1200`

Defines maximum number of encrypted advertising data key material to save

---

## CONFIG_BT_NIMBLE_MAX_EXT_ADV_INSTANCES

**Maximum number of extended advertising instances.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:638`

Change this option to set maximum number of extended advertising
instances. Minimum there is always one instance of
advertising. Enter how many more advertising instances you
want.
For ESP32C2, ESP32C6 and ESP32H2, each extended advertising instance
will take about 0.5k DRAM.

---

## CONFIG_BT_NIMBLE_MAX_PERIODIC_ADVERTISER_LIST

**Maximum number of periodic advertiser list**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended scanning > Enable periodic sync
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:716`

Set this option to set the upper limit for number of periodic advertiser list.

---

## CONFIG_BT_NIMBLE_MAX_PERIODIC_SYNCS

**Maximum number of periodic advertising syncs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended scanning > Enable periodic sync
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:705`

Set this option to set the upper limit for number of periodic sync
connections. This should be less than maximum connections allowed by
controller.

---

## CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_DEFAULT

**Default alloc mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:22`

---

## CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_EXTERNAL

**External SPIRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:18`

---

## CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_INTERNAL

**Internal memory**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:15`

---

## CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_IRAM_8BIT

**Internal IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:25`

Allows to use IRAM memory region as 8bit accessible region.

Every unaligned (8bit or 16bit) access will result in an exception
and incur penalty of certain clock cycles per unaligned read/write.

---

## CONFIG_BT_NIMBLE_MESH

**Enable BLE mesh functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:453`

Enable BLE Mesh example present in upstream mynewt-nimble and not maintained by Espressif.

IDF maintains ESP-BLE-MESH as the official Mesh solution. Please refer to ESP-BLE-MESH guide at:
`https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/esp-ble-mesh/ble-mesh-index.html`

---

## CONFIG_BT_NIMBLE_MESH_DEVICE_NAME

**Set mesh device name**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:529`

This value defines Bluetooth Mesh device/node name

---

## CONFIG_BT_NIMBLE_MESH_FRIEND

**Enable mesh friend functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:522`

Enable this option to be able to act as a Friend Node

---

## CONFIG_BT_NIMBLE_MESH_GATT_PROXY

**Enable GATT Proxy functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:498`

This option enables support for the Mesh GATT Proxy Service,
i.e. the ability to act as a proxy between a Mesh GATT Client
and a Mesh network

---

## CONFIG_BT_NIMBLE_MESH_LOW_POWER

**Enable mesh low power mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:515`

Enable this option to be able to act as a Low Power Node

---

## CONFIG_BT_NIMBLE_MESH_NODE_COUNT

**Set mesh node count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:536`

Defines mesh node count.

---

## CONFIG_BT_NIMBLE_MESH_PB_ADV

**Enable mesh provisioning over advertising bearer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality > Enable BLE mesh provisioning
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:480`

Enable this option to allow the device to be provisioned over
the advertising bearer

---

## CONFIG_BT_NIMBLE_MESH_PB_GATT

**Enable mesh provisioning over GATT bearer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality > Enable BLE mesh provisioning
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:489`

Enable this option to allow the device to be provisioned over the GATT
bearer

---

## CONFIG_BT_NIMBLE_MESH_PROV

**Enable BLE mesh provisioning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:473`

Enable mesh provisioning

---

## CONFIG_BT_NIMBLE_MESH_PROVISIONER

**Enable BLE mesh provisioner**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:543`

Enable mesh provisioner.

---

## CONFIG_BT_NIMBLE_MESH_PROXY

**Enable mesh proxy functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:464`

Enable proxy. This is automatically set whenever NIMBLE_MESH_PB_GATT or
NIMBLE_MESH_GATT_PROXY is set

---

## CONFIG_BT_NIMBLE_MESH_RELAY

**Enable mesh relay functionality**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE mesh functionality
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:508`

Support for acting as a Mesh Relay Node

---

## CONFIG_BT_NIMBLE_MSYS_1_BLOCK_COUNT

**MSYS_1 Block Count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:314`

MSYS is a system level mbuf registry. For prepare write & prepare
responses MBUFs are allocated out of msys_1 pool. For NIMBLE_MESH
enabled cases, this block count is increased by 8 than user defined
count.

---

## CONFIG_BT_NIMBLE_MSYS_1_BLOCK_SIZE

**MSYS_1 Block Size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:324`

Dynamic memory size of block 1

---

## CONFIG_BT_NIMBLE_MSYS_2_BLOCK_COUNT

**MSYS_2 Block Count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:331`

Dynamic memory count

---

## CONFIG_BT_NIMBLE_MSYS_2_BLOCK_SIZE

**MSYS_2 Block Size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:337`

Dynamic memory size of block 2

---

## CONFIG_BT_NIMBLE_MSYS_BUF_FROM_HEAP

**Get Msys Mbuf from heap**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:343`

This option sets the source of the shared msys mbuf memory between
the Host and the Controller. Allocate the memory from the heap if
this option is sets, from the mempool otherwise.

---

## CONFIG_BT_NIMBLE_NVS_PERSIST

**Persist the BLE Bonding keys in NVS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:182`

Enable this flag to make bonding persistent across device reboots

---

## CONFIG_BT_NIMBLE_OPTIMIZE_MULTI_CONN

**Enable the optimization of multi-connection**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1185`

This option enables the use of vendor-specific APIs for multi-connections, which can
greatly enhance the stability of coexistence between numerous central and peripheral
devices. It will prohibit the usage of standard APIs.

---

## CONFIG_BT_NIMBLE_PERIODIC_ADV_ENH

**Periodic adv enhancements(adi support)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising > Enable periodic advertisement.
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:669`

Enable the periodic advertising enhancements

---

## CONFIG_BT_NIMBLE_PERIODIC_ADV_SYNC_TRANSFER

**Enable Transfer Sync Events**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising > Enable periodic advertisement.
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:675`

This enables controller transfer periodic sync events to host

---

## CONFIG_BT_NIMBLE_PERIODIC_ADV_WITH_RESPONSES

**Enable Periodic Advertisement with Response (EXPERIMENTAL)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE 5 feature > Enable extended advertising > Enable periodic advertisement.
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:682`

This enables controller PAwR (Periodic Advertisement with Response).

---

## CONFIG_BT_NIMBLE_PINNED_TO_CORE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_PINNED_TO_CORE_0

**Core 0 (PRO CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > The CPU core on which NimBLE host will run
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:118`

---

## CONFIG_BT_NIMBLE_PINNED_TO_CORE_1

**Core 1 (APP CPU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > The CPU core on which NimBLE host will run
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:120`

---

## CONFIG_BT_NIMBLE_PRINT_ERR_NAME

**Enable feature to print Error description**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:254`

Enable feature to give useful explanation for HCI errors

---

## CONFIG_BT_NIMBLE_PROX_SERVICE

**Proximity service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:843`

Enable Proximity Service support

---

## CONFIG_BT_NIMBLE_ROLE_BROADCASTER

**Enable BLE Broadcaster role**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:154`

Enables broadcaster role

---

## CONFIG_BT_NIMBLE_ROLE_CENTRAL

**Enable BLE Central role**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:140`

Enables central role

---

## CONFIG_BT_NIMBLE_ROLE_OBSERVER

**Enable BLE Observer role**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:161`

Enables observer role

---

## CONFIG_BT_NIMBLE_ROLE_PERIPHERAL

**Enable BLE Peripheral role**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:147`

Enable peripheral role

---

## CONFIG_BT_NIMBLE_RPA_TIMEOUT

**RPA timeout in seconds**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:445`

Time interval between RPA address change.

---

## CONFIG_BT_NIMBLE_SECURITY_ENABLE

**Enable BLE SM feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:199`

Enable BLE sm feature

---

## CONFIG_BT_NIMBLE_SMP_ID_RESET

**Reset device identity when all bonding records are deleted**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:189`

There are tracking risks associated with using a fixed or static IRK.
If enabled this option, NimBLE will assign a new randomly-generated IRK
when all pairing and bonding records are deleted. This would decrease the ability
of a previously paired peer to be used to determine whether a device
with which it previously shared an IRK is within range.

---

## CONFIG_BT_NIMBLE_SM_LEGACY

**Security manager legacy pairing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:206`

Enable security manager legacy pairing

---

## CONFIG_BT_NIMBLE_SM_LVL

**Security level**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:236`

LE Security Mode 1 Levels:
1. No Security
2. Unauthenticated pairing with encryption
3. Authenticated pairing with encryption
4. Authenticated LE Secure Connections pairing with encryption using a 128-bit strength encryption key.

---

## CONFIG_BT_NIMBLE_SM_SC

**Security manager secure connections (4.2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:213`

Enable security manager secure connections

---

## CONFIG_BT_NIMBLE_SM_SC_DEBUG_KEYS

**Use predefined public-private key pair**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature > Security manager secure connections (4.2)
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:220`

If this option is enabled, SM uses predefined DH key pair as described
in Core Specification, Vol. 3, Part H, 2.3.5.6.1. This allows to
decrypt air traffic easily and thus should only be used for debugging.

---

## CONFIG_BT_NIMBLE_SM_SC_ONLY

**Enable Secure Connections Only Mode**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Enable BLE SM feature
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:247`

Enable Secure Connections Only Mode

---

## CONFIG_BT_NIMBLE_SPS_SERVICE

**Serial Port service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:891`

Enable Serial Port Service support

---

## CONFIG_BT_NIMBLE_SUBRATE

**Enable Subrate Change**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1370`

Enable connection subrate change feature

---

## CONFIG_BT_NIMBLE_SVC_BAS_BATTERY_LEVEL_NOTIFY

**BAS Battery Level NOTIFY permission**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Battery service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:929`

Enable/Disable notifications on BAS Battery Level Characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_FIRMWARE_REVISION

**Firmware Revision**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:962`

Enable the DIS Firmware Revision characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_HARDWARE_REVISION

**Hardware Revision**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:955`

Enable the DIS Hardware Revision characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_INCLUDED

**DIS as an Included Service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:990`

Use DIS as an included service

---

## CONFIG_BT_NIMBLE_SVC_DIS_MANUFACTURER_NAME

**Manufacturer Name**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:941`

Enable the DIS characteristic Manufacturer Name String characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_PNP_ID

**PnP ID**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:983`

Enable the DIS PnP ID characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_SERIAL_NUMBER

**Serial Number**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:948`

Enable the DIS Serial Number characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_SOFTWARE_REVISION

**Software Revision**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:969`

Enable the DIS Software Revision characteristic

---

## CONFIG_BT_NIMBLE_SVC_DIS_SYSTEM_ID

**System ID**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Device Information Service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:976`

Enable the DIS System ID characteristic

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEARANCE

**External appearance of the device**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:305`

Standard BLE GAP Appearance value in HEX format e.g. 0x02C0

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE

**Write**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Appearance write permissions
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1006`

Enable write permission (BLE_GATT_CHR_F_WRITE)

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_AUTHEN

**Write with authentication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Appearance write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1018`

Enable write with authentication permission (BLE_GATT_CHR_F_WRITE_AUTHEN)

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_AUTHOR

**Write with authorisation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Appearance write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1024`

Enable write with authorisation permission (BLE_GATT_CHR_F_WRITE_AUTHOR)

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_ENC

**Write with encryption**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Appearance write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1012`

Enable write with encryption permission (BLE_GATT_CHR_F_WRITE_ENC)

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_PERM

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_PERM_ATHN

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_PERM_ATHR

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_APPEAR_WRITE_PERM_ENC

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_CAR_CHAR_NOT_SUPP

**Characteristic not supported**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Characteristic - Central Address Resolution
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1065`

---

## CONFIG_BT_NIMBLE_SVC_GAP_CAR_NOT_SUPP

**Central Address Resolution not supported**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Characteristic - Central Address Resolution
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1068`

---

## CONFIG_BT_NIMBLE_SVC_GAP_CAR_SUPP

**Central Address Resolution supported**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP Service > GAP Characteristic - Central Address Resolution
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1071`

---

## CONFIG_BT_NIMBLE_SVC_GAP_CENT_ADDR_RESOLUTION

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_DEVICE_NAME

**BLE GAP default device name**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:275`

The Device Name characteristic shall contain the name of the device as an UTF-8 string.
This name can be changed by using API ble_svc_gap_device_name_set()

---

## CONFIG_BT_NIMBLE_SVC_GAP_GATT_SECURITY_LEVEL

**LE GATT Security Level Characteristic**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1165`

Enable the LE GATT Security Level Characteristic

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE

**Write**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP device name write permissions
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1083`

Enable write permission (BLE_GATT_CHR_F_WRITE)

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_AUTHEN

**Write with authentication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP device name write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1096`

Enable write with authentication permission (BLE_GATT_CHR_F_WRITE_AUTHEN)

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_AUTHOR

**Write with authorisation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP device name write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1103`

Enable write with authorisation permission (BLE_GATT_CHR_F_WRITE_AUTHOR)

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_ENC

**Write with encryption**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > GAP device name write permissions > Write
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1089`

Enable write with encryption permission (BLE_GATT_CHR_F_WRITE_ENC)

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_PERM

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_PERM_AUTHEN

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_PERM_AUTHOR

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_NAME_WRITE_PERM_ENC

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_SVC_GAP_PPCP_MAX_CONN_INTERVAL

**PPCP Connection Interval Max (Unit: 1.25 ms)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Peripheral Preferred Connection Parameters (PPCP) settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1113`

Peripheral Preferred Connection Parameter: Connection Interval maximum value
Interval Max = value * 1.25 ms

---

## CONFIG_BT_NIMBLE_SVC_GAP_PPCP_MIN_CONN_INTERVAL

**PPCP Connection Interval Min (Unit: 1.25 ms)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Peripheral Preferred Connection Parameters (PPCP) settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1121`

Peripheral Preferred Connection Parameter: Connection Interval minimum value
Interval Min = value * 1.25 ms

---

## CONFIG_BT_NIMBLE_SVC_GAP_PPCP_SLAVE_LATENCY

**PPCP Slave Latency**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Peripheral Preferred Connection Parameters (PPCP) settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1129`

Peripheral Preferred Connection Parameter: Slave Latency

---

## CONFIG_BT_NIMBLE_SVC_GAP_PPCP_SUPERVISION_TMO

**PPCP Supervision Timeout (Uint: 10 ms)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Peripheral Preferred Connection Parameters (PPCP) settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1136`

Peripheral Preferred Connection Parameter: Supervision Timeout
Timeout = Value * 10 ms

---

## CONFIG_BT_NIMBLE_SVC_GAP_RPA_ONLY

**Resolvable Private Address Only characteristic**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1171`

Enable the Resolvable Private Address Only characteristic

---

## CONFIG_BT_NIMBLE_SVC_HID_MAX_INSTANCES

**Maximum HID service instances**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Human Interface Device service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:909`

Defines maximum number of HID service instances

---

## CONFIG_BT_NIMBLE_SVC_HID_MAX_RPTS

**Maximum HID Report characteristics per service instance**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services > Human Interface Device service
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:916`

Defines maximum number of report characteristics per service instance

---

## CONFIG_BT_NIMBLE_TEST_THROUGHPUT_TEST

**Throughput Test Mode enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:808`

Enable the throughput test mode

---

## CONFIG_BT_NIMBLE_TPS_SERVICE

**Tx Power service**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > BLE Services
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:873`

Enable Tx Power Service support

---

## CONFIG_BT_NIMBLE_TRANSPORT_ACL_FROM_LL_COUNT

**ACL Buffer count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:352`

The number of ACL data buffers allocated for host.

---

## CONFIG_BT_NIMBLE_TRANSPORT_ACL_SIZE

**Transport ACL Buffer size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:359`

This is the maximum size of the data portion of HCI ACL data packets.
It does not include the HCI data header (of 4 bytes)

---

## CONFIG_BT_NIMBLE_TRANSPORT_EVT_COUNT

**Transport Event Buffer count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:377`

This is the high priority HCI events' buffer size. High-priority
event buffers are for everything except advertising reports. If there
are no free high-priority event buffers then host will try to allocate a
low-priority buffer instead

---

## CONFIG_BT_NIMBLE_TRANSPORT_EVT_DISCARD_COUNT

**Discardable Transport Event Buffer count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:387`

This is the low priority HCI events' buffer size. Low-priority event
buffers are only used for advertising reports. If there are no free
low-priority event buffers, then an incoming advertising report will
get dropped

---

## CONFIG_BT_NIMBLE_TRANSPORT_EVT_SIZE

**Transport Event Buffer size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Memory Settings
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:367`

This is the size of each HCI event buffer in bytes. In case of
extended advertising, packets can be fragmented. 257 bytes is the
maximum size of a packet.

---

## CONFIG_BT_NIMBLE_TRANSPORT_UART

**Enable Uart Transport**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1247`

Use UART transport

---

## CONFIG_BT_NIMBLE_TRANSPORT_UART_PARITY_EVEN

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_TRANSPORT_UART_PARITY_NONE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_TRANSPORT_UART_PARITY_ODD

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_BT_NIMBLE_TRANSPORT_UART_PORT

**Uart port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1254`

Uart port

---

## CONFIG_BT_NIMBLE_UART_RX_PIN

**UART Rx pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1319`

Rx pin for Nimble Transport

---

## CONFIG_BT_NIMBLE_UART_TX_PIN

**UART Tx pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1326`

Tx pin for Nimble Transport

---

## CONFIG_BT_NIMBLE_USE_ESP_TIMER

**Enable Esp Timer for Nimble**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:821`

Set this option to use Esp Timer which has higher priority timer instead of FreeRTOS timer

---

## CONFIG_BT_NIMBLE_VS_SUPPORT

**Enable support for VSC and VSE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1179`

This option is used to enable support for sending Vendor Specific HCI commands and handling
Vendor Specific HCI Events.

---

## CONFIG_BT_NIMBLE_WHITELIST_SIZE

**BLE white list size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > NimBLE Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:799`

BLE list size

---

## CONFIG_BT_PBAC_ENABLED

**PBAP Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:241`

This enables the Phone Book Access Profile Client

---

## CONFIG_BT_PBAC_PREFERRED_MTU

**PBAP Client Preferred MTU**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > PBAP Client
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:256`

MTU is limited by the max MTU of transport layer, and should not be smaller than 255,
but can be set to zero to use a default MTU of transport layer

---

## CONFIG_BT_PBAC_SUPPORTED_FEAT

**PBAP Client Supported Features**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth > PBAP Client
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:249`

Set the supported features of PBAP Client, the default value is supported all features

---

## CONFIG_BT_RELEASE_IRAM

**Release Bluetooth text (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/Kconfig:72`

This option release Bluetooth text section and merge Bluetooth data, bss & text into
a large free heap region when esp_bt_mem_release is called, total saving ~21kB or more of IRAM.
ESP32-C2 only 3 configurable PMP entries available, rest of them are hard-coded.
We cannot split the memory into 3 different regions (IRAM, BLE-IRAM, DRAM).
So this option will disable the PMP (ESP_SYSTEM_PMP_IDRAM_SPLIT)

---

## CONFIG_BT_SDP_ATTR_LEN

**Single BT SDP attribute allocated length (bytes)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:154`

This is the maximum allowed size for a single SDP attribute.
Any attributes that exceed this size are truncated.
The default value is 300.

---

## CONFIG_BT_SDP_COMMON_ENABLED

**BT SDP COMMON**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:136`

This enables common SDP operation, such as SDP record creation and deletion.

---

## CONFIG_BT_SDP_PAD_LEN

**One or more BT SDP attributes total allocated length (bytes)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:144`

This is the total size of all SDP attributes allowed.
Any attributes that exceed this size are truncated.
The default value is 300.

---

## CONFIG_BT_SMP_ENABLE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_BT_SMP_MAX_BONDS

**BT/BLE maximum bond device count**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:1250`

The number of security records for peer devices.

---

## CONFIG_BT_SMP_SLAVE_CON_PARAMS_UPD_ENABLE

**Slave enable connection parameters update during pairing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Bluetooth Low Energy > Include BLE security module(SMP)
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:422`

In order to reduce the pairing time, slave actively initiates connection parameters
update during pairing.

---

## CONFIG_BT_SPP_ENABLED

**SPP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options > Classic Bluetooth
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:121`

This enables the Serial Port Profile

---

## CONFIG_BT_STACK_NO_LOG

**Disable BT debug logs (minimize bin size)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > Bluedroid Options
- **Kconfig:** `/opt/esp/idf/components/bt/host/bluedroid/Kconfig.in:465`

This select can save the rodata code size

---

## CONFIG_CAM_CTLR_DVP_CAM_ISR_CACHE_SAFE

**DVP ISR Cache-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Camera Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_cam/Kconfig:29`

Ensure the DVP driver ISR is Cache-Safe. When enabled, the ISR handler
will be available when the cache is disabled.

---

## CONFIG_CAM_CTLR_ISP_DVP_ISR_CACHE_SAFE

**ISP_DVP ISR Cache-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Camera Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_cam/Kconfig:17`

Ensure the ISP_DVP driver ISR is Cache-Safe. When enabled, the ISR handler
will be available when the cache is disabled.

---

## CONFIG_CAM_CTLR_MIPI_CSI_ISR_CACHE_SAFE

**CSI ISR Cache-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Camera Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_cam/Kconfig:5`

Ensure the CSI driver ISR is Cache-Safe. When enabled, the ISR handler
will be available when the cache is disabled.

---

## CONFIG_COMPILER_ASSERT_NDEBUG_EVALUATE

**Enable the evaluation of the expression inside assert(X) when NDEBUG is set**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:411`

When NDEBUG is set, assert(X) will not cause code to trigger an assertion.
With this option set, assert(X) will still evaluate the expression X, though
the result will never cause an assertion. This means that if X is a function
then the function will be called.

This is not according to the standard, which states that the assert(X) should
be replaced with ((void)0) if NDEBUG is defined.

In ESP-IDF v6.0 the default behavior will change to "no" to be in line with the
standard.

---

## CONFIG_COMPILER_CXX_EXCEPTIONS

**Enable C++ exceptions**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:477`

Enabling this option compiles all IDF C++ files with exception support enabled.

Disabling this option disables C++ exception support in all compiled files, and any libstdc++ code
which throws an exception will abort instead.

Enabling this option currently adds an additional ~500 bytes of heap overhead
when an exception is thrown in user code for the first time.

---

## CONFIG_COMPILER_CXX_EXCEPTIONS_EMG_POOL_SIZE

**Emergency Pool Size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Compiler options > Enable C++ exceptions
- **Kconfig:** `/opt/esp/idf/Kconfig:489`

Size (in bytes) of the emergency memory pool for C++ exceptions. This pool will be used to allocate
memory for thrown exceptions when there is not enough memory on the heap.

---

## CONFIG_COMPILER_CXX_RTTI

**Enable C++ run-time type info (RTTI)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:497`

Enabling this option compiles all C++ files with RTTI support enabled.
This increases binary size (typically by tens of kB) but allows using
dynamic_cast conversion and typeid operator.

---

## CONFIG_COMPILER_DISABLE_DEFAULT_ERRORS

**Disable errors for default warnings**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:582`

Enable this option if you do not want default warnings to be considered as errors,
especially when updating IDF.

This is a temporary flag that could help to allow upgrade while having
some time to address the warnings raised by those default warnings.
Alternatives are:
1) fix code (preferred),
2) remove specific warnings,
3) do not consider specific warnings as error.

---

## CONFIG_COMPILER_DISABLE_GCC12_WARNINGS

**Disable new warnings introduced in GCC 12**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:596`

Enable this option if use GCC 12 or newer, and want to disable warnings which don't appear with
GCC 11.

---

## CONFIG_COMPILER_DISABLE_GCC13_WARNINGS

**Disable new warnings introduced in GCC 13**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:603`

Enable this option if use GCC 13 or newer, and want to disable warnings which don't appear with
GCC 12.

---

## CONFIG_COMPILER_DISABLE_GCC14_WARNINGS

**Disable new warnings introduced in GCC 14**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:610`

Enable this option if use GCC 14 or newer, and want to disable warnings which don't appear with
GCC 13.

---

## CONFIG_COMPILER_DUMP_RTL_FILES

**Dump RTL files during compilation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:617`

If enabled, RTL files will be produced during compilation. These files
can be used by other tools, for example to calculate call graphs.

---

## CONFIG_COMPILER_FLOAT_LIB_FROM_GCCLIB

**libgcc**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Compiler float lib source
- **Kconfig:** `/opt/esp/idf/Kconfig:439`

---

## CONFIG_COMPILER_FLOAT_LIB_FROM_RVFPLIB

**librvfp**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Compiler float lib source
- **Kconfig:** `/opt/esp/idf/Kconfig:441`

---

## CONFIG_COMPILER_HIDE_PATHS_MACROS

**Replace ESP-IDF and project paths in binaries**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:462`

When expanding the __FILE__ and __BASE_FILE__ macros, replace paths inside ESP-IDF
with paths relative to the placeholder string "IDF", and convert paths inside the
project directory to relative paths.

This allows building the project with assertions or other code that embeds file paths,
without the binary containing the exact path to the IDF or project directories.

This option passes -fmacro-prefix-map options to the GCC command line. To replace additional
paths in your binaries, modify the project CMakeLists.txt file to pass custom -fmacro-prefix-map or
-ffile-prefix-map arguments.

---

## CONFIG_COMPILER_NO_MERGE_CONSTANTS

**Disable merging const sections**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:547`

Disable merging identical constants (string/floating-point) across compilation units.
This helps in better size analysis of the application binary as the rodata section
distribution is more uniform across libraries. On downside, it may increase
the binary size and hence should be used during development phase only.

---

## CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_DISABLE

**Disabled (sets -DNDEBUG)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Assertion level
- **Kconfig:** `/opt/esp/idf/Kconfig:403`

If assertions are disabled, -DNDEBUG is added to CPPFLAGS.

---

## CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_ENABLE

**Enabled**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Assertion level
- **Kconfig:** `/opt/esp/idf/Kconfig:390`

Enable assertions. Assertion content and line number will be printed on failure.

---

## CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_SILENT

**Silent (saves code size)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Assertion level
- **Kconfig:** `/opt/esp/idf/Kconfig:396`

Enable silent assertions. Failed assertions will abort(), user needs to
use the aborting address to find the line number with the failed assertion.

---

## CONFIG_COMPILER_OPTIMIZATION_ASSERTION_LEVEL

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_COMPILER_OPTIMIZATION_CHECKS_SILENT

**Disable messages in ESP_RETURN_ON_* and ESP_EXIT_ON_* macros**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:452`

If enabled, the error messages will be discarded in following check macros:
- ESP_RETURN_ON_ERROR
- ESP_EXIT_ON_ERROR
- ESP_RETURN_ON_FALSE
- ESP_EXIT_ON_FALSE

---

## CONFIG_COMPILER_OPTIMIZATION_DEBUG

**Debug (-Og)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Optimization Level
- **Kconfig:** `/opt/esp/idf/Kconfig:365`

---

## CONFIG_COMPILER_OPTIMIZATION_NONE

**Debug without optimization (-O0)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Optimization Level
- **Kconfig:** `/opt/esp/idf/Kconfig:371`

---

## CONFIG_COMPILER_OPTIMIZATION_PERF

**Optimize for performance (-O2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Optimization Level
- **Kconfig:** `/opt/esp/idf/Kconfig:369`

---

## CONFIG_COMPILER_OPTIMIZATION_SIZE

**Optimize for size (-Os with GCC, -Oz with Clang)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Optimization Level
- **Kconfig:** `/opt/esp/idf/Kconfig:367`

---

## CONFIG_COMPILER_ORPHAN_SECTIONS_PLACE

**Place silently**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Orphan sections handling
- **Kconfig:** `/opt/esp/idf/Kconfig:666`

Places orphan sections without a warning/error message.

---

## CONFIG_COMPILER_ORPHAN_SECTIONS_WARNING

**Place with warning**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Orphan sections handling
- **Kconfig:** `/opt/esp/idf/Kconfig:661`

Places orphan sections with a warning message.

---

## CONFIG_COMPILER_RT_LIB_CLANGRT

**libclang_rt**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Compiler runtime library
- **Kconfig:** `/opt/esp/idf/Kconfig:637`

---

## CONFIG_COMPILER_RT_LIB_GCCLIB

**libgcc**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Compiler runtime library
- **Kconfig:** `/opt/esp/idf/Kconfig:634`

---

## CONFIG_COMPILER_RT_LIB_HOST

**Host**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Compiler runtime library
- **Kconfig:** `/opt/esp/idf/Kconfig:640`

---

## CONFIG_COMPILER_RT_LIB_NAME

- **Type:** `unknown`
- **Default:** `gcc`

---

## CONFIG_COMPILER_SAVE_RESTORE_LIBCALLS

**Enable -msave-restore flag to reduce code size**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:570`

Adds -msave-restore to C/C++ compilation flags.

When this flag is enabled, compiler will call library functions to
save/restore registers in function prologues/epilogues. This results
in lower overall code size, at the expense of slightly reduced performance.

This option can be enabled for RISC-V targets only.

---

## CONFIG_COMPILER_STACK_CHECK

- **Type:** `string`
- **Default:** `n`

Stack smashing protection.

---

## CONFIG_COMPILER_STACK_CHECK_MODE_ALL

**Overall**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Stack smashing protection mode
- **Kconfig:** `/opt/esp/idf/Kconfig:537`

---

## CONFIG_COMPILER_STACK_CHECK_MODE_NONE

**None**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Compiler options > Stack smashing protection mode
- **Kconfig:** `/opt/esp/idf/Kconfig:531`

---

## CONFIG_COMPILER_STACK_CHECK_MODE_NORM

**Normal**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Stack smashing protection mode
- **Kconfig:** `/opt/esp/idf/Kconfig:533`

---

## CONFIG_COMPILER_STACK_CHECK_MODE_STRONG

**Strong**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options > Stack smashing protection mode
- **Kconfig:** `/opt/esp/idf/Kconfig:535`

---

## CONFIG_COMPILER_STATIC_ANALYZER

**Enable compiler static analyzer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:672`

Enable compiler static analyzer. This may produce false-positive results and increases compile time.

---

## CONFIG_COMPILER_WARN_WRITE_STRINGS

**Enable -Wwrite-strings warning flag**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Compiler options
- **Kconfig:** `/opt/esp/idf/Kconfig:556`

Adds -Wwrite-strings flag for the C/C++ compilers.

For C, this gives string constants the type ``const char[]`` so that
copying the address of one into a non-const ``char *`` pointer
produces a warning. This warning helps to find at compile time code
that tries to write into a string constant.

For C++, this warns about the deprecated conversion from string
literals to ``char *``.

---

## CONFIG_CONSOLE_SORTED_HELP

**Enable sorted help**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Console Library
- **Kconfig:** `/opt/esp/idf/components/console/Kconfig:3`

Instead of listing the commands in the order of registration, the help command lists
the available commands in sorted order, if this option is enabled.

---

## CONFIG_DAC_ADAU1961

**Analog Devices ADAU1961 DAC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:107`

---

## CONFIG_DAC_CTRL_FUNC_IN_IRAM

**Place DAC control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:DAC Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_dac/Kconfig:3`

Place DAC control functions (e.g. 'dac_oneshot_output_voltage') into IRAM,
so that this function can be IRAM-safe and able to be called in the other IRAM interrupt context.
Enabling this option can improve driver performance as well.

---

## CONFIG_DAC_DMA_AUTO_16BIT_ALIGN

**Align the continuous data to 16 bit automatically**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:DAC Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_dac/Kconfig:25`

Whether to left shift the continuous data to align every bytes to 16 bits in the driver.
On ESP32, although the DAC resolution is only 8 bits,
the hardware requires 16 bits data in continuous mode.
By enabling this option, the driver will left shift 8 bits for the input data automatically.
Only disable this option when you decide to do this step by yourself.
Note that the driver will allocate a new piece of memory to save the converted data.

---

## CONFIG_DAC_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:DAC Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_dac/Kconfig:18`

whether to enable the debug log message for DAC driver.
Note that, this option only controls the DAC driver log, won't affect other drivers.

---

## CONFIG_DAC_I2C_ADDR

**I2C address**

- **Type:** `unknown`
- **Default:** `0x20`
- **Menu:** Audio Board > Custom Audio Board > DAC I2C control interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:137`

I2C Address of the DAC control interface

---

## CONFIG_DAC_I2C_SCL

**SCL pin**

- **Type:** `unknown`
- **Default:** `22`
- **Menu:** Audio Board > Custom Audio Board > DAC I2C control interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:129`

I2C SCL pin of the DAC control interface

---

## CONFIG_DAC_I2C_SDA

**SDA pin**

- **Type:** `unknown`
- **Default:** `21`
- **Menu:** Audio Board > Custom Audio Board > DAC I2C control interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:122`

I2C SDA pin of the DAC control interface

---

## CONFIG_DAC_ISR_IRAM_SAFE

**DAC ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:DAC Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_dac/Kconfig:11`

Ensure the DAC interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_DAC_MA120

**Infineon MA120 ClassD AMP**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:100`

---

## CONFIG_DAC_MA120X0

**Infineon MA120X0 ClassD AMP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:103`

---

## CONFIG_DAC_MAX98357

**Analog Devices MAX98357 DAC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:110`

---

## CONFIG_DAC_PCM5102A

**TI PCM5102A based DAC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:97`

---

## CONFIG_DAC_PCM51XX

**TI PCM51XX/TAS57XX based DAC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:94`

---

## CONFIG_DAC_PT8211

**Princeton Technology PT8211**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:116`

---

## CONFIG_DAC_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy DAC Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:81`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_DAC_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy DAC Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:73`

Whether to suppress the deprecation warnings when using legacy dac driver (driver/dac.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_DAC_TAS5805M

**Texas Instruments TAS5805M**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:113`

---

## CONFIG_DAC_TAS5805M_EQ_SUPPORT

**Enable TAS5805M equalizer support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > TAS5805M DSP Support
- **Kconfig:** `components/audio_board/Kconfig.projbuild:200`

When enabled, build includes optional TAS5805M equalizer support.
Equalizer configuration and runtime control remain available via
the device UI. Default: disabled.

---

## CONFIG_DMA2D_ISR_IRAM_SAFE

**2D-DMA ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > 2D-DMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:110`

This will ensure the 2D-DMA interrupt handler is IRAM-Safe, allow to avoid flash
cache misses, and also be able to run whilst the cache is disabled.
(e.g. SPI Flash write).

---

## CONFIG_DMA2D_OPERATION_FUNC_IN_IRAM

**Place 2D-DMA operation functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > 2D-DMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:102`

Place 2D-DMA all operation functions, including control functions (e.g. start/stop/append/reset) and setter
functions (e.g. connect/strategy/callback registration) into IRAM, so that these functions can be IRAM-safe
and able to be called in the other IRAM interrupt context. It also helps optimizing the performance.

---

## CONFIG_DSP_ANSI

**ANSI C**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > DSP Optimization
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:17`

---

## CONFIG_DSP_MAX_FFT_SIZE

- **Type:** `unknown`
- **Default:** `4096`

---

## CONFIG_DSP_MAX_FFT_SIZE_1024

**1024**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:37`

---

## CONFIG_DSP_MAX_FFT_SIZE_16384

**16384**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:45`

---

## CONFIG_DSP_MAX_FFT_SIZE_2048

**2048**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:39`

---

## CONFIG_DSP_MAX_FFT_SIZE_32768

**32768**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:47`

---

## CONFIG_DSP_MAX_FFT_SIZE_4096

**4096**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:41`

---

## CONFIG_DSP_MAX_FFT_SIZE_512

**512**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:35`

---

## CONFIG_DSP_MAX_FFT_SIZE_8192

**8192**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > DSP Library > Maximum FFT length
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:43`

---

## CONFIG_DSP_OPTIMIZATION

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_DSP_OPTIMIZATIONS_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_DSP_OPTIMIZED

**Optimized**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > DSP Library > DSP Optimization
- **Kconfig:** `managed_components/espressif__esp-dsp/Kconfig:19`

---

## CONFIG_DW_GDMA_CTRL_FUNC_IN_IRAM

- **Type:** `string`
- **Default:** `n`

Place DW_GDMA control functions (e.g. dw_gdma_channel_continue) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_DW_GDMA_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > DW_GDMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:91`

Whether to enable the debug log message for DW_GDMA driver.
Note that, this option only controls the DW_GDMA driver log, won't affect other drivers.

---

## CONFIG_DW_GDMA_GETTER_FUNC_IN_IRAM

- **Type:** `string`
- **Default:** `n`

Place DW_GDMA getter functions (e.g. dw_gdma_link_list_get_item) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_DW_GDMA_ISR_IRAM_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the DW_GDMA interrupt handler is IRAM-Safe, allow to avoid flash
cache misses, and also be able to run whilst the cache is disabled.
(e.g. SPI Flash write).

---

## CONFIG_DW_GDMA_OBJ_DRAM_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the DW_GDMA object is DRAM-Safe, allow to avoid external memory
cache misses, and also be accessible whilst the cache is disabled.

---

## CONFIG_DW_GDMA_SETTER_FUNC_IN_IRAM

- **Type:** `string`
- **Default:** `n`

Place DW_GDMA setter functions (e.g. dw_gdma_channel_set_block_markers) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_EFUSE_CODE_SCHEME_COMPAT_3_4

**3/4 and None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager > Coding Scheme Compatibility
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:60`

---

## CONFIG_EFUSE_CODE_SCHEME_COMPAT_NONE

**None Only**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager > Coding Scheme Compatibility
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:58`

---

## CONFIG_EFUSE_CODE_SCHEME_COMPAT_REPEAT

**Repeat, 3/4 and None (common table does not support it)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager > Coding Scheme Compatibility
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:62`

---

## CONFIG_EFUSE_CUSTOM_TABLE

**Use custom eFuse table**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:3`

Allows to generate a structure for eFuse from the CSV file.

---

## CONFIG_EFUSE_CUSTOM_TABLE_FILENAME

**Custom eFuse CSV file**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > eFuse Bit Manager > Use custom eFuse table
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:9`

Name of the custom eFuse CSV filename. This path is evaluated
relative to the project root directory.

---

## CONFIG_EFUSE_MAX_BLK_LEN

- **Type:** `unknown`
- **Default:** `256`

---

## CONFIG_EFUSE_VIRTUAL

**Simulate eFuse operations in RAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:17`

If "n" - No virtual mode. All eFuse operations are real and use eFuse registers.
If "y" - The virtual mode is enabled and all eFuse operations (read and write) are redirected
to RAM instead of eFuse registers, all permanent changes (via eFuse) are disabled.
Log output will state changes that would be applied, but they will not be.

If it is "y", then SECURE_FLASH_ENCRYPTION_MODE_RELEASE cannot be used.
Because the EFUSE VIRT mode is for testing only.

During startup, the eFuses are copied into RAM. This mode is useful for fast tests.

---

## CONFIG_EFUSE_VIRTUAL_KEEP_IN_FLASH

**Keep eFuses in flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager > Simulate eFuse operations in RAM
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:32`

In addition to the "Simulate eFuse operations in RAM" option, this option just adds
a feature to keep eFuses after reboots in flash memory. To use this mode the partition_table
should have the `efuse` partition. partition.csv: "efuse_em, data, efuse,   ,   0x2000,"

During startup, the eFuses are copied from flash or,
in case if flash is empty, from real eFuse to RAM and then update flash.
This mode is useful when need to keep changes after reboot
(testing secure_boot and flash_encryption).

---

## CONFIG_EFUSE_VIRTUAL_LOG_ALL_WRITES

**Log all virtual writes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > eFuse Bit Manager > Simulate eFuse operations in RAM
- **Kconfig:** `/opt/esp/idf/components/efuse/Kconfig:45`

If enabled, log efuse burns. This shows changes that would be made.

---

## CONFIG_ENABLE_WIFI_PROVISIONING

**enable WiFi provisioning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:253`

Enable WiFi provisioning through improv WiFi (https://github.com/improv-wifi), try it using https://web.esphome.io/

---

## CONFIG_ENV_GPIO_IN_RANGE_MAX

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_ENV_GPIO_OUT_RANGE_MAX

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_ENV_GPIO_RANGE_MAX

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_ENV_GPIO_RANGE_MIN

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP32S3_DATA_CACHE_16KB

**16KB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:74`

---

## CONFIG_ESP32S3_DATA_CACHE_32KB

**32KB**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:76`

---

## CONFIG_ESP32S3_DATA_CACHE_4WAYS

**4 ways**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache associated ways
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:95`

---

## CONFIG_ESP32S3_DATA_CACHE_64KB

**64KB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:78`

---

## CONFIG_ESP32S3_DATA_CACHE_8WAYS

**8 ways**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache associated ways
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:97`

---

## CONFIG_ESP32S3_DATA_CACHE_LINE_16B

**16 Bytes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache line size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:112`

---

## CONFIG_ESP32S3_DATA_CACHE_LINE_32B

**32 Bytes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache line size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:115`

---

## CONFIG_ESP32S3_DATA_CACHE_LINE_64B

**64 Bytes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Data cache line size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:117`

---

## CONFIG_ESP32S3_DATA_CACHE_LINE_SIZE

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_ESP32S3_DATA_CACHE_SIZE

- **Type:** `unknown`
- **Default:** `0x8000`

---

## CONFIG_ESP32S3_DATA_CACHE_WRAP

- **Type:** `string`
- **Default:** `n`

If enabled, data cache will use wrap mode to read spi flash or spi ram.
The wrap length equals to ESP32S3_DATA_CACHE_LINE_SIZE.
However, it depends on complex conditions.

---

## CONFIG_ESP32S3_DCACHE_ASSOCIATED_WAYS

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_ESP32S3_FIXED_STATIC_RAM_SIZE

**Fixed Static RAM size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings > Memory > Use fixed static RAM size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.memory:21`

RAM size dedicated for static variables (.data & .bss sections).
This value is less than the chips total memory, as not all of it can be used for this purpose.
E.g. parts are used by the software bootloader, and will only be available
as heap memory after app startup.

---

## CONFIG_ESP32S3_ICACHE_ASSOCIATED_WAYS

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_16KB

**16KB**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:11`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_32KB

**32KB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:13`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_4WAYS

**4 ways**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache associated ways
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:28`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_8WAYS

**8 ways**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache associated ways
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:30`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_LINE_16B

**16 Bytes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache line size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:45`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_LINE_32B

**32 Bytes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Cache config > Instruction cache line size
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cache:48`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_LINE_SIZE

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_SIZE

- **Type:** `unknown`
- **Default:** `0x4000`

---

## CONFIG_ESP32S3_INSTRUCTION_CACHE_WRAP

- **Type:** `string`
- **Default:** `n`

If enabled, instruction cache will use wrap mode to read spi flash or spi ram.
The wrap length equals to ESP32S3_INSTRUCTION_CACHE_LINE_SIZE.
However, it depends on complex conditions.

---

## CONFIG_ESP32S3_MEMMAP_TRACEMEM

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP32S3_MEMMAP_TRACEMEM_TWOBANKS

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP32S3_REV_MAX_FULL

- **Type:** `unknown`
- **Default:** `99`

---

## CONFIG_ESP32S3_REV_MIN_0

**Rev v0.0 (ECO0)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Chip revision > Minimum Supported ESP32-S3 Revision
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.hw_support:12`

---

## CONFIG_ESP32S3_REV_MIN_1

**Rev v0.1 (ECO1)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Chip revision > Minimum Supported ESP32-S3 Revision
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.hw_support:14`

---

## CONFIG_ESP32S3_REV_MIN_2

**Rev v0.2 (ECO2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Chip revision > Minimum Supported ESP32-S3 Revision
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.hw_support:16`

---

## CONFIG_ESP32S3_REV_MIN_FULL

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP32S3_RTCDATA_IN_FAST_MEM

**Place RTC_DATA_ATTR and RTC_RODATA_ATTR variables into RTC fast memory segment**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Memory
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.memory:3`

This option allows to place .rtc_data and .rtc_rodata sections into
RTC fast memory segment to free the slow memory region for ULP programs.

---

## CONFIG_ESP32S3_TRACEMEM_RESERVE_DRAM

- **Type:** `unknown`
- **Default:** `0x0`

---

## CONFIG_ESP32S3_TRAX

**Use TRAX tracing feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Trace memory
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.tracemem:11`

The esp32-s3 contains a feature which allows you to trace the execution path the processor
has taken through the program. This is stored in a chunk of 32K (16K for single-processor)
of memory that can't be used for general purposes anymore. Disable this if you do not know
what this is.

---

## CONFIG_ESP32S3_TRAX_TWOBANKS

**Reserve memory for tracing both pro as well as app cpu execution**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Trace memory > Use TRAX tracing feature
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.tracemem:21`

The esp32-s3 contains a feature which allows you to trace the execution path the processor
has taken through the program. This is stored in a chunk of 32K (16K for single-processor)
of memory that can't be used for general purposes anymore. Disable this if you do not know
what this is.

---

## CONFIG_ESP32S3_UNIVERSAL_MAC_ADDRESSES

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_ESP32S3_UNIVERSAL_MAC_ADDRESSES_FOUR

**Four**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > MAC Config > Number of universally administered (by IEEE) MAC address
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.mac:26`

---

## CONFIG_ESP32S3_UNIVERSAL_MAC_ADDRESSES_TWO

**Two**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > MAC Config > Number of universally administered (by IEEE) MAC address
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.mac:20`

---

## CONFIG_ESP32S3_USE_FIXED_STATIC_RAM_SIZE

**Use fixed static RAM size**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Memory
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.memory:10`

If this option is disabled, the DRAM part of the heap starts right after the .bss section,
within the dram0_0 region. As a result, adding or removing some static variables
will change the available heap size.

If this option is enabled, the DRAM part of the heap starts right after the dram0_0 region,
where its length is set with ESP32S3_FIXED_STATIC_RAM_SIZE

---

## CONFIG_ESP32_KORVO_DU1906_ADC_ES7243

**ESP32_KORVO_DU1906_ADC_ES7243**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > ESP32 KORVO DU1906 Board ADC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:81`

---

## CONFIG_ESP32_KORVO_DU1906_BOARD

**ESP32_KORVO_DU1906**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:39`

---

## CONFIG_ESP32_KORVO_DU1906_DAC_ES7148

**ESP32_KORVO_DU1906_DAC_ES7148**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > ESP32 KORVO DU1906 Board DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:69`

---

## CONFIG_ESP32_KORVO_DU1906_DAC_TAS5805M

**ESP32_KORVO_DU1906_DAC_TAS5805M**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > ESP32 KORVO DU1906 Board DAC chip
- **Kconfig:** `components/audio_board/Kconfig.projbuild:67`

---

## CONFIG_ESP32_S2_KALUGA_1_V1_2_BOARD

**ESP32-S2-Kaluga-1 v1.2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:41`

---

## CONFIG_ESP32_UDP_LOGGER_AUTOSTART_INIT_NETIF

**Call esp_netif_init() in esp32_udp_logger_autostart()**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:58`

Disable this when your application already initializes networking
(for example, components that bring up ESP-NETIF/LwIP before
esp32-udp-logger is started).

---

## CONFIG_ESP32_UDP_LOGGER_DROP_ON_FULL

**Drop logs if queue is full**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:43`

---

## CONFIG_ESP32_UDP_LOGGER_ENABLED

**Enable esp32-udp-logger**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:3`

---

## CONFIG_ESP32_UDP_LOGGER_ENABLE_MDNS

**Enable mDNS hostname (esp32-udp-logger-XXXX.local)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:53`

---

## CONFIG_ESP32_UDP_LOGGER_MAX_LINE

**Max log line length**

- **Type:** `unknown`
- **Default:** `256`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:19`

---

## CONFIG_ESP32_UDP_LOGGER_PORT

**UDP log port (TX)**

- **Type:** `unknown`
- **Default:** `9999`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:7`

---

## CONFIG_ESP32_UDP_LOGGER_PREFIX_DEVICE

**Prefix each line with [hostname]**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:48`

---

## CONFIG_ESP32_UDP_LOGGER_QUEUE_DEPTH

**Queue depth (# of buffered log lines)**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:25`

---

## CONFIG_ESP32_UDP_LOGGER_RX_PORT

**UDP command port (RX)**

- **Type:** `unknown`
- **Default:** `9998`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:13`

---

## CONFIG_ESP32_UDP_LOGGER_SERVICE

**Advertise mDNS service _esp32udplog._udp**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:67`

---

## CONFIG_ESP32_UDP_LOGGER_TASK_PRIO

**Sender task priority**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:37`

---

## CONFIG_ESP32_UDP_LOGGER_TASK_STACK

**Sender task stack (bytes)**

- **Type:** `unknown`
- **Default:** `3072`
- **Menu:** Component config > esp32-udp-logger > Enable esp32-udp-logger
- **Kconfig:** `components/udp_logger/Kconfig:31`

---

## CONFIG_ESPHID_TASK_SIZE_BLE

**Task stack size for ESP HID BLE**

- **Type:** `unknown`
- **Default:** `4096`
- **Menu:** Component config > ESP HID
- **Kconfig:** `/opt/esp/idf/components/esp_hid/Kconfig:10`

This is the stack size for the BLE HID task.
Default is 4096 bytes.

---

## CONFIG_ESPHID_TASK_SIZE_BT

**Task stack size for ESP HID BR/EDR**

- **Type:** `unknown`
- **Default:** `2048`
- **Menu:** Component config > ESP HID
- **Kconfig:** `/opt/esp/idf/components/esp_hid/Kconfig:2`

This is the stack size for the BT HID task.
Default is 2048 bytes.

---

## CONFIG_ESPTOOLPY_AFTER

- **Type:** `unknown`
- **Default:** `hard_reset`

---

## CONFIG_ESPTOOLPY_AFTER_NORESET

**Stay in bootloader**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > After flashing
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:192`

---

## CONFIG_ESPTOOLPY_AFTER_RESET

**Reset after flashing**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > After flashing
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:190`

---

## CONFIG_ESPTOOLPY_BEFORE

- **Type:** `unknown`
- **Default:** `default_reset`

---

## CONFIG_ESPTOOLPY_BEFORE_NORESET

**No reset**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Before flashing
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:171`

---

## CONFIG_ESPTOOLPY_BEFORE_RESET

**Reset to bootloader**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > Before flashing
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:169`

---

## CONFIG_ESPTOOLPY_FLASHFREQ

- **Type:** `unknown`
- **Default:** `80m`

---

## CONFIG_ESPTOOLPY_FLASHFREQ_120M

**120 MHz (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI speed
- **Kconfig:** `/opt/esp/idf/components/esptool_py/../spi_flash/esp32s3/Kconfig.flash_freq:4`

- Optional feature for QSPI Flash. Read docs and enable `CONFIG_SPI_FLASH_HPM_ENA` first!
- Flash 120 MHz SDR mode is stable.
- Flash 120 MHz DDR mode is an experimental feature, it works when
  the temperature is stable.

  Risks:
  If your chip powers on at a certain temperature, then after the temperature
  increases or decreases by approximately 20 Celsius degrees (depending on the
  chip), the program will crash randomly.

---

## CONFIG_ESPTOOLPY_FLASHFREQ_20M

**20 MHz**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI speed
- **Kconfig:** `/opt/esp/idf/components/esptool_py/../spi_flash/esp32s3/Kconfig.flash_freq:22`

---

## CONFIG_ESPTOOLPY_FLASHFREQ_40M

**40 MHz**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI speed
- **Kconfig:** `/opt/esp/idf/components/esptool_py/../spi_flash/esp32s3/Kconfig.flash_freq:20`

---

## CONFIG_ESPTOOLPY_FLASHFREQ_80M

**80 MHz**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > Flash SPI speed
- **Kconfig:** `/opt/esp/idf/components/esptool_py/../spi_flash/esp32s3/Kconfig.flash_freq:18`

---

## CONFIG_ESPTOOLPY_FLASHMODE

- **Type:** `unknown`
- **Default:** `dio`

---

## CONFIG_ESPTOOLPY_FLASHMODE_DIO

**DIO**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > Flash SPI mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:53`

---

## CONFIG_ESPTOOLPY_FLASHMODE_DOUT

**DOUT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:56`

---

## CONFIG_ESPTOOLPY_FLASHMODE_OPI

**OPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:59`

---

## CONFIG_ESPTOOLPY_FLASHMODE_QIO

**QIO**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:47`

---

## CONFIG_ESPTOOLPY_FLASHMODE_QOUT

**QOUT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash SPI mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:50`

---

## CONFIG_ESPTOOLPY_FLASHSIZE

- **Type:** `unknown`
- **Default:** `2MB`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_128MB

**128 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:132`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_16MB

**16 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:126`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_1MB

**1 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:118`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_2MB

**2 MB**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:120`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_32MB

**32 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:128`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_4MB

**4 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:122`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_64MB

**64 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:130`

---

## CONFIG_ESPTOOLPY_FLASHSIZE_8MB

**8 MB**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash size
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:124`

---

## CONFIG_ESPTOOLPY_FLASH_MODE_AUTO_DETECT

**Choose flash mode automatically (please read help)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:18`

This config option helps decide whether flash is Quad or Octal, but please note some limitations:

1. If the flash chip is an Octal one, even if one of "QIO", "QOUT", "DIO", "DOUT" options is
   selected in `ESPTOOLPY_FLASHMODE`, our code will automatically change the
   mode to "OPI" and the sample mode will be STR.
2. If the flash chip is a Quad one, even if "OPI" is selected in `ESPTOOLPY_FLASHMODE`, our code will
   automatically change the mode to "DIO".
3. This option is mainly to improve the out-of-box experience of developers. It doesn't guarantee
   the feature-complete. Some code still rely on `ESPTOOLPY_OCT_FLASH`. Please do not rely on this option
   when you are pretty sure that you are using Octal flash.
   In this case, please enable `ESPTOOLPY_OCT_FLASH` option, then you can choose `DTR` sample mode
   in `ESPTOOLPY_FLASH_SAMPLE_MODE`. Otherwise, only `STR` mode is available.
4. Enabling this feature reduces available internal RAM size (around 900 bytes).
   If your IRAM space is insufficient and you're aware of your flash type,
   disable this option and select corresponding flash type options.

---

## CONFIG_ESPTOOLPY_FLASH_SAMPLE_MODE_DTR

**DTR Mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config > Flash Sampling Mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:71`

---

## CONFIG_ESPTOOLPY_FLASH_SAMPLE_MODE_STR

**STR Mode**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Serial flasher config > Flash Sampling Mode
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:69`

---

## CONFIG_ESPTOOLPY_HEADER_FLASHSIZE_UPDATE

**Detect flash size when flashing bootloader**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:147`

If this option is set, flashing the project will automatically detect
the flash size of the target chip and update the bootloader image
before it is flashed.

Enabling this option turns off the image protection against corruption
by a SHA256 digest. Updating the bootloader image before flashing would
invalidate the digest.

---

## CONFIG_ESPTOOLPY_MONITOR_BAUD

- **Type:** `unknown`
- **Default:** `115200`

---

## CONFIG_ESPTOOLPY_NO_STUB

**Disable download stub**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:4`

The flasher tool sends a precompiled download stub first by default. That stub allows things
like compressed downloads and more. Usually you should not need to disable that feature

---

## CONFIG_ESPTOOLPY_OCT_FLASH

**Enable Octal Flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Serial flasher config
- **Kconfig:** `/opt/esp/idf/components/esptool_py/Kconfig.projbuild:13`

---

## CONFIG_ESP_AI_THINKER_ES8388_BOARD

**ESP-AI-Thinker-ES8388 v2.2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:43`

---

## CONFIG_ESP_ALLOW_BSS_SEG_EXTERNAL_MEMORY

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_BRINGUP_BYPASS_CPU_CLK_SETTING

- **Type:** `string`
- **Default:** `n`

This option is only used for new chip bringup, when
clock support isn't done yet. So with this option,
we use xtal on FPGA as the clock source.

---

## CONFIG_ESP_BRINGUP_BYPASS_RANDOM_SETTING

- **Type:** `string`
- **Default:** `n`

This option is only used for new chip bringup, when
RNG isn't done yet. So with this option, we use 0x5A
to fill the random buffers

---

## CONFIG_ESP_BROWNOUT_DET

**Hardware brownout detect & reset**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:5`

The ESP32-S3 has a built-in brownout detector which can detect if the voltage is lower than
a specific value. If this happens, it will reset the chip in order to prevent unintended
behaviour.

---

## CONFIG_ESP_BROWNOUT_DET_LVL

- **Type:** `unknown`
- **Default:** `7`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_1

**3.30V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:37`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_2

**3.19V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:35`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_3

**2.98V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:33`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_4

**2.84V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:31`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_5

**2.67V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:29`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_6

**2.56V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:27`

---

## CONFIG_ESP_BROWNOUT_DET_LVL_SEL_7

**2.44V**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Power Supplier > Brownout Detector > Hardware brownout detect & reset > Brownout voltage level
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./power_supply/port/esp32s3/Kconfig.power:25`

---

## CONFIG_ESP_BROWNOUT_USE_INTR

- **Type:** `string`
- **Default:** `y`

This config allows to trigger an interrupt when brownout detected. Software restart will be done
at the end of the default callback.
This is because for some special workflow, the chip needs do more things when brownout happens
before restart instead of restarting directly. This part needs to be done in callback function
of interrupt.

---

## CONFIG_ESP_CLK_RC32K_NOT_TO_USE

- **Type:** `string`
- **Default:** `n`

Due to the poor low-temperature characteristics of
RC32K (it cannot operate below -40 degrees Celsius),
please avoid using it whenever possible

---

## CONFIG_ESP_COEX_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_COEX_EXTERNAL_COEXIST_ENABLE

**External Coexistence**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:23`

If enabled, HW External coexistence arbitration is managed by GPIO pins.
It can support three types of wired combinations so far which are 1-wired/2-wired/3-wired.
User can select GPIO pins in application code with configure interfaces.

This function depends on BT-off
because currently we do not support external coex and internal coex simultaneously.

---

## CONFIG_ESP_COEX_GPIO_DEBUG

**GPIO debugging for coexistence**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:43`

Support coexistence GPIO debugging

---

## CONFIG_ESP_COEX_GPIO_DEBUG_DIAG_GENERAL

**General**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence > Debugging Diagram
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:58`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_DIAG_WIFI

**Wi-Fi**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence > Debugging Diagram
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:60`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_COUNT

**Max number of debugging GPIOs**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:65`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX0

**Actual IO num for Debug IO ID0**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:70`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX1

**Actual IO num for Debug IO ID1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:84`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX10

**Actual IO num for Debug IO ID10**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:210`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX11

**Actual IO num for Debug IO ID11**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:224`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX2

**Actual IO num for Debug IO ID2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:98`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX3

**Actual IO num for Debug IO ID3**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:112`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX4

**Actual IO num for Debug IO ID4**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:126`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX5

**Actual IO num for Debug IO ID5**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:140`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX6

**Actual IO num for Debug IO ID6**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:154`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX7

**Actual IO num for Debug IO ID7**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:168`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX8

**Actual IO num for Debug IO ID8**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:182`

---

## CONFIG_ESP_COEX_GPIO_DEBUG_IO_IDX9

**Actual IO num for Debug IO ID9**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wireless Coexistence > GPIO debugging for coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:196`

---

## CONFIG_ESP_COEX_POWER_MANAGEMENT

**Support power management under coexistence**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:35`

If enabled, coexist power management will be enabled.

---

## CONFIG_ESP_COEX_SW_COEXIST_ENABLE

**Software controls WiFi/Bluetooth coexistence**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wireless Coexistence
- **Kconfig:** `/opt/esp/idf/components/esp_coex/Kconfig:9`

If enabled, WiFi & Bluetooth coexistence is controlled by software rather than hardware.
Recommended for heavy traffic scenarios. Both coexistence configuration options are
automatically managed, no user intervention is required.
If only Bluetooth is used, it is recommended to disable this option to reduce binary file
size.

---

## CONFIG_ESP_CONSOLE_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Channel for console output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:292`

---

## CONFIG_ESP_CONSOLE_ROM_SERIAL_PORT_NUM

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP_CONSOLE_SECONDARY_NONE

**No secondary console**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Channel for console secondary output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:306`

---

## CONFIG_ESP_CONSOLE_SECONDARY_USB_SERIAL_JTAG

**USB_SERIAL_JTAG PORT**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Channel for console secondary output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:308`

This option supports output through USB_SERIAL_JTAG port when the UART0 port is not connected.
The output currently only supports non-blocking mode without using the console.
If you want to output in blocking mode with REPL or input through USB_SERIAL_JTAG port,
please change the primary config to ESP_CONSOLE_USB_SERIAL_JTAG above.

---

## CONFIG_ESP_CONSOLE_UART

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_CONSOLE_UART_BAUDRATE

**UART console baud rate**

- **Type:** `unknown`
- **Default:** `115200`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:411`

This baud rate is used by both the ESP-IDF Bootloader and the app (including
boot log output and default standard input/output/error of the app).

The app's maximum baud rate depends on the UART clock source. If Power Management is disabled,
the UART clock source is the APB clock and all baud rates in the available range will be sufficiently
accurate. If Power Management is enabled, REF_TICK clock source is used so the baud rate is divided
from 1MHz. Baud rates above 1Mbps are not possible and values between 500Kbps and 1Mbps may not be
accurate.

If the configuration is different in the Bootloader binary compared to the app binary, UART
is reconfigured after the bootloader exits and the app starts.

---

## CONFIG_ESP_CONSOLE_UART_CUSTOM

**Custom UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Channel for console output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:290`

---

## CONFIG_ESP_CONSOLE_UART_CUSTOM_NUM_0

**UART0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > UART peripheral to use for console output (0-1)
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:342`

---

## CONFIG_ESP_CONSOLE_UART_CUSTOM_NUM_1

**UART1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > UART peripheral to use for console output (0-1)
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:344`

---

## CONFIG_ESP_CONSOLE_UART_DEFAULT

**Default: UART0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Channel for console output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:281`

---

## CONFIG_ESP_CONSOLE_UART_NUM

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP_CONSOLE_UART_RX_GPIO

**UART RX on GPIO<num>**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:387`

This GPIO is used for console UART RX input in the ESP-IDF Bootloader and the app (including
default standard input of the app). Value -1 means to continue using the default console UART
RX pin.

Note: The default ESP-IDF Bootloader configures this pin but doesn't read anything from the UART.

If the configuration is different in the Bootloader binary compared to the app binary, UART
is reconfigured after the bootloader exits and the app starts.

---

## CONFIG_ESP_CONSOLE_UART_TX_GPIO

**UART TX on GPIO<num>**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:366`

This GPIO is used for console UART TX output in the ESP-IDF Bootloader and the app (including
boot log output and default standard output and standard error of the app). Value -1 means to
continue using the default console UART TX pin.

If the configuration is different in the Bootloader binary compared to the app binary, UART
is reconfigured after the bootloader exits and the app starts.

---

## CONFIG_ESP_CONSOLE_USB_CDC

**USB CDC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Channel for console output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:283`

---

## CONFIG_ESP_CONSOLE_USB_CDC_RX_BUF_SIZE

**Size of USB CDC RX buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:432`

Set the size of USB CDC RX buffer. Increase the buffer size if your application
is often receiving data over USB CDC.

---

## CONFIG_ESP_CONSOLE_USB_CDC_SUPPORT_ETS_PRINTF

**Enable esp_rom_printf / ESP_EARLY_LOG via USB CDC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:441`

If enabled, esp_rom_printf and ESP_EARLY_LOG output will also be sent over USB CDC.
Disabling this option saves about 1kB or RAM.

---

## CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG

**USB Serial/JTAG Controller**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Channel for console output
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:287`

---

## CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_COREDUMP_CAPTURE_DRAM

**Include whole .bss and .data sections and heap data into core dump file**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:50`

Storing these sections can help with easier debugging and troubleshooting.
However, additional storage space will be required in the core dump partition.
At least 128KB should be reserved, but the actual amount required may vary based
on the application's DRAM usage.
Note that sections located in external RAM will not be stored.

---

## CONFIG_ESP_COREDUMP_CHECKSUM_CRC32

**Use CRC32 for integrity verification**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Core dump data integrity check
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:43`

---

## CONFIG_ESP_COREDUMP_CHECKSUM_SHA256

**Use SHA256 for integrity verification**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Core dump data integrity check
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:45`

---

## CONFIG_ESP_COREDUMP_CHECK_BOOT

**Check core dump data integrity on boot**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:61`

When enabled, if any data are found on the flash core dump partition,
they will be checked by calculating their checksum.

---

## CONFIG_ESP_COREDUMP_DATA_FORMAT_BIN

**Binary format**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Core dump data format
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:31`

---

## CONFIG_ESP_COREDUMP_DATA_FORMAT_ELF

**ELF format**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Core dump data format
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:33`

---

## CONFIG_ESP_COREDUMP_DECODE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_ESP_COREDUMP_DECODE_DISABLE

**Don't decode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Handling of UART core dumps in IDF Monitor
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:144`

---

## CONFIG_ESP_COREDUMP_DECODE_INFO

**Decode and show summary (info_corefile)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Handling of UART core dumps in IDF Monitor
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:142`

---

## CONFIG_ESP_COREDUMP_ENABLE

- **Type:** `string`
- **Default:** `n`

Enables/disable core dump module.

---

## CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH

**Flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Data destination
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:15`

---

## CONFIG_ESP_COREDUMP_ENABLE_TO_NONE

**None**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Core dump > Data destination
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:21`

---

## CONFIG_ESP_COREDUMP_ENABLE_TO_UART

**UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump > Data destination
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:18`

---

## CONFIG_ESP_COREDUMP_FLASH_NO_OVERWRITE

**Don't overwrite existing core dump**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:98`

Don't overwrite an existing core dump already present in flash.
Enable this option to only keep the first of multiple core dumps.

If enabled, the core dump partition must be erased before the first
core dump can be written.

---

## CONFIG_ESP_COREDUMP_LOGS

**Enable coredump logs for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:75`

Enable/disable coredump logs. Logs strings from espcoredump component are
placed in DRAM. Disabling these helps to save ~5KB of internal memory.

---

## CONFIG_ESP_COREDUMP_MAX_TASKS_NUM

**Maximum number of tasks**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:83`

Maximum number of tasks snapshots in core dump.

---

## CONFIG_ESP_COREDUMP_STACK_SIZE

**Reserved stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:116`

Size of the memory to be reserved for core dump stack. If 0 core dump process will run on
the stack of crashed task/ISR, otherwise special stack will be allocated.
To ensure that core dump itself will not overflow task/ISR stack set this to the value around 1300-1800
depending on the chosen checksum calculation method. SHA256 method needs more stack space than CRC32.
NOTE: It eats DRAM.

---

## CONFIG_ESP_COREDUMP_SUMMARY_STACKDUMP_SIZE

**Size of the stack dump buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:130`

Size of the buffer that would be reserved for extracting backtrace info summary.
This buffer will contain the stack dump of the crashed task. This dump is useful in generating backtrace

---

## CONFIG_ESP_COREDUMP_UART_DELAY

**Delay before print to UART**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Core dump
- **Kconfig:** `/opt/esp/idf/components/espcoredump/Kconfig:90`

Config delay (in ms) before printing core dump to UART.
Delay can be interrupted by pressing Enter key.

---

## CONFIG_ESP_COREDUMP_USE_STACK_SIZE

- **Type:** `string`
- **Default:** `n`

Force the use of a custom DRAM stack for coredump when Task stacks can be in PSRAM.

---

## CONFIG_ESP_CRYPTO_DPA_PROTECTION_AT_STARTUP

**Enable crypto DPA protection at startup**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific > Crypto DPA Protection
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:5`

This config controls the DPA (Differential Power Analysis) protection
knob for the crypto peripherals. DPA protection dynamically adjusts
clock frequency of the crypto peripheral. DPA protection helps to make it
difficult to perform SCA attacks on the crypto peripherals. However,
there is also associated performance impact based on the security level
set. Please refer to the TRM for more details.

---

## CONFIG_ESP_CRYPTO_DPA_PROTECTION_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_ESP_CRYPTO_DPA_PROTECTION_LEVEL_HIGH

**Security level high**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific > Crypto DPA Protection > Enable crypto DPA protection at startup > DPA protection level
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:29`

---

## CONFIG_ESP_CRYPTO_DPA_PROTECTION_LEVEL_LOW

**Security level low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific > Crypto DPA Protection > Enable crypto DPA protection at startup > DPA protection level
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:23`

---

## CONFIG_ESP_CRYPTO_DPA_PROTECTION_LEVEL_MEDIUM

**Security level medium**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific > Crypto DPA Protection > Enable crypto DPA protection at startup > DPA protection level
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:26`

---

## CONFIG_ESP_CRYPTO_FORCE_ECC_CONSTANT_TIME_POINT_MUL

**Forcefully enable ECC constant time point multiplication operations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:40`

If enabled, the app startup code will burn the ECC_FORCE_CONST_TIME efuse bit to force the
ECC peripheral to always perform constant time point multiplication operations,
irrespective of the ECC_MULT_SECURITY_MODE status bit that is present in the ECC_MULT_CONF_REG
register. By default, ESP-IDF configures the ECC peripheral to perform constant time point
multiplication operations, so enabling this config would provide security enhancement only in
the cases when trusted boot is not enabled and the attacker tries carrying out non-constant
time point multiplication operations by changing the default ESP-IDF configurations.
Performing constant time operations protect the ECC multiplication operations from timing attacks.

---

## CONFIG_ESP_DEBUG_INCLUDE_OCD_STUB_BINS

**Preload OpenOCD stub binaries to speed up debugging. 8K memory will be reserved**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:584`

OpenOCD uses stub code to access flash during programming or when inserting and removing
SW flash breakpoints.
To execute stub code, OpenOCD allocates memory on the target device, backs up the existing memory,
loads the stub binary, runs the binary, and then restores the original memory.
This process can be time-consuming, especially when using USB serial JTAG.
By enabling this option, 8K of memory in RAM will be preallocated with the stub code,
eliminating the need to back up and restore the memory region.

---

## CONFIG_ESP_DEBUG_OCDAWARE

**Make exception and panic handlers JTAG/OCD aware**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:597`

The FreeRTOS panic and unhandled exception handlers can detect a JTAG OCD debugger and
instead of panicking, have the debugger stop on the offending instruction.

---

## CONFIG_ESP_DEBUG_STUBS_ENABLE

**OpenOCD debug stubs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:576`

Debug stubs are used by OpenOCD to execute pre-compiled onboard code
which does some useful debugging stuff, e.g. GCOV data dump.

---

## CONFIG_ESP_DEBUG_WOLFSSL

**Enable debug logs for wolfSSL**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:103`

Enable detailed debug prints for wolfSSL SSL library.

---

## CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ

- **Type:** `unknown`
- **Default:** `160`

---

## CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_160

**160 MHz**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > CPU frequency
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cpu:13`

---

## CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240

**240 MHz**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > CPU frequency
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cpu:15`

---

## CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_40

**40 MHz**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > CPU frequency
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cpu:8`

---

## CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_80

**80 MHz**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > CPU frequency
- **Kconfig:** `/opt/esp/idf/components/esp_system/./port/soc/esp32s3/Kconfig.cpu:11`

---

## CONFIG_ESP_ECDSA_ENABLE_P192_CURVE

**Enable ECDSA 192-curve operations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Security Specific
- **Kconfig:** `/opt/esp/idf/components/esp_security/Kconfig:54`

By default, only the 256-bit curve operations are allowed. If this configuration is enabled,
it will set the eFuse to allow ECDSA operations using both the 192-bit and 256-bit curves.

---

## CONFIG_ESP_EFUSE_BLOCK_REV_MAX_FULL

- **Type:** `unknown`
- **Default:** `199`

---

## CONFIG_ESP_EFUSE_BLOCK_REV_MIN_FULL

**Minimum Supported ESP32-S3 eFuse Block Revision**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > Hardware Settings > Chip revision
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.hw_support:49`

Required minimum eFuse Block revision. ESP-IDF will check it at the 2nd bootloader stage
whether the current image can work correctly for this eFuse Block revision.
So that to avoid running an incompatible image on a SoC that contains breaking change in the eFuse Block.
If you want to update this value to run the image that not compatible with the current eFuse Block revision,
please contact to Espressif's business team for details:
https://www.espressif.com.cn/en/contact-us/sales-questions

---

## CONFIG_ESP_ENABLE_PVT

**Auto adjust hp & lp voltage using pvt function (MUST ENABLE FOR MP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:299`

If enabled, hp & lp voltage can be auto adjust by PVT characteristic.
Otherwise, internal voltage will be set to fix dbias.
This is a must for stable mass production. Disable for debugging only.

---

## CONFIG_ESP_ERR_TO_NAME_LOOKUP

**Enable lookup of error code strings**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Common ESP-related
- **Kconfig:** `/opt/esp/idf/components/esp_common/Kconfig:3`

Functions esp_err_to_name() and esp_err_to_name_r() return string representations of error codes from a
pre-generated lookup table. This option can be used to turn off the use of the look-up table in order to
save memory but this comes at the price of sacrificing distinguishable (meaningful) output string
representations.

---

## CONFIG_ESP_EVENT_LOOP_PROFILING

**Enable event loop profiling**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Event Loop Library
- **Kconfig:** `/opt/esp/idf/components/esp_event/Kconfig:3`

Enables collections of statistics in the event loop library such as the number of events posted
to/recieved by an event loop, number of callbacks involved, number of events dropped to to a full event
loop queue, run time of event handlers, and number of times/run time of each event handler.

---

## CONFIG_ESP_EVENT_POST_FROM_IRAM_ISR

**Support posting events from ISRs placed in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Event Loop Library > Support posting events from ISRs
- **Kconfig:** `/opt/esp/idf/components/esp_event/Kconfig:17`

Enable posting events from interrupt handlers placed in IRAM. Enabling this option places API functions
esp_event_post and esp_event_post_to in IRAM.

---

## CONFIG_ESP_EVENT_POST_FROM_ISR

**Support posting events from ISRs**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Event Loop Library
- **Kconfig:** `/opt/esp/idf/components/esp_event/Kconfig:11`

Enable posting events from interrupt handlers.

---

## CONFIG_ESP_GDBSTUB_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_GDBSTUB_MAX_TASKS

**Maximum number of tasks supported by GDB Stub**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > GDB Stub > Enable listing FreeRTOS tasks through GDB Stub
- **Kconfig:** `/opt/esp/idf/components/esp_gdbstub/Kconfig:30`

Set the number of tasks which GDB Stub will support.

---

## CONFIG_ESP_GDBSTUB_SUPPORT_TASKS

**Enable listing FreeRTOS tasks through GDB Stub**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > GDB Stub
- **Kconfig:** `/opt/esp/idf/components/esp_gdbstub/Kconfig:20`

If enabled, GDBStub can supply the list of FreeRTOS tasks to GDB.
Thread list can be queried from GDB using 'info threads' command.
Note that if GDB task lists were corrupted, this feature may not work.
If GDBStub fails, try disabling this feature.

---

## CONFIG_ESP_HOST_WIFI_ENABLED

**Host WiFi Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:9`

---

## CONFIG_ESP_HTTPS_OTA_ALLOW_HTTP

**Allow HTTP for OTA (WARNING: ONLY FOR TESTING PURPOSE, READ HELP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTPS OTA
- **Kconfig:** `/opt/esp/idf/components/esp_https_ota/Kconfig:12`

It is highly recommended to keep HTTPS (along with server certificate validation) enabled.
Enabling this option comes with potential risk of:
- Non-encrypted communication channel with server
- Accepting firmware upgrade image from server with fake identity

---

## CONFIG_ESP_HTTPS_OTA_DECRYPT_CB

**Provide decryption callback**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTPS OTA
- **Kconfig:** `/opt/esp/idf/components/esp_https_ota/Kconfig:3`

Exposes an additional callback whereby firmware data could be decrypted
before being processed by OTA update component. This can help to integrate
external encryption related format and removal of such encapsulation layer
from firmware image.

---

## CONFIG_ESP_HTTPS_OTA_EVENT_POST_TIMEOUT

**Time in millisecond to wait for posting event**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > ESP HTTPS OTA
- **Kconfig:** `/opt/esp/idf/components/esp_https_ota/Kconfig:21`

This config option helps in setting the time in millisecond to wait for event to be posted to the
system default event loop. Set it to -1 if you need to set timeout to portMAX_DELAY.

---

## CONFIG_ESP_HTTPS_SERVER_CERT_SELECT_HOOK

**Enable certificate selection hook**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTPS server
- **Kconfig:** `/opt/esp/idf/components/esp_https_server/Kconfig:16`

Enable certificate selection hook for ESP HTTPS Server. When enabled, this allows the server to
dynamically select the appropriate certificate based on the client's Server Name Indication (SNI).
This is useful for hosting multiple domains on a single server with different SSL certificates.

---

## CONFIG_ESP_HTTPS_SERVER_ENABLE

**Enable ESP_HTTPS_SERVER component**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTPS server
- **Kconfig:** `/opt/esp/idf/components/esp_https_server/Kconfig:3`

Enable ESP HTTPS server component

---

## CONFIG_ESP_HTTPS_SERVER_EVENT_POST_TIMEOUT

**Time in millisecond to wait for posting event**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > ESP HTTPS server
- **Kconfig:** `/opt/esp/idf/components/esp_https_server/Kconfig:9`

This config option helps in setting the time in millisecond to wait for event to be posted to the
system default event loop. Set it to -1 if you need to set timeout to portMAX_DELAY.

---

## CONFIG_ESP_HTTP_CLIENT_ENABLE_BASIC_AUTH

**Enable HTTP Basic Authentication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTP client
- **Kconfig:** `/opt/esp/idf/components/esp_http_client/Kconfig:10`

This option will enable HTTP Basic Authentication. It is disabled by default as Basic
auth uses unencrypted encoding, so it introduces a vulnerability when not using TLS

---

## CONFIG_ESP_HTTP_CLIENT_ENABLE_CUSTOM_TRANSPORT

**Enable custom transport**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTP client
- **Kconfig:** `/opt/esp/idf/components/esp_http_client/Kconfig:25`

This option will enable injection of a custom tcp_transport handle, so the http operation
will be performed on top of the user defined transport abstraction (if configured)

---

## CONFIG_ESP_HTTP_CLIENT_ENABLE_DIGEST_AUTH

**Enable HTTP Digest Authentication**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP HTTP client
- **Kconfig:** `/opt/esp/idf/components/esp_http_client/Kconfig:17`

This option will enable HTTP Digest Authentication. It is enabled by default, but use of this
configuration is not recommended as the password can be derived from the exchange, so it introduces
a vulnerability when not using TLS

---

## CONFIG_ESP_HTTP_CLIENT_ENABLE_HTTPS

**Enable https**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP HTTP client
- **Kconfig:** `/opt/esp/idf/components/esp_http_client/Kconfig:4`

This option will enable https protocol by linking esp-tls library and initializing SSL transport

---

## CONFIG_ESP_HTTP_CLIENT_EVENT_POST_TIMEOUT

**Time in millisecond to wait for posting event**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > ESP HTTP client
- **Kconfig:** `/opt/esp/idf/components/esp_http_client/Kconfig:32`

This config option helps in setting the time in millisecond to wait for event to be posted to the
system default event loop. Set it to -1 if you need to set timeout to portMAX_DELAY.

---

## CONFIG_ESP_INTR_IN_IRAM

**Place esp_intr_alloc functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:308`

---

## CONFIG_ESP_INT_WDT

**Interrupt watchdog**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:449`

This watchdog timer can detect if the FreeRTOS tick interrupt has not been called for a certain time,
either because a task turned off interrupts and did not turn them on for a long time, or because an
interrupt handler did not return. It will try to invoke the panic handler first and failing that
reset the SoC.

---

## CONFIG_ESP_INT_WDT_CHECK_CPU1

**Also watch CPU1 tick interrupt**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Interrupt watchdog
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:467`

Also detect if interrupts on CPU 1 are disabled for too long.

---

## CONFIG_ESP_INT_WDT_TIMEOUT_MS

**Interrupt watchdog timeout (ms)**

- **Type:** `unknown`
- **Default:** `300`
- **Menu:** Component config > ESP System Settings > Interrupt watchdog
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:458`

The timeout of the watchdog, in milliseconds. Make this higher than the FreeRTOS tick rate.

---

## CONFIG_ESP_IPC_ENABLE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_IPC_ISR_ENABLE

- **Type:** `string`
- **Default:** `y`

The IPC ISR feature is similar to the IPC feature except that the callback function is executed in the
context of a High Priority Interrupt. The IPC ISR feature is intended for low latency execution of simple
callbacks written in assembly on another CPU. Due to being run in a High Priority Interrupt, the assembly
callbacks must be written with particular restrictions (see "IPC" and "High-Level Interrupt" docs for more
details).

---

## CONFIG_ESP_IPC_TASK_STACK_SIZE

**Inter-Processor Call (IPC) task stack size**

- **Type:** `unknown`
- **Default:** `1280`
- **Menu:** Component config > IPC (Inter-Processor Call)
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:662`

Configure the IPC tasks stack size. An IPC task runs on each core (in dual core mode), and allows for
cross-core function calls. See IPC documentation for more details. The default IPC stack size should be
enough for most common simple use cases. However, users can increase/decrease the stack size to their
needs.

---

## CONFIG_ESP_IPC_USES_CALLERS_PRIORITY

**IPC runs at caller's priority**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > IPC (Inter-Processor Call)
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:675`

If this option is not enabled then the IPC task will keep behavior same as prior to that of ESP-IDF v4.0,
hence IPC task will run at (configMAX_PRIORITIES - 1) priority.

---

## CONFIG_ESP_LYRATD_MSC_V2_1_BOARD

**ESP32-LyraTD-MSC V2.1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:33`

---

## CONFIG_ESP_LYRATD_MSC_V2_2_BOARD

**ESP32-LyraTD-MSC V2.2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:35`

---

## CONFIG_ESP_LYRAT_MINI_V1_1_BOARD

**ESP32-Lyrat-Mini V1.1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:37`

---

## CONFIG_ESP_LYRAT_V4_2_BOARD

**ESP32-Lyrat V4.2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:31`

---

## CONFIG_ESP_LYRAT_V4_3_BOARD

**ESP32-Lyrat V4.3**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Audio board
- **Kconfig:** `components/audio_board/Kconfig.projbuild:29`

---

## CONFIG_ESP_MAC_ADDR_UNIVERSE_BT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_MAC_ADDR_UNIVERSE_ETH

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_MAC_ADDR_UNIVERSE_IEEE802154

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_MAC_ADDR_UNIVERSE_WIFI_AP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_MAC_ADDR_UNIVERSE_WIFI_STA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_MAC_IGNORE_MAC_CRC_ERROR

**Ignore MAC CRC error (not recommended)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > MAC Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:51`

If you have an invalid MAC CRC (ESP_ERR_INVALID_CRC) problem
and you still want to use this chip, you can enable this option to bypass such an error.
This applies to both MAC_FACTORY and CUSTOM_MAC efuses.

---

## CONFIG_ESP_MAC_UNIVERSAL_MAC_ADDRESSES

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_ESP_MAC_UNIVERSAL_MAC_ADDRESSES_FOUR

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_MAC_UNIVERSAL_MAC_ADDRESSES_ONE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_MAC_UNIVERSAL_MAC_ADDRESSES_TWO

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_MAC_USE_CUSTOM_MAC_AS_BASE_MAC

**Enable using custom mac as base mac**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > MAC Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:60`

When this configuration is enabled, the user can invoke `esp_read_mac` to obtain the desired type of
MAC using a custom MAC as the base MAC.

---

## CONFIG_ESP_MAIN_TASK_AFFINITY

- **Type:** `unknown`
- **Default:** `0x0`

---

## CONFIG_ESP_MAIN_TASK_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Main task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:240`

---

## CONFIG_ESP_MAIN_TASK_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Main task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:242`

---

## CONFIG_ESP_MAIN_TASK_AFFINITY_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Main task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:245`

---

## CONFIG_ESP_MAIN_TASK_STACK_SIZE

**Main task stack size**

- **Type:** `unknown`
- **Default:** `3584`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:225`

Configure the "main task" stack size. This is the stack of the task
which calls app_main(). If app_main() returns then this task is deleted
and its stack memory is freed.

---

## CONFIG_ESP_MINIMAL_SHARED_STACK_SIZE

**Minimal allowed size for shared stack**

- **Type:** `unknown`
- **Default:** `2048`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:256`

Minimal value of size, in bytes, accepted to execute a expression
with shared stack.

---

## CONFIG_ESP_MM_CACHE_MSYNC_C2M_CHUNKED_OPS

**Enable esp_cache_msync C2M chunked operation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MM: Memory Management Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_mm/Kconfig:3`

`esp_cache_msync` C2M direction takes critical sections, which means during
the operation, the interrupts are disabled. Whereas Cache writebacks for
large buffers could be especially time intensive, and might cause interrupts
to be disabled for a significant amount of time.

Sometimes you want other ISRs to be responded during this C2M process.
This option is to slice one C2M operation into multiple chunks,
with CONFIG_ESP_MM_CACHE_MSYNC_C2M_CHUNKED_OPS_MAX_LEN max len. This will give you
a breath during the C2M process as sometimes the C2M process is quite long.

Note if the buffer processed by the `esp_cache_msync` (C2M sliced) is interrupted by an ISR,
and this ISR also accesses this buffer, this may lead to data coherence issue.

---

## CONFIG_ESP_MM_CACHE_MSYNC_C2M_CHUNKED_OPS_MAX_LEN

**Max len in bytes per C2M chunk**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MM: Memory Management Configurations > Enable esp_cache_msync C2M chunked operation
- **Kconfig:** `/opt/esp/idf/components/esp_mm/Kconfig:20`

Max len in bytes per C2M chunk, operations with size over the max len will be
sliced into multiple chunks.

---

## CONFIG_ESP_NETIF_BRIDGE_EN

**Enable LwIP IEEE 802.1D bridge**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:89`

Enable LwIP IEEE 802.1D bridge support in ESP-NETIF. Note that "Number of clients store data in netif"
(LWIP_NUM_NETIF_CLIENT_DATA) option needs to be properly configured to be LwIP bridge available!

---

## CONFIG_ESP_NETIF_IP_LOST_TIMER_INTERVAL

**IP Address lost timer interval (seconds)**

- **Type:** `unknown`
- **Default:** `120`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:3`

The value of 0 indicates the IP lost timer is disabled, otherwise the timer is enabled.

The IP address may be lost because of some reasons, e.g. when the station disconnects
from soft-AP, or when DHCP IP renew fails etc. If the IP lost timer is enabled, it will
be started every time the IP is lost. Event SYSTEM_EVENT_STA_LOST_IP will be raised if
the timer expires. The IP lost timer is stopped if the station get the IP again before
the timer expires.

---

## CONFIG_ESP_NETIF_L2_TAP

**Enable netif L2 TAP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:63`

A user program can read/write link layer (L2) frames from/to ESP TAP device.
The ESP TAP device can be currently associated only with Ethernet physical interfaces.

---

## CONFIG_ESP_NETIF_L2_TAP_MAX_FDS

**Maximum number of opened L2 TAP File descriptors**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP NETIF Adapter > Enable netif L2 TAP support
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:70`

Maximum number of opened File descriptors (FD's) associated with ESP TAP device. ESP TAP FD's take up
a certain amount of memory, and allowing fewer FD's to be opened at the same time conserves memory.

---

## CONFIG_ESP_NETIF_L2_TAP_RX_QUEUE_SIZE

**Size of L2 TAP Rx queue**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP NETIF Adapter > Enable netif L2 TAP support
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:79`

Maximum number of frames queued in opened File descriptor. Once the queue is full, the newly arriving
frames are dropped until the queue has enough room to accept incoming traffic (Tail Drop queue
management).

---

## CONFIG_ESP_NETIF_LOOPBACK

**Loopback**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter > TCP/IP Stack Library
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:37`

Dummy implementation of esp-netif functionality which connects driver transmit
to receive function. This option is for testing purpose only

---

## CONFIG_ESP_NETIF_PROVIDE_CUSTOM_IMPLEMENTATION

**Use only ESP-NETIF headers**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:16`

No implementation of ESP-NETIF functions is provided.
This option is used for adding a custom TCP/IP stack and defining related
esp_netif functionality

---

## CONFIG_ESP_NETIF_RECEIVE_REPORT_ERRORS

**Use esp_err_t to report errors from esp_netif_receive**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:55`

Enable if esp_netif_receive() should return error code. This is useful to inform upper layers
that packet input to TCP/IP stack failed, so the upper layers could implement flow control.
This option is disabled by default due to backward compatibility and will be enabled in v6.0 (IDF-7194)

---

## CONFIG_ESP_NETIF_REPORT_DATA_TRAFFIC

**Report data traffic via events**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:48`

Enable if esp_netif_transmit() and esp_netif_receive() should generate events. This can be useful
to blink data traffic indication lights.

---

## CONFIG_ESP_NETIF_SET_DNS_PER_DEFAULT_NETIF

**Enable DNS server per interface**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP NETIF Adapter
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:97`

Enable this option to use the DNS server which belongs to the selected default network interface.
This feature collects DNS server and netif information from LWIP core modules.
Whenever a new default netif is selected, global DNS servers in LWIP are updated with the netif
related servers.

---

## CONFIG_ESP_NETIF_TCPIP_LWIP

**LwIP**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP NETIF Adapter > TCP/IP Stack Library
- **Kconfig:** `/opt/esp/idf/components/esp_netif/Kconfig:30`

lwIP is a small independent implementation of the TCP/IP protocol suite.

---

## CONFIG_ESP_NETIF_USES_TCPIP_WITH_BSD_API

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_PANIC_HANDLER_IRAM

**Place panic handler code in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:562`

If this option is disabled (default), the panic handler code is placed in flash not IRAM.
This means that if ESP-IDF crashes while flash cache is disabled, the panic handler will
automatically re-enable flash cache before running GDB Stub or Core Dump. This adds some minor
risk, if the flash cache status is also corrupted during the crash.

If this option is enabled, the panic handler code (including required UART functions) is placed
in IRAM. This may be necessary to debug some complex issues with crashes while flash cache is
disabled (for example, when writing to SPI flash) or when flash cache is corrupted when an exception
is triggered.

---

## CONFIG_ESP_PARTITION_ENABLE_STATS

**Host test statistics enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Partition API Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_partition/Kconfig:3`

This option enables gathering host test statistics and SPI flash wear levelling simulation.

---

## CONFIG_ESP_PARTITION_ERASE_CHECK

**Check if flash is erased before writing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Partition API Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_partition/Kconfig:10`

This option controls whether the partition implementation checks
if the flash is erased before writing to it.
This is necessary for SPIFFS, which expects to be able to write without erasing first.

---

## CONFIG_ESP_PERIPH_CTRL_FUNC_IN_IRAM

**Place peripheral control functions into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Peripheral Control
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:226`

Place peripheral control functions (e.g. periph_module_reset) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_ESP_PHY_CALIBRATION_AND_DATA_STORAGE

**Store phy calibration data in NVS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:8`

If this option is enabled, NVS will be initialized and calibration data will be loaded from there.
PHY calibration will be skipped on deep sleep wakeup. If calibration data is not found, full
calibration will be performed and stored in NVS. Normally, only partial calibration will be performed.
If this option is disabled, full calibration will be performed.

If it's easy that your board calibrate bad data, choose 'n'.
Two cases for example, you should choose 'n':
1.If your board is easy to be booted up with antenna disconnected.
2.Because of your board design, each time when you do calibration, the result are too unstable.
If unsure, choose 'y'.

---

## CONFIG_ESP_PHY_CALIBRATION_MODE

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP_PHY_DEBUG

**Enable PHY Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:195`

Enabling this option allows different kinds of phy debugging features.

---

## CONFIG_ESP_PHY_DEFAULT_INIT_IF_INVALID

**Reset default PHY init data if invalid**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Use a partition to store PHY init data
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:40`

If enabled, PHY init data will be restored to default if
it cannot be verified successfully to avoid endless bootloops.

If unsure, choose 'n'.

---

## CONFIG_ESP_PHY_DISABLE_PLL_TRACK

**Disable phy pll track(only for experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Enable PHY Debug
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:201`

Disable pll track. This configuration option is used for experimental.
PLL track helps the PHY module adapt to temperature changes, ensuring stable performance.
When pll enabled, the ESP PHY module will periodically track and adjust PLL parameters.

---

## CONFIG_ESP_PHY_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_PHY_ENABLE_CERT_TEST

**Enable RF certification test functions**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:130`

If enabled, you can use RF certification test APIs.

---

## CONFIG_ESP_PHY_ENABLE_USB

**Keep the USB PHY enabled when initializing WiFi**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:113`

On some ESP targets, the USB PHY can interfere with WiFi thus lowering WiFi performance.
As a result, on those affected ESP targets, the ESP PHY library's initialization will automatically
disable the USB PHY to get best WiFi performance.
This option controls whether or not the ESP PHY library will keep the USB PHY enabled on
initialization.

Note: This option can be disabled to increase WiFi performance. However, disabling this option will
also mean that the USB PHY cannot be used while WiFi is enabled.

---

## CONFIG_ESP_PHY_IMPROVE_RX_11B

**Improve Wi-Fi receive 11b pkts**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:160`

This is a workaround to improve Wi-Fi receive 11b pkts for some modules using AC-DC power supply with
high interference, enable this option will sacrifice Wi-Fi OFDM receive performance.
But to guarantee 11b receive performance serves as a bottom line in this case.

---

## CONFIG_ESP_PHY_INIT_DATA_ERROR

**Terminate operation when PHY init data error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Use a partition to store PHY init data > Support multiple PHY init data bin
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:74`

If enabled, when an error occurs while the PHY init data is updated,
the program will terminate and restart.
If not enabled, the PHY init data will not be updated when an error occurs.

---

## CONFIG_ESP_PHY_INIT_DATA_IN_PARTITION

**Use a partition to store PHY init data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:23`

If enabled, PHY init data will be loaded from a partition.
When using a custom partition table, make sure that PHY data
partition is included (type: 'data', subtype: 'phy').
With default partition tables, this is done automatically.
If PHY init data is stored in a partition, it has to be flashed there,
otherwise runtime error will occur.

If this option is not enabled, PHY init data will be embedded
into the application binary.

If unsure, choose 'n'.

---

## CONFIG_ESP_PHY_IRAM_OPT

**PHY IRAM speed optimization**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:187`

Select this option to place frequently called PHY library functions in IRAM.
When this option is disabled, more than 1.1Kbytes of IRAM memory will be saved,
but PHY performance will be reduced. This config only affect esp32c2 now.

---

## CONFIG_ESP_PHY_MAC_BB_PD

**Power down MAC and baseband of Wi-Fi and Bluetooth when PHY is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:96`

If enabled, the MAC and baseband of Wi-Fi and Bluetooth will be powered
down when PHY is disabled. Enabling this setting reduces power consumption
by a small amount but increases RAM use by approximately 4 KB(Wi-Fi only),
2 KB(Bluetooth only) or 5.3 KB(Wi-Fi + Bluetooth).

---

## CONFIG_ESP_PHY_MAX_TX_POWER

- **Type:** `unknown`
- **Default:** `20`

---

## CONFIG_ESP_PHY_MAX_WIFI_TX_POWER

**Max WiFi TX power (dBm)**

- **Type:** `unknown`
- **Default:** `20`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:84`

Set maximum transmit power for WiFi radio. Actual transmit power for high
data rates may be lower than this setting.

---

## CONFIG_ESP_PHY_MULTIPLE_INIT_DATA_BIN

**Support multiple PHY init data bin**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Use a partition to store PHY init data
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:51`

If enabled, the corresponding PHY init data type can be automatically switched
according to the country code. China's PHY init data bin is used by default.
Can be modified by country information in API esp_wifi_set_country().
The priority of switching the PHY init data type is:
1. Country configured by API esp_wifi_set_country()
and the parameter policy is WIFI_COUNTRY_POLICY_MANUAL.
2. Country notified by the connected AP.
3. Country configured by API esp_wifi_set_country()
and the parameter policy is WIFI_COUNTRY_POLICY_AUTO.

---

## CONFIG_ESP_PHY_MULTIPLE_INIT_DATA_BIN_EMBED

**Support embedded multiple phy init data bin to app bin**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Use a partition to store PHY init data > Support multiple PHY init data bin
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:66`

If enabled, multiple phy init data bin will embedded into app bin
If not enabled, multiple phy init data bin will still leave alone, and need to be flashed by users.

---

## CONFIG_ESP_PHY_PLL_TRACK_DEBUG

**Enable pll track logging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:175`

If enabled, there will be some logs while pll tracking

---

## CONFIG_ESP_PHY_PLL_TRACK_PERIOD_MS

**Set the period of the pll track**

- **Type:** `unknown`
- **Default:** `1000`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:169`

Set the period of the pll track.

---

## CONFIG_ESP_PHY_RECORD_USED_TIME

**Record PHY used time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:181`

Select to support record and query phy used time.

---

## CONFIG_ESP_PHY_REDUCE_TX_POWER

**Reduce PHY TX power when brownout reset**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:106`

When brownout reset occurs, reduce PHY TX power to keep the code running.

---

## CONFIG_ESP_PHY_RF_CAL_FULL

**Calibration full**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Calibration mode
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:150`

---

## CONFIG_ESP_PHY_RF_CAL_NONE

**Calibration none**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PHY > Calibration mode
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:148`

---

## CONFIG_ESP_PHY_RF_CAL_PARTIAL

**Calibration partial**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > PHY > Calibration mode
- **Kconfig:** `/opt/esp/idf/components/esp_phy/Kconfig:146`

---

## CONFIG_ESP_PROTOCOMM_DISCONNECT_AFTER_BLE_STOP

- **Type:** `string`
- **Default:** `n`

Terminate connection after calling protocomm_ble_stop

---

## CONFIG_ESP_PROTOCOMM_KEEP_BLE_ON_AFTER_BLE_STOP

- **Type:** `string`
- **Default:** `n`

Keep BT on after calling protocomm_ble_stop

---

## CONFIG_ESP_PROTOCOMM_SUPPORT_SECURITY_PATCH_VERSION

- **Type:** `string`
- **Default:** `y`

Enable support of security patch version. This is a hidden config option
kept for external components like "network_provisioning" to find out if
protocomm component support security patch version. This config option
also indicates availability of a new API `protocomm_get_sec_version`.
Please refer to Protocomm documentation in ESP-IDF Programming guide for
more details.

---

## CONFIG_ESP_PROTOCOMM_SUPPORT_SECURITY_VERSION_0

**Support protocomm security version 0 (no security)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Protocomm
- **Kconfig:** `/opt/esp/idf/components/protocomm/Kconfig:3`

Enable support of security version 0.
Disabling this option saves some code size.
Consult the Enabling protocomm security version section of the
Protocomm documentation in ESP-IDF Programming guide for more details.

---

## CONFIG_ESP_PROTOCOMM_SUPPORT_SECURITY_VERSION_1

**Support protocomm security version 1 (Curve25519 key exchange + AES-CTR encryption/decryption)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Protocomm
- **Kconfig:** `/opt/esp/idf/components/protocomm/Kconfig:12`

Enable support of security version 1.
Disabling this option saves some code size.
Consult the Enabling protocomm security version section of the
Protocomm documentation in ESP-IDF Programming guide for more details.

---

## CONFIG_ESP_PROTOCOMM_SUPPORT_SECURITY_VERSION_2

**Support protocomm security version 2 (SRP6a-based key exchange + AES-GCM encryption/decryption)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Protocomm
- **Kconfig:** `/opt/esp/idf/components/protocomm/Kconfig:21`

Enable support of security version 2.
Disabling this option saves some code size.
Consult the Enabling protocomm security version section of the
Protocomm documentation in ESP-IDF Programming guide for more details.

---

## CONFIG_ESP_REGI2C_CTRL_FUNC_IN_IRAM

**Place regi2c control functions into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Peripheral Control
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:233`

Place analog i2c master control functions (e.g. regi2c_ctrl_read_reg, regi2c_ctrl_write_reg) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_ESP_REV_MAX_FULL

- **Type:** `unknown`
- **Default:** `99`

---

## CONFIG_ESP_REV_MIN_FULL

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP_REV_NEW_CHIP_TEST

**Internal test mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Chip revision
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:7`

For internal chip testing, a small number of new versions chips didn't
update the version field in eFuse, you can enable this option to force the
software recognize the chip version based on the rev selected in menuconfig.

---

## CONFIG_ESP_ROM_CONSOLE_OUTPUT_SECONDARY

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_GET_CLK_FREQ

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_CACHE_SUSPEND_WAITI_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_CACHE_WRITEBACK_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_CRC_BE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_CRC_LE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_ENCRYPTED_WRITES_USING_LEGACY_DRV

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_ERASE_0_REGION_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_ETS_PRINTF_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_FLASH_COUNT_PAGES_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_HAL_WDT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_JPEG_DECODE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_LAYOUT_TABLE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_MZ_CRC32

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_NEWLIB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_NEWLIB_32BIT_TIME

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_NEWLIB_NANO_FORMAT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_OUTPUT_PUTC_FUNC

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_RETARGETABLE_LOCKING

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_SPI_FLASH

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_SPI_FLASH_MMAP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_SW_FLOAT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_HAS_VERSION

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_NEEDS_SET_CACHE_MMU_SIZE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_NEEDS_SWSETUP_WORKAROUND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_PRINT_IN_IRAM

**Place print functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-ROM
- **Kconfig:** `/opt/esp/idf/components/esp_rom/Kconfig:3`

---

## CONFIG_ESP_ROM_RAM_APP_NEEDS_MMU_INIT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_SUPPORT_DEEP_SLEEP_WAKEUP_STUB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_UART_CLK_IS_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_ROM_USB_OTG_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_ESP_ROM_USB_SERIAL_DEVICE_NUM

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_ESP_SLEEP_CACHE_SAFE_ASSERTION

**Check the cache safety of the sleep wakeup code in sleep process**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:183`

Enabling it will check the cache safety of the code before the flash power is ready after
light sleep wakeup, and check PM_SLP_IRAM_OPT related code cache safety. This option is
only for code quality inspection. Enabling it will increase the time overhead of entering
and exiting sleep. It is not recommended to enable it in the release version.

---

## CONFIG_ESP_SLEEP_DEBUG

**esp sleep debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:193`

Enable esp sleep debug.

---

## CONFIG_ESP_SLEEP_EVENT_CALLBACKS

**Enable registration of sleep event callbacks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:206`

If enabled, it allows user to register sleep event callbacks. It is primarily designed for internal
developers and customers can use PM_LIGHT_SLEEP_CALLBACKS as an alternative.

NOTE: These callbacks are executed from the IDLE task context hence you cannot have any blocking calls
in your callbacks.

NOTE: Enabling these callbacks may change sleep duration calculations based on time spent in
callback and hence it is highly recommended to keep them as short as possible.

---

## CONFIG_ESP_SLEEP_FLASH_LEAKAGE_WORKAROUND

**Pull-up Flash CS pin in light sleep**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:89`

All IOs will be set to isolate(floating) state by default during sleep.
Since the power supply of SPI Flash is not lost during lightsleep, if its CS pin is recognized as
low level(selected state) in the floating state, there will be a large current leakage, and the
data in Flash may be corrupted by random signals on other SPI pins.
Select this option will set the CS pin of Flash to PULL-UP state during sleep, but this will
increase the sleep current about 10 uA.
If you are developing with esp32xx modules, you must select this option, but if you are developing
with chips, you can also pull up the CS pin of SPI Flash in the external circuit to save power
consumption caused by internal pull-up during sleep.
(!!! Don't deselect this option if you don't have external SPI Flash CS pin pullups.)

---

## CONFIG_ESP_SLEEP_GPIO_ENABLE_INTERNAL_RESISTORS

**Allow to enable internal pull-up/downs for the Deep-Sleep wakeup IOs**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:199`

When using rtc gpio wakeup source during deepsleep without external pull-up/downs, you may want to
make use of the internal ones.

---

## CONFIG_ESP_SLEEP_GPIO_RESET_WORKAROUND

**light sleep GPIO reset workaround**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:134`

esp32c2, esp32c3, esp32s3, esp32c5, esp32c6 and esp32h2 will reset at wake-up if GPIO is received
a small electrostatic pulse during light sleep, with specific condition

- GPIO needs to be configured as input-mode only
- The pin receives a small electrostatic pulse, and reset occurs when the pulse
  voltage is higher than 6 V

For GPIO set to input mode only, it is not a good practice to leave it open/floating,
The hardware design needs to controlled it with determined supply or ground voltage
is necessary.

This option provides a software workaround for this issue. Configure to isolate all
GPIO pins in sleep state.

---

## CONFIG_ESP_SLEEP_MSPI_NEED_ALL_IO_PU

**Pull-up all SPI pins in light sleep**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:121`

To reduce leakage current, some types of SPI Flash/RAM only need to pull up the CS pin
during light sleep. But there are also some kinds of SPI Flash/RAM that need to pull up
all pins. It depends on the SPI Flash/RAM chip used.

---

## CONFIG_ESP_SLEEP_POWER_DOWN_FLASH

**Power down flash in light sleep when there is no SPIRAM or SPIRAM has independent power supply**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:71`

If enabled, chip will try to power down flash as part of esp_light_sleep_start(), which costs
more time when chip wakes up. Can only be enabled if there is no SPIRAM configured.

This option will power down flash under a strict but relatively safe condition. Also, it is possible to
power down flash under a relaxed condition by using esp_sleep_pd_config() to set ESP_PD_DOMAIN_VDDSDIO
to ESP_PD_OPTION_OFF. It should be noted that there is a risk in powering down flash, you can refer
`ESP-IDF Programming Guide/API Reference/System API/Sleep Modes/Power-down of Flash` for more details.

---

## CONFIG_ESP_SLEEP_PSRAM_LEAKAGE_WORKAROUND

**Pull-up PSRAM CS pin in light sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:105`

All IOs will be set to isolate(floating) state by default during sleep.
Since the power supply of PSRAM is not lost during lightsleep, if its CS pin is recognized as
low level(selected state) in the floating state, there will be a large current leakage, and the
data in PSRAM may be corrupted by random signals on other SPI pins.
Select this option will set the CS pin of PSRAM to PULL-UP state during sleep, but this will
increase the sleep current about 10 uA.
If you are developing with esp32xx modules, you must select this option, but if you are developing
with chips, you can also pull up the CS pin of PSRAM in the external circuit to save power
consumption caused by internal pull-up during sleep.
(!!! Don't deselect this option if you don't have external PSRAM CS pin pullups.)

---

## CONFIG_ESP_SLEEP_RTC_BUS_ISO_WORKAROUND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_SLEEP_WAIT_FLASH_READY_EXTRA_DELAY

**Extra delay (in us) after flash powerdown sleep wakeup to wait flash ready**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > Hardware Settings > Sleep Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:154`

When the chip exits sleep, the CPU and the flash chip are powered on at the same time.
CPU will run rom code (deepsleep) or ram code (lightsleep) first, and then load or execute
code from flash.

Some flash chips need sufficient time to pass between power on and first read operation.
By default, without any extra delay, this time is approximately 900us, although
some flash chip types need more than that.

(!!! Please adjust this value according to the Data Sheet of SPI Flash used in your project.)
In Flash Data Sheet, the parameters that define the Flash ready timing after power-up (minimum
time from Vcc(min) to CS active) are usually named tVSL in ELECTRICAL CHARACTERISTICS chapter,
and the configuration value here should be:
ESP_SLEEP_WAIT_FLASH_READY_EXTRA_DELAY = tVSL - 900

For esp32 and esp32s3, the default extra delay is set to 2000us. When optimizing startup time
for applications which require it, this value may be reduced.

If you are seeing "flash read err, 1000" message printed to the console after deep sleep reset
on esp32, or triggered RTC_WDT/LP_WDT after lightsleep wakeup, try increasing this value.
(For esp32, the delay will be executed in both deep sleep and light sleep wake up flow.
For chips after esp32, the delay will be executed only in light sleep flow, the delay
controlled by the EFUSE_FLASH_TPUW in ROM will be executed in deepsleep wake up flow.)

---

## CONFIG_ESP_SPI_BUS_LOCK_FUNCS_IN_IRAM

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_SPI_BUS_LOCK_ISR_FUNCS_IN_IRAM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_SYSTEM_ALLOW_RTC_FAST_MEM_AS_HEAP

**Enable RTC fast memory for dynamic allocations**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:118`

This config option allows to add RTC fast memory region to system heap with capability
similar to that of DRAM region but without DMA. Speed wise RTC fast memory operates on
APB clock and hence does not have much performance impact.

---

## CONFIG_ESP_SYSTEM_BBPLL_RECALIB

**Re-calibration BBPLL at startup**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:636`

This configuration helps to address an BBPLL inaccurate issue when boot from certain bootloader version,
which may increase about the boot-up time by about 200 us. Disable this when your bootloader is built with
ESP-IDF version v5.2 and above.

---

## CONFIG_ESP_SYSTEM_CHECK_INT_LEVEL_4

**Level 4 interrupt**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Interrupt level to use for Interrupt Watchdog and other system checks
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:617`

Using level 4 interrupt for Interrupt Watchdog, IPC_ISR and other system checks.

---

## CONFIG_ESP_SYSTEM_CHECK_INT_LEVEL_5

**Level 5 interrupt**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Interrupt level to use for Interrupt Watchdog and other system checks
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:611`

Using level 5 interrupt for Interrupt Watchdog, IPC_ISR and other system checks.

---

## CONFIG_ESP_SYSTEM_EVENT_QUEUE_SIZE

**System event queue size**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:213`

Config system event queue size in different application.

---

## CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE

**Event loop task stack size**

- **Type:** `unknown`
- **Default:** `2304`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:219`

Config system event task stack size in different application.

---

## CONFIG_ESP_SYSTEM_GDBSTUB_RUNTIME

**GDBStub at runtime**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > GDB Stub
- **Kconfig:** `/opt/esp/idf/components/esp_gdbstub/Kconfig:9`

Enable builtin GDBStub.
This allows to debug the target device using serial port:
- Run 'idf.py monitor'.
- Wait for the device to initialize.
- Press Ctrl+C to interrupt the execution and enter GDB attached to your device for debugging.
NOTE: all UART input will be handled by GDBStub.

---

## CONFIG_ESP_SYSTEM_HW_PC_RECORD

**Hardware PC recording**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:645`

This option will enable the PC recording function of assist_debug module. The PC value of the CPU will be
recorded to PC record register in assist_debug module in real time. When an exception occurs and the CPU
is reset, this register will be kept, then we can use the recorded PC to debug the causes of the reset.

---

## CONFIG_ESP_SYSTEM_HW_STACK_GUARD

**Hardware stack guard**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:627`

This config allows to trigger a panic interrupt when Stack Pointer register goes out of allocated stack
memory bounds.

---

## CONFIG_ESP_SYSTEM_IN_IRAM

**Place system functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:12`

The following system functions will be placed in IRAM if this option is enabled:
- system startup
- system time
- system error
- system restart
- system crosscore
- system debug
- system APB backup DMA lock
- system application tick hook
- Unified Behavior Sanitizer (UBSAN) hook
- Interrupt watchdog handler
- XTAL32K watchdog timer
- USB CDC functions for the esp_rom_printf (if ESP_CONSOLE_USB_CDC_SUPPORT_ETS_PRINTF=y)
- IPC and IPC ISR

---

## CONFIG_ESP_SYSTEM_MEMPROT_FEATURE

**Enable memory protection**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Memory protection
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:191`

If enabled, the permission control module watches all the memory access and fires the panic handler
if a permission violation is detected. This feature automatically splits
the SRAM memory into data and instruction segments and sets Read/Execute permissions
for the instruction part (below given splitting address) and Read/Write permissions
for the data part (above the splitting address). The memory protection is effective
on all access through the IRAM0 and DRAM0 buses.

---

## CONFIG_ESP_SYSTEM_MEMPROT_FEATURE_LOCK

**Lock memory protection settings**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Memory protection > Enable memory protection
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:203`

Once locked, memory protection settings cannot be changed anymore.
The lock is reset only on the chip startup.

---

## CONFIG_ESP_SYSTEM_MEMPROT_FEATURE_VIA_TEE

**Enable memory protection (via TEE)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Memory protection
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:184`

This option enables the default memory protection provided by TEE.

---

## CONFIG_ESP_SYSTEM_NO_BACKTRACE

**No backtracing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Backtracing method
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:134`

When selected, no backtracing will be performed at runtime. By using idf.py monitor, it
is still possible to get a backtrace when a panic occurs.

---

## CONFIG_ESP_SYSTEM_PANIC_GDBSTUB

**GDBStub on panic**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Panic handler behaviour
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:58`

Invoke gdbstub on the serial port, allowing for gdb to attach to it to do a postmortem
of the crash.

---

## CONFIG_ESP_SYSTEM_PANIC_PRINT_HALT

**Print registers and halt**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Panic handler behaviour
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:38`

Outputs the relevant registers over the serial port and halt the
processor. Needs a manual reset to restart.

---

## CONFIG_ESP_SYSTEM_PANIC_PRINT_REBOOT

**Print registers and reboot**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Panic handler behaviour
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:45`

Outputs the relevant registers over the serial port and immediately
reset the processor.

---

## CONFIG_ESP_SYSTEM_PANIC_REBOOT_DELAY_SECONDS

**Panic reboot delay (Seconds)**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:67`

After the panic handler executes, you can specify a number of seconds to
wait before the device reboots.

---

## CONFIG_ESP_SYSTEM_PANIC_SILENT_REBOOT

**Silent reboot**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Panic handler behaviour
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:52`

Just resets the processor without outputting anything

---

## CONFIG_ESP_SYSTEM_PMP_IDRAM_SPLIT

**Enable IRAM/DRAM split protection**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Memory protection
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:162`

If enabled, the CPU watches all the memory access and raises an exception in case
of any memory violation. This feature automatically splits
the SRAM memory, using PMP, into data and instruction segments and sets Read/Execute permissions
for the instruction part (below given splitting address) and Read/Write permissions
for the data part (above the splitting address). The memory protection is effective
on all access through the IRAM0 and DRAM0 buses.

---

## CONFIG_ESP_SYSTEM_PMP_LP_CORE_RESERVE_MEM_EXECUTABLE

**Make LP core reserved memory executable from HP core**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Memory protection > Enable IRAM/DRAM split protection
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:174`

If enabled, user can run code available in LP Core image.

Warning: on ESP32-P4 this will also mark the memory area used for BOOTLOADER_RESERVE_RTC_MEM
as executable. If you consider this a security risk then do not activate this option.

---

## CONFIG_ESP_SYSTEM_RTC_EXT_OSC

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_SYSTEM_RTC_EXT_XTAL

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_SYSTEM_RTC_EXT_XTAL_BOOTSTRAP_CYCLES

**Bootstrap cycles for external 32kHz crystal**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:94`

To reduce the startup time of an external RTC crystal,
we bootstrap it with a 32kHz square wave for a fixed number of cycles.
Setting 0 will disable bootstrapping (if disabled, the crystal may take
longer to start up or fail to oscillate under some conditions).

If this value is too high, a faulty crystal may initially start and then fail.
If this value is too low, an otherwise good crystal may not start.

To accurately determine if the crystal has started,
set a larger "Number of cycles for RTC_SLOW_CLK calibration" (about 3000).

---

## CONFIG_ESP_SYSTEM_RTC_FAST_MEM_AS_HEAP_DEPCHECK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_SYSTEM_SINGLE_CORE_MODE

- **Type:** `string`
- **Default:** `n`

Only initialize and use the main core.

---

## CONFIG_ESP_SYSTEM_USE_EH_FRAME

**Generate and use eh_frame for backtracing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Backtracing method
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:140`

Generate DWARF information for each function of the project. These information will parsed and used to
perform backtracing when panics occur. Activating this option will activate asynchronous frame
unwinding and generation of both .eh_frame and .eh_frame_hdr sections, resulting in a bigger binary
size (20% to 100% larger). The main purpose of this option is to be able to have a backtrace parsed
and printed by the program itself, regardless of the serial monitor used.
This option is not recommended to be used for production.

---

## CONFIG_ESP_SYSTEM_USE_FRAME_POINTER

**Use CPU Frame Pointer register**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Backtracing method
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:150`

This configuration allows the compiler to allocate CPU register s0 as the frame pointer. The main usage
of the frame pointer is to be able to generate a backtrace from the panic handler on exception.
Enabling this option results in bigger and slightly slower code since all functions will have
to populate this register and won't be able to use it as a general-purpose register anymore.

---

## CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU0

**Watch CPU0 Idle Task**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Enable Task Watchdog Timer > Initialize Task Watchdog Timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:518`

If this option is enabled, the Task Watchdog Timer will watch the CPU0
Idle Task. Having the Task Watchdog watch the Idle Task allows for detection
of CPU starvation as the Idle Task not being called is usually a symptom of
CPU starvation. Starvation of the Idle Task is detrimental as FreeRTOS household
tasks depend on the Idle Task getting some runtime every now and then.

---

## CONFIG_ESP_TASK_WDT_CHECK_IDLE_TASK_CPU1

**Watch CPU1 Idle Task**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Enable Task Watchdog Timer > Initialize Task Watchdog Timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:529`

If this option is enabled, the Task Watchdog Timer will wach the CPU1
Idle Task.

---

## CONFIG_ESP_TASK_WDT_EN

**Enable Task Watchdog Timer**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:474`

The Task Watchdog Timer can be used to make sure individual tasks are still
running. Enabling this option will enable the Task Watchdog Timer. It can be
either initialized automatically at startup or initialized after startup
(see Task Watchdog Timer API Reference)

---

## CONFIG_ESP_TASK_WDT_INIT

**Initialize Task Watchdog Timer on startup**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP System Settings > Enable Task Watchdog Timer
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:492`

Enabling this option will cause the Task Watchdog Timer to be initialized
automatically at startup.

---

## CONFIG_ESP_TASK_WDT_PANIC

**Invoke panic handler on Task Watchdog timeout**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Enable Task Watchdog Timer > Initialize Task Watchdog Timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:500`

If this option is enabled, the Task Watchdog Timer will be configured to
trigger the panic handler when it times out. This can also be configured
at run time (see Task Watchdog Timer API Reference)

---

## CONFIG_ESP_TASK_WDT_TIMEOUT_S

**Task Watchdog timeout period (seconds)**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > ESP System Settings > Enable Task Watchdog Timer > Initialize Task Watchdog Timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:509`

Timeout period configuration for the Task Watchdog Timer in seconds.
This is also configurable at run time (see Task Watchdog Timer API Reference)

---

## CONFIG_ESP_TASK_WDT_USE_ESP_TIMER

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_TIMER_IMPL_SYSTIMER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_TIMER_IMPL_TG0_LAC

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_TIMER_INTERRUPT_LEVEL

**Interrupt level**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:39`

This sets the interrupt priority level for esp_timer ISR.
A higher value reduces interrupt latency by minimizing the timer processing delay.

---

## CONFIG_ESP_TIMER_IN_IRAM

**Place esp_timer functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:3`

---

## CONFIG_ESP_TIMER_ISR_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > timer interrupt core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:95`

---

## CONFIG_ESP_TIMER_ISR_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > timer interrupt core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:97`

---

## CONFIG_ESP_TIMER_ISR_AFFINITY_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > timer interrupt core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:100`

---

## CONFIG_ESP_TIMER_PROFILING

**Enable esp_timer profiling features**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:7`

If enabled, esp_timer_dump will dump information such as number of times the timer was started,
number of times the timer has triggered, and the total time it took for the callback to run.
This option has some effect on timer performance and the amount of memory used for timer
storage, and should only be used for debugging/testing purposes.

---

## CONFIG_ESP_TIMER_SHOW_EXPERIMENTAL

**show esp_timer's experimental features**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:48`

This shows some hidden features of esp_timer.
Note that they may break other features, use them with care.

---

## CONFIG_ESP_TIMER_SUPPORTS_ISR_DISPATCH_METHOD

**Support ISR dispatch method**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:105`

Allows using ESP_TIMER_ISR dispatch method (ESP_TIMER_TASK dispatch method is also available).
- ESP_TIMER_TASK - Timer callbacks are dispatched from a high-priority esp_timer task.
- ESP_TIMER_ISR - Timer callbacks are dispatched directly from the timer interrupt handler.
The ISR dispatch can be used, in some cases, when a callback is very simple
or need a lower-latency.

---

## CONFIG_ESP_TIMER_TASK_AFFINITY

- **Type:** `unknown`
- **Default:** `0x0`

---

## CONFIG_ESP_TIMER_TASK_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > esp_timer task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:71`

---

## CONFIG_ESP_TIMER_TASK_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > esp_timer task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:73`

---

## CONFIG_ESP_TIMER_TASK_AFFINITY_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Timer (High Resolution Timer) > esp_timer task core affinity
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:76`

---

## CONFIG_ESP_TIMER_TASK_STACK_SIZE

**High-resolution timer task stack size**

- **Type:** `unknown`
- **Default:** `3584`
- **Menu:** Component config > ESP Timer (High Resolution Timer)
- **Kconfig:** `/opt/esp/idf/components/esp_timer/Kconfig:25`

Configure the stack size of "timer_task" task. This task is used
to dispatch callbacks of timers created using ets_timer and esp_timer
APIs. If you are seeing stack overflow errors in timer task, increase
this value.

Note that this is not the same as FreeRTOS timer task. To configure
FreeRTOS timer task size, see "FreeRTOS timer task stack size" option
in "FreeRTOS".

---

## CONFIG_ESP_TIME_FUNCS_USE_ESP_TIMER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_TIME_FUNCS_USE_NONE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_TIME_FUNCS_USE_RTC_TIMER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_TLS_CLIENT_SESSION_TICKETS

**Enable client session tickets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:35`

Enable session ticket support as specified in RFC5077.

---

## CONFIG_ESP_TLS_DYN_BUF_STRATEGY_SUPPORTED

- **Type:** `string`
- **Default:** `y`

Enable support for dynamic buffer strategy for ESP-TLS. This is the hidden config option kept
for external components like OTA, to find out whether the dynamic buffer strategy is supported
for particular ESP-IDF version.

---

## CONFIG_ESP_TLS_INSECURE

**Allow potentially insecure options**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:84`

You can enable some potentially insecure options. These options should only be used for testing purposes.
Only enable these options if you are very sure.

---

## CONFIG_ESP_TLS_OCSP_CHECKALL

**Enabled full OCSP checks for ESP-TLS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:109`

Enable a fuller set of OCSP checks: checking revocation status of intermediate certificates,
optional fallbacks to CRLs, etc.

---

## CONFIG_ESP_TLS_PSK_VERIFICATION

**Enable PSK verification**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:73`

Enable support for pre shared key ciphers, supported for both mbedTLS as well as
wolfSSL TLS library.

---

## CONFIG_ESP_TLS_SERVER_CERT_SELECT_HOOK

**Certificate selection hook**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:54`

Ability to configure and use a certificate selection callback during server handshake,
to select a certificate to present to the client based on the TLS extensions supplied in
the client hello (alpn, sni, etc).

---

## CONFIG_ESP_TLS_SERVER_MIN_AUTH_MODE_OPTIONAL

**ESP-TLS Server: Set minimum Certificate Verification mode to Optional**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:62`

When this option is enabled, the peer (here, the client) certificate is checked by the server,
however the handshake continues even if verification failed. By default, the
peer certificate is not checked and ignored by the server.

mbedtls_ssl_get_verify_result() can be called after the handshake is complete to
retrieve status of verification.

---

## CONFIG_ESP_TLS_SERVER_SESSION_TICKETS

**Enable server session tickets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:41`

Enable session ticket support as specified in RFC5077

---

## CONFIG_ESP_TLS_SERVER_SESSION_TICKET_TIMEOUT

**Server session ticket timeout in seconds**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-TLS > Enable server session tickets
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:47`

Sets the session ticket timeout used in the tls server.

---

## CONFIG_ESP_TLS_SKIP_SERVER_CERT_VERIFY

**Skip server certificate verification by default (WARNING: ONLY FOR TESTING PURPOSE, READ HELP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS > Allow potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:90`

After enabling this option the esp-tls client will skip the server certificate verification
by default. Note that this option will only modify the default behaviour of esp-tls client
regarding server cert verification. The default behaviour should only be applicable when
no other option regarding the server cert verification is opted in the esp-tls config
(e.g. crt_bundle_attach, use_global_ca_store etc.).
WARNING : Enabling this option comes with a potential risk of establishing a TLS connection
with a server which has a fake identity, provided that the server certificate
is not provided either through API or other mechanism like ca_store etc.

---

## CONFIG_ESP_TLS_USE_DS_PERIPHERAL

**Use Digital Signature (DS) Peripheral with ESP-TLS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:26`

Enable use of the Digital Signature Peripheral for ESP-TLS.The DS peripheral
can only be used when it is appropriately configured for TLS.
Consult the ESP-TLS documentation in ESP-IDF Programming Guide for more details.

---

## CONFIG_ESP_TLS_USE_SECURE_ELEMENT

**Use Secure Element (ATECC608A) with ESP-TLS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:16`

Enable use of Secure Element for ESP-TLS, this enables internal support for
ATECC608A peripheral, which can be used for TLS connection.

---

## CONFIG_ESP_TLS_USING_MBEDTLS

**mbedTLS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-TLS > Choose SSL/TLS library for ESP-TLS (See help for more Info)
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:9`

---

## CONFIG_ESP_TLS_USING_WOLFSSL

**wolfSSL (License info in wolfSSL directory README)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-TLS > Choose SSL/TLS library for ESP-TLS (See help for more Info)
- **Kconfig:** `/opt/esp/idf/components/esp-tls/Kconfig:11`

---

## CONFIG_ESP_WIFI_11KV_SUPPORT

**Enable 802.11k, 802.11v APIs Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:645`

Select this option to enable 802.11k 802.11v APIs(RRM and BTM support).

---

## CONFIG_ESP_WIFI_11R_SUPPORT

**Enable 802.11R (Fast Transition) Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:723`

Select this option to enable WiFi Fast Transition Support.

---

## CONFIG_ESP_WIFI_AMPDU_RX_ENABLED

**WiFi AMPDU RX**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:194`

Select this option to enable AMPDU RX feature

---

## CONFIG_ESP_WIFI_AMPDU_TX_ENABLED

**WiFi AMPDU TX**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:175`

Select this option to enable AMPDU TX feature

---

## CONFIG_ESP_WIFI_AMSDU_TX_ENABLED

**WiFi AMSDU TX**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:215`

Select this option to enable AMSDU TX feature

---

## CONFIG_ESP_WIFI_BSS_MAX_IDLE_SUPPORT

**Enable bss max idle support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:368`

Enables bss max idle support for Station and SoftAP. BSS max idle period enables an SoftAP to indicate
a time period during which the AP does not disassociate a STA due to nonreceipt of frames from the STA.
For station, max idle period is default 10 (1000TUs) and can be set through
ESP_WIFI_SLP_DEFAULT_MAX_ACTIVE_TIME. For softap, bss max idle parameters will be set through
bss_max_idle_cfg in wifi_ap_config_t.

---

## CONFIG_ESP_WIFI_CACHE_TX_BUFFER_NUM

**Max number of WiFi cache TX buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:105`

Set the number of WiFi cache TX buffer number.

For each TX packet from uplayer, such as LWIP etc, WiFi driver needs to allocate a static TX
buffer and makes a copy of uplayer packet. If WiFi driver fails to allocate the static TX buffer,
it caches the uplayer packets to a dedicated buffer queue, this option is used to configure the
size of the cached TX queue.

---

## CONFIG_ESP_WIFI_CONTROLLER_TARGET

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_ESP_WIFI_CSI_ENABLED

**WiFi CSI(Channel State Information)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:166`

Select this option to enable CSI(Channel State Information) feature. CSI takes about
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM KB of RAM. If CSI is not used, it is better to disable
this feature in order to save memory.

---

## CONFIG_ESP_WIFI_DEBUG_PRINT

**Print debug messages from WPA Supplicant**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:819`

Select this option to print logging information from WPA supplicant,
this includes handshake information and key hex dumps depending
on the project logging level.

Enabling this could increase the build size ~60kb
depending on the project logging level.

---

## CONFIG_ESP_WIFI_DPP_SUPPORT

**Enable DPP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:716`

Select this option to enable WiFi Easy Connect Support.

---

## CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM

**Max number of WiFi dynamic RX buffers**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:42`

Set the number of WiFi dynamic RX buffers, 0 means unlimited RX buffers will be allocated
(provided sufficient free RAM). The size of each dynamic RX buffer depends on the size of
the received data frame.

For each received data frame, the WiFi driver makes a copy to an RX buffer and then delivers
it to the high layer TCP/IP stack. The dynamic RX buffer is freed after the higher layer has
successfully received the data frame.

For some applications, WiFi data frames may be received faster than the application can
process them. In these cases we may run out of memory if RX buffer number is unlimited (0).

If a dynamic RX buffer limit is set, it should be at least the number of static RX buffers.

---

## CONFIG_ESP_WIFI_DYNAMIC_RX_MGMT_BUF

- **Type:** `unknown`
- **Default:** `0`

---

## CONFIG_ESP_WIFI_DYNAMIC_RX_MGMT_BUFFER

**Dynamic**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Type of WiFi RX MGMT buffers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:147`

---

## CONFIG_ESP_WIFI_DYNAMIC_TX_BUFFER

**Dynamic**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Type of WiFi TX buffers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:80`

---

## CONFIG_ESP_WIFI_DYNAMIC_TX_BUFFER_NUM

**Max number of WiFi dynamic TX buffers**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:118`

Set the number of WiFi dynamic TX buffers. The size of each dynamic TX buffer is not fixed,
it depends on the size of each transmitted data frame.

For each transmitted frame from the higher layer TCP/IP stack, the WiFi driver makes a copy
of it in a TX buffer. For some applications, especially UDP applications, the upper layer
can deliver frames faster than WiFi layer can transmit. In these cases, we may run out of TX
buffers.

---

## CONFIG_ESP_WIFI_EAP_TLS1_3

**Enable EAP-TLS v1.3 Support for WiFi Enterprise connection**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Use MbedTLS crypto APIs > Use MbedTLS TLS client for WiFi Enterprise connection
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:612`

Select this option to support EAP with TLS v1.3.
This configuration still supports compatibility with EAP-TLS v1.2.
Please note that enabling this configuration will cause every application which
uses TLS go for TLS1.3 if server supports that. TLS1.3 is still in development in mbedtls
and there may be interoperability issues with this. Please modify your application to set
max version as TLS1.2 if you want to enable TLS1.3 only for WiFi connection.

---

## CONFIG_ESP_WIFI_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ESP_WIFI_ENABLE_DUMP_CTRL_BFRP

**Enable Wi-Fi dump BFRP frames**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:789`

Enable Wi-Fi dump BFRP frames.

---

## CONFIG_ESP_WIFI_ENABLE_DUMP_CTRL_NDPA

**Enable Wi-Fi dump NDPA frames**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:782`

Enable Wi-Fi dump NDPA frames.

---

## CONFIG_ESP_WIFI_ENABLE_DUMP_HESIGB

**Enable Wi-Fi dump HE-SIGB which is contained in DL HE MU PPDUs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:768`

Enable Wi-Fi dump HE-SIGB which is contained in DL HE MU PPDUs.

---

## CONFIG_ESP_WIFI_ENABLE_DUMP_MU_CFO

**Enable Wi-Fi dump MU CFO**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:775`

Enable Wi-Fi dump MU CFO.

---

## CONFIG_ESP_WIFI_ENABLE_ROAMING_APP

**Advanced support for Wi-Fi Roaming (Experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:697`

Enable Espressif's roaming app to allow for efficient Wi-Fi roaming.
This includes configurable periodic environment scans, maintaining a cache of the
best APs, handling low rssi events etc.

Risk Warning
Please note that this feature is still experimental and enabling this potentially can
lead to unpredictable scanning, connection and roaming attempts.
We are still working on tuning and optimising this feature to ensure reliable and stable use.

---

## CONFIG_ESP_WIFI_ENABLE_SAE_H2E

**Enable SAE-H2E**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Enable WPA3-Personal
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:318`

Select this option to enable SAE-H2E

---

## CONFIG_ESP_WIFI_ENABLE_SAE_PK

**Enable SAE-PK**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Enable WPA3-Personal
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:311`

Select this option to enable SAE-PK

---

## CONFIG_ESP_WIFI_ENABLE_WIFI_RX_MU_STATS

**Enable Wi-Fi DL MU-MIMO and DL OFDMA reception statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Enable Wi-Fi reception statistics
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:752`

Enable Wi-Fi DL MU-MIMO and DL OFDMA reception statistics. Will use 10932 bytes memory.

---

## CONFIG_ESP_WIFI_ENABLE_WIFI_RX_STATS

**Enable Wi-Fi reception statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:744`

Enable Wi-Fi reception statistics. Total support 2 access category. Each access category
will use 190 bytes memory.

---

## CONFIG_ESP_WIFI_ENABLE_WIFI_TX_STATS

**Enable Wi-Fi transmission statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:736`

Enable Wi-Fi transmission statistics. Total support 4 access category. Each access category
will use 346 bytes memory.

---

## CONFIG_ESP_WIFI_ENABLE_WPA3_OWE_STA

**Enable OWE STA**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:333`

Select this option to allow the device to establish OWE connection with eligible AP's.
PMF (Protected Management Frames) is a prerequisite feature for a WPA3 connection, it needs to be
explicitly configured before attempting connection. Please refer to the Wi-Fi Driver API Guide
for details.

---

## CONFIG_ESP_WIFI_ENABLE_WPA3_SAE

**Enable WPA3-Personal**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:301`

Select this option to allow the device to establish a WPA3-Personal connection with eligible AP's.
PMF (Protected Management Frames) is a prerequisite feature for a WPA3 connection, it needs to be
explicitly configured before attempting connection. Please refer to the Wi-Fi Driver API Guide
for details.

---

## CONFIG_ESP_WIFI_ENHANCED_LIGHT_SLEEP

**WiFi modem automatically receives the beacon**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:440`

The wifi modem automatically receives the beacon frame during light sleep.

---

## CONFIG_ESP_WIFI_ENTERPRISE_SUPPORT

**Enable enterprise option**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:836`

Select this to enable/disable enterprise connection support.

disabling this will reduce binary size.
disabling this will disable the use of any esp_wifi_sta_wpa2_ent_* (as APIs will be meaningless)

Note that when using bigger certificates on low-power chips without crypto
hardware acceleration, it is recommended to adjust the task watchdog timer (TWDT)
if it is enabled. For precise information on timing requirements, you can check
performance numbers at https://github.com/espressif/mbedtls/wiki/Performance-Numbers.

---

## CONFIG_ESP_WIFI_ENT_FREE_DYNAMIC_BUFFER

**Free dynamic buffers during WiFi enterprise connection**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Enable enterprise option
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:850`

Select this configuration to free dynamic buffers during WiFi enterprise connection.
This will enable chip to reduce heap consumption during WiFi enterprise connection.

---

## CONFIG_ESP_WIFI_ESPNOW_MAX_ENCRYPT_NUM

**Maximum espnow encrypt peers number**

- **Type:** `unknown`
- **Default:** `7`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:560`

Maximum number of encrypted peers supported by espnow.
The number of hardware keys for encryption is fixed. And the espnow and SoftAP share the same
hardware keys. So this configuration will affect the maximum connection number of SoftAP.
Maximum espnow encrypted peers number + maximum number of connections of SoftAP = Max hardware
keys number. When using ESP mesh, this value should be set to a maximum of 6.

---

## CONFIG_ESP_WIFI_EXTRA_IRAM_OPT

**WiFi EXTRA IRAM speed optimization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:283`

Select this option to place additional frequently called Wi-Fi library functions
in IRAM. When this option is disabled, more than 5Kbytes of IRAM memory will be saved
but Wi-Fi throughput will be reduced.

---

## CONFIG_ESP_WIFI_FTM_ENABLE

**WiFi FTM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:397`

Enable feature Fine Timing Measurement for calculating WiFi Round-Trip-Time (RTT).

---

## CONFIG_ESP_WIFI_FTM_INITIATOR_SUPPORT

**FTM Initiator support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > WiFi FTM
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:404`

---

## CONFIG_ESP_WIFI_FTM_RESPONDER_SUPPORT

**FTM Responder support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > WiFi FTM
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:409`

---

## CONFIG_ESP_WIFI_GCMP_SUPPORT

**WiFi GCMP Support(GCMP128 and GCMP256)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:421`

Select this option to enable GCMP support. GCMP support is compulsory for WiFi Suite-B support.

---

## CONFIG_ESP_WIFI_GMAC_SUPPORT

**WiFi GMAC Support(GMAC128 and GMAC256)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:428`

Select this option to enable GMAC support. GMAC support is compulsory for WiFi 192 bit certification.

---

## CONFIG_ESP_WIFI_IRAM_OPT

**WiFi IRAM speed optimization**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:274`

Select this option to place frequently called Wi-Fi library functions in IRAM.
When this option is disabled, more than 10Kbytes of IRAM memory will be saved
but Wi-Fi throughput will be reduced.

---

## CONFIG_ESP_WIFI_MBEDTLS_CRYPTO

**Use MbedTLS crypto APIs**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:581`

Select this option to enable the use of MbedTLS crypto APIs.
The internal crypto support within the supplicant is limited
and may not suffice for all new security features, including WPA3.

It is recommended to always keep this option enabled. Additionally,
note that MbedTLS can leverage hardware acceleration if available,
resulting in significantly faster cryptographic operations.

---

## CONFIG_ESP_WIFI_MBEDTLS_TLS_CLIENT

**Use MbedTLS TLS client for WiFi Enterprise connection**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Use MbedTLS crypto APIs
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:600`

Select this option to use MbedTLS TLS client for WPA2 enterprise connection.
Please note that from MbedTLS-3.0 onwards, MbedTLS does not support SSL-3.0
TLS-v1.0, TLS-v1.1 versions. In case your server is using one of these version,
it is advisable to update your server.
Please disable this option for compatibility with older TLS versions.

---

## CONFIG_ESP_WIFI_MBO_SUPPORT

**Enable Multi Band Operation Certification Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:687`

Select this option to enable WiFi Multiband operation certification support.

---

## CONFIG_ESP_WIFI_MGMT_SBUF_NUM

**WiFi mgmt short buffer number**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:264`

Set the maximum number of Wi-Fi management short buffers. These buffers are dynamically allocated,
with their size determined by the length of the management packet to be sent. When a management
packet is less than 64 bytes, the Wi-Fi driver classifies it as a short management packet and
assigns it to one of these buffers.

---

## CONFIG_ESP_WIFI_NAN_ENABLE

**WiFi Aware**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:573`

Enable WiFi Aware (NAN) feature.

---

## CONFIG_ESP_WIFI_NETWORK_ASSISTED_ROAMING_IP_RENEW_SKIP

**Skip IP renew during BTM based roaming**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming Methods > Support Network Assisted roaming using 802.11v
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:93`

Station will not ask for IP renew after a BTM based roaming. Before enabling please
make sure your network supports this.

---

## CONFIG_ESP_WIFI_NETWORK_ASSISTED_ROAMING_RETRY_COUNT

**Retry count after which to switch to legacy roaming**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming Methods > Support Network Assisted roaming using 802.11v
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:83`

Retry threshold after which the station should stop using Network Assisted
roaming methods and start using legacy roaming instead.

---

## CONFIG_ESP_WIFI_NVS_ENABLED

**WiFi NVS flash**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:222`

Select this option to enable WiFi NVS flash

---

## CONFIG_ESP_WIFI_ROAMING_BACKOFF_TIME

**Default time to wait between subsequent roaming attempts.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:152`

Time to wait (in seconds) by station before registering for the RSSI event again
or start continuous montoring to find better AP.

---

## CONFIG_ESP_WIFI_ROAMING_HOME_CHANNEL_DWELL_TIME

**Home channel dwell time scanning between consecutive channels**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:119`

If connected, duration for which the station will return to it's home channel for Tx/Rx of
frames stored in buffers between scanning on consecutive channels.

---

## CONFIG_ESP_WIFI_ROAMING_LEGACY_ROAMING

**Support Legacy roaming approach**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming Methods
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:65`

Roaming between APs that do not support 802.11v.
This will allow station to roam even when connection is not BTM supported,
by forcefully disconnecting from current AP and connecting to better AP.

---

## CONFIG_ESP_WIFI_ROAMING_LOW_RSSI_OFFSET

**Offset by which to reset the RSSI Threshold after attempt to roam.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers > Use Low RSSI to trigger roaming.
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:19`

Decide the offset by which to decrease the Low RSSI threshold set by ESP_WIFI_ROAMING_LOW_RSSI_THRESHOLD
after each failed attempt to roam. This allows for the station to keep scanning for better AP's after
the Low RSSI threshold is reached in a stepped manner, rather than only attempting to roam the first time
the current AP's RSSI breaches the set RSSI threshold.
Setting 0 here may cause station to be flooded with low rssi events,
therefore that's not recommended to be kept.

---

## CONFIG_ESP_WIFI_ROAMING_LOW_RSSI_ROAMING

**Use Low RSSI to trigger roaming.**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:3`

Enable to use a RSSI threshold to trigger roaming.

---

## CONFIG_ESP_WIFI_ROAMING_LOW_RSSI_THRESHOLD

**WiFi RSSI threshold to trigger roaming**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers > Use Low RSSI to trigger roaming.
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:9`

WiFi RSSI threshold to trigger roaming value in dBm (-99 to -1). Values under -30 dbm
might lead to a flood of low rssi events. This interferes with normal functioning and
TX/Rx performance.

---

## CONFIG_ESP_WIFI_ROAMING_MAX_CANDIDATES

**Max Candidates in the network**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:142`

Max candidates that can be considered while scanning as a part of the
network at one time.

---

## CONFIG_ESP_WIFI_ROAMING_NETWORK_ASSISTED_ROAM

**Support Network Assisted roaming using 802.11v**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming Methods
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:73`

Roaming between APs using network assisted Roaming.
This involves  BSS Transition Management mechanisms outlined in 802.11v.
Note that this moves the responsibility to the AP's network, and hence isn't
guaranteed to cause the station to attempt to roam each time.

---

## CONFIG_ESP_WIFI_ROAMING_PERIODIC_RRM_MONITORING

**Send periodic neighbor report request to AP for internal list updation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:160`

This option will enable station to keep sending RRM neighbor list request to AP and
update its internal list.

---

## CONFIG_ESP_WIFI_ROAMING_PERIODIC_SCAN_MONITOR

**Conduct periodic scans to check if a better AP is available**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:32`

Conduct periodic scans periodically to check if a better AP is available.

---

## CONFIG_ESP_WIFI_ROAMING_PERIODIC_SCAN_THRESHOLD

**Threshold at which to begin periodic scanning for a better AP.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers > Conduct periodic scans to check if a better AP is available
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:38`

Threshold at which the station will begin scanning to find an AP with better RSSI.

---

## CONFIG_ESP_WIFI_ROAMING_RRM_MONITOR_THRESHOLD

**Threshold for sending periodic neighbor report requests**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Send periodic neighbor report request to AP for internal list updation
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:178`

The RSSI threshold beyond which we start sending periodic neighbor report requests.

---

## CONFIG_ESP_WIFI_ROAMING_RRM_MONITOR_TIME

**Time interval (in seconds) between neighbor report requests to an AP**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Send periodic neighbor report request to AP for internal list updation
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:168`

Enable this to send periodic neighbor report requests to the AP.
These neighbor report requests provide information about other APs in the same managed
network. This information is used for more intelligent roaming.

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_CHAN_LIST

**Preferred channel list for scanning**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:127`

Channels your wireless network operates on to allow for faster scanning.
Specify the channels(between 1-14) in a comma separated manner.

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_EXPIRY_WINDOW

**Scan results expiry window (in seconds)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:134`

Duration for which the results from the most recent scans can be used
by the roaming app for determining the roaming candidates.

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_MAX_SCAN_TIME

**Maximum duration (in milliseconds) of station's per channel active scan time**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:112`

Maximum duration of active scanning per channel in milliseconds.

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_MIN_SCAN_TIME

**Minimum duration (in milliseconds) of station's per channel active scan**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Scan Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:105`

Minimum duration of active scanning per channel in milliseconds.

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_MONITOR_INTERVAL

**Time intervals (in seconds) at which station will initiate a scan**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers > Conduct periodic scans to check if a better AP is available
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:46`

Intervals at which station will periodically scan to check if better AP is available

---

## CONFIG_ESP_WIFI_ROAMING_SCAN_ROAM_RSSI_DIFF

**RSSI difference b/w current AP and candidate AP to initiate connection**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Advanced support for Wi-Fi Roaming (Experimental) > Configure roaming App > Roaming triggers > Conduct periodic scans to check if a better AP is available
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/wifi_apps/roaming_app/src/Kconfig.roaming:54`

Minimum RSSI difference b/w current AP and a potential roaming candidate AP
to trigger a roaming attempt.

---

## CONFIG_ESP_WIFI_RRM_SUPPORT

**Enable 802.11k APIs Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Enable 802.11k, 802.11v APIs Support
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:651`

Select this option to enable 802.11k APIs(RRM support).
Only APIs which are helpful for network assisted roaming
are supported for now.
Enable this option with RRM enabled in sta config
to make device ready for network assisted roaming.
RRM: Radio measurements enable STAs to understand the radio environment,
it enables STAs to observe and gather data on radio link performance
and on the radio environment. Current implementation adds beacon report,
link measurement, neighbor report.

---

## CONFIG_ESP_WIFI_RX_BA_WIN

**WiFi AMPDU RX BA window size**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > Wi-Fi > WiFi AMPDU RX
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:200`

Set the size of WiFi Block Ack RX window. Generally a bigger value means higher throughput and better
compatibility but more memory. Most of time we should NOT change the default value unless special
reason, e.g. test the maximum UDP RX throughput with iperf etc. For iperf test in shieldbox, the
recommended value is 9~12. If PSRAM is used and WiFi memory is preferred to allocate in PSRAM first,
the default and minimum value should be 16 to achieve better throughput and compatibility with both
stations and APs.

---

## CONFIG_ESP_WIFI_RX_IRAM_OPT

**WiFi RX IRAM speed optimization**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:292`

Select this option to place frequently called Wi-Fi library RX functions in IRAM.
When this option is disabled, more than 17Kbytes of IRAM memory will be saved
but Wi-Fi performance will be reduced.

---

## CONFIG_ESP_WIFI_RX_MGMT_BUF_NUM_DEF

**Max number of WiFi RX MGMT buffers**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:156`

Set the number of WiFi RX_MGMT buffers.

For Management buffers, the number of dynamic and static management buffers is the same.
In order to prevent memory fragmentation, the management buffer type should be set to static first.

---

## CONFIG_ESP_WIFI_SCAN_CACHE

**Keep scan results in cache**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:679`

Keep scan results in cache, if not enabled, those
will be flushed immediately.

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_OPT

**Wifi sleep optimize when beacon lost**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:447`

Enable wifi sleep optimization when beacon loss occurs and immediately enter
sleep mode when the WiFi module detects beacon loss.

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_OVER_THRESHOLD_AUTO

**Auto**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost > Beacon strategy when beacon loss exceeds threshold
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:507`

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_OVER_THRESHOLD_DROP

**Drop beacon**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost > Beacon strategy when beacon loss exceeds threshold
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:505`

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_OVER_THRESHOLD_RECEIVE

**Receive beacon**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost > Beacon strategy when beacon loss exceeds threshold
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:503`

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_THRESHOLD

**Maximum number of consecutive lost beacons allowed**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:461`

Maximum number of consecutive lost beacons allowed, the WiFi Rx state behaviour
will be determined by ESP_WIFI_SLP_BEACON_LOST_OVER_THRESHOLD_POLICY
when the number of consecutive beacons lost is greater than the given threshold.

---

## CONFIG_ESP_WIFI_SLP_BEACON_LOST_TIMEOUT

**Beacon loss timeout**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:453`

Timeout time for close rf phy when beacon loss occurs, Unit: 1024 microsecond.

---

## CONFIG_ESP_WIFI_SLP_DEFAULT_MAX_ACTIVE_TIME

**Maximum keep alive time**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:378`

Only for station. If no packet has been sent within ESP_WIFI_SLP_DEFAULT_MAX_ACTIVE_TIME, a null data
packet will be sent to maintain the connection with the AP. If ESP_WIFI_BSS_MAX_IDLE_SUPPORT is set,
it will be sent as bss max idle period in association request. unit: seconds.

---

## CONFIG_ESP_WIFI_SLP_DEFAULT_MIN_ACTIVE_TIME

**Minimum active time**

- **Type:** `unknown`
- **Default:** `50`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:358`

Only for station in WIFI_PS_MIN_MODEM or WIFI_PS_MAX_MODEM. When the station enters the active state,
it will work for at least ESP_WIFI_SLP_DEFAULT_MIN_ACTIVE_TIME. If a data packet is received or sent
during this period, the time will be refreshed. If the time is up, but the station still has packets
to receive or send, the time will also be refreshed. unit: milliseconds.

---

## CONFIG_ESP_WIFI_SLP_DEFAULT_WAIT_BROADCAST_DATA_TIME

**Minimum wait broadcast data time**

- **Type:** `unknown`
- **Default:** `15`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:387`

Only for station in WIFI_PS_MIN_MODEM or WIFI_PS_MAX_MODEM. When the station knows through the beacon
that AP will send broadcast packet, it will wait for ESP_WIFI_SLP_DEFAULT_WAIT_BROADCAST_DATA_TIME
before entering the sleep process. If a broadcast packet is received with more data bits, the time
will refreshed. unit: milliseconds.

---

## CONFIG_ESP_WIFI_SLP_IRAM_OPT

**WiFi SLP IRAM speed optimization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:343`

Select this option to place called Wi-Fi library TBTT process and receive beacon functions in IRAM.
Some functions can be put in IRAM either by ESP_WIFI_IRAM_OPT and ESP_WIFI_RX_IRAM_OPT, or this one.
If already enabled ESP_WIFI_IRAM_OPT, the other 7.3KB IRAM memory would be taken by this option.
If already enabled ESP_WIFI_RX_IRAM_OPT, the other 1.3KB IRAM memory would be taken by this option.
If neither of them are enabled, the other 7.4KB IRAM memory would be taken by this option.
Wi-Fi power-save mode average current would be reduced if this option is enabled.

---

## CONFIG_ESP_WIFI_SLP_PHY_OFF_DELTA_TIMEOUT_TIME

**Delta timeout time for RF PHY off**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:480`

Delta timeout time for rf phy off, When the beacon is lost, the next rf phy off will
be delayed for the time specified by the configuration item. Unit: 1024 microsecond.

---

## CONFIG_ESP_WIFI_SLP_PHY_ON_DELTA_EARLY_TIME

**Delta early time for RF PHY on**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:471`

Delta early time for rf phy on, When the beacon is lost, the next rf phy on will
be earlier the time specified by the configuration item, Unit: 32 microsecond.

---

## CONFIG_ESP_WIFI_SLP_SAMPLE_BEACON_COUNT

**Sample beacons at wifi connected to adjust beacon parameters**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Beacon Sample Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:520`

Numble of sampled beacons at wifi connected to adjust beacon parameters.

---

## CONFIG_ESP_WIFI_SLP_SAMPLE_BEACON_DIFFERENCE_PERCENT

**Difference percentage triggers unstable event**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Beacon Sample Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:528`

Difference triggers event WIFI_EVENT_STA_BEACON_OFFSET_UNSTABLE
when the actual rx beacon probability continuously falls below
the expected probability by this value.
unit: percentage

---

## CONFIG_ESP_WIFI_SLP_SAMPLE_BEACON_FEATURE

**Sample beacon to calculate beacon offset**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Wifi sleep optimize when beacon lost
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:511`

Select to enable feature sampling beacons to calculate beacon offset.

---

## CONFIG_ESP_WIFI_SLP_SAMPLE_BEACON_RESAMPLE_PERIOD

**Resample period**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Beacon Sample Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:549`

Resample period if beacon drop is active under ESP_WIFI_SLP_BEACON_LOST_DROP_BEACON_AUTO mode.
It means never resample if setting this value to 0.
unit: hours

---

## CONFIG_ESP_WIFI_SLP_SAMPLE_BEACON_STANDARD_PERCENT

**Standard percentage triggers beacon drop**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi > Beacon Sample Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:539`

Standard triggers beacon drop when the expected rx beacon probability
falls below this value under ESP_WIFI_SLP_BEACON_LOST_DROP_BEACON_AUTO mode.
unit: percentage

---

## CONFIG_ESP_WIFI_SOFTAP_BEACON_MAX_LEN

**Max length of WiFi SoftAP Beacon**

- **Type:** `unknown`
- **Default:** `752`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:241`

ESP-MESH utilizes beacon frames to detect and resolve root node conflicts (see documentation). However
the default length of a beacon frame can simultaneously hold only five root node identifier structures,
meaning that a root node conflict of up to five nodes can be detected at one time. In the occurrence of
more root nodes conflict involving more than five root nodes, the conflict resolution process will
detect five of the root nodes, resolve the conflict, and re-detect more root nodes. This process will
repeat until all root node conflicts are resolved. However this process can generally take a very long
time.

To counter this situation, the beacon frame length can be increased such that more root nodes can be
detected simultaneously. Each additional root node will require 36 bytes and should be added on top of
the default beacon frame length of
752 bytes. For example, if you want to detect 10 root nodes simultaneously, you need to set the beacon
frame length as
932 (752+36*5).

Setting a longer beacon length also assists with debugging as the conflicting root nodes can be
identified more quickly.

---

## CONFIG_ESP_WIFI_SOFTAP_SAE_SUPPORT

**Enable WPA3 Personal(SAE) SoftAP**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Enable WPA3-Personal
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:325`

Select this option to enable SAE support in softAP mode.

---

## CONFIG_ESP_WIFI_SOFTAP_SUPPORT

**WiFi SoftAP Support**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:434`

WiFi module can be compiled without SoftAP to save code size.

---

## CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM

**Max number of WiFi static RX buffers**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:26`

Set the number of WiFi static RX buffers. Each buffer takes approximately 1.6KB of RAM.
The static rx buffers are allocated when esp_wifi_init is called, they are not freed
until esp_wifi_deinit is called.

WiFi hardware use these buffers to receive all 802.11 frames.
A higher number may allow higher throughput but increases memory use. If ESP_WIFI_AMPDU_RX_ENABLED
is enabled, this value is recommended to set equal or bigger than ESP_WIFI_RX_BA_WIN in order to
achieve better throughput and compatibility with both stations and APs.

---

## CONFIG_ESP_WIFI_STATIC_RX_MGMT_BUFFER

**Static**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > Type of WiFi RX MGMT buffers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:145`

---

## CONFIG_ESP_WIFI_STATIC_TX_BUFFER

**Static**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Type of WiFi TX buffers
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:78`

---

## CONFIG_ESP_WIFI_STATIC_TX_BUFFER_NUM

**Max number of WiFi static TX buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:90`

Set the number of WiFi static TX buffers. Each buffer takes approximately 1.6KB of RAM.
The static RX buffers are allocated when esp_wifi_init() is called, they are not released
until esp_wifi_deinit() is called.

For each transmitted data frame from the higher layer TCP/IP stack, the WiFi driver makes a
copy of it in a TX buffer.  For some applications especially UDP applications, the upper
layer can deliver frames faster than WiFi layer can transmit. In these cases, we may run out
of TX buffers.

---

## CONFIG_ESP_WIFI_STA_DISCONNECTED_PM_ENABLE

**Power Management for station at disconnected**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:414`

Select this option to enable power_management for station when disconnected.
Chip will do modem-sleep when rf module is not in use any more.

---

## CONFIG_ESP_WIFI_SUITE_B_192

**Enable NSA suite B support with 192 bit key**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:635`

Select this option to enable 192 bit NSA suite-B.
This is necessary to support WPA3 192 bit security.

---

## CONFIG_ESP_WIFI_TARGET_ESP32

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_ESP_WIFI_TASK_PINNED_TO_CORE_0

**Core 0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi > WiFi Task Core ID
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:235`

---

## CONFIG_ESP_WIFI_TASK_PINNED_TO_CORE_1

**Core 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > WiFi Task Core ID
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:237`

---

## CONFIG_ESP_WIFI_TESTING_OPTIONS

**Add DPP testing code**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:830`

Select this to enable unity test for DPP.

---

## CONFIG_ESP_WIFI_TX_BA_WIN

**WiFi AMPDU TX BA window size**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > Wi-Fi > WiFi AMPDU TX
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:182`

Set the size of WiFi Block Ack TX window. Generally a bigger value means higher throughput but
more memory. Most of time we should NOT change the default value unless special reason, e.g.
test the maximum UDP TX throughput with iperf etc. For iperf test in shieldbox, the recommended
value is 9~12.

---

## CONFIG_ESP_WIFI_TX_BUFFER_TYPE

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_ESP_WIFI_TX_HETB_QUEUE_NUM

**WiFi TX HE TB QUEUE number for STA HE TB PPDU transmission**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:759`

Set the maximum number of queue that can be aggregated by the STA in the A-MPDU carried in the
HE TB PPDU.

---

## CONFIG_ESP_WIFI_WAPI_PSK

**Enable WAPI PSK support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:627`

Select this option to enable WAPI-PSK
which is a Chinese National Standard Encryption for Wireless LANs (GB 15629.11-2003).

---

## CONFIG_ESP_WIFI_WNM_SUPPORT

**Enable 802.11v APIs Support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > Enable 802.11k, 802.11v APIs Support
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:666`

Select this option to enable 802.11v APIs(BTM support).
Only APIs which are helpful for network assisted roaming
are supported for now.
Enable this option with BTM enabled in sta config
to make device ready for network assisted roaming.
BTM: BSS transition management enables an AP to request a station to transition
to a specific AP, or to indicate to a station a set of preferred APs.

---

## CONFIG_ESP_WIFI_WPS_PASSPHRASE

**Get WPA2 passphrase in WPS config**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > WPS Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:805`

Select this option to get passphrase during WPS configuration.
This option fakes the virtual display capabilities to get the
configuration in passphrase mode.
Not recommended to be used since WPS credentials should not
be shared to other devices, making it in readable format increases
that risk, also passphrase requires pbkdf2 to convert in psk.

---

## CONFIG_ESP_WIFI_WPS_SOFTAP_REGISTRAR

**Add WPS Registrar support in SoftAP mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:729`

Select this option to enable WPS registrar support in softAP mode.

---

## CONFIG_ESP_WIFI_WPS_STRICT

**Strictly validate all WPS attributes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi > WPS Configuration Options
- **Kconfig:** `/opt/esp/idf/components/esp_wifi/Kconfig:797`

Select this option to enable validate each WPS attribute
rigorously. Disabling this add the workarounds with various APs.
Enabling this may cause inter operability issues with some APs.

---

## CONFIG_ESP_XT_WDT

**Initialize XTAL32K watchdog timer on startup**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:537`

This watchdog timer can detect oscillation failure of the XTAL32K_CLK. When such a failure
is detected the hardware can be set up to automatically switch to BACKUP32K_CLK and generate
an interrupt.

---

## CONFIG_ESP_XT_WDT_BACKUP_CLK_ENABLE

**Automatically switch to BACKUP32K_CLK when timer expires**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP System Settings > Initialize XTAL32K watchdog timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:554`

Enable this to automatically switch to BACKUP32K_CLK as the source of RTC_SLOW_CLK when
the watchdog timer expires.

---

## CONFIG_ESP_XT_WDT_TIMEOUT

**XTAL32K watchdog timeout period**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP System Settings > Initialize XTAL32K watchdog timer on startup
- **Kconfig:** `/opt/esp/idf/components/esp_system/Kconfig:546`

Timeout period configuration for the XTAL32K watchdog timer based on RTC_CLK.

---

## CONFIG_ETH_DMA_BUFFER_SIZE

**Ethernet DMA buffer size (Byte)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:84`

Set the size of each buffer used by Ethernet MAC DMA.
!! Important !! Make sure it is 64B aligned for ESP32P4!

---

## CONFIG_ETH_DMA_RX_BUFFER_NUM

**Amount of Ethernet DMA Rx buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:92`

Number of DMA receive buffers. Each buffer's size is ETH_DMA_BUFFER_SIZE.
Larger number of buffers could increase throughput somehow.

---

## CONFIG_ETH_DMA_TX_BUFFER_NUM

**Amount of Ethernet DMA Tx buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:101`

Number of DMA transmit buffers. Each buffer's size is ETH_DMA_BUFFER_SIZE.
Larger number of buffers could increase throughput somehow.

---

## CONFIG_ETH_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_ETH_IRAM_OPTIMIZATION

**Enable IRAM optimization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:120`

If enabled, functions related to RX/TX are placed into IRAM. It can improve Ethernet throughput.
If disabled, all functions are placed into FLASH.

---

## CONFIG_ETH_OPENETH_DMA_RX_BUFFER_NUM

**Number of Ethernet DMA Rx buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support OpenCores Ethernet MAC (for use with QEMU)
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:171`

Number of DMA receive buffers, each buffer is 1600 bytes.

---

## CONFIG_ETH_OPENETH_DMA_TX_BUFFER_NUM

**Number of Ethernet DMA Tx buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support OpenCores Ethernet MAC (for use with QEMU)
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:178`

Number of DMA transmit buffers, each buffer is 1600 bytes.

---

## CONFIG_ETH_PHY_INTERFACE_RMII

**Reduced Media Independent Interface (RMII)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller > PHY interface
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:22`

---

## CONFIG_ETH_RMII_CLK_INPUT

**Input RMII clock from external**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller > RMII clock mode
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:34`

MAC will get RMII clock from outside.
Note that ESP32 only supports GPIO0 to input the RMII clock.

---

## CONFIG_ETH_RMII_CLK_IN_GPIO

- **Type:** `unknown`
- **Default:** ``

ESP32 only supports GPIO0 to input the RMII clock.

---

## CONFIG_ETH_RMII_CLK_OUTPUT

**Output RMII clock from internal**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller > RMII clock mode
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:40`

ESP32 can generate RMII clock by internal APLL.
This clock can be routed to the external PHY device.
ESP32 supports to route the RMII clock to GPIO0/16/17.

---

## CONFIG_ETH_RMII_CLK_OUTPUT_GPIO0

**Output RMII clock from GPIO0 (Experimental!)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:60`

GPIO0 can be set to output a pre-divided PLL clock. Enabling this option will configure
GPIO0 to output a 50MHz clock. In fact this clock doesn't have directly relationship with
EMAC peripheral. Sometimes this clock may not work well with your PHY chip.
WARNING: If you want the Ethernet to work with WiFi, don’t select ESP32 as RMII CLK output
as it would result in clock instability!

---

## CONFIG_ETH_RMII_CLK_OUT_GPIO

**RMII clock GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:72`

Set the GPIO number to output RMII Clock.
WARNING: If you want the Ethernet to work with WiFi, don’t select ESP32 as RMII CLK output
as it would result in clock instability!

---

## CONFIG_ETH_SOFT_FLOW_CONTROL

**Enable software flow control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support ESP32 internal EMAC controller
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:110`

Ethernet MAC engine on ESP32 doesn't feature a flow control logic.
The MAC driver can perform a software flow control if you enable this option.
Note that, if the RX buffer number is small, enabling software flow control will
cause obvious performance loss.

---

## CONFIG_ETH_SPI_ETHERNET_DM9051

**Use DM9051**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support SPI to Ethernet Module
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:137`

DM9051 is a fast Ethernet controller with an SPI interface.
It's also integrated with a 10/100M PHY and MAC.
Select this to enable DM9051 driver.

---

## CONFIG_ETH_SPI_ETHERNET_KSZ8851SNL

**Use KSZ8851SNL**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support SPI to Ethernet Module
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:153`

The KSZ8851SNL is a single-chip Fast Ethernet controller consisting of
a 10/100 physical layer transceiver (PHY), a MAC, and a Serial Peripheral Interface (SPI).
Select this to enable KSZ8851SNL driver.

---

## CONFIG_ETH_SPI_ETHERNET_W5500

**Use W5500 (MAC RAW)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet > Support SPI to Ethernet Module
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:144`

W5500 is a HW TCP/IP embedded Ethernet controller.
TCP/IP stack, 10/100 Ethernet MAC and PHY are embedded in a single chip.
However the driver in ESP-IDF only enables the RAW MAC mode,
making it compatible with the software TCP/IP stack.
Say yes to enable W5500 driver.

---

## CONFIG_ETH_TRANSMIT_MUTEX

**Enable Transmit Mutex**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:186`

Prevents multiple accesses when Ethernet interface is used as shared resource and multiple
functionalities might try to access it at a time.

---

## CONFIG_ETH_USE_ESP32_EMAC

**Support ESP32 internal EMAC controller**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:7`

ESP32 integrates a 10/100M Ethernet MAC controller.

---

## CONFIG_ETH_USE_OPENETH

**Support OpenCores Ethernet MAC (for use with QEMU)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ethernet
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:161`

OpenCores Ethernet MAC driver can be used when an ESP-IDF application
is executed in QEMU. This driver is not supported when running on a
real chip.

---

## CONFIG_ETH_USE_SPI_ETHERNET

**Support SPI to Ethernet Module**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Ethernet
- **Kconfig:** `/opt/esp/idf/components/esp_eth/Kconfig:129`

ESP-IDF can also support some SPI-Ethernet modules.

---

## CONFIG_ETM_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > ETM Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/Kconfig:243`

whether to enable the debug log message for ETM core driver.
Note that, this option only controls the ETM related driver log, won't affect other drivers.

---

## CONFIG_FATFS_ALLOC_PREFER_EXTRAM

**Prefer external RAM when allocating FATFS buffers**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:186`

When the option is enabled, internal buffers used by FATFS will be allocated
from external RAM. If the allocation from external RAM fails, the buffer will
be allocated from the internal RAM.
Disable this option if optimizing for performance. Enable this option if
optimizing for internal memory size.

---

## CONFIG_FATFS_API_ENCODING_ANSI_OEM

**API uses ANSI/OEM encoding**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > API character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:135`

---

## CONFIG_FATFS_API_ENCODING_UTF_8

**API uses UTF-8 encoding**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > API character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:137`

---

## CONFIG_FATFS_CODEPAGE

- **Type:** `unknown`
- **Default:** `437`

---

## CONFIG_FATFS_CODEPAGE_437

**US (CP437)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:48`

---

## CONFIG_FATFS_CODEPAGE_720

**Arabic (CP720)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:50`

---

## CONFIG_FATFS_CODEPAGE_737

**Greek (CP737)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:52`

---

## CONFIG_FATFS_CODEPAGE_771

**KBL (CP771)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:54`

---

## CONFIG_FATFS_CODEPAGE_775

**Baltic (CP775)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:56`

---

## CONFIG_FATFS_CODEPAGE_850

**Latin 1 (CP850)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:58`

---

## CONFIG_FATFS_CODEPAGE_852

**Latin 2 (CP852)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:60`

---

## CONFIG_FATFS_CODEPAGE_855

**Cyrillic (CP855)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:62`

---

## CONFIG_FATFS_CODEPAGE_857

**Turkish (CP857)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:64`

---

## CONFIG_FATFS_CODEPAGE_860

**Portuguese (CP860)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:66`

---

## CONFIG_FATFS_CODEPAGE_861

**Icelandic (CP861)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:68`

---

## CONFIG_FATFS_CODEPAGE_862

**Hebrew (CP862)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:70`

---

## CONFIG_FATFS_CODEPAGE_863

**Canadian French (CP863)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:72`

---

## CONFIG_FATFS_CODEPAGE_864

**Arabic (CP864)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:74`

---

## CONFIG_FATFS_CODEPAGE_865

**Nordic (CP865)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:76`

---

## CONFIG_FATFS_CODEPAGE_866

**Russian (CP866)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:78`

---

## CONFIG_FATFS_CODEPAGE_869

**Greek 2 (CP869)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:80`

---

## CONFIG_FATFS_CODEPAGE_932

**Japanese (DBCS) (CP932)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:82`

---

## CONFIG_FATFS_CODEPAGE_936

**Simplified Chinese (DBCS) (CP936)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:84`

---

## CONFIG_FATFS_CODEPAGE_949

**Korean (DBCS) (CP949)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:86`

---

## CONFIG_FATFS_CODEPAGE_950

**Traditional Chinese (DBCS) (CP950)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:88`

---

## CONFIG_FATFS_CODEPAGE_DYNAMIC

**Dynamic (all code pages supported)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > OEM Code Page
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:46`

---

## CONFIG_FATFS_DONT_TRUST_FREE_CLUSTER_CNT

**Don't trust free cluster count**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > FAT Filesystem support > File system free space calculation behavior
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:331`

If 1, the file system will not trust the free cluster count in the FSINFO (in FATFS struct).
This may result in more accurate output from `f_getfree()` function but increased overhead.

---

## CONFIG_FATFS_DONT_TRUST_LAST_ALLOC

**Don't trust allocated cluster number**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > FAT Filesystem support > File system free space calculation behavior
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:339`

If 1, the file system will not trust the last allocated cluster number in the FSINFO (in FATFS struct).
This may result in more accurate output from `f_getfree()` function but increased overhead.

---

## CONFIG_FATFS_FAST_SEEK_BUFFER_SIZE

**Fast seek CLMT buffer size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:266`

If fast seek algorithm is enabled, this defines the size of
CLMT buffer used by this algorithm in 32-bit word units.
This value should be chosen based on prior knowledge of
maximum elements of each file entry would store.

---

## CONFIG_FATFS_FS_LOCK

**Number of simultaneously open files protected by lock function**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:141`

This option sets the FATFS configuration value _FS_LOCK.
The option _FS_LOCK switches file lock function to control duplicated file open
and illegal operation to open objects.

* 0: Disable file lock function. To avoid volume corruption, application
should avoid illegal open, remove and rename to the open objects.

* >0: Enable file lock function. The value defines how many files/sub-directories
can be opened simultaneously under file lock control.

Note that the file lock control is independent of re-entrancy.

---

## CONFIG_FATFS_IMMEDIATE_FSYNC

**Enable automatic f_sync**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:291`

Enables automatic calling of f_sync() to flush recent file changes after each call of vfs_fat_write(),
vfs_fat_pwrite(), vfs_fat_link(), vfs_fat_truncate() and vfs_fat_ftruncate() functions.
This feature improves file-consistency and size reporting accuracy for the FatFS,
at a price on decreased performance due to frequent disk operations

---

## CONFIG_FATFS_LFN_HEAP

**Long filename buffer in heap**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > Long filename support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:20`

---

## CONFIG_FATFS_LFN_NONE

**No long filenames**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support > Long filename support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:18`

---

## CONFIG_FATFS_LFN_STACK

**Long filename buffer on stack**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > Long filename support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:22`

---

## CONFIG_FATFS_LINK_LOCK

**Perform the whole link operation under lock**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:306`

If enabled, the whole link operation (including file copying) is performed under lock.
This ensures that the link operation is atomic, but may cause performance for large files.
It may create less fragmented file copy.

---

## CONFIG_FATFS_MAX_LFN

**Max long filename length**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:118`

Maximum long filename length. Can be reduced to save RAM.

---

## CONFIG_FATFS_PER_FILE_CACHE

**Use separate cache for each file**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:169`

This option affects FATFS configuration value _FS_TINY.

If this option is set, _FS_TINY is 0, and each open file has its own cache,
size of the cache is equal to the _MAX_SS variable (512 or 4096 bytes).
This option uses more RAM if more than 1 file is open, but needs less reads
and writes to the storage for some operations.

If this option is not set, _FS_TINY is 1, and single cache is used for
all open files, size is also equal to _MAX_SS variable. This reduces the
amount of heap used when multiple files are open, but increases the number
of read and write operations which FATFS needs to make.

---

## CONFIG_FATFS_PRINT_FLOAT

**Make fatfs f_printf() support floating point argument**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:235`

---

## CONFIG_FATFS_PRINT_LLI

**Make fatfs f_printf() support long long argument**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:230`

---

## CONFIG_FATFS_SECTOR_4096

**4096**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support > Sector size
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:33`

---

## CONFIG_FATFS_SECTOR_512

**512**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > Sector size
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:31`

---

## CONFIG_FATFS_STRF_ENCODE_ANSI

**0:ANSI/OEM in current CP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > FatFS string functions: convert character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:253`

---

## CONFIG_FATFS_STRF_ENCODE_UTF16BE

**2:Unicode in UTF-16BE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > FatFS string functions: convert character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:259`

---

## CONFIG_FATFS_STRF_ENCODE_UTF16LE

**1:Unicode in UTF-16LE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > FatFS string functions: convert character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:256`

---

## CONFIG_FATFS_STRF_ENCODE_UTF8

**3:Unicode in UTF-8**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > FatFS string functions: convert character encoding
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:262`

---

## CONFIG_FATFS_TIMEOUT_MS

**Timeout for acquiring a file lock, ms**

- **Type:** `unknown`
- **Default:** `10000`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:158`

This option sets FATFS configuration value _FS_TIMEOUT, scaled to milliseconds.
Sets the number of milliseconds FATFS will wait to acquire a mutex when
operating on an open file. For example, if one task is performing a lengthy
operation, another task will wait for the first task to release the lock,
and time out after amount of time set by this option.

---

## CONFIG_FATFS_USE_DYN_BUFFERS

**Use dynamic buffers**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:314`

If enabled, the buffers used by FATFS will be allocated separately from the rest of the structure.
This option is useful when using multiple FATFS instances with different
sector sizes, as the buffers will be allocated according to the sector size.
If disabled, the greatest sector size will be used for all FATFS instances.
(In most cases, this would be the sector size of Wear Levelling library)
This might cause more memory to be used than necessary.

---

## CONFIG_FATFS_USE_FASTSEEK

**Enable fast seek algorithm when using lseek function through VFS FAT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:198`

The fast seek feature enables fast backward/long seek operations without
FAT access by using an in-memory CLMT (cluster link map table).
Please note, fast-seek is only allowed for read-mode files, if a
file is opened in write-mode, the seek mechanism will automatically fallback
to the default implementation.

---

## CONFIG_FATFS_USE_LABEL

**Use FATFS volume label**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:300`

Allows FATFS volume label to be specified using f_setlabel

---

## CONFIG_FATFS_USE_STRFUNC_NONE

**0:Disable**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FAT Filesystem support > Enable string functions, f_gets(), f_putc(), f_puts() and f_printf()
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:220`

---

## CONFIG_FATFS_USE_STRFUNC_WITHOUT_CRLF_CONV

**1:Enable without LF-CRLF conversion**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > Enable string functions, f_gets(), f_putc(), f_puts() and f_printf()
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:223`

---

## CONFIG_FATFS_USE_STRFUNC_WITH_CRLF_CONV

**2:Enable with LF-CRLF conversion**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FAT Filesystem support > Enable string functions, f_gets(), f_putc(), f_puts() and f_printf()
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:226`

---

## CONFIG_FATFS_VFS_FSTAT_BLKSIZE

**Default block size**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:276`

If set to 0, the 'newlib' library's default size (BLKSIZ) is used (128 B).
If set to a non-zero value, the value is used as the block size.
Default file buffer size is set to this value
and the buffer is allocated when first attempt of reading/writing to a file is made.
Increasing this value improves fread() speed, however the heap usage is increased as well.

NOTE: The block size value is shared by all the filesystem functions
accessing target media for given file descriptor!
See 'Improving I/O performance' section of 'Maximizing Execution Speed' documentation page
for more details.

---

## CONFIG_FATFS_VOLUME_COUNT

**Number of volumes**

- **Type:** `unknown`
- **Default:** `2`
- **Menu:** Component config > FAT Filesystem support
- **Kconfig:** `/opt/esp/idf/components/fatfs/Kconfig:3`

Number of volumes (logical drives) to use.

---

## CONFIG_FREERTOS_CHECK_MUTEX_GIVEN_BY_OWNER

**Check that mutex semaphore is given by owner task**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:433`

If enabled, assert that when a mutex semaphore is given, the task giving the semaphore is the task
which is currently holding the mutex.

---

## CONFIG_FREERTOS_CHECK_PORT_CRITICAL_COMPLIANCE

**Tests compliance with Vanilla FreeRTOS port*_CRITICAL calls**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:575`

If enabled, context of port*_CRITICAL calls (ISR or Non-ISR) would be checked to be in compliance with
Vanilla FreeRTOS. e.g Calling port*_CRITICAL from ISR context would cause assert failure

---

## CONFIG_FREERTOS_CHECK_STACKOVERFLOW_CANARY

**Check using canary bytes (Method 2)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Kernel > configCHECK_FOR_STACK_OVERFLOW
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:73`

Places some magic bytes at the end of the stack area and on each context switch, check if these
bytes are still intact. More thorough than just checking the pointer, but also slightly slower.
(configCHECK_FOR_STACK_OVERFLOW = 2)

---

## CONFIG_FREERTOS_CHECK_STACKOVERFLOW_NONE

**No checking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configCHECK_FOR_STACK_OVERFLOW
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:61`

Do not check for stack overflows (configCHECK_FOR_STACK_OVERFLOW = 0)

---

## CONFIG_FREERTOS_CHECK_STACKOVERFLOW_PTRVAL

**Check by stack pointer value (Method 1)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configCHECK_FOR_STACK_OVERFLOW
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:66`

Check for stack overflows on each context switch by checking if the stack pointer is in a valid
range. Quick but does not detect stack overflows that happened between context switches
(configCHECK_FOR_STACK_OVERFLOW = 1)

---

## CONFIG_FREERTOS_CORETIMER_0

**Timer 0 (int 6, level 1)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Tick timer source (Xtensa Only)
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:494`

Select this to use timer 0

---

## CONFIG_FREERTOS_CORETIMER_1

**Timer 1 (int 15, level 3)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Tick timer source (Xtensa Only)
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:500`

Select this to use timer 1

---

## CONFIG_FREERTOS_CORETIMER_SYSTIMER_LVL1

**SYSTIMER 0 (level 1)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Port > Tick timer source (Xtensa Only)
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:506`

Select this to use systimer with the 1 interrupt priority.

---

## CONFIG_FREERTOS_CORETIMER_SYSTIMER_LVL3

**SYSTIMER 0 (level 3)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Tick timer source (Xtensa Only)
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:512`

Select this to use systimer with the 3 interrupt priority.

---

## CONFIG_FREERTOS_DEBUG_OCDAWARE

- **Type:** `string`
- **Default:** `y`

Hidden option, gets selected by CONFIG_ESP_DEBUG_OCDAWARE

---

## CONFIG_FREERTOS_ENABLE_BACKWARD_COMPATIBILITY

**configENABLE_BACKWARD_COMPATIBILITY**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:156`

Enable backward compatibility with APIs prior to FreeRTOS v8.0.0. (see
configENABLE_BACKWARD_COMPATIBILITY documentation for more details).

---

## CONFIG_FREERTOS_ENABLE_STATIC_TASK_CLEAN_UP

**Enable static task clean up hook (DEPRECATED)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:421`

THIS OPTION IS DEPRECATED. Use FREERTOS_TASK_PRE_DELETION_HOOK instead.

Enable this option to make FreeRTOS call the static task clean up hook when a task is deleted.

Note: Users will need to provide a ``void vPortCleanUpTCB ( void *pxTCB )`` callback

---

## CONFIG_FREERTOS_ENABLE_TASK_SNAPSHOT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_FPU_IN_ISR

**Use float in Level 1 ISR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:464`

When enabled, the usage of float type is allowed inside Level 1 ISRs. Note that usage of float types in
higher level interrupts is still not permitted.

---

## CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS

**configGENERATE_RUN_TIME_STATS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:289`

Enables collection of run time statistics for each task (see configGENERATE_RUN_TIME_STATS
documentation for more details).

Note: The clock used for run time statistics can be configured in FREERTOS_RUN_TIME_STATS_CLK.

---

## CONFIG_FREERTOS_HZ

**configTICK_RATE_HZ**

- **Type:** `unknown`
- **Default:** `100`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:33`

Sets the FreeRTOS tick interrupt frequency in Hz (see configTICK_RATE_HZ documentation for more
details).

---

## CONFIG_FREERTOS_IDLE_TASK_STACKSIZE

**configMINIMAL_STACK_SIZE (Idle task stack size)**

- **Type:** `unknown`
- **Default:** `1536`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:92`

Sets the idle task stack size in bytes (see configMINIMAL_STACK_SIZE documentation for more details).

Note:

- ESP-IDF specifies stack sizes in bytes instead of words.
- The default size is enough for most use cases.
- The stack size may need to be increased above the default if the app installs idle or thread local
  storage cleanup hooks that use a lot of stack memory.
- Conversely, the stack size can be reduced to the minimum if non of the idle features are used.

---

## CONFIG_FREERTOS_IDLE_TIME_BEFORE_SLEEP

**configEXPECTED_IDLE_TIME_BEFORE_SLEEP**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TICKLESS_IDLE
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:336`

FreeRTOS will enter light sleep mode if no tasks need to run for this number of ticks.
You can enable PM_PROFILING feature in esp_pm components and dump the sleep status with
esp_pm_dump_locks, if the proportion of rejected sleeps is too high, please increase
this value to improve scheduling efficiency

---

## CONFIG_FREERTOS_INTERRUPT_BACKTRACE

**Enable backtrace from interrupt to task context**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:453`

If this option is enabled, interrupt stack frame will be modified to point to the code of the
interrupted task as its return address. This helps the debugger (or the panic handler) show a backtrace
from the interrupt to the task which was interrupted. This also works for nested interrupts: higher
level interrupt stack can be traced back to the lower level interrupt. This option adds 4 instructions
to the interrupt dispatching code.

---

## CONFIG_FREERTOS_IN_IRAM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_ISR_STACKSIZE

**ISR stack size**

- **Type:** `unknown`
- **Default:** `1536`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:443`

The interrupt handlers have their own stack. The size of the stack can be defined here. Each processor
has its own stack, so the total size occupied will be twice this.

---

## CONFIG_FREERTOS_MAX_TASK_NAME_LEN

**configMAX_TASK_NAME_LEN**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:146`

Sets the maximum number of characters for task names (see configMAX_TASK_NAME_LEN documentation for
more details).

Note: For most uses, the default of 16 characters is sufficient.

---

## CONFIG_FREERTOS_NO_AFFINITY

- **Type:** `unknown`
- **Default:** `0x7FFFFFFF`

---

## CONFIG_FREERTOS_NUMBER_OF_CORES

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_FREERTOS_OPTIMIZED_SCHEDULER

**configUSE_PORT_OPTIMISED_TASK_SELECTION**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:42`

Enables port specific task selection method. This option can speed up the search of ready tasks
when scheduling (see configUSE_PORT_OPTIMISED_TASK_SELECTION documentation for more details).

---

## CONFIG_FREERTOS_PLACE_FUNCTIONS_INTO_FLASH

**Place FreeRTOS functions into Flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:559`

When enabled the selected Non-ISR FreeRTOS functions will be placed into Flash memory instead of IRAM.
This saves up to 8KB of IRAM depending on which functions are used.

---

## CONFIG_FREERTOS_PLACE_ISR_FUNCTIONS_INTO_FLASH

**Place FreeRTOS functions called from ISR context into Flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Place FreeRTOS functions into Flash
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:566`

When enabled additional FreeRTOS functions which maybe called from an ISR context are
also placed in Flash, thus freeing up more IRAM.
This option is only applicable when the SPI_FLASH_AUTO_SUSPEND feature is supported.

---

## CONFIG_FREERTOS_PLACE_SNAPSHOT_FUNS_INTO_FLASH

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_PORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_QUEUE_REGISTRY_SIZE

**configQUEUE_REGISTRY_SIZE**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:237`

Set the size of the queue registry (see configQUEUE_REGISTRY_SIZE documentation for more details).

Note: A value of 0 will disable queue registry functionality

---

## CONFIG_FREERTOS_RUN_TIME_COUNTER_TYPE_U32

**uint32_t**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configGENERATE_RUN_TIME_STATS > configRUN_TIME_COUNTER_TYPE
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:308`

configRUN_TIME_COUNTER_TYPE is set to uint32_t

---

## CONFIG_FREERTOS_RUN_TIME_COUNTER_TYPE_U64

**uint64_t**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configGENERATE_RUN_TIME_STATS > configRUN_TIME_COUNTER_TYPE
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:313`

configRUN_TIME_COUNTER_TYPE is set to uint64_t

---

## CONFIG_FREERTOS_RUN_TIME_STATS_USING_CPU_CLK

**Use CPU Clock for run time stats**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Choose the clock source for run time stats
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:546`

CPU Clock will be used as the clock source for the generation of run time stats. The CPU Clock has
a frequency dependent on ESP_DEFAULT_CPU_FREQ_MHZ and Dynamic Frequency Scaling (DFS). Therefore
the CPU Clock frequency can fluctuate between 80 to 240MHz. Run time stats generated using the CPU
Clock represents the number of CPU cycles each task is allocated and DOES NOT reflect the amount of
time each task runs for (as CPU clock frequency can change). If the CPU clock consistently runs at
the maximum frequency of 240MHz, it will overflow in approximately 17 seconds.

---

## CONFIG_FREERTOS_RUN_TIME_STATS_USING_ESP_TIMER

**Use ESP TIMER for run time stats**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port > Choose the clock source for run time stats
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:539`

ESP Timer will be used as the clock source for FreeRTOS run time stats. The ESP Timer runs at a
frequency of 1MHz regardless of Dynamic Frequency Scaling. Therefore the ESP Timer will overflow in
approximately 4290 seconds.

---

## CONFIG_FREERTOS_SMP

**Run the Amazon SMP FreeRTOS kernel instead (FEATURE UNDER DEVELOPMENT)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:7`

Amazon has released an SMP version of the FreeRTOS Kernel which can be found via the following link:
https://github.com/FreeRTOS/FreeRTOS-Kernel/tree/smp

IDF has added an experimental port of this SMP kernel located in
components/freertos/FreeRTOS-Kernel-SMP. Enabling this option will cause IDF to use the Amazon SMP
kernel. Note that THIS FEATURE IS UNDER ACTIVE DEVELOPMENT, users use this at their own risk.

Leaving this option disabled will mean the IDF FreeRTOS kernel is used instead, which is located in:
components/freertos/FreeRTOS-Kernel. Both kernel versions are SMP capable, but differ in
their implementation and features.

---

## CONFIG_FREERTOS_SUPPORT_STATIC_ALLOCATION

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_SYSTICK_USES_CCOUNT

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_FREERTOS_SYSTICK_USES_SYSTIMER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_TASK_CREATE_ALLOW_EXT_MEM

**Allow external memory as an argument to xTaskCreateStatic (READ HELP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Extra
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:587`

Accessing memory in PSRAM has certain restrictions, so task stacks allocated by xTaskCreate
are by default allocated from internal RAM.

This option allows for passing memory allocated from SPIRAM to be passed to xTaskCreateStatic.
This should only be used for tasks where the stack is never accessed while the cache is disabled.

Extra notes for ESP32:

Because some bits of the ESP32 code environment cannot be recompiled with the cache workaround,
normally tasks cannot be safely run with their stack residing in external memory; for this reason
xTaskCreate (and related task creation functions) always allocate stack in internal memory and
xTaskCreateStatic will check if the memory passed to it is in internal memory.
If you have a task that needs a large amount of stack and does not call on ROM code in any way
(no direct calls, but also no Bluetooth/WiFi), you can try enable this to
cause xTaskCreateStatic to allow tasks stack in external memory.

---

## CONFIG_FREERTOS_TASK_FUNCTION_WRAPPER

**Wrap task functions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:361`

If enabled, all FreeRTOS task functions will be enclosed in a wrapper function. If a task function
mistakenly returns (i.e. does not delete), the call flow will return to the wrapper function. The
wrapper function will then log an error and abort the application. This option is also required for GDB
backtraces and C++ exceptions to work correctly inside top-level task functions.

---

## CONFIG_FREERTOS_TASK_NOTIFICATION_ARRAY_ENTRIES

**configTASK_NOTIFICATION_ARRAY_ENTRIES**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:246`

Set the size of the task notification array of each task. When increasing this value, keep in
mind that this means additional memory for each and every task on the system.
However, task notifications in general are more light weight compared to alternatives
such as semaphores.

---

## CONFIG_FREERTOS_TASK_PRE_DELETION_HOOK

**Enable task pre-deletion hook**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:407`

Enable this option to make FreeRTOS call a user provided hook function right before it deletes a task
(i.e., frees/releases a dynamically/statically allocated task's memory). This is useful if users want
to know when a task is actually deleted (in case the task's deletion is delegated to the IDLE task).

If this config option is enabled, users must define a ``void vTaskPreDeletionHook( void * pxTCB )``
hook function in their application.

---

## CONFIG_FREERTOS_THREAD_LOCAL_STORAGE_POINTERS

**configNUM_THREAD_LOCAL_STORAGE_POINTERS**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:81`

Set the number of thread local storage pointers in each task (see
configNUM_THREAD_LOCAL_STORAGE_POINTERS documentation for more details).

Note: In ESP-IDF, this value must be at least 1. Index 0 is reserved for use by the pthreads API
thread-local-storage. Other indexes can be used for any desired purpose.

---

## CONFIG_FREERTOS_TICK_SUPPORT_CORETIMER

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_FREERTOS_TICK_SUPPORT_SYSTIMER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_FREERTOS_TIMER_QUEUE_LENGTH

**configTIMER_QUEUE_LENGTH**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:228`

Set the timer task's command queue length (see configTIMER_QUEUE_LENGTH documentation for more
details).

---

## CONFIG_FREERTOS_TIMER_SERVICE_TASK_CORE_AFFINITY

- **Type:** `unknown`
- **Default:** `0x7FFFFFFF`

---

## CONFIG_FREERTOS_TIMER_SERVICE_TASK_NAME

**configTIMER_SERVICE_TASK_NAME**

- **Type:** `unknown`
- **Default:** `Tmr Svc`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:180`

Sets the timer task's name (see configTIMER_SERVICE_TASK_NAME documentation for more details).

---

## CONFIG_FREERTOS_TIMER_TASK_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS > configTIMER_SERVICE_TASK_CORE_AFFINITY
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:195`

---

## CONFIG_FREERTOS_TIMER_TASK_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS > configTIMER_SERVICE_TASK_CORE_AFFINITY
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:197`

---

## CONFIG_FREERTOS_TIMER_TASK_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS > configTIMER_SERVICE_TASK_CORE_AFFINITY
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:200`

---

## CONFIG_FREERTOS_TIMER_TASK_PRIORITY

**configTIMER_TASK_PRIORITY**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:211`

Sets the timer task's priority (see configTIMER_TASK_PRIORITY documentation for more details).

---

## CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH

**configTIMER_TASK_STACK_DEPTH**

- **Type:** `unknown`
- **Default:** `2048`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TIMERS
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:219`

Set the timer task's stack size (see configTIMER_TASK_STACK_DEPTH documentation for more details).

---

## CONFIG_FREERTOS_TLSP_DELETION_CALLBACKS

**Enable thread local storage pointers deletion callbacks**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:396`

ESP-IDF provides users with the ability to free TLSP memory by registering TLSP deletion callbacks.
These callbacks are automatically called by FreeRTOS when a task is deleted. When this option is turned
on, the memory reserved for TLSPs in the TCB is doubled to make space for storing the deletion
callbacks. If the user does not wish to use TLSP deletion callbacks then this option could be turned
off to save space in the TCB memory.

---

## CONFIG_FREERTOS_UNICORE

**Run FreeRTOS only on first core**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:23`

This version of FreeRTOS normally takes control of all cores of the CPU. Select this if you only want
to start it on the first core. This is needed when e.g. another process needs complete control over the
second core.

---

## CONFIG_FREERTOS_USE_APPLICATION_TASK_TAG

**configUSE_APPLICATION_TASK_TAG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:349`

Enables task tagging functionality and its associated API (see configUSE_APPLICATION_TASK_TAG
documentation for more details).

---

## CONFIG_FREERTOS_USE_IDLE_HOOK

**configUSE_IDLE_HOOK**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:107`

Enables the idle task application hook (see configUSE_IDLE_HOOK documentation for more details).

Note:

- The application must provide the hook function ``void vApplicationIdleHook( void );``
- ``vApplicationIdleHook()`` is called from FreeRTOS idle task(s)
- The FreeRTOS idle hook is NOT the same as the ESP-IDF Idle Hook, but both can be enabled
  simultaneously.

---

## CONFIG_FREERTOS_USE_LIST_DATA_INTEGRITY_CHECK_BYTES

**configUSE_LIST_DATA_INTEGRITY_CHECK_BYTES**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:272`

Enable list integrity checker
(see configUSE_LIST_DATA_INTEGRITY_CHECK_BYTES documentation for more details).

---

## CONFIG_FREERTOS_USE_PASSIVE_IDLE_HOOK

**Use FreeRTOS minimal idle hook**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:120`

Enables the minimal idle task application hook (see configUSE_IDLE_HOOK documentation for more
details).

Note:

- The application must provide the hook function ``void vApplicationPassiveIdleHook( void );``
- ``vApplicationPassiveIdleHook()`` is called from FreeRTOS minimal idle task(s)

---

## CONFIG_FREERTOS_USE_STATS_FORMATTING_FUNCTIONS

**configUSE_STATS_FORMATTING_FUNCTIONS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel > configUSE_TRACE_FACILITY
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:263`

Set configUSE_TRACE_FACILITY and configUSE_STATS_FORMATTING_FUNCTIONS to 1 to include the
``vTaskList()`` and ``vTaskGetRunTimeStats()`` functions in the build (see
configUSE_STATS_FORMATTING_FUNCTIONS documentation for more details).

---

## CONFIG_FREERTOS_USE_TICKLESS_IDLE

**configUSE_TICKLESS_IDLE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:319`

If power management support is enabled, FreeRTOS will be able to put the system into light sleep mode
when no tasks need to run for a number of ticks. This number can be set using
FREERTOS_IDLE_TIME_BEFORE_SLEEP option. This feature is also known as "automatic light sleep".

Note that timers created using esp_timer APIs may prevent the system from entering sleep mode, even
when no tasks need to run. To skip unnecessary wake-up initialize a timer with the
"skip_unhandled_events" option as true.

If disabled, automatic light sleep support will be disabled.

---

## CONFIG_FREERTOS_USE_TICK_HOOK

**configUSE_TICK_HOOK**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:133`

Enables the tick hook (see configUSE_TICK_HOOK documentation for more details).

Note:

- The application must provide the hook function ``void vApplicationTickHook( void );``
- ``vApplicationTickHook()`` is called from FreeRTOS's tick handling function ``xTaskIncrementTick()``
- The FreeRTOS tick hook is NOT the same as the ESP-IDF Tick Interrupt Hook, but both can be enabled
  simultaneously.

---

## CONFIG_FREERTOS_USE_TIMERS

**configUSE_TIMERS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:164`

Enable FreeRTOS Software Timers. Normally the timer task will only get pulled into the build
and created if any software timer related functions are used. This is achieved through IDF
defining a weak empty function for xTimerCreateTimerTask, which should take effect if timers.c
is not pulled into the build.

In certain special cases (if you use configUSE_TRACE_FACILITY=y and event groups) the linker will
still pull in the xTimerCreateTimerTask from timers.c even if the function that utilized it gets
discarded due to not being used.

In these cases you can use this option to force the timer task to be disabled.

---

## CONFIG_FREERTOS_USE_TRACE_FACILITY

**configUSE_TRACE_FACILITY**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:256`

Enables additional structure members and functions to assist with execution visualization and tracing
(see configUSE_TRACE_FACILITY documentation for more details).

---

## CONFIG_FREERTOS_VTASKLIST_INCLUDE_COREID

**Enable display of xCoreID in vTaskList**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Kernel
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:280`

If enabled, this will include an extra column when vTaskList is called to display the CoreID the task
is pinned to (0,1) or -1 if not pinned.

---

## CONFIG_FREERTOS_WATCHPOINT_END_OF_STACK

**Enable stack overflow debug watchpoint**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > FreeRTOS > Port
- **Kconfig:** `/opt/esp/idf/components/freertos/Kconfig:373`

FreeRTOS can check if a stack has overflown its bounds by checking either the value of the stack
pointer or by checking the integrity of canary bytes. (See FREERTOS_CHECK_STACKOVERFLOW for more
information.) These checks only happen on a context switch, and the situation that caused the stack
overflow may already be long gone by then. This option will use the last debug memory watchpoint to
allow breaking into the debugger (or panic'ing) as soon as any of the last 32 bytes on the stack of a
task are overwritten. The side effect is that using gdb, you effectively have one hardware watchpoint
less because the last one is overwritten as soon as a task switch happens.

Another consequence is that due to alignment requirements of the watchpoint, the usable stack size
decreases by up to 60 bytes. This is because the watchpoint region has to be aligned to its size and
the size for the stack watchpoint in IDF is 32 bytes.

This check only triggers if the stack overflow writes within 32 bytes near the end of the stack, rather
than overshooting further, so it is worth combining this approach with one of the other stack overflow
check methods.

When this watchpoint is hit, gdb will stop with a SIGTRAP message. When no JTAG OCD is attached,
esp-idf will panic on an unhandled debug exception.

---

## CONFIG_GCOV_ENABLED

**Coverage analyzer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Host-test config
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/host_test/main/Kconfig:3`

Enables coverage analyzing for host tests.

---

## CONFIG_GDMA_CTRL_FUNC_IN_IRAM

**Place GDMA control functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > GDMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:3`

Place GDMA control functions (like start/stop/append/reset) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_GDMA_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > GDMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:25`

If enabled, GDMA driver component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_GDMA_ISR_HANDLER_IN_IRAM

**Place GDMA ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > GDMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:11`

Place GDMA ISR handler functions in IRAM to reduce latency caused by cache miss.

---

## CONFIG_GDMA_ISR_IRAM_SAFE

**GDMA ISR IRAM-Safe (Deprecated. Read Help!)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > GDMA Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./dma/Kconfig.dma:35`

This will ensure the GDMA interrupt handler is IRAM-Safe, allow to avoid flash
cache misses, and also be able to run whilst the cache is disabled.
(e.g. SPI Flash write).
If this option is enabled, ALL GDMA channel's ISR handlers should be placed in IRAM, which is a overkill.
It's recommend to set the "isr_cache_safe" in the "gdma_channel_alloc_config_t".
Then other GDMA channels won't be influenced.

---

## CONFIG_GDMA_OBJ_DRAM_SAFE

- **Type:** `string`
- **Default:** `y`

This will ensure the GDMA object is DRAM-Safe, allow to avoid external memory
cache misses, and also be accessible whilst the cache is disabled.

---

## CONFIG_GPIO_CTRL_FUNC_IN_IRAM

**Place GPIO control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:GPIO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gpio/Kconfig:11`

Place GPIO control functions (like intr_disable/set_level) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.

---

## CONFIG_GPIO_ESP32_SUPPORT_SWITCH_SLP_PULL

**Support light sleep GPIO pullup/pulldown configuration for ESP32**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:GPIO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gpio/Kconfig:2`

This option is intended to fix the bug that ESP32 is not able to switch to configured
pullup/pulldown mode in sleep.
If this option is selected, chip will automatically emulate the behaviour of switching,
and about 450B of source codes would be placed into IRAM.

---

## CONFIG_GPTIMER_CTRL_FUNC_IN_IRAM

**Place GPTimer control functions in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:GPTimer Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gptimer/Kconfig:11`

Place GPTimer control functions (like start/stop) in IRAM, to reduce latency caused by cache miss.
If enabled, these functions can also be called when cache is disabled.

---

## CONFIG_GPTIMER_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:GPTimer Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gptimer/Kconfig:35`

If enabled, GPTimer component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_GPTIMER_ISR_CACHE_SAFE

**Allow GPTimer ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:GPTimer Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gptimer/Kconfig:19`

Enable this option to allow the GPTimer Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the GPTimer functionality is still required to operate correctly.

---

## CONFIG_GPTIMER_ISR_HANDLER_IN_IRAM

**Place GPTimer ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:GPTimer Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_gptimer/Kconfig:4`

Place GPTimer ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_GPTIMER_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `y`

This will ensure the GPTimer object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_GPTIMER_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Timer Group Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:115`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_GPTIMER_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Timer Group Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:107`

Whether to suppress the deprecation warnings when using legacy timer group driver (driver/timer.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_HAL_ASSERTION_DISABLE

**Disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > Default HAL assertion level
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:16`

---

## CONFIG_HAL_ASSERTION_ENABLE

**Enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > Default HAL assertion level
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:22`

---

## CONFIG_HAL_ASSERTION_EQUALS_SYSTEM

**Same as system assertion level**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > Default HAL assertion level
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:14`

---

## CONFIG_HAL_ASSERTION_SILENT

**Silent**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > Default HAL assertion level
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:19`

---

## CONFIG_HAL_DEFAULT_ASSERTION_LEVEL

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_HAL_ECDSA_GEN_SIG_CM

**Enable countermeasure for ECDSA signature generation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL)
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:94`

Enable this option to apply the countermeasure for ECDSA signature operation
This countermeasure masks the real ECDSA sign operation
under dummy sign operations to add randomness in the generated power signature.
This countermeasure is only necessary for ESP32-H2 < v1.2.

---

## CONFIG_HAL_LOG_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_HAL_LOG_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:53`

---

## CONFIG_HAL_LOG_LEVEL_ERROR

**Error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:47`

---

## CONFIG_HAL_LOG_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:51`

---

## CONFIG_HAL_LOG_LEVEL_NONE

**No output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:45`

---

## CONFIG_HAL_LOG_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:55`

---

## CONFIG_HAL_LOG_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL) > HAL layer log verbosity
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:49`

---

## CONFIG_HAL_SYSTIMER_USE_ROM_IMPL

**Use ROM implementation of SysTimer HAL driver**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL)
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:68`

Enable this flag to use HAL functions from ROM instead of ESP-IDF.

If keeping this as "n" in your project, you will have less free IRAM.
If making this as "y" in your project, you will increase free IRAM,
but you will lose the possibility to debug this module, and some new
features will be added and bugs will be fixed in the IDF source
but cannot be synced to ROM.

---

## CONFIG_HAL_WDT_USE_ROM_IMPL

**Use ROM implementation of WDT HAL driver**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Abstraction Layer (HAL) and Low Level (LL)
- **Kconfig:** `/opt/esp/idf/components/hal/Kconfig:81`

Enable this flag to use HAL functions from ROM instead of ESP-IDF.

If keeping this as "n" in your project, you will have less free IRAM.
If making this as "y" in your project, you will increase free IRAM,
but you will lose the possibility to debug this module, and some new
features will be added and bugs will be fixed in the IDF source
but cannot be synced to ROM.

---

## CONFIG_HEAP_ABORT_WHEN_ALLOCATION_FAILS

**Abort if memory allocation fails**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:115`

When enabled, if a memory allocation operation fails it will cause a system abort.

---

## CONFIG_HEAP_PLACE_FUNCTION_INTO_FLASH

**Force the entire heap component to be placed in flash memory**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:134`

Enable this flag to save up RAM space by placing the heap component in the flash memory

Note that it is only safe to enable this configuration if no functions from esp_heap_caps.h
or esp_heap_trace.h are called from IRAM ISR which runs when cache is disabled.

---

## CONFIG_HEAP_POISONING_COMPREHENSIVE

**Comprehensive**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Heap corruption detection
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:16`

---

## CONFIG_HEAP_POISONING_DISABLED

**Basic (no poisoning)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Heap memory debugging > Heap corruption detection
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:12`

---

## CONFIG_HEAP_POISONING_LIGHT

**Light impact**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Heap corruption detection
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:14`

---

## CONFIG_HEAP_TASK_TRACKING

**Enable heap task tracking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:94`

Enables tracking the task responsible for each heap allocation.

Note: Allocating or freeing memory or using the task tracking API will lead to a crash when the
scheduler is not working (e.g, after calling vTaskSuspendAll).

---

## CONFIG_HEAP_TLSF_USE_ROM_IMPL

**Use ROM implementation of heap tlsf library**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:121`

Enable this flag to use heap functions from ROM instead of ESP-IDF.

If keeping this as "n" in your project, you will have less free IRAM.
If making this as "y" in your project, you will increase free IRAM,
but you will lose the possibility to debug this module, and some new
features will be added and bugs will be fixed in the IDF source
but cannot be synced to ROM.

---

## CONFIG_HEAP_TRACE_HASH_MAP

**Use hash map mechanism to access heap trace records**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:45`

Enable this flag to use a hash map to increase performance in handling
heap trace records.

Heap trace standalone supports storing records as a list, or a list + hash map.

Using only a list takes less memory, but calls to 'free' will get slower as the
list grows. This is particularly affected when using HEAP_TRACE_ALL mode.

By using a list + hash map, calls to 'free' remain fast, at the cost of
additional memory to store the hash map.

---

## CONFIG_HEAP_TRACE_HASH_MAP_IN_EXT_RAM

**Place hash map in external RAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Use hash map mechanism to access heap trace records
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:61`

When enabled this configuration forces the hash map to be placed in external RAM.

---

## CONFIG_HEAP_TRACE_HASH_MAP_SIZE

**The number of entries in the hash map**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Heap memory debugging > Use hash map mechanism to access heap trace records
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:68`

Defines the number of entries in the heap trace hashmap. Each entry takes 8 bytes.
The bigger this number is, the better the performance. Recommended range: 200 - 2000.

---

## CONFIG_HEAP_TRACING

- **Type:** `string`
- **Default:** `n`

Enables/disables heap tracing API.

---

## CONFIG_HEAP_TRACING_OFF

**Disabled**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Heap memory debugging > Heap tracing
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:30`

---

## CONFIG_HEAP_TRACING_STACK_DEPTH

**Heap tracing stack depth**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:76`

Number of stack frames to save when tracing heap operation callers.

More stack frames uses more memory in the heap trace buffer (and slows down allocation), but
can provide useful information.

---

## CONFIG_HEAP_TRACING_STANDALONE

**Standalone**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Heap tracing
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:32`

---

## CONFIG_HEAP_TRACING_TOHOST

**Host-based**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Heap tracing
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:34`

---

## CONFIG_HEAP_TRACK_DELETED_TASKS

**Keep information about the memory usage of deleted tasks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging > Enable heap task tracking
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:102`

When enabled, this configuration allows the user to keep trace of the memory usage
of a task that has been deleted.

This allows the user to verify that no memory allocated within a task remains unfreed
before terminating the task

Note that this feature cannot keep track of a task deletion if the task is allocated statically

---

## CONFIG_HEAP_USE_HOOKS

**Use allocation and free hooks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Heap memory debugging
- **Kconfig:** `/opt/esp/idf/components/heap/Kconfig:89`

Enable the user to implement function hooks triggered for each successful allocation and free.

---

## CONFIG_HTTPD_ERR_RESP_NO_DELAY

**Use TCP_NODELAY socket option when sending HTTP error responses**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:21`

Using TCP_NODEALY socket option ensures that HTTP error response reaches the client before the
underlying socket is closed. Please note that turning this off may cause multiple test failures

---

## CONFIG_HTTPD_LOG_PURGE_DATA

**Log purged content data at Debug level**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:40`

Enabling this will log discarded binary HTTP request data at Debug level.
For large content data this may not be desirable as it will clutter the log.

---

## CONFIG_HTTPD_MAX_REQ_HDR_LEN

**HTTP Request Header Length limit**

- **Type:** `unknown`
- **Default:** `1024`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:4`

This sets the default limit for the HTTP request header length. The limit can be
configured at run time by setting max_req_hdr_len member of httpd_config_t structure.
The memory allocated will depend on the actual header length. Hence keeping a sufficiently
large max header length is recommended.

---

## CONFIG_HTTPD_MAX_URI_LEN

**Max HTTP URI Length**

- **Type:** `unknown`
- **Default:** `512`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:15`

This sets the maximum supported size of HTTP request URI to be processed by the server

---

## CONFIG_HTTPD_PURGE_BUF_LEN

**Length of temporary buffer for purging data**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:28`

This sets the size of the temporary buffer used to receive and discard any remaining data that is
received from the HTTP client in the request, but not processed as part of the server HTTP request
handler.

If the remaining data is larger than the available buffer size, the buffer will be filled in multiple
iterations. The buffer should be small enough to fit on the stack, but large enough to avoid excessive
iterations.

---

## CONFIG_HTTPD_QUEUE_WORK_BLOCKING

**httpd_queue_work as blocking API**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:53`

This makes httpd_queue_work() API to wait until a message space is available on UDP control socket.
It internally uses a counting semaphore with count set to `LWIP_UDP_RECVMBOX_SIZE` to achieve this.
This config will slightly change API behavior to block until message gets delivered on control socket.

---

## CONFIG_HTTPD_SERVER_EVENT_POST_TIMEOUT

**Time in millisecond to wait for posting event**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:60`

This config option helps in setting the time in millisecond to wait for event to be posted to the
system default event loop. Set it to -1 if you need to set timeout to portMAX_DELAY.

---

## CONFIG_HTTPD_WS_SUPPORT

**WebSocket server support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > HTTP Server
- **Kconfig:** `/opt/esp/idf/components/esp_http_server/Kconfig:47`

This sets the WebSocket server support.

---

## CONFIG_I2C_ENABLE_DEBUG_LOG

**Enable I2C debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:I2C Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2c/Kconfig:12`

whether to enable the debug log message for I2C driver.
Note that this option only controls the I2C driver log, will not affect other drivers.

note: This cannot be used in the I2C legacy driver.

---

## CONFIG_I2C_ENABLE_SLAVE_DRIVER_VERSION_2

**Enable I2C slave driver version 2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:I2C Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2c/Kconfig:21`

I2C slave version 2 solves some existing known issues. Such as write/read workflow, stretch handling, etc.

---

## CONFIG_I2C_ISR_IRAM_SAFE

**I2C ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:I2C Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2c/Kconfig:3`

Ensure the I2C interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).
note: This cannot be used in the I2C legacy driver.

---

## CONFIG_I2C_MASTER_ISR_HANDLER_IN_IRAM

**Place I2C master ISR handler into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:I2C Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2c/Kconfig:27`

Place I2C master ISR handler into IRAM for better performance and fewer cache misses.

---

## CONFIG_I2C_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy I2C Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:158`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_I2S_ENABLE_DEBUG_LOG

**Enable I2S debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:I2S Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2s/Kconfig:11`

whether to enable the debug log message for I2S driver.
Note that, this option only controls the I2S driver log, will not affect other drivers.

---

## CONFIG_I2S_ISR_IRAM_SAFE

**I2S ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:I2S Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_i2s/Kconfig:3`

Ensure the I2S interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_I2S_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy I2S Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:149`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_I2S_SLOT_32BIT

- **Type:** `string`
- **Default:** `n`

Forces the I2S slot width to 32 bits regardless of sample depth. Required for DACs that only
support 32-bit or 24-bit frames (e.g. MA120X0P whose OTP default frame size is 32 bits).
The IDF zero-pads 16-bit samples to fill the 32-bit slot automatically.

---

## CONFIG_I2S_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy I2S Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:141`

Whether to suppress the deprecation warnings when using legacy i2s driver (driver/i2s.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_I2S_USE_MSB_FORMAT

- **Type:** `string`
- **Default:** `n`

Specifies if the I2S should use MSB format instead. Some DACs (such as PT8211) need this enabled to ensure
compatibility with their LSBJ timing under their native bit-depth. This option is only adjusted by the DAC
choice and is not exposed to menuconfig.
See https://github.com/schreibfaul1/ESP32-audioI2S/blob/1d9b299d0197f99fc70335295adcd226dc220f88/src/Audio.cpp#L4929

---

## CONFIG_IDF_CI_BUILD

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_CMAKE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_IDF_DOC_BUILD

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_ENV_BRINGUP

- **Type:** `string`
- **Default:** `n`

- This option is ONLY used when doing new chip bringup.
- This option will only enable necessary hw / sw settings for running
  a hello_world application.

---

## CONFIG_IDF_ENV_FPGA

- **Type:** `string`
- **Default:** `n`

- This option is for internal use only.
- Enabling this option will help enable all FPGA support so as to
  run ESP-IDF on an FPGA. This can help reproduce some issues that
  only happens on FPGA condition, or when you have to burn some
  efuses multiple times.

---

## CONFIG_IDF_EXPERIMENTAL_FEATURES

**Make experimental features visible**

- **Type:** `string`
- **Default:** `n`
- **Kconfig:** `/opt/esp/idf/Kconfig:692`

By enabling this option, ESP-IDF experimental feature options will be visible.

Note you should still enable a certain experimental feature option to use it, and you
should read the corresponding risk warning and known issue list carefully.

Current experimental feature list:

- CONFIG_ESPTOOLPY_FLASHFREQ_120M && CONFIG_ESPTOOLPY_FLASH_SAMPLE_MODE_DTR
- CONFIG_SPIRAM_SPEED_120M && CONFIG_SPIRAM_MODE_OCT
- CONFIG_BOOTLOADER_CACHE_32BIT_ADDR_QUAD_FLASH
- CONFIG_ESP_WIFI_EAP_TLS1_3
- CONFIG_ESP_WIFI_ENABLE_ROAMING_APP
- CONFIG_USB_HOST_EXT_PORT_RESET_ATTEMPTS
- CONFIG_LIBC_PICOLIBC

---

## CONFIG_IDF_FIRMWARE_CHIP_ID

- **Type:** `unknown`
- **Default:** `0x0009`

---

## CONFIG_IDF_INIT_VERSION

- **Type:** `unknown`
- **Default:** `$IDF_INIT_VERSION`

---

## CONFIG_IDF_TARGET

- **Type:** `unknown`
- **Default:** `esp32s3`

---

## CONFIG_IDF_TARGET_ARCH

- **Type:** `unknown`
- **Default:** `xtensa`

---

## CONFIG_IDF_TARGET_ARCH_RISCV

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ARCH_XTENSA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_IDF_TARGET_ESP32

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32C2

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32C3

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32C5

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32C6

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32C61

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32H2

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32H21

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32H4

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32P4

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32S2

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TARGET_ESP32S3

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_IDF_TARGET_LINUX

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TOOLCHAIN

- **Type:** `unknown`
- **Default:** `$IDF_TOOLCHAIN`

---

## CONFIG_IDF_TOOLCHAIN_CLANG

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IDF_TOOLCHAIN_GCC

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_IEEE802154_ASSERT

**Enrich the assert information**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:116`

Enabling this option to print more information when assert.

---

## CONFIG_IEEE802154_CCA_CARRIER

**Carrier sense only**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable > Clear Channel Assessment (CCA) mode
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:23`

configure the CCA mode to Carrier sense only

---

## CONFIG_IEEE802154_CCA_CARRIER_AND_ED

**Carrier sense AND energy above threshold**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable > Clear Channel Assessment (CCA) mode
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:38`

configure the CCA mode to Carrier sense AND energy above threshold

---

## CONFIG_IEEE802154_CCA_CARRIER_OR_ED

**Carrier sense OR energy above threshold**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable > Clear Channel Assessment (CCA) mode
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:33`

configure the CCA mode to Carrier sense OR energy above threshold

---

## CONFIG_IEEE802154_CCA_ED

**Energy above threshold**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable > Clear Channel Assessment (CCA) mode
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:28`

configure the CCA mode to Energy above threshold

---

## CONFIG_IEEE802154_CCA_MODE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_IEEE802154_CCA_THRESHOLD

**CCA detection threshold**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:52`

set the CCA threshold, in dB

---

## CONFIG_IEEE802154_DEBUG

**Enable IEEE802154 Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:92`

Enabling this option allows different kinds of IEEE802154 debug output.
All IEEE802154 debug features increase the size of the final binary.

---

## CONFIG_IEEE802154_DEBUG_ASSERT_MONITOR

**Enable IEEE802154 assert monitor**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:100`

Enabling this option to monitor and detect certain abnormal or unexpected
states during the operation of the IEEE 802.15.4. When this option is enabled,
it will perform additional runtime checks and assertions.

---

## CONFIG_IEEE802154_ENABLED

**IEEE802154 Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:4`

---

## CONFIG_IEEE802154_MULTI_PAN_ENABLE

**Enable multi-pan feature for frame filter**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:68`

Enable IEEE802154 multi-pan

---

## CONFIG_IEEE802154_PENDING_TABLE_SIZE

**Pending table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:60`

set the pending table size

---

## CONFIG_IEEE802154_RECORD

**Record the information with IEEE802154 state and event**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:124`

Enabling this option to add some probe codes in the driver, and record these information.

---

## CONFIG_IEEE802154_RECORD_ABORT

**Enable record abort information for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:177`

Enabling this option to record the abort, when assert, the recorded
abort will be printed.

---

## CONFIG_IEEE802154_RECORD_ABORT_SIZE

**Record abort table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event > Enable record abort information for debugging
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:185`

set the record abort table size

---

## CONFIG_IEEE802154_RECORD_CMD

**Enable record command information for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:161`

Enabling this option to record the command, when assert, the recorded
command will be printed.

---

## CONFIG_IEEE802154_RECORD_CMD_SIZE

**Record command table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event > Enable record command information for debugging
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:169`

set the record command table size

---

## CONFIG_IEEE802154_RECORD_EVENT

**Enable record event information for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:131`

Enabling this option to record event, when assert, the recorded event will be printed.

---

## CONFIG_IEEE802154_RECORD_EVENT_SIZE

**Record event table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event > Enable record event information for debugging
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:138`

set the record event table size

---

## CONFIG_IEEE802154_RECORD_STATE

**Enable record state information for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:146`

Enabling this option to record state, when assert, the recorded state will be printed.

---

## CONFIG_IEEE802154_RECORD_STATE_SIZE

**Record state table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > Record the information with IEEE802154 state and event > Enable record state information for debugging
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:153`

set the record state table size

---

## CONFIG_IEEE802154_RECORD_TXRX_FRAME

**Enable record txrx packets for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:193`

Enabling this option to record the tx and rx packets

---

## CONFIG_IEEE802154_RECORD_TXRX_FRAME_SIZE

**Record frame table size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > Enable record txrx packets for debugging
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:200`

set the record frame table size

---

## CONFIG_IEEE802154_RX_BUFFER_SIZE

**The number of 802.15.4 receive buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:8`

The number of 802.15.4 receive buffers

---

## CONFIG_IEEE802154_RX_BUFFER_STATISTIC

**Rx buffer statistic**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:109`

Enabling this option to count IEEE802154 rx buffer when allocating or freeing.

---

## CONFIG_IEEE802154_SLEEP_ENABLE

**Enable IEEE802154 light sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:83`

Enabling this option allows the IEEE802.15.4 module to be powered down during automatic light sleep,
which reduces current consumption.

---

## CONFIG_IEEE802154_TIMING_OPTIMIZATION

**Enable throughput optimization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4 > IEEE802154 Enable
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:75`

Enabling this option increases throughput by ~5% at the expense of ~2.1k
IRAM code size increase.

---

## CONFIG_IEEE802154_TXRX_STATISTIC

**Enable record tx/rx packets information for debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > IEEE 802.15.4
- **Kconfig:** `/opt/esp/idf/components/ieee802154/Kconfig:208`

Enabling this option to record the tx and rx

---

## CONFIG_INVERT_BCLK_LEVEL

**Invert Logic Level BCLK**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > Logic-Level-Settings
- **Kconfig:** `components/audio_board/Kconfig.projbuild:359`

Invert the Logic-Level of BCLK.

---

## CONFIG_INVERT_MCLK_LEVEL

**Invert Logic Level MCLK**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > Logic-Level-Settings
- **Kconfig:** `components/audio_board/Kconfig.projbuild:349`

Invert the Logic-Level of MCLK.

---

## CONFIG_INVERT_WORD_SELECT_LEVEL

**Invert Logic Level WordSelect**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > Logic-Level-Settings
- **Kconfig:** `components/audio_board/Kconfig.projbuild:354`

Some Dacs can bridge their outputs. Word-Select is used for selecting the played audio channel. This will switch L/R.

---

## CONFIG_ISP_CTRL_FUNC_IN_IRAM

**Place ISP control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:ISP Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_isp/Kconfig:11`

Place ISP control functions into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.
Enabling this option can improve driver performance as well.

Function list:
- `esp_isp_sharpen_configure`

---

## CONFIG_ISP_ISR_IRAM_SAFE

**ISP driver ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:ISP Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_isp/Kconfig:4`

Ensure the ISP driver ISR is IRAM-Safe. When enabled, the ISR handler
will be available when the cache is disabled.

---

## CONFIG_JPEG_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:JPEG-Codec Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_jpeg/Kconfig:4`

whether to enable the debug log message for JPEG driver.
Note that, this option only controls the JPEG driver log, won't affect other drivers.
Please also note, enable this option will make jpeg codec process speed much slower.

---

## CONFIG_LCD_DSI_ISR_IRAM_SAFE

**DSI LCD ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:LCD Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_lcd/Kconfig:31`

Ensure the LCD interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).
If you want the LCD driver to keep flushing the screen even when cache ops disabled,
you can enable this option. Note, this will also increase the IRAM usage.

---

## CONFIG_LCD_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:LCD Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_lcd/Kconfig:2`

whether to enable the debug log message for LCD driver.
Note that, this option only controls the LCD driver log, won't affect other drivers.

---

## CONFIG_LCD_RGB_ISR_IRAM_SAFE

**RGB LCD ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:LCD Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_lcd/Kconfig:10`

Ensure the LCD interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).
If you want the LCD driver to keep flushing the screen even when cache ops disabled,
you can enable this option. Note, this will also increase the IRAM usage.

---

## CONFIG_LCD_RGB_RESTART_IN_VSYNC

**Always restart RGB LCD transmission in VSYNC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:LCD Controller Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_lcd/Kconfig:21`

Reset the GDMA channel every VBlank to stop permanent desyncs from happening.
Only need to enable it when in your application, the DMA can't deliver data
as fast as the LCD consumes it.

---

## CONFIG_LEDC_CTRL_FUNC_IN_IRAM

**Place LEDC control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:LEDC Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_ledc/Kconfig:3`

Place LEDC control functions (ledc_update_duty and ledc_stop) into IRAM,
so that these functions can be IRAM-safe and able to be called in an IRAM context.
Enabling this option can improve driver performance as well.

---

## CONFIG_LIBC_LOCKS_PLACE_IN_IRAM

**Place lock API in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:18`

Enable this option to include be able to call the lock API from
code that runs while cache is disabled, e.g. IRAM interrupts.

---

## CONFIG_LIBC_MISC_IN_IRAM

**Place misc libc functions (abort/assert/stdatomics) in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:14`

---

## CONFIG_LIBC_NEWLIB

**NewLib**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC > LibC to build application with
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:7`

---

## CONFIG_LIBC_NEWLIB_NANO_FORMAT

**Enable 'nano' formatting options for printf/scanf family**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:76`

In most chips the ROM contains parts of newlib C library, including printf/scanf family
of functions. These functions have been compiled with so-called "nano"
formatting option. This option doesn't support 64-bit integer formats and C99
features, such as positional arguments.

For more details about "nano" formatting option, please see newlib readme file,
search for '--enable-newlib-nano-formatted-io':
https://sourceware.org/git/?p=newlib-cygwin.git;a=blob_plain;f=newlib/README;hb=HEAD

If this option is enabled and the ROM contains functions from newlib-nano, the build system
will use functions available in ROM, reducing the application binary size.
Functions available in ROM run faster than functions which run from flash. Functions available
in ROM can also run when flash instruction cache is disabled.

Some chips (e.g. ESP32-C6) has the full formatting versions of printf/scanf in ROM instead of
the nano versions and in this building with newlib nano might actually increase the size of
the binary. Which functions are present in ROM can be seen from ROM caps:
ESP_ROM_HAS_NEWLIB_NANO_FORMAT and ESP_ROM_HAS_NEWLIB_NORMAL_FORMAT.

If you need 64-bit integer formatting support or C99 features, keep this
option disabled.

---

## CONFIG_LIBC_OPTIMIZED_MISALIGNED_ACCESS

**Use performance-optimized memXXX/strXXX functions on misaligned memory access**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:146`

Enables performance-optimized implementations of memory and string functions
when handling misaligned memory.

This increases the image size by ~1000 bytes.

Optimized functions include:
    - memcpy
    - memset
    - memmove
    - str[n]cpy
    - str[n]cmp

---

## CONFIG_LIBC_PICOLIBC

**Picolibc (EXPERIMENTAL)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > LibC to build application with
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:9`

---

## CONFIG_LIBC_STDIN_LINE_ENDING_CR

**CR**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC > Line ending for console input
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:72`

---

## CONFIG_LIBC_STDIN_LINE_ENDING_CRLF

**CRLF**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Line ending for console input
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:68`

---

## CONFIG_LIBC_STDIN_LINE_ENDING_LF

**LF**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Line ending for console input
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:70`

---

## CONFIG_LIBC_STDOUT_LINE_ENDING_CR

**CR**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Line ending for console output
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:47`

---

## CONFIG_LIBC_STDOUT_LINE_ENDING_CRLF

**CRLF**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC > Line ending for console output
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:43`

---

## CONFIG_LIBC_STDOUT_LINE_ENDING_LF

**LF**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Line ending for console output
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:45`

---

## CONFIG_LIBC_TIME_SYSCALL_USE_HRT

**High-resolution timer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Timers used for gettimeofday function
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:138`

---

## CONFIG_LIBC_TIME_SYSCALL_USE_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Timers used for gettimeofday function
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:141`

---

## CONFIG_LIBC_TIME_SYSCALL_USE_RTC

**RTC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LibC > Timers used for gettimeofday function
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:135`

---

## CONFIG_LIBC_TIME_SYSCALL_USE_RTC_HRT

**RTC and high-resolution timer**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LibC > Timers used for gettimeofday function
- **Kconfig:** `/opt/esp/idf/components/newlib/Kconfig:131`

---

## CONFIG_LOG_COLORS

**Color**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:3`

Enable ANSI terminal color codes. Logs (info, errors, warnings) will contain color codes.
In order to view these, your terminal program must support ANSI color codes.

This is disabled by default, as colors are added in IDF Monitor. If you want to use new lines
in the messages or you are using a different terminal program, you may want to enable this option.

---

## CONFIG_LOG_COLORS_SUPPORT

**Allow enabling color output at run time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:14`

Enables support for color codes in the esp_log() function. If CONFIG_LOG_COLORS is enabled, this option
is always active. If CONFIG_LOG_COLORS is disabled, this option allows you to still handle color codes
in specific files by defining ESP_LOG_COLOR_DISABLED as 0 before including esp_log.h.

Note that enabling this option may slightly increase IRAM usage due to additional color handling
functionality. It provides flexibility to manage color output even when CONFIG_LOG_COLORS is turned off.

---

## CONFIG_LOG_DEFAULT_LEVEL

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_LOG_DEFAULT_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:26`

---

## CONFIG_LOG_DEFAULT_LEVEL_ERROR

**Error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:20`

---

## CONFIG_LOG_DEFAULT_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:24`

---

## CONFIG_LOG_DEFAULT_LEVEL_NONE

**No output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:18`

---

## CONFIG_LOG_DEFAULT_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:28`

---

## CONFIG_LOG_DEFAULT_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Default log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:22`

---

## CONFIG_LOG_DYNAMIC_LEVEL_CONTROL

**Enable dynamic log level changes at runtime**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log Level > Level Settings
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:18`

Enabling this option allows dynamic changes to the log level at runtime
(using esp_log_level_set()), providing the ability to increase or decrease
the log level during program execution.
If disabled, the log level remains static once set at compile-time and calling
esp_log_level_set() will have no effect.
If binary size is a critical consideration and dynamic log level changes are not needed,
consider disabling this option when LOG_TAG_LEVEL_IMPL_NONE=y to minimize program size.

---

## CONFIG_LOG_IN_IRAM

**Place logging functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log
- **Kconfig:** `/opt/esp/idf/components/log/Kconfig:55`

---

## CONFIG_LOG_MASTER_LEVEL

**Enable global master log level**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Level Settings
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:3`

Enables an additional global "master" log level check that occurs before a log tag cache
lookup. This is useful if you want to compile in a lot of logs that are selectable at
runtime, but avoid the performance hit during periods where you don't want log output.

Examples include remote log forwarding, or disabling logs during a time-critical or
CPU-intensive section and re-enabling them later. Results in larger program size
depending on number of logs compiled in.

If enabled, defaults to LOG_DEFAULT_LEVEL and can be set using
esp_log_set_level_master(). This check takes precedence over ESP_LOG_LEVEL_LOCAL.

---

## CONFIG_LOG_MAXIMUM_EQUALS_DEFAULT

**Same as default**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:59`

---

## CONFIG_LOG_MAXIMUM_LEVEL

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_LOG_MAXIMUM_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:70`

---

## CONFIG_LOG_MAXIMUM_LEVEL_ERROR

**Error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:61`

---

## CONFIG_LOG_MAXIMUM_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:67`

---

## CONFIG_LOG_MAXIMUM_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:73`

---

## CONFIG_LOG_MAXIMUM_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Maximum log verbosity
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.level:64`

---

## CONFIG_LOG_MODE_BINARY

**Binary Log Mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Settings > Log Mode
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.settings:21`

Enables binary logging with host-side format string expansion. In this mode, the
format argument of ESP_LOGx, ESP_EARLY_LOG, and ESP_DRAM_LOG macros is stored in a
NOLOAD section, not included in the final binary file. This reduces flash usage by
approximately 10% - 35%. The esp_log() function uses the binary log handler to output
messages. Instead of sending the full log string, the chip transmits only the
addresses of these strings (if present in the ELF file). If the format string
cannot be found in the ELF file, the chip sends the entire string. The host-side
monitor tool, which has access to the ELF file, reconstructs the log message using
the format string.
This reduces firmware size by eliminating format strings from
flash memory and removing the usage of printf-like functions, potentially freeing up
a few kilobytes of space. To further reduce firmware size, wrap string data with ESP_LOG_ATTR_STR.

---

## CONFIG_LOG_MODE_BINARY_EN

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_LOG_MODE_TEXT

**Text Log Mode**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Settings > Log Mode
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.settings:13`

Enables text-based logging, where log messages are stored in a human-readable format.
This mode is useful for development and debugging, as it allows logs to be easily
read and interpreted without additional processing.

---

## CONFIG_LOG_MODE_TEXT_EN

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_LOG_TAG_LEVEL_CACHE_ARRAY

**Array**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Level Settings > Cache implementation
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:84`

This option enables the use of a simple array-based cache implementation for storing and
retrieving log tag levels. There is no additional code that reorders the cache for fast lookups.
Suitable for projects where memory usage optimization is crucial and the simplicity of implementation
is preferred.

---

## CONFIG_LOG_TAG_LEVEL_CACHE_BINARY_MIN_HEAP

**Binary Min-Heap**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log Level > Level Settings > Cache implementation
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:92`

This option enables the use of a binary min-heap-based cache implementation for efficient
storage and retrieval of log tag levels. It does automatically optimizing cache for fast lookups.
Suitable for projects where speed of lookup is critical and memory usage can accommodate
the overhead of maintaining a binary min-heap structure.

---

## CONFIG_LOG_TAG_LEVEL_IMPL_CACHE_AND_LINKED_LIST

**Cache + Linked List**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log Level > Level Settings > Method of tag level checks
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:56`

Select this option to use a hybrid mode: cache in combination with the linked list
for log tag level checks. This hybrid approach offers a balance between speed and memory usage.

The cache stores recently accessed log tags and their corresponding log levels, providing
faster lookups for frequently used tags. The cache approach compares the tag pointers, which is
faster than comparing the whole strings.

For less frequently used tags, the linked list is used to search for the log level, which may be
slower for a large number of tags but has lower memory requirements compared to a full cache.

This hybrid approach aims to improve the efficiency of log level retrieval by combining the benefits
of both cache and linked list implementations.

---

## CONFIG_LOG_TAG_LEVEL_IMPL_CACHE_SIZE

**Log Tag Cache Size**

- **Type:** `unknown`
- **Default:** `31`
- **Menu:** Component config > Log > Log Level > Level Settings
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:101`

This option sets the size of the cache used for log tag entries. The cache stores recently accessed
log tags and their corresponding log levels, which helps improve the efficiency of log level retrieval.
The value must be a power of 2 minus 1 (e.g., 1, 3, 7, 15, 31, 63, 127, 255, ...)
to ensure proper cache behavior. For LOG_TAG_LEVEL_CACHE_ARRAY option the value can be any,
without restrictions.

Note: A larger cache size can improve lookup performance for frequently used log tags but may consume
more memory. Conversely, a smaller cache size reduces memory usage but may lead to more frequent cache
evictions for less frequently used log tags.

---

## CONFIG_LOG_TAG_LEVEL_IMPL_LINKED_LIST

**Linked List**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Level Settings > Method of tag level checks
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:47`

Select this option to use the linked list-only implementation (no cache) for log level retrieval.
This approach searches the linked list of all tags for the log level, which may be slower
for a large number of tags but may have lower memory requirements than the CACHE approach.
The linked list approach compares the whole strings of log tags for finding the log level.

---

## CONFIG_LOG_TAG_LEVEL_IMPL_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log Level > Level Settings > Method of tag level checks
- **Kconfig:** `/opt/esp/idf/components/log/././Kconfig.level_settings:38`

This option disables the ability to set the log level per tag.
The ability to change the log level at runtime depends on LOG_DYNAMIC_LEVEL_CONTROL.
If LOG_DYNAMIC_LEVEL_CONTROL is disabled, then changing the log level at runtime
using `esp_log_level_set()` is not possible.
This implementation is suitable for highly constrained environments.

---

## CONFIG_LOG_TIMESTAMP_SOURCE_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:60`

---

## CONFIG_LOG_TIMESTAMP_SOURCE_RTOS

**Milliseconds Since Boot**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:64`

---

## CONFIG_LOG_TIMESTAMP_SOURCE_SYSTEM

**System Time (HH:MM:SS.sss)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:68`

---

## CONFIG_LOG_TIMESTAMP_SOURCE_SYSTEM_FULL

**System Time (YY-MM-DD HH:MM:SS.sss)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:72`

---

## CONFIG_LOG_TIMESTAMP_SOURCE_UNIX

**Unix time in milliseconds**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format > Timestamp
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:77`

---

## CONFIG_LOG_TIMESTAMP_SUPPORT

**Allow enabling timestamp output at run time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Format
- **Kconfig:** `/opt/esp/idf/components/log/./Kconfig.format:84`

Enables support for timestamp in the esp_log() function.
If CONFIG_LOG_TIMESTAMP_SOURCE_NONE, this option allows you to still handle timestamp
in specific files by defining ESP_LOG_TIMESTAMP_DISABLED as 0 before including esp_log.h.

Note that enabling this option may slightly increase IRAM usage due to additional timestamp handling
functionality. It provides flexibility to manage timestamp output even when
CONFIG_LOG_TIMESTAMP_SOURCE_NONE.

---

## CONFIG_LOG_VERSION

- **Type:** `unknown`
- **Default:** `1`

This configuration sets the log version number based on the chosen log version.

---

## CONFIG_LOG_VERSION_1

**V1**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Log > Log version
- **Kconfig:** `/opt/esp/idf/components/log/Kconfig:29`

Select this option to use Log V1. Recommended for projects with strict stack constraints
or that prioritize performance over flexibility.

---

## CONFIG_LOG_VERSION_2

**V2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Log > Log version
- **Kconfig:** `/opt/esp/idf/components/log/Kconfig:35`

Select this option to use Log V2. Recommended for projects that require smaller binaries,
runtime log formatting configuration, or advanced logging features.

---

## CONFIG_LWIP_API_LIB_DEBUG

**Enable api lib debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1446`

---

## CONFIG_LWIP_AUTOIP

**Enable IPV4 Link-Local Addressing (AUTOIP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:444`

Enabling this option allows the device to self-assign an address
in the 169.256/16 range if none is assigned statically or via DHCP.

See RFC 3927.

---

## CONFIG_LWIP_AUTOIP_MAX_CONFLICTS

**Max IP conflicts before rate limiting**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > Enable IPV4 Link-Local Addressing (AUTOIP)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:468`

If the AUTOIP functionality detects this many IP conflicts while
self-assigning an address, it will go into a rate limited mode.

---

## CONFIG_LWIP_AUTOIP_RATE_LIMIT_INTERVAL

**Rate limited interval (seconds)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > Enable IPV4 Link-Local Addressing (AUTOIP)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:477`

If rate limiting self-assignment requests, wait this long between
each request.

---

## CONFIG_LWIP_AUTOIP_TRIES

**DHCP Probes before self-assigning IPv4 LL address**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > Enable IPV4 Link-Local Addressing (AUTOIP)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:454`

DHCP client will send this many probes before self-assigning a
link local address.

From LWIP help: "This can be set as low as 1 to get an AutoIP
address very quickly, but you should be prepared to handle a
changing IP address when DHCP overrides AutoIP." (In the case of
ESP-IDF, this means multiple SYSTEM_EVENT_STA_GOT_IP events.)

---

## CONFIG_LWIP_BRIDGEIF_DEBUG

**Enable bridge generic debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1511`

---

## CONFIG_LWIP_BRIDGEIF_FDB_DEBUG

**Enable bridge FDB debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1516`

---

## CONFIG_LWIP_BRIDGEIF_FW_DEBUG

**Enable bridge forwarding debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1521`

---

## CONFIG_LWIP_BRIDGEIF_MAX_PORTS

**Maximum number of bridge ports**

- **Type:** `unknown`
- **Default:** `7`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1222`

Set maximum number of ports a bridge can consists of.

---

## CONFIG_LWIP_BROADCAST_PING

**Respond to broadcast pings**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > ICMP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1091`

---

## CONFIG_LWIP_CHECKSUM_CHECK_ICMP

**Enable LWIP ICMP checksums**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Checksums
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:864`

Enable checksum checking for received ICMP messages

---

## CONFIG_LWIP_CHECKSUM_CHECK_IP

**Enable LWIP IP checksums**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Checksums
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:852`

Enable checksum checking for received IP messages

---

## CONFIG_LWIP_CHECKSUM_CHECK_UDP

**Enable LWIP UDP checksums**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Checksums
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:858`

Enable checksum checking for received UDP messages

---

## CONFIG_LWIP_CHECK_THREAD_SAFETY

**Checks that lwip API runs in expected context**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:49`

Enable to check that the project does not violate lwip thread safety.
If enabled, all lwip functions that require thread awareness run an assertion
to verify that the TCP/IP core functionality is either locked or accessed
from the correct thread.

---

## CONFIG_LWIP_DEBUG

**Enable LWIP Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1416`

Enabling this option allows different kinds of lwIP debug output.

All lwIP debug features increase the size of the final binary.

---

## CONFIG_LWIP_DEBUG_ESP_LOG

**Route LWIP debugs through ESP_LOG interface**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1424`

Enabling this option routes all enabled LWIP debugs through ESP_LOGD.

---

## CONFIG_LWIP_DHCPS

**DHCPS: Enable IPv4 Dynamic Host Configuration Protocol Server (DHCPS)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > DHCP server
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:392`

Enabling this option allows the device to run the DHCP server
(to dynamically assign IPv4 addresses to clients).

---

## CONFIG_LWIP_DHCPS_ADD_DNS

**Always add DNS option in DHCP responses**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > DHCP server > DHCPS: Enable IPv4 Dynamic Host Configuration Protocol Server (DHCPS)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:428`

This allows the DNS option to be optional in the DHCP offers,
depending on the server's runtime configuration.
When enabled, the DHCP server will always add the DNS option to DHCP responses.
If a DNS server is not explicitly configured, the server's IP address will be used
as the fallback for the DNS option.
When disabled, the DHCP server will only include the DNS option in responses
if a DNS server has been explicitly configured.
This option will be removed in IDF v6.x

---

## CONFIG_LWIP_DHCPS_LEASE_UNIT

**Multiplier for lease time, in seconds**

- **Type:** `unknown`
- **Default:** `60`
- **Menu:** Component config > LWIP > DHCP server > DHCPS: Enable IPv4 Dynamic Host Configuration Protocol Server (DHCPS)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:400`

The DHCP server is calculating lease time multiplying the sent
and received times by this number of seconds per unit.
The default is 60, that equals one minute.

---

## CONFIG_LWIP_DHCPS_MAX_STATION_NUM

**Maximum number of stations**

- **Type:** `unknown`
- **Default:** `8`
- **Menu:** Component config > LWIP > DHCP server > DHCPS: Enable IPv4 Dynamic Host Configuration Protocol Server (DHCPS)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:410`

The maximum number of DHCP clients that are connected to the server.
After this number is exceeded, DHCP server removes of the oldest device
from it's address pool, without notification.

---

## CONFIG_LWIP_DHCPS_STATIC_ENTRIES

**Enable ARP static entries**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > DHCP server > DHCPS: Enable IPv4 Dynamic Host Configuration Protocol Server (DHCPS)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:420`

Enabling this option allows DHCP server to support temporary static ARP entries
for DHCP Client. This will help the DHCP server to send the DHCP OFFER and DHCP ACK using IP unicast.

---

## CONFIG_LWIP_DHCP_COARSE_TIMER_SECS

**DHCP coarse timer interval(s)**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:382`

Set DHCP coarse interval in seconds.
A higher value will be less precise but cost less power consumption.

---

## CONFIG_LWIP_DHCP_DEBUG

**Enable DHCP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1471`

---

## CONFIG_LWIP_DHCP_DISABLE_CLIENT_ID

**DHCP: Disable Use of HW address as client identification**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:337`

This option could be used to disable DHCP client identification with its MAC address.
(Client id is used by DHCP servers to uniquely identify clients and are included
in the DHCP packets as an option 61)
Set this option to "y" in order to exclude option 61 from DHCP packets.

---

## CONFIG_LWIP_DHCP_DISABLE_VENDOR_CLASS_ID

**DHCP: Disable Use of vendor class identification**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:347`

This option could be used to disable DHCP client vendor class identification.
Set this option to "y" in order to exclude option 60 from DHCP packets.

---

## CONFIG_LWIP_DHCP_DOES_ACD_CHECK

**DHCP provides Address Conflict Detection (ACD)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Choose how DHCP validates offered IP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:330`

---

## CONFIG_LWIP_DHCP_DOES_ARP_CHECK

**DHCP provides simple ARP check**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Choose how DHCP validates offered IP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:327`

---

## CONFIG_LWIP_DHCP_DOES_NOT_CHECK_OFFERED_IP

**DHCP does not detect conflict on the offered IP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Choose how DHCP validates offered IP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:332`

---

## CONFIG_LWIP_DHCP_GET_NTP_SRV

**Request NTP servers from DHCP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > SNTP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1122`

If enabled, LWIP will add 'NTP' to Parameter-Request Option sent via DHCP-request.
DHCP server might reply with an NTP server address in option 42.
SNTP callback for such replies should be set accordingly (see sntp_servermode_dhcp() func.)

---

## CONFIG_LWIP_DHCP_MAX_NTP_SERVERS

**Maximum number of NTP servers acquired via DHCP**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > SNTP > Request NTP servers from DHCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1130`

Set maximum number of NTP servers acquired via DHCP-offer.
Should be less or equal to "Maximum number of NTP servers", any extra servers would be just ignored.

---

## CONFIG_LWIP_DHCP_OPTIONS_LEN

**DHCP total option length**

- **Type:** `unknown`
- **Default:** `69`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:364`

Set total length of outgoing DHCP option msg. Generally bigger value means it can carry more
options and values. If your code meets LWIP_ASSERT due to option value is too long.
Please increase the LWIP_DHCP_OPTIONS_LEN value.

---

## CONFIG_LWIP_DHCP_RESTORE_LAST_IP

**DHCP: Restore last IP obtained from DHCP server**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:355`

When this option is enabled, DHCP client tries to re-obtain last valid IP address obtained from DHCP
server. Last valid DHCP configuration is stored in nvs and restored after reset/power-up. If IP is still
available, there is no need for sending discovery message to DHCP server and save some time.

---

## CONFIG_LWIP_DHCP_STATE_DEBUG

**Enable DHCP state tracking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1466`

---

## CONFIG_LWIP_DNS_DEBUG

**Enable DNS debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1501`

---

## CONFIG_LWIP_DNS_MAX_HOST_IP

**Maximum number of IP addresses per host**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > LWIP > DNS
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1172`

Maximum number of IP addresses that can be returned by DNS queries for a single host.

---

## CONFIG_LWIP_DNS_MAX_SERVERS

**Maximum number of DNS servers**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > LWIP > DNS
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1178`

Set maximum number of DNS servers.
If fallback DNS servers are supported,
the number of DNS servers needs to be greater than or equal to 3.

---

## CONFIG_LWIP_DNS_SETSERVER_WITH_NETIF

**Enable DNS server settings with netif**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > DNS
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1201`

This option allows collecting DNS server settings per netif using
configurable callback function.
It's typically used with CONFIG_ESP_NETIF_SET_DNS_PER_DEFAULT_NETIF
which configures a callback to collect the DNS info on esp_netif layer.

---

## CONFIG_LWIP_DNS_SUPPORT_MDNS_QUERIES

**Enable mDNS queries in resolving host name**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:58`

If this feature is enabled, standard API such as gethostbyname
support .local addresses by sending one shot multicast mDNS
query

---

## CONFIG_LWIP_ENABLE

**Enable LwIP stack**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:2`

Builds normally if selected. Excludes LwIP from build if unselected, even if it is a
dependency of a component or application.
Some applications can switch their IP stacks, e.g., when switching between chip
and Linux targets (LwIP stack vs. Linux IP stack). Since the LwIP dependency cannot
easily be excluded based on a Kconfig option, it has to be a dependency in all cases.
This switch allows the LwIP stack to be built selectively, even if it is a dependency.

---

## CONFIG_LWIP_ENABLE_LCP_ECHO

**Enable LCP ECHO**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1022`

Enable LCP echo keepalive requests

---

## CONFIG_LWIP_ESP_GRATUITOUS_ARP

**Send gratuitous ARP periodically**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:268`

Enable this option allows to send gratuitous ARP periodically.

This option solve the compatibility issues.If the ARP table of the AP is old, and the AP
doesn't send ARP request to update it's ARP table, this will lead to the STA sending IP packet fail.
Thus we send gratuitous ARP periodically to let AP update it's ARP table.

---

## CONFIG_LWIP_ESP_LWIP_ASSERT

**Enable LWIP ASSERT checks**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1229`

Enable this option keeps LWIP assertion checks enabled.
It is recommended to keep this option enabled.

If asserts are disabled for the entire project, they are also disabled
for LWIP and this option is ignored.

---

## CONFIG_LWIP_ESP_MLDV6_REPORT

**Send mldv6 report periodically**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:286`

Enable this option allows to send mldv6 report periodically.

This option solve the issue that failed to receive multicast data.
Some routers fail to forward multicast packets.
To solve this problem, send multicast mdlv6 report to routers regularly.

---

## CONFIG_LWIP_ETHARP_DEBUG

**Enable etharp debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1441`

---

## CONFIG_LWIP_EXTRA_IRAM_OPTIMIZATION

**Enable LWIP IRAM optimization for TCP part**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:93`

If this feature is enabled, some tcp part functions relating to RX/TX in LWIP will be
put into IRAM, it can improve TCP throughput. On the other hand, it needs about 17KB
IRAM for these optimizations.

---

## CONFIG_LWIP_FALLBACK_DNS_SERVER_ADDRESS

**DNS fallback server address**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > DNS > Enable DNS fallback server support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1194`

This option allows you to config dns fallback server address.

---

## CONFIG_LWIP_FALLBACK_DNS_SERVER_SUPPORT

**Enable DNS fallback server support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > DNS
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1187`

Enable this feature to support DNS fallback server.

---

## CONFIG_LWIP_FORCE_ROUTER_FORWARDING

**LWIP Force Router Forwarding Enable/Disable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > LWIP NDP6 Enable/Disable
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:119`

This option is used to set the the router flag for the NA packets.
When enabled, the router flag in NA packet will always set to 1,
otherwise, never set router flag for NA packets.

---

## CONFIG_LWIP_GARP_TMR_INTERVAL

**GARP timer interval(seconds)**

- **Type:** `unknown`
- **Default:** `60`
- **Menu:** Component config > LWIP > Send gratuitous ARP periodically
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:279`

Set the timer interval for gratuitous ARP. The default value is 60s

---

## CONFIG_LWIP_HOOK_DHCP_EXTRA_OPTION_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > DHCP extra option hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1342`

---

## CONFIG_LWIP_HOOK_DHCP_EXTRA_OPTION_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > DHCP extra option hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1340`

---

## CONFIG_LWIP_HOOK_DHCP_EXTRA_OPTION_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > DHCP extra option hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1338`

---

## CONFIG_LWIP_HOOK_DNS_EXTERNAL_RESOLVE_SELECT_CUSTOM

- **Type:** `string`
- **Default:** `n`

This hidden option helps configure the DNS external resolve
hook for external components like OpenThread. It ensures that
`LWIP_HOOK_DNS_EXT_RESOLVE_CUSTOM` is selected without directly
adding a dependency in the choice construct.

---

## CONFIG_LWIP_HOOK_DNS_EXT_RESOLVE_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > DNS external resolve Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1385`

---

## CONFIG_LWIP_HOOK_DNS_EXT_RESOLVE_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > DNS external resolve Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1383`

---

## CONFIG_LWIP_HOOK_IP6_INPUT_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 packet input
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1409`

---

## CONFIG_LWIP_HOOK_IP6_INPUT_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > IPv6 packet input
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1407`

---

## CONFIG_LWIP_HOOK_IP6_INPUT_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 packet input
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1405`

---

## CONFIG_LWIP_HOOK_IP6_ROUTE_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 route Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1283`

---

## CONFIG_LWIP_HOOK_IP6_ROUTE_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 route Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1281`

---

## CONFIG_LWIP_HOOK_IP6_ROUTE_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > IPv6 route Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1279`

---

## CONFIG_LWIP_HOOK_IP6_SELECT_SRC_ADDR_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 source address selection Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1323`

---

## CONFIG_LWIP_HOOK_IP6_SELECT_SRC_ADDR_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 source address selection Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1321`

---

## CONFIG_LWIP_HOOK_IP6_SELECT_SRC_ADDR_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > IPv6 source address selection Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1319`

---

## CONFIG_LWIP_HOOK_ND6_GET_GW_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 get gateway Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1303`

---

## CONFIG_LWIP_HOOK_ND6_GET_GW_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > IPv6 get gateway Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1301`

---

## CONFIG_LWIP_HOOK_ND6_GET_GW_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > IPv6 get gateway Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1299`

---

## CONFIG_LWIP_HOOK_NETCONN_EXT_RESOLVE_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > Netconn external resolve Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1361`

---

## CONFIG_LWIP_HOOK_NETCONN_EXT_RESOLVE_DEFAULT

**Default (weak) implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > Netconn external resolve Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1359`

---

## CONFIG_LWIP_HOOK_NETCONN_EXT_RESOLVE_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > Netconn external resolve Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1357`

---

## CONFIG_LWIP_HOOK_TCP_ISN_CUSTOM

**Custom implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > TCP ISN Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1263`

---

## CONFIG_LWIP_HOOK_TCP_ISN_DEFAULT

**Default implementation**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Hooks > TCP ISN Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1261`

---

## CONFIG_LWIP_HOOK_TCP_ISN_NONE

**No hook declared**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Hooks > TCP ISN Hook
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1259`

---

## CONFIG_LWIP_ICMP

**ICMP: Enable ICMP**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > ICMP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1080`

Enable ICMP module for check network stability

---

## CONFIG_LWIP_ICMP6_DEBUG

**Enable ICMP6 debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1481`

---

## CONFIG_LWIP_ICMP_DEBUG

**Enable ICMP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1461`

---

## CONFIG_LWIP_IP4_FRAG

**Enable fragment outgoing IP4 packets**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:205`

Enabling this option allows fragmenting outgoing IP4 packets if their size
exceeds MTU.

---

## CONFIG_LWIP_IP4_REASSEMBLY

**Enable reassembly incoming fragmented IP4 packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:221`

Enabling this option allows reassemblying incoming fragmented IP4 packets.

---

## CONFIG_LWIP_IP6_DEBUG

**Enable IP6 debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1476`

---

## CONFIG_LWIP_IP6_FRAG

**Enable fragment outgoing IP6 packets**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:213`

Enabling this option allows fragmenting outgoing IP6 packets if their size
exceeds MTU.

---

## CONFIG_LWIP_IP6_REASSEMBLY

**Enable reassembly incoming fragmented IP6 packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:228`

Enabling this option allows reassemblying incoming fragmented IP6 packets.

---

## CONFIG_LWIP_IPV4

**Enable IPv4**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:486`

Enable IPv4 stack. If you want to use IPv6 only TCP/IP stack, disable this.

---

## CONFIG_LWIP_IPV4_NAPT

**Enable NAT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable IP forwarding
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:248`

Enabling this option allows Network Address and Port Translation.

---

## CONFIG_LWIP_IPV4_NAPT_PORTMAP

**Enable NAT Port Mapping**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable IP forwarding > Enable NAT
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:255`

Enabling this option allows Port Forwarding or Port mapping.

---

## CONFIG_LWIP_IPV6

**Enable IPv6**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:492`

Enable IPv6 function. If not use IPv6 function, set this option to n.
If disabling LWIP_IPV6 then some other components (asio) will
no longer be available.

---

## CONFIG_LWIP_IPV6_AUTOCONFIG

**Enable IPV6 stateless address autoconfiguration (SLAAC)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable IPv6
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:500`

Enabling this option allows the devices to IPV6 stateless address autoconfiguration (SLAAC).

See RFC 4862.

---

## CONFIG_LWIP_IPV6_DHCP6

**Enable DHCPv6 stateless address autoconfiguration**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:535`

Enable DHCPv6 for IPv6 stateless address autoconfiguration.
Note that the dhcpv6 client has to be started using dhcp6_enable_stateless(netif);
Note that the stateful address autoconfiguration is not supported.

---

## CONFIG_LWIP_IPV6_FORWARD

**Enable IPv6 forwarding between interfaces**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable IPv6
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:517`

Forwarding IPv6 packets between interfaces is only required when acting as
a router.

---

## CONFIG_LWIP_IPV6_MEMP_NUM_ND6_QUEUE

**Max number of IPv6 packets to queue during MAC resolution**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:907`

Config max number of IPv6 packets to queue during MAC resolution.

---

## CONFIG_LWIP_IPV6_ND6_NUM_DESTINATIONS

**Max number of entries in IPv6 destinations cache**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:937`

Maximum number of entries in IPv6 destinations cache

---

## CONFIG_LWIP_IPV6_ND6_NUM_NEIGHBORS

**Max number of entries in IPv6 neighbor cache**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:915`

Config max number of entries in IPv6 neighbor cache

---

## CONFIG_LWIP_IPV6_ND6_NUM_PREFIXES

**Max number of entries in IPv6 on-link prefixes cache**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:923`

Maximum number of entries in IPv6 on-link prefixes cache

---

## CONFIG_LWIP_IPV6_ND6_NUM_ROUTERS

**Max number of entries in IPv6 default routers cache**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:930`

Maximum number of entries in IPv6 default routers cache

---

## CONFIG_LWIP_IPV6_NUM_ADDRESSES

**Number of IPv6 addresses on each network interface**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > LWIP > Enable IPv6
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:509`

The maximum number of IPv6 addresses on each interface. Any additional
addresses will be discarded.

---

## CONFIG_LWIP_IPV6_RDNSS_MAX_DNS_SERVERS

**Use IPv6 Router Advertisement Recursive DNS Server Option**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:525`

Use IPv6 Router Advertisement Recursive DNS Server Option (as per RFC 6106) to
copy a defined maximum number of DNS servers to the DNS module.
Set this option to a number of desired DNS servers advertised in the RA protocol.
This feature is disabled when set to 0.

---

## CONFIG_LWIP_IP_DEBUG

**Enable IP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1456`

---

## CONFIG_LWIP_IP_DEFAULT_TTL

**The value for Time-To-Live used by transport layers**

- **Type:** `unknown`
- **Default:** `64`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:198`

Set value for Time-To-Live used by transport layers.

---

## CONFIG_LWIP_IP_FORWARD

**Enable IP forwarding**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:242`

Enabling this option allows packets forwarding across multiple interfaces.

---

## CONFIG_LWIP_IP_REASS_MAX_PBUFS

**The maximum amount of pbufs waiting to be reassembled**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:235`

Set the maximum amount of pbufs waiting to be reassembled.

---

## CONFIG_LWIP_IRAM_OPTIMIZATION

**Enable LWIP IRAM optimization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:82`

If this feature is enabled, some functions relating to RX/TX in LWIP will be
put into IRAM, it can improve UDP/TCP throughput by >10% for single core mode,
it doesn't help too much for dual core mode. On the other hand, it needs about
10KB IRAM for these optimizations.

If this feature is disabled, all lwip functions will be put into FLASH.

---

## CONFIG_LWIP_L2_TO_L3_COPY

**Enable copy between Layer2 and Layer3 packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:66`

If this feature is enabled, all traffic from layer2(WIFI Driver) will be
copied to a new buffer before sending it to layer3(LWIP stack), freeing
the layer2 buffer.
Please be notified that the total layer2 receiving buffer is fixed and
ESP32 currently supports 25 layer2 receiving buffer, when layer2 buffer
runs out of memory, then the incoming packets will be dropped in hardware.
The layer3 buffer is allocated from the heap, so the total layer3 receiving
buffer depends on the available heap size, when heap runs out of memory,
no copy will be sent to layer3 and packet will be dropped in layer2.
Please make sure you fully understand the impact of this feature before
enabling it.

---

## CONFIG_LWIP_LCP_ECHOINTERVAL

**Echo interval (s)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > Enable PPP support > Enable LCP ECHO
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1029`

Interval in seconds between keepalive LCP echo requests, 0 to disable.

---

## CONFIG_LWIP_LCP_MAXECHOFAILS

**Maximum echo failures**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > Enable PPP support > Enable LCP ECHO
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1037`

Number of consecutive unanswered echo requests before failure is indicated.

---

## CONFIG_LWIP_LOCAL_HOSTNAME

**Local netif hostname**

- **Type:** `unknown`
- **Default:** `espressif`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:14`

The default name this device will report to other devices on the network.
Could be updated at runtime with esp_netif_set_hostname()

---

## CONFIG_LWIP_LOOPBACK_MAX_PBUFS

**Max queued loopback packets per interface**

- **Type:** `unknown`
- **Default:** `8`
- **Menu:** Component config > LWIP > Support per-interface loopback
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:559`

Configure the maximum number of packets which can be queued for
loopback on a given interface. Reducing this number may cause packets
to be dropped, but will avoid filling memory with queued packet data.

---

## CONFIG_LWIP_MAX_ACTIVE_TCP

**Maximum active TCP Connections**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:571`

The maximum number of simultaneously active TCP
connections. The practical maximum limit is
determined by available heap memory at runtime.

Changing this value by itself does not substantially
change the memory usage of LWIP, except for preventing
new TCP connections after the limit is reached.

---

## CONFIG_LWIP_MAX_LISTENING_TCP

**Maximum listening TCP Connections**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:584`

The maximum number of simultaneously listening TCP
connections. The practical maximum limit is
determined by available heap memory at runtime.

Changing this value by itself does not substantially
change the memory usage of LWIP, except for preventing
new listening TCP connections after the limit is reached.

---

## CONFIG_LWIP_MAX_RAW_PCBS

**Maximum LWIP RAW PCBs**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > LWIP > LWIP RAW API
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1100`

The maximum number of simultaneously active LWIP
RAW protocol control blocks. The practical maximum
limit is determined by available heap memory at runtime.

---

## CONFIG_LWIP_MAX_SOCKETS

**Max number of open sockets**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:128`

The practical maximum limit is
determined by available heap memory at runtime.

Sockets take up a certain amount of memory, and allowing fewer
sockets to be open at the same time conserves memory. Specify
the maximum amount of sockets here. The valid value is from 1
to 253. If using value above 61, update CMakeLists defining
FD_SETSIZE to the number of sockets used plus the
expected open files (minimum of +3 for stdout, stderr and stdin).

---

## CONFIG_LWIP_MAX_UDP_PCBS

**Maximum active UDP control blocks**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > LWIP > UDP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:821`

The maximum number of active UDP "connections" (ie
UDP sockets sending/receiving data).
The practical maximum limit is determined by available
heap memory at runtime.

---

## CONFIG_LWIP_MLDV6_TMR_INTERVAL

**mldv6 report timer interval(seconds)**

- **Type:** `unknown`
- **Default:** `40`
- **Menu:** Component config > LWIP > Send mldv6 report periodically
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:297`

Set the timer interval for mldv6 report. The default value is 30s

---

## CONFIG_LWIP_MULTICAST_PING

**Respond to multicast pings**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > ICMP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1086`

---

## CONFIG_LWIP_NAPT_DEBUG

**Enable NAPT debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1506`

---

## CONFIG_LWIP_ND6

**LWIP NDP6 Enable/Disable**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:111`

This option is used to disable the Network Discovery Protocol (NDP) if it is not required.
Please use this option with caution, as the NDP is essential for IPv6 functionality within a local network.

---

## CONFIG_LWIP_NETBUF_RECVINFO

**Enable IP_PKTINFO option**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:191`

Enabling this option allows checking for the destination address
of a received IPv4 Packet.

---

## CONFIG_LWIP_NETIF_DEBUG

**Enable netif debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1431`

---

## CONFIG_LWIP_NETIF_LOOPBACK

**Support per-interface loopback**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:550`

Enabling this option means that if a packet is sent with a destination
address equal to the interface's own IP address, it will "loop back" and
be received by this interface.
Disabling this option disables support of loopback interface in lwIP

---

## CONFIG_LWIP_NETIF_STATUS_CALLBACK

**Enable status callback for network interfaces**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:544`

Enable callbacks when the network interface is up/down and addresses are changed.

---

## CONFIG_LWIP_NUM_NETIF_CLIENT_DATA

**Number of clients store data in netif**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:375`

Number of clients that may store data in client_data member array of struct netif.

---

## CONFIG_LWIP_PBUF_DEBUG

**Enable pbuf debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1436`

---

## CONFIG_LWIP_PPP_CHAP_SUPPORT

**Enable CHAP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:984`

Enable Challenge Handshake Authentication Protocol (CHAP) support

---

## CONFIG_LWIP_PPP_DEBUG_ON

**Enable PPP debug log output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1045`

Enable PPP debug log output

---

## CONFIG_LWIP_PPP_ENABLE_IPV4

**Enable IPV4 support for PPP connections (IPCP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:950`

Enable IPCP protocol in PPP negotiations, which assigns IPv4 addresses to the PPP client,
as well as IPv4 DNS servers.
You can disable this if your modem supports IPv6 only.

---

## CONFIG_LWIP_PPP_ENABLE_IPV6

**Enable IPV6 support for PPP connections (IPV6CP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:959`

Enable IPV6 support in PPP for the local link between the DTE (processor) and DCE (modem).
There are some modems which do not support the IPV6 addressing in the local link.
If they are requested for IPV6CP negotiation, they may time out.
This would in turn fail the configuration for the whole link.
If your modem is not responding correctly to PPP Phase Network, try to disable IPV6 support.

---

## CONFIG_LWIP_PPP_MPPE_SUPPORT

**Enable MPPE support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:998`

Enable Microsoft Point-to-Point Encryption (MPPE) support

---

## CONFIG_LWIP_PPP_MSCHAP_SUPPORT

**Enable MSCHAP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:991`

Enable Microsoft version of the Challenge-Handshake Authentication Protocol (MSCHAP) support

---

## CONFIG_LWIP_PPP_NOTIFY_PHASE_SUPPORT

**Enable Notify Phase Callback**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:970`

Enable to set a callback which is called on change of the internal PPP state machine.

---

## CONFIG_LWIP_PPP_PAP_SUPPORT

**Enable PAP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:977`

Enable Password Authentication Protocol (PAP) support

---

## CONFIG_LWIP_PPP_SERVER_SUPPORT

**Enable PPP server support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1005`

Enable to use PPP server

---

## CONFIG_LWIP_PPP_SUPPORT

**Enable PPP support**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:944`

Enable PPP stack. Now only PPP over serial is possible.

---

## CONFIG_LWIP_PPP_VJ_HEADER_COMPRESSION

**Enable VJ IP Header compression**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1012`

Enable support for VJ header compression.
Please disable this if you're using NAPT on PPP interface,
since the compressed IP header might not be correctly interpreted
in NAT causing the compressed packet to be dropped.

---

## CONFIG_LWIP_SLIP_DEBUG_ON

**Enable SLIP debug log output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable SLIP support (new/experimental)
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1071`

Enable SLIP debug log output

---

## CONFIG_LWIP_SLIP_SUPPORT

**Enable SLIP support (new/experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1063`

Enable SLIP stack. Now only SLIP over serial is possible.

SLIP over serial support is experimental and unsupported.

---

## CONFIG_LWIP_SNTP_DEBUG

**Enable SNTP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1496`

---

## CONFIG_LWIP_SNTP_MAXIMUM_STARTUP_DELAY

**Maximum startup delay (ms)**

- **Type:** `unknown`
- **Default:** `5000`
- **Menu:** Component config > LWIP > SNTP > Enable SNTP startup delay
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1159`

RFC 4330 recommends a startup delay before sending the initial request.
LWIP calculates this delay to a random number of milliseconds between 0 and this value.

---

## CONFIG_LWIP_SNTP_MAX_SERVERS

**Maximum number of NTP servers**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > LWIP > SNTP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1113`

Set maximum number of NTP servers used by LwIP SNTP module.
First argument of sntp_setserver/sntp_setservername functions
is limited to this value.

---

## CONFIG_LWIP_SNTP_STARTUP_DELAY

**Enable SNTP startup delay**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > SNTP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1148`

It is recommended (RFC 4330) to delay the initial request after by a random timeout from 1 to 5 minutes
to reduce potential load of NTP servers after simultaneous power-up of many devices.
This option disables this initial delay. Please use this option with care, it could improve
a single device responsiveness but might cause peaks on the network after reset.
Another option to address responsiveness of devices while using the initial random delay
is to adjust LWIP_SNTP_MAXIMUM_STARTUP_DELAY.

---

## CONFIG_LWIP_SNTP_UPDATE_DELAY

**Request interval to update time (ms)**

- **Type:** `unknown`
- **Default:** `3600000`
- **Menu:** Component config > LWIP > SNTP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1139`

This option allows you to set the time update period via SNTP.
Default is 1 hour. Must not be below 15 seconds by specification.
(SNTPv4 RFC 4330 enforces a minimum update time of 15 seconds).

---

## CONFIG_LWIP_SOCKETS_DEBUG

**Enable socket debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1451`

---

## CONFIG_LWIP_SO_LINGER

**Enable SO_LINGER processing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:150`

Enabling this option allows SO_LINGER processing.
l_onoff = 1,l_linger can set the timeout.

If l_linger=0, When a connection is closed, TCP will terminate the connection.
This means that TCP will discard any data packets stored in the socket send buffer
and send an RST to the peer.

If l_linger!=0,Then closesocket() calls to block the process until
the remaining data packets has been sent or timed out.

---

## CONFIG_LWIP_SO_RCVBUF

**Enable SO_RCVBUF option**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:185`

Enabling this option allows checking for available data on a netconn.

---

## CONFIG_LWIP_SO_REUSE

**Enable SO_REUSEADDR option**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:164`

Enabling this option allows binding to a port which remains in
TIME_WAIT.

---

## CONFIG_LWIP_SO_REUSE_RXTOALL

**SO_REUSEADDR copies broadcast/multicast to all matches**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > Enable SO_REUSEADDR option
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:171`

Enabling this option means that any incoming broadcast or multicast
packet will be copied to all of the local sockets that it matches
(may be more than one if SO_REUSEADDR is set on the socket.)

This increases memory overhead as the packets need to be copied,
however they are only copied per matching socket. You can safely
disable it if you don't plan to receive broadcast or multicast
traffic on more than one socket at a time.

---

## CONFIG_LWIP_STATS

**Enable LWIP statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:262`

Enabling this option allows LWIP statistics

---

## CONFIG_LWIP_TCPIP_CORE_LOCKING

**Enable tcpip core locking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:29`

If Enable tcpip core locking,Creates a global mutex that is held
during TCPIP thread operations.Can be locked by client code to perform
lwIP operations without changing into TCPIP thread using callbacks.
See LOCK_TCPIP_CORE() and UNLOCK_TCPIP_CORE().

If disable tcpip core locking,TCP IP will perform tasks through context switching

---

## CONFIG_LWIP_TCPIP_CORE_LOCKING_INPUT

**Enable tcpip core locking input**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable tcpip core locking
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:40`

when LWIP_TCPIP_CORE_LOCKING is enabled, this lets tcpip_input() grab the
mutex for input packets as well, instead of allocating a message and passing
it to tcpip_thread.

---

## CONFIG_LWIP_TCPIP_RECVMBOX_SIZE

**TCPIP task receive mail box size**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:304`

Set TCPIP task receive mail box size. Generally bigger value means higher throughput
but more memory. The value should be bigger than UDP/TCP mail box size.

---

## CONFIG_LWIP_TCPIP_TASK_AFFINITY

- **Type:** `unknown`
- **Default:** `0x7FFFFFFF`

---

## CONFIG_LWIP_TCPIP_TASK_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP/IP task affinity
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:893`

---

## CONFIG_LWIP_TCPIP_TASK_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP/IP task affinity
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:895`

---

## CONFIG_LWIP_TCPIP_TASK_AFFINITY_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > TCP/IP task affinity
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:891`

---

## CONFIG_LWIP_TCPIP_TASK_PRIO

**LWIP TCP/IP Task Priority**

- **Type:** `unknown`
- **Default:** `18`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:21`

LWIP tcpip task priority. In case of high throughput, this parameter
could be changed up to (configMAX_PRIORITIES-1).

---

## CONFIG_LWIP_TCPIP_TASK_STACK_SIZE

**TCP/IP Task Stack Size**

- **Type:** `unknown`
- **Default:** `3072`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:872`

Configure TCP/IP task stack size, used by LWIP to process multi-threaded TCP/IP operations.
Setting this stack too small will result in stack overflow crashes.

---

## CONFIG_LWIP_TCP_ACCEPTMBOX_SIZE

**Default TCP accept mail box size**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:704`

Set TCP accept mail box size. Generally bigger value means supporting larger backlogs
but more memory. The recommended value is 6, but applications can set it to a lower value
if listening servers are meant to have a smaller backlog.

TCP accept mail box is a per socket mail box, when the application listens for connections
with a given listening TCP socket. If the mailbox is full, LWIP will send a RST packet and
the client will fail to connect.

---

## CONFIG_LWIP_TCP_DEBUG

**Enable TCP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1486`

---

## CONFIG_LWIP_TCP_FIN_WAIT_TIMEOUT

**Maximum FIN segment lifetime**

- **Type:** `unknown`
- **Default:** `20000`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:646`

Set maximum segment lifetime in milliseconds.

---

## CONFIG_LWIP_TCP_HIGH_SPEED_RETRANSMISSION

**TCP high speed retransmissions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:597`

Speed up the TCP retransmission interval. If disabled,
it is recommended to change the number of SYN retransmissions to 6,
and TCP initial rto time to 3000.

---

## CONFIG_LWIP_TCP_MAXRTX

**Maximum number of retransmissions of data segments**

- **Type:** `unknown`
- **Default:** `12`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:605`

Set maximum number of retransmissions of data segments.

---

## CONFIG_LWIP_TCP_MSL

**Maximum segment lifetime (MSL)**

- **Type:** `unknown`
- **Default:** `60000`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:640`

Set maximum segment lifetime in milliseconds.

---

## CONFIG_LWIP_TCP_MSS

**Maximum Segment Size (MSS)**

- **Type:** `unknown`
- **Default:** `1440`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:620`

Set maximum segment size for TCP transmission.

Can be set lower to save RAM, the default value 1460(ipv4)/1440(ipv6) will give best throughput.
IPv4 TCP_MSS Range: 576 <= TCP_MSS <= 1460
IPv6 TCP_MSS Range: 1220<= TCP_MSS <= 1440

---

## CONFIG_LWIP_TCP_OOSEQ_MAX_PBUFS

**The maximum number of pbufs queued on OOSEQ per pcb**

- **Type:** `unknown`
- **Default:** `4`
- **Menu:** Component config > LWIP > TCP > Queue incoming out-of-order segments
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:735`

If LWIP_TCP_OOSEQ_MAX_PBUFS = 0, TCP will not control the number of OOSEQ pbufs.

In a poor network environment, many out-of-order tcp pbufs will be received.
These out-of-order pbufs will be cached in the TCP out-of-order queue which will
cause Wi-Fi/Ethernet fail to release RX buffer in time.
It is possible that all RX buffers for MAC layer are used by OOSEQ.

Control the number of out-of-order pbufs to ensure
that the MAC layer has enough RX buffer to receive packets.

In the Wi-Fi scenario, recommended OOSEQ PBUFS Range:
0 <= TCP_OOSEQ_MAX_PBUFS <= CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM/(MAX_TCP_NUMBER + 1)

In the Ethernet scenario,recommended Ethernet OOSEQ PBUFS Range:
0 <= TCP_OOSEQ_MAX_PBUFS <= CONFIG_ETH_DMA_RX_BUFFER_NUM/(MAX_TCP_NUMBER + 1)

Within the recommended value range, the larger the value, the better the performance.

MAX_TCP_NUMBER represent Maximum number of TCP connections in Wi-Fi(STA+SoftAP) and Ethernet scenario.

---

## CONFIG_LWIP_TCP_OOSEQ_TIMEOUT

**Timeout for each pbuf queued in TCP OOSEQ, in RTOs.**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > LWIP > TCP > Queue incoming out-of-order segments
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:727`

The timeout value is TCP_OOSEQ_TIMEOUT * RTO.

---

## CONFIG_LWIP_TCP_OVERSIZE_DISABLE

**Disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP > Pre-allocate transmit PBUF size
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:789`

---

## CONFIG_LWIP_TCP_OVERSIZE_MSS

**MSS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > TCP > Pre-allocate transmit PBUF size
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:785`

---

## CONFIG_LWIP_TCP_OVERSIZE_QUARTER_MSS

**25% MSS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP > Pre-allocate transmit PBUF size
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:787`

---

## CONFIG_LWIP_TCP_QUEUE_OOSEQ

**Queue incoming out-of-order segments**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:718`

Queue incoming out-of-order segments for later use.

Disable this option to save some RAM during TCP sessions, at the expense
of increased retransmissions if segments arrive out of order.

---

## CONFIG_LWIP_TCP_RCV_SCALE

**Set TCP receiving window scaling factor**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > LWIP > TCP > Support TCP window scale
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:801`

Enable this feature to support TCP window scaling.

---

## CONFIG_LWIP_TCP_RECVMBOX_SIZE

**Default TCP receive mail box size**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:683`

Set TCP receive mail box size. Generally bigger value means higher throughput
but more memory. The recommended value is: LWIP_TCP_WND_DEFAULT/TCP_MSS + 2, e.g. if
LWIP_TCP_WND_DEFAULT=14360, TCP_MSS=1436, then the recommended receive mail box size is
(14360/1436 + 2) = 12.

TCP receive mail box is a per socket mail box, when the application receives packets
from TCP socket, LWIP core firstly posts the packets to TCP receive mail box and the
application then fetches the packets from mail box. It means LWIP can caches maximum
LWIP_TCP_RECCVMBOX_SIZE packets for each TCP socket, so the maximum possible cached TCP packets
for all TCP sockets is LWIP_TCP_RECCVMBOX_SIZE multiples the maximum TCP socket number. In other
words, the bigger LWIP_TCP_RECVMBOX_SIZE means more memory.
On the other hand, if the receive mail box is too small, the mail box may be full. If the
mail box is full, the LWIP drops the packets. So generally we need to make sure the TCP
receive mail box is big enough to avoid packet drop between LWIP core and application.

---

## CONFIG_LWIP_TCP_RTO_TIME

**Default TCP rto time**

- **Type:** `unknown`
- **Default:** `1500`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:809`

Set default TCP rto time for a reasonable initial rto.
In bad network environment, recommend set value of rto time to 1500.

---

## CONFIG_LWIP_TCP_SACK_OUT

**Support sending selective acknowledgements**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP > Queue incoming out-of-order segments
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:762`

TCP will support sending selective acknowledgements (SACKs).

---

## CONFIG_LWIP_TCP_SND_BUF_DEFAULT

**Default send buffer size**

- **Type:** `unknown`
- **Default:** `5760`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:652`

Set default send buffer size for new TCP sockets.

Per-socket send buffer size can be changed at runtime
with lwip_setsockopt(s, TCP_SNDBUF, ...).

This value must be at least 2x the MSS size, and the default
is 4x the default MSS size.

Setting a smaller default SNDBUF size can save some RAM, but
will decrease performance.

---

## CONFIG_LWIP_TCP_SYNMAXRTX

**Maximum number of retransmissions of SYN segments**

- **Type:** `unknown`
- **Default:** `12`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:612`

Set maximum number of retransmissions of SYN segments.

---

## CONFIG_LWIP_TCP_TMR_INTERVAL

**TCP timer interval(ms)**

- **Type:** `unknown`
- **Default:** `250`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:631`

Set TCP timer interval in milliseconds.

Can be used to speed connections on bad networks.
A lower value will redeliver unacked packets faster.

---

## CONFIG_LWIP_TCP_WND_DEFAULT

**Default receive window size**

- **Type:** `unknown`
- **Default:** `5760`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:669`

Set default TCP receive window size for new TCP sockets.

Per-socket receive window size can be changed at runtime
with lwip_setsockopt(s, TCP_WINDOW, ...).

Setting a smaller default receive window size can save some RAM,
but will significantly decrease performance.

---

## CONFIG_LWIP_TIMERS_ONDEMAND

**Enable LWIP Timers on demand**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:101`

If this feature is enabled, IGMP and MLD6 timers will be activated only
when joining groups or receiving QUERY packets.

This feature will reduce the power consumption for applications which do not
use IGMP and MLD6.

---

## CONFIG_LWIP_UDP_DEBUG

**Enable UDP debug messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable LWIP Debug
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1491`

---

## CONFIG_LWIP_UDP_RECVMBOX_SIZE

**Default UDP receive mail box size**

- **Type:** `unknown`
- **Default:** `6`
- **Menu:** Component config > LWIP > UDP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:831`

Set UDP receive mail box size. The recommended value is 6.

UDP receive mail box is a per socket mail box, when the application receives packets
from UDP socket, LWIP core firstly posts the packets to UDP receive mail box and the
application then fetches the packets from mail box. It means LWIP can caches maximum
UDP_RECCVMBOX_SIZE packets for each UDP socket, so the maximum possible cached UDP packets
for all UDP sockets is UDP_RECCVMBOX_SIZE multiples the maximum UDP socket number. In other
words, the bigger UDP_RECVMBOX_SIZE means more memory.
On the other hand, if the receive mail box is too small, the mail box may be full. If the
mail box is full, the LWIP drops the packets. So generally we need to make sure the UDP
receive mail box is big enough to avoid packet drop between LWIP core and application.

---

## CONFIG_LWIP_USE_ESP_GETADDRINFO

**Enable esp_getaddrinfo() instead of lwip_getaddrinfo()**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > DNS
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1210`

Use esp_getaddrinfo() for DNS lookups instead of lwip_getaddrinfo().
This function correctly handles the AF_UNSPEC flag for resolving
both IPv4 and IPv6 addresses. Available only when both IPv4 and IPv6
are enabled.

---

## CONFIG_LWIP_USE_EXTERNAL_MBEDTLS

**Use mbedTLS instead of internal polarSSL**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > Enable PPP support
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:1052`

This option uses mbedTLS crypto functions (instead of internal PolarSSL
implementation) for PPP authentication modes (PAP, CHAP, etc.).
You can use this option to address symbol duplication issues, since
the internal functions are not namespaced (e.g. md5_init()).

---

## CONFIG_LWIP_USE_ONLY_LWIP_SELECT

**Support LWIP socket select() only (DEPRECATED)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:144`

This option is deprecated. Do not use this option, use VFS_SUPPORT_SELECT instead.

---

## CONFIG_LWIP_WND_SCALE

**Support TCP window scale**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > LWIP > TCP
- **Kconfig:** `/opt/esp/idf/components/lwip/Kconfig:794`

Enable this feature to support TCP window scaling.

---

## CONFIG_MA120X0_NCLIP_PIN

**Clip indication pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:239`

GPIO number low if clip observed

---

## CONFIG_MA120X0_NENABLE_INVERT

**Invert nEnable pin logic (active-low)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:218`

When enabled, the nEnable pin is driven LOW to enable the
amplifier and HIGH to disable it (active-low, as per the
MA120x0 datasheet). When disabled, HIGH = on, LOW = off.

---

## CONFIG_MA120X0_NENABLE_PIN

**Master enable/disable for ma120x0**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:212`

GPIO number to control enable/disable.

---

## CONFIG_MA120X0_NERR_PIN

**NERR monitor pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:233`

GPIO number to monitor NERROR.

---

## CONFIG_MA120X0_NMUTE_PIN

**Master mute/unmute for ma120x0**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:227`

GPIO number to controm mute/unmute.

---

## CONFIG_MA120X0_VOL_MAX_DB

**Maximum volume level (dB, ≤ 0)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:245`

Register value at slider position 100. 0 dB is the
maximum undistorted output level of the MA120x0.

---

## CONFIG_MA120X0_VOL_MIN_DB

**Minimum volume level (dB)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > Merus MA120x0 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:253`

Register value at slider position 0. Values below -60 dB
are practically inaudible on most systems.

---

## CONFIG_MA120_ENABLE_PIN

**Master enable/disable for ma120x0**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Audio Board > Custom Audio Board > Merus MA120 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:265`

GPIO number to control enable/disable.

---

## CONFIG_MA120_NMUTE_PIN

**Master mute/unmute for ma120x0**

- **Type:** `unknown`
- **Default:** `2`
- **Menu:** Audio Board > Custom Audio Board > Merus MA120 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:271`

GPIO number to controm mute/unmute.

---

## CONFIG_MASTER_I2S_BCK_PIN

**Master i2s bck**

- **Type:** `unknown`
- **Default:** `23`
- **Menu:** Audio Board > Custom Audio Board > I2S master interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:155`

Master audio interface bit clock.

---

## CONFIG_MASTER_I2S_DATAOUT_PIN

**Master i2s data out**

- **Type:** `unknown`
- **Default:** `14`
- **Menu:** Audio Board > Custom Audio Board > I2S master interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:174`

Master audio interface data out.

---

## CONFIG_MASTER_I2S_LRCK_PIN

**Master i2s lrck**

- **Type:** `unknown`
- **Default:** `2`
- **Menu:** Audio Board > Custom Audio Board > I2S master interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:165`

Master audio interface left/right sync clock.

---

## CONFIG_MASTER_I2S_MCLK_PIN

**Master i2s mclk**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Audio Board > Custom Audio Board > I2S master interface
- **Kconfig:** `components/audio_board/Kconfig.projbuild:148`

Master audio interface master clock.

---

## CONFIG_MAX98357_MUTE_PIN

**Master mute/mode for MAX98357**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > MAX98357 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:317`

GPIO number to control mute/mode. This require a special resistor to select the correct mode.
You need to put a serie resistor to select the mode (GPIO --> Resistor --> SD):
    LEFT only :  0ohm (direct connection)
    RIGHT only : 220Kohm
    (L+R)/2 :    1Mohm
Refer to Analog Devices' doc for more infos.
This is labeled "SD" on chip/boards.

---

## CONFIG_MBEDTLS_AES_C

**AES block cipher**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:931`

---

## CONFIG_MBEDTLS_AES_INTERRUPT_LEVEL

**AES hardware interrupt level**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > mbedTLS > Enable hardware AES acceleration > Use interrupt for long AES operations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:417`

This config helps to set the interrupt priority level for the AES peripheral.
Value 0 (default) means that there is no preference regarding the interrupt
priority level and any level from 1 to 3 can be selected (based on the availability).
Note: Higher value indicates high interrupt priority.

---

## CONFIG_MBEDTLS_AES_USE_INTERRUPT

**Use interrupt for long AES operations**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Enable hardware AES acceleration
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:407`

Use an interrupt to coordinate long AES operations.

This allows other code to run on the CPU while an AES operation is pending.
Otherwise the CPU busy-waits.

---

## CONFIG_MBEDTLS_AES_USE_PSEUDO_ROUND_FUNC

**Enable AES hardware's pseudo round function**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:428`

Enables the pseudo round function of the AES peripheral.
Enabling this would impact the performance of the AES operations.
For more info regarding the performance impact, please checkout the pseudo round function section of the
security guide.

---

## CONFIG_MBEDTLS_AES_USE_PSEUDO_ROUND_FUNC_STRENGTH

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_MBEDTLS_AES_USE_PSEUDO_ROUND_FUNC_STRENGTH_HIGH

**High**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable AES hardware's pseudo round function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:454`

---

## CONFIG_MBEDTLS_AES_USE_PSEUDO_ROUND_FUNC_STRENGTH_LOW

**Low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable AES hardware's pseudo round function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:448`

---

## CONFIG_MBEDTLS_AES_USE_PSEUDO_ROUND_FUNC_STRENGTH_MEDIUM

**Medium**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable AES hardware's pseudo round function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:451`

---

## CONFIG_MBEDTLS_ALLOW_WEAK_CERTIFICATE_VERIFICATION

**Allow weak certificate verification**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1290`

This options allows weak certificate verification by skipping the hostname verification.
It is not recommended to use this option.

---

## CONFIG_MBEDTLS_ASYMMETRIC_CONTENT_LEN

**Asymmetric in/out fragment length**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:74`

If enabled, this option allows customizing TLS in/out fragment length
in asymmetric way. Please note that enabling this with default values
saves 12KB of dynamic memory per TLS connection.

---

## CONFIG_MBEDTLS_ATCA_HW_ECDSA_SIGN

**Enable hardware ECDSA sign acceleration when using ATECC608A**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:641`

This option enables hardware acceleration for ECDSA sign function, only
when using ATECC608A cryptoauth chip.

---

## CONFIG_MBEDTLS_ATCA_HW_ECDSA_VERIFY

**Enable hardware ECDSA verify acceleration when using ATECC608A**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:648`

This option enables hardware acceleration for ECDSA sign function, only
when using ATECC608A cryptoauth chip.

---

## CONFIG_MBEDTLS_BLOWFISH_C

**Blowfish block cipher (read help)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:948`

Enables the Blowfish block cipher (not used for TLS sessions.)

The Blowfish cipher is not used for mbedTLS TLS sessions but can be
used for other purposes. Read up on the limitations of Blowfish (including
Sweet32) before enabling.

---

## CONFIG_MBEDTLS_CAMELLIA_C

**Camellia block cipher**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:935`

---

## CONFIG_MBEDTLS_CCM_C

**CCM (Counter with CBC-MAC) block cipher modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:965`

Enable Counter with CBC-MAC (CCM) modes for AES and/or Camellia ciphers.

Disabling this option saves some code size.

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE

**Enable trusted root certificate bundle**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificate Bundle
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:321`

Enable support for large number of default root certificates

When enabled this option allows user to store default as well
as customer specific root certificates in compressed format rather
than storing full certificate. For the root certificates the public key and the subject name
will be stored.

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_CMN

**Use only the most common certificates from the default bundles**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle > Default certificate bundle options
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:339`

Use only the most common certificates from the default bundles, reducing the size with 50%,
while still having around 99% coverage.

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_FULL

**Use the full default certificate bundle**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle > Default certificate bundle options
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:337`

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_NONE

**Do not use the default certificate bundle**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle > Default certificate bundle options
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:344`

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEPRECATED_LIST

**Add deprecated root certificates**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:359`

Include the deprecated list of root certificates in the bundle.
This list gets updated when a certificate is removed from the Mozilla's
NSS root certificate store. This config can be enabled if you would like
to ensure that none of the certificates that were deployed in the product
are affected because of the update to bundle. In turn, enabling this
config keeps expired, retracted certificates in the bundle and it may
pose a security risk.

- Deprecated cert list may grow based based on sync with upstream bundle
- Deprecated certs would be be removed in ESP-IDF (next) major release

---

## CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_MAX_CERTS

**Maximum no of certificates allowed in certificate bundle**

- **Type:** `unknown`
- **Default:** `200`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:374`

---

## CONFIG_MBEDTLS_CHACHA20_C

**Chacha20 stream cipher**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1195`

Enable support for Chacha20 stream cipher.

---

## CONFIG_MBEDTLS_CHACHAPOLY_C

**ChaCha20-Poly1305 AEAD algorithm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Chacha20 stream cipher
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1201`

Enable support for ChaCha20-Poly1305 AEAD algorithm.

---

## CONFIG_MBEDTLS_CLIENT_SSL_SESSION_TICKETS

**TLS: Client Support for RFC 5077 SSL session tickets**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:913`

Client support for RFC 5077 session tickets. See mbedTLS documentation for more details.
Disabling this option will save some code size.

---

## CONFIG_MBEDTLS_CMAC_C

**Enable CMAC mode for block ciphers**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:389`

Enable the CMAC (Cipher-based Message Authentication Code) mode for
block ciphers.

---

## CONFIG_MBEDTLS_CUSTOM_CERTIFICATE_BUNDLE

**Add custom certificates to the default bundle**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:348`

---

## CONFIG_MBEDTLS_CUSTOM_CERTIFICATE_BUNDLE_PATH

**Custom certificate bundle path**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mbedTLS > Certificate Bundle > Enable trusted root certificate bundle > Add custom certificates to the default bundle
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:352`

Name of the custom certificate directory or file. This path is evaluated
relative to the project root directory.

---

## CONFIG_MBEDTLS_CUSTOM_MEM_ALLOC

**Custom alloc mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:38`

---

## CONFIG_MBEDTLS_DEBUG

**Enable mbedTLS debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:139`

Enable mbedTLS debugging functions at compile time.

If this option is enabled, you can include
"mbedtls/esp_debug.h" and call mbedtls_esp_enable_debug_log()
at runtime in order to enable mbedTLS debug output via the ESP
log mechanism.

---

## CONFIG_MBEDTLS_DEBUG_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_MBEDTLS_DEBUG_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedTLS debugging > Set mbedTLS debugging level
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:161`

---

## CONFIG_MBEDTLS_DEBUG_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedTLS debugging > Set mbedTLS debugging level
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:159`

---

## CONFIG_MBEDTLS_DEBUG_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedTLS debugging > Set mbedTLS debugging level
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:163`

---

## CONFIG_MBEDTLS_DEBUG_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedTLS debugging > Set mbedTLS debugging level
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:157`

---

## CONFIG_MBEDTLS_DEFAULT_MEM_ALLOC

**Default alloc mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:35`

---

## CONFIG_MBEDTLS_DES_C

**DES block cipher (legacy, insecure)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:939`

Enables the DES block cipher to support 3DES-based TLS ciphersuites.

3DES is vulnerable to the Sweet32 attack and should only be enabled
if absolutely necessary.

---

## CONFIG_MBEDTLS_DHM_C

**Diffie-Hellman-Merkle key exchange (DHM)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1053`

Enable DHM. Needed to use DHE-xxx TLS ciphersuites.

Note that the security of Diffie-Hellman key exchanges depends on
a suitable prime being used for the exchange. Please see detailed
warning text about this in file `mbedtls/dhm.h` file.

---

## CONFIG_MBEDTLS_DYNAMIC_BUFFER

**Using dynamic TX/RX buffer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:100`

Using dynamic TX/RX buffer. After enabling this option, mbedTLS will
allocate TX buffer when need to send data and then free it if all data
is sent, allocate RX buffer when need to receive data and then free it
when all data is used or read by upper layer.

By default, when SSL is initialized, mbedTLS also allocate TX and
RX buffer with the default value of "MBEDTLS_SSL_OUT_CONTENT_LEN" or
"MBEDTLS_SSL_IN_CONTENT_LEN", so to save more heap, users can set
the options to be an appropriate value.

---

## CONFIG_MBEDTLS_DYNAMIC_FREE_CA_CERT

**Free SSL CA certificate after its usage**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Using dynamic TX/RX buffer > Free private key and DHM data after its usage
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:129`

Free CA certificate after its usage in the handshake process.
This option will decrease the heap footprint for the TLS handshake, but may lead to a problem:
If the respective ssl object needs to perform the TLS handshake again,
the CA certificate should once again be registered to the ssl object.

---

## CONFIG_MBEDTLS_DYNAMIC_FREE_CONFIG_DATA

**Free private key and DHM data after its usage**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Using dynamic TX/RX buffer
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:117`

Free private key and DHM data after its usage in handshake process.

The option will decrease heap cost when handshake, but also lead to problem:

Because all certificate, private key and DHM data are freed so users should register
certificate and private key to ssl config object again.

---

## CONFIG_MBEDTLS_ECC_OTHER_CURVES_SOFT_FALLBACK

**Fallback to software implementation for curves not supported in hardware**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable hardware ECC acceleration
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:567`

Fallback to software implementation of ECC point multiplication and point verification
for curves not supported in hardware.

---

## CONFIG_MBEDTLS_ECDH_C

**Elliptic Curve Diffie-Hellman (ECDH)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1063`

Enable ECDH. Needed to use ECDHE-xxx TLS ciphersuites.

---

## CONFIG_MBEDTLS_ECDH_LEGACY_CONTEXT

**Use a backward compatible ECDH context (Experimental)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:210`

Use the legacy ECDH context format.
Define this option only if you enable MBEDTLS_ECP_RESTARTABLE or if you
want to access ECDH context fields directly.

---

## CONFIG_MBEDTLS_ECDSA_C

**Elliptic Curve DSA**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Elliptic Curve Diffie-Hellman (ECDH)
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1070`

Enable ECDSA. Needed to use ECDSA-xxx TLS ciphersuites.

---

## CONFIG_MBEDTLS_ECDSA_DETERMINISTIC

**Enable deterministic ECDSA**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:708`

Standard ECDSA is "fragile" in the sense that lack of entropy when signing
may result in a compromise of the long-term signing key.

---

## CONFIG_MBEDTLS_ECJPAKE_C

**Elliptic curve J-PAKE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1077`

Enable ECJPAKE. Needed to use ECJPAKE-xxx TLS ciphersuites.

---

## CONFIG_MBEDTLS_ECP_C

**Elliptic Curve Ciphers**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1032`

---

## CONFIG_MBEDTLS_ECP_DP_BP256R1_ENABLED

**Enable BP256R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1140`

support for DP Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_BP384R1_ENABLED

**Enable BP384R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1147`

support for DP Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_BP512R1_ENABLED

**Enable BP512R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1154`

support for DP Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_CURVE25519_ENABLED

**Enable CURVE25519 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1161`

Enable support for CURVE25519 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP192K1_ENABLED

**Enable SECP192K1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1119`

Enable support for SECP192K1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP192R1_ENABLED

**Enable SECP192R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1084`

Enable support for SECP192R1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP224K1_ENABLED

**Enable SECP224K1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1126`

Enable support for SECP224K1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP224R1_ENABLED

**Enable SECP224R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1091`

Enable support for SECP224R1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP256K1_ENABLED

**Enable SECP256K1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1133`

Enable support for SECP256K1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP256R1_ENABLED

**Enable SECP256R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1098`

Enable support for SECP256R1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP384R1_ENABLED

**Enable SECP384R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1105`

Enable support for SECP384R1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_DP_SECP521R1_ENABLED

**Enable SECP521R1 curve**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1112`

Enable support for SECP521R1 Elliptic Curve.

---

## CONFIG_MBEDTLS_ECP_FIXED_POINT_OPTIM

**Enable fixed-point multiplication optimisations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1177`

This configuration option enables optimizations to speedup (about 3 ~ 4 times) the ECP
fixed point multiplication using pre-computed tables in the flash memory.
Enabling this configuration option increases the flash footprint
(about 29KB if all Elliptic Curve selected) in the application binary.

# end of Elliptic Curve options

---

## CONFIG_MBEDTLS_ECP_NIST_OPTIM

**NIST 'modulo p' optimisations**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1168`

NIST 'modulo p' optimisations increase Elliptic Curve operation performance.

Disabling this option saves some code size.

---

## CONFIG_MBEDTLS_ECP_RESTARTABLE

**Enable mbedTLS ecp restartable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:381`

Enable "non-blocking" ECC operations that can return early and be resumed.

---

## CONFIG_MBEDTLS_ERROR_STRINGS

**Enable error code to error string conversion**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1236`

Enables mbedtls_strerror() for converting error codes to error strings.
Disabling this config can save some code/rodata size as the error
string conversion implementation is replaced with an empty stub.

---

## CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC

**External SPIRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:31`

---

## CONFIG_MBEDTLS_FS_IO

**Enable functions that use the filesystem**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1280`

This option enables functions in mbedTLS that use the filesystem.
It uses the default filesystem support for the target,
which is added through vfs component for ESP32 based targets or by
the host system when the target is Linux.

---

## CONFIG_MBEDTLS_GCM_C

**GCM (Galois/Counter) block cipher modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:974`

Enable Galois/Counter Mode for AES and/or Camellia ciphers.

This option is generally faster than CCM.

---

## CONFIG_MBEDTLS_GCM_SUPPORT_NON_AES_CIPHER

**Enable support for non-AES ciphers in GCM operation**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:476`

Enable this config to support fallback to software definitions for a non-AES
cipher GCM operation as we support hardware acceleration only for AES cipher.
Some of the non-AES ciphers used in a GCM operation are DES, ARIA, CAMELLIA,
CHACHA20, BLOWFISH.

If this config is disabled, performing a non-AES cipher GCM operation with
the config MBEDTLS_HARDWARE_AES enabled will result in calculation of an
AES-GCM operation instead for the given input values and thus could lead
to failure in certificate validation which would ultimately lead to a SSL
handshake failure.

This config being by-default enabled leads to an increase in binary size
footprint of ~2.5KB.
In case you are sure that your use case (for example, client and server
configurations in case of a TLS handshake) would not involve any GCM
operations using a non-AES cipher, you can safely disable this config,
leading to reduction in binary size footprint.

---

## CONFIG_MBEDTLS_HARDWARE_AES

**Enable hardware AES acceleration**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:397`

Enable hardware accelerated AES encryption & decryption.

Note that if the ESP32 CPU is running at 240MHz, hardware AES does not
offer any speed boost over software AES.

---

## CONFIG_MBEDTLS_HARDWARE_ECC

**Enable hardware ECC acceleration**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:559`

Enable hardware accelerated ECC point multiplication and point verification for points
on curve SECP192R1 and SECP256R1 in mbedTLS

---

## CONFIG_MBEDTLS_HARDWARE_ECDSA_SIGN

**Enable ECDSA signing using on-chip ECDSA peripheral**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:581`

Enable hardware accelerated ECDSA peripheral to sign data
on curve SECP192R1 and SECP256R1 in mbedTLS.

Note that for signing, the private key has to be burnt in an efuse key block
with key purpose set to ECDSA_KEY.
If no key is burnt, it will report an error

The key should be burnt in little endian format. espefuse.py utility handles it internally
but care needs to be taken while burning using esp_efuse APIs

---

## CONFIG_MBEDTLS_HARDWARE_ECDSA_SIGN_CONSTANT_TIME_CM

**Make ECDSA signature operation pseudo constant time for software**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable Software Countermeasure for ECDSA signing using on-chip ECDSA peripheral
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:618`

This option adds a delay after the actual ECDSA signature operation
so that the entire operation appears to be constant time for the software.
This fix helps in protecting the device only in case of remote timing attack on the ECDSA private key.
For e.g., When an interface is exposed by the device to perform ECDSA signature
of an arbitrary message.
The signature time would appear to be constant to the external entity after enabling
this option.
This countermeasure can be safely disabled for ESP32-H2 v1.2 and above.

---

## CONFIG_MBEDTLS_HARDWARE_ECDSA_SIGN_MASKING_CM

**Mask original ECDSA sign operation under dummy sign operations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable Software Countermeasure for ECDSA signing using on-chip ECDSA peripheral
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:604`

The ECDSA peripheral before ESP32-H2 v1.2 does not offer constant time ECDSA sign operation.
This time can be observed through power profiling of the device,
making the ECDSA private key vulnerable to side-channel timing attacks.
This countermeasure masks the real ECDSA sign operation
under dummy sign operations to add randomness in the generated power signature.
It is highly recommended to also enable Secure Boot for the device in addition to this countermeasure
so that only trusted software can execute on the device.
This countermeasure can be safely disabled for ESP32-H2 v1.2 and above.

---

## CONFIG_MBEDTLS_HARDWARE_ECDSA_VERIFY

**Enable ECDSA signature verification using on-chip ECDSA peripheral**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:633`

Enable hardware accelerated ECDSA peripheral to verify signature
on curve SECP192R1 and SECP256R1 in mbedTLS.

---

## CONFIG_MBEDTLS_HARDWARE_GCM

**Enable partially hardware accelerated GCM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:464`

Enable partially hardware accelerated GCM. GHASH calculation is still done
in software.

If MBEDTLS_HARDWARE_GCM is disabled and MBEDTLS_HARDWARE_AES is enabled then
mbedTLS will still use the hardware accelerated AES block operation, but
on a single block at a time.

---

## CONFIG_MBEDTLS_HARDWARE_MPI

**Enable hardware MPI (bignum) acceleration**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:499`

Enable hardware accelerated multiple precision integer operations.

Hardware accelerated multiplication, modulo multiplication,
and modular exponentiation for up to SOC_RSA_MAX_BIT_LEN bit results.

These operations are used by RSA.

---

## CONFIG_MBEDTLS_HARDWARE_SHA

**Enable hardware SHA acceleration**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:543`

Enable hardware accelerated SHA1, SHA256, SHA384 & SHA512 in mbedTLS.

Due to a hardware limitation, on the ESP32 hardware acceleration is only
guaranteed if SHA digests are calculated one at a time. If more
than one SHA digest is calculated at the same time, one will
be calculated fully in hardware and the rest will be calculated
(at least partially calculated) in software. This happens automatically.

SHA hardware acceleration is faster than software in some situations but
slower in others. You should benchmark to find the best setting for you.

---

## CONFIG_MBEDTLS_HAVE_TIME

**Enable mbedtls time support**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:655`

Enable use of time.h functions (time() and gmtime()) by mbedTLS.

This option doesn't require the system time to be correct, but enables
functionality that requires relative timekeeping - for example periodic
expiry of TLS session tickets or session cache entries.

Disabling this option will save some firmware size, particularly if
the rest of the firmware doesn't call any standard timekeeping
functions.

---

## CONFIG_MBEDTLS_HAVE_TIME_DATE

**Enable mbedtls certificate expiry check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedtls time support
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:679`

Enables X.509 certificate expiry checks in mbedTLS.

If this option is disabled (default) then X.509 certificate
"valid from" and "valid to" timestamp fields are ignored.

If this option is enabled, these fields are compared with the
current system date and time. The time is retrieved using the
standard time() and gmtime() functions. If the certificate is not
valid for the current system time then verification will fail with
code MBEDTLS_X509_BADCERT_FUTURE or MBEDTLS_X509_BADCERT_EXPIRED.

Enabling this option requires adding functionality in the firmware
to set the system clock to a valid timestamp before using TLS. The
recommended way to do this is via ESP-IDF's SNTP functionality, but
any method can be used.

In the case where only a small number of certificates are trusted by
the device, please carefully consider the tradeoffs of enabling this
option. There may be undesired consequences, for example if all
trusted certificates expire while the device is offline and a TLS
connection is required to update. Or if an issue with the SNTP
server means that the system time is invalid for an extended period
after a reset.

---

## CONFIG_MBEDTLS_HKDF_C

**HKDF algorithm (RFC 5869)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1208`

Enable support for the Hashed Message Authentication Code
(HMAC)-based key derivation function (HKDF).

---

## CONFIG_MBEDTLS_INTERNAL_MEM_ALLOC

**Internal memory**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:28`

---

## CONFIG_MBEDTLS_IRAM_8BIT_MEM_ALLOC

**Internal IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Memory allocation strategy
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:41`

Allows to use IRAM memory region as 8bit accessible region.

TLS input and output buffers will be allocated in IRAM section which is 32bit aligned
memory. Every unaligned (8bit or 16bit) access will result in an exception
and incur penalty of certain clock cycles per unaligned read/write.

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_DHE_PSK

**Enable DHE-PSK based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Enable pre-shared-key ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:795`

Enable to support Diffie-Hellman PSK (pre-shared-key) TLS authentication modes.

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_DHE_RSA

**Enable DHE-RSA based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:822`

Enable to support ciphersuites with prefix TLS-DHE-RSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECDHE_ECDSA

**Enable ECDHE-ECDSA based ciphersuite modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Support Elliptic Curve based ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:847`

Enable to support ciphersuites with prefix TLS-ECDHE-ECDSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECDHE_PSK

**Enable ECDHE-PSK based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Enable pre-shared-key ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:802`

Enable to support Elliptic-Curve-Diffie-Hellman PSK (pre-shared-key) TLS authentication modes.

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECDHE_RSA

**Enable ECDHE-RSA based ciphersuite modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Support Elliptic Curve based ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:840`

Enable to support ciphersuites with prefix TLS-ECDHE-RSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECDH_ECDSA

**Enable ECDH-ECDSA based ciphersuite modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Support Elliptic Curve based ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:854`

Enable to support ciphersuites with prefix TLS-ECDH-ECDSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECDH_RSA

**Enable ECDH-RSA based ciphersuite modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Support Elliptic Curve based ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:861`

Enable to support ciphersuites with prefix TLS-ECDH-RSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ECJPAKE

**Enable ECJPAKE based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:868`

Enable to support ciphersuites with prefix TLS-ECJPAKE-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_ELLIPTIC_CURVE

**Support Elliptic Curve based ciphersuites**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:829`

Enable to show Elliptic Curve based ciphersuite mode options.

Disabling all Elliptic Curve ciphersuites saves code size and
can give slightly faster TLS handshakes, provided the server supports
RSA-only ciphersuite modes.

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_PSK

**Enable PSK based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Enable pre-shared-key ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:788`

Enable to support symmetric key PSK (pre-shared-key) TLS key exchange modes.

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_RSA

**Enable RSA-only based ciphersuite modes**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:816`

Enable to support ciphersuites with prefix TLS-RSA-WITH-

---

## CONFIG_MBEDTLS_KEY_EXCHANGE_RSA_PSK

**Enable RSA-PSK based ciphersuite modes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods > Enable pre-shared-key ciphersuites
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:809`

Enable to support RSA PSK (pre-shared-key) TLS authentication modes.

---

## CONFIG_MBEDTLS_LARGE_KEY_SOFTWARE_MPI

**Fallback to software implementation for larger MPI values**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable hardware MPI (bignum) acceleration
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:511`

Fallback to software implementation for RSA key lengths
larger than SOC_RSA_MAX_BIT_LEN. If this is not active
then the ESP will be unable to process keys greater
than SOC_RSA_MAX_BIT_LEN.

---

## CONFIG_MBEDTLS_MPI_INTERRUPT_LEVEL

**MPI hardware interrupt level**

- **Type:** `unknown`
- **Default:** `0`
- **Menu:** Component config > mbedTLS > Enable hardware MPI (bignum) acceleration > Use interrupt for MPI exp-mod operations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:532`

This config helps to set the interrupt priority level for the MPI peripheral.
Value 0 (default) means that there is no preference regarding the interrupt
priority level and any level from 1 to 3 can be selected (based on the availability).
Note: Higher value indicates high interrupt priority.

---

## CONFIG_MBEDTLS_MPI_USE_INTERRUPT

**Use interrupt for MPI exp-mod operations**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Enable hardware MPI (bignum) acceleration
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:522`

Use an interrupt to coordinate long MPI operations.

This allows other code to run on the CPU while an MPI operation is pending.
Otherwise the CPU busy-waits.

---

## CONFIG_MBEDTLS_NIST_KW_C

**NIST key wrapping (KW) and KW padding (KWP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:983`

Enable NIST key wrapping and key wrapping padding.

---

## CONFIG_MBEDTLS_PEM_PARSE_C

**Read & Parse PEM formatted certificates**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificates
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1000`

Enable decoding/parsing of PEM formatted certificates.

If your certificates are all in the simpler DER format, disabling
this option will save some code size.

---

## CONFIG_MBEDTLS_PEM_WRITE_C

**Write PEM formatted certificates**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificates
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1009`

Enable writing of PEM formatted certificates.

If writing certificate data only in DER format, disabling this
option will save some code size.

---

## CONFIG_MBEDTLS_PKCS7_C

**Enable PKCS number 7**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:257`

Enable PKCS number 7 core for using PKCS number 7-formatted signatures.

---

## CONFIG_MBEDTLS_PK_PARSE_EC_COMPRESSED

**Enable the support for parsing public keys of type Short Weierstrass**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Elliptic Curve Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1044`

Enable the support for parsing public keys of type Short Weierstrass
(MBEDTLS_ECP_DP_SECP_XXX and MBEDTLS_ECP_DP_BP_XXX) which are using the
compressed point format. This parsing is done through ECP module's functions.

---

## CONFIG_MBEDTLS_PK_PARSE_EC_EXTENDED

**Enhance support for reading EC keys**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Elliptic Curve Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1036`

Enhance support for reading EC keys using variants of SEC1 not allowed by
RFC 5915 and RFC 5480.

---

## CONFIG_MBEDTLS_PLATFORM_TIME_ALT

**Enable mbedtls time support: platform-specific**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable mbedtls time support
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:670`

Enabling this config will provide users with a function
"mbedtls_platform_set_time()" that allows to set an alternative
time function pointer.

---

## CONFIG_MBEDTLS_POLY1305_C

**Poly1305 MAC algorithm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1189`

Enable support for Poly1305 MAC algorithm.

---

## CONFIG_MBEDTLS_PSK_MODES

**Enable pre-shared-key ciphersuites**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Key Exchange Methods
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:780`

Enable to show configuration for different types of pre-shared-key TLS authentatication methods.

Leaving this options disabled will save code size if they are not used.

---

## CONFIG_MBEDTLS_RIPEMD160_C

**Enable RIPEMD-160 hash algorithm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:992`

Enable the RIPEMD-160 hash algorithm.

---

## CONFIG_MBEDTLS_ROM_MD5

**Use MD5 implementation in ROM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:575`

Use ROM MD5 in mbedTLS.

---

## CONFIG_MBEDTLS_SERVER_SSL_SESSION_TICKETS

**TLS: Server Support for RFC 5077 SSL session tickets**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:921`

Server support for RFC 5077 session tickets. See mbedTLS documentation for more details.
Disabling this option will save some code size.

---

## CONFIG_MBEDTLS_SHA1_C

**Enable the SHA-1 cryptographic hash algorithm**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:715`

Enabling MBEDTLS_SHA1_C adds support for SHA-1.
SHA-1 is considered a weak message digest and its use constitutes
a security risk.
Disabling this configuration option could impact TLS 1.2 / Wi-Fi Enterprise compatibility
with certain older certificates that rely on SHA-1 for digital signatures.
Before proceeding, ensure that all your certificates are using stronger hash algorithms,
such as SHA-256 (part of the SHA-2 family).
If you're using older certificates or if you're unsure about the impact on your product,
please consider testing the changes in a controlled environment for individual features
like OTA updates, cloud connectivity, secure local control, etc.

---

## CONFIG_MBEDTLS_SHA3_C

**Enable the SHA3 cryptographic hash algorithm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:736`

Enabling MBEDTLS_SHA3_C adds support for SHA3.
Enabling this configuration option increases the flash footprint
by almost 4KB.

---

## CONFIG_MBEDTLS_SHA512_C

**Enable the SHA-384 and SHA-512 cryptographic hash algorithms**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:730`

Enable MBEDTLS_SHA512_C adds support for SHA-384 and SHA-512.

---

## CONFIG_MBEDTLS_SSL_ALPN

**Support ALPN (Application Layer Protocol Negotiation)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:906`

Disabling this option will save some code size if it is not needed.

---

## CONFIG_MBEDTLS_SSL_CID_IN_LEN_MAX

**Maximum length of CIDs used for incoming DTLS messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > DTLS-based configurations > Support for the DTLS Connection ID extension
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:290`

Maximum length of CIDs used for incoming DTLS messages

---

## CONFIG_MBEDTLS_SSL_CID_OUT_LEN_MAX

**Maximum length of CIDs used for outgoing DTLS messages**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > DTLS-based configurations > Support for the DTLS Connection ID extension
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:298`

Maximum length of CIDs used for outgoing DTLS messages

---

## CONFIG_MBEDTLS_SSL_CID_PADDING_GRANULARITY

**Record plaintext padding**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:264`

Controls the use of record plaintext padding in TLS 1.3 and
when using the Connection ID extension in DTLS 1.2.

The padding will always be chosen so that the length of the
padded plaintext is a multiple of the value of this option.

Notes:
    A value of 1 means that no padding will be used for outgoing records.
    On systems lacking division instructions, a power of two should be preferred.

---

## CONFIG_MBEDTLS_SSL_CONTEXT_SERIALIZATION

**Enable serialization of the TLS context structures**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:228`

Enable serialization of the TLS context structures
This is a local optimization in handling a single, potentially long-lived connection.

See mbedTLS documentation for required API and more details.
Disabling this option will save some code size.

---

## CONFIG_MBEDTLS_SSL_DTLS_CONNECTION_ID

**Support for the DTLS Connection ID extension**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > DTLS-based configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:283`

Enable support for the DTLS Connection ID extension which allows to
identify DTLS connections across changes in the underlying transport.

---

## CONFIG_MBEDTLS_SSL_DTLS_SRTP

**Enable support for negotiation of DTLS-SRTP (RFC 5764)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > DTLS-based configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:306`

Enable support for negotiation of DTLS-SRTP (RFC 5764) through the use_srtp extension.

See mbedTLS documentation for required API and more details.
Disabling this option will save some code size.

---

## CONFIG_MBEDTLS_SSL_IN_CONTENT_LEN

**TLS maximum incoming fragment length**

- **Type:** `unknown`
- **Default:** `16384`
- **Menu:** Component config > mbedTLS > Asymmetric in/out fragment length
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:82`

This defines maximum incoming fragment length, overriding default
maximum content length (MBEDTLS_SSL_MAX_CONTENT_LEN).

---

## CONFIG_MBEDTLS_SSL_KEEP_PEER_CERTIFICATE

**Keep peer certificate after handshake completion**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:239`

Keep the peer's certificate after completion of the handshake.
Disabling this option will save about 4kB of heap and some code size.

See mbedTLS documentation for required API and more details.

---

## CONFIG_MBEDTLS_SSL_KEYING_MATERIAL_EXPORT

**Enable keying material export**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:248`

Enable shared symmetric keys export for TLS sessions using mbedtls_ssl_export_keying_material()
after SSL handshake. The process for deriving the keys is specified in RFC 5705 for TLS 1.2
and in RFC 8446, Section 7.5, for TLS 1.3.

---

## CONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN

**TLS maximum message content length**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:53`

Maximum TLS message length (in bytes) supported by mbedTLS.

16384 is the default and this value is required to comply
fully with TLS standards.

However you can set a lower value in order to save RAM. This
is safe if the other end of the connection supports Maximum
Fragment Length Negotiation Extension (max_fragment_length,
see RFC6066) or you know for certain that it will never send a
message longer than a certain number of bytes.

If the value is set too low, symptoms are a failed TLS
handshake or a return value of MBEDTLS_ERR_SSL_INVALID_RECORD
(-0x7200).

---

## CONFIG_MBEDTLS_SSL_OUT_CONTENT_LEN

**TLS maximum outgoing fragment length**

- **Type:** `unknown`
- **Default:** `4096`
- **Menu:** Component config > mbedTLS > Asymmetric in/out fragment length
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:91`

This defines maximum outgoing fragment length, overriding default
maximum content length (MBEDTLS_SSL_MAX_CONTENT_LEN).

---

## CONFIG_MBEDTLS_SSL_PROTO_DTLS

**Support DTLS protocol (all versions)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:899`

Requires TLS 1.2 to be enabled for DTLS 1.2

---

## CONFIG_MBEDTLS_SSL_PROTO_GMTSSL1_1

**Support GM/T SSL 1.1 protocol**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:892`

Provisions for GM/T SSL 1.1 support

---

## CONFIG_MBEDTLS_SSL_PROTO_TLS1_2

**Support TLS 1.2 protocol**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:887`

---

## CONFIG_MBEDTLS_SSL_PROTO_TLS1_3

**Support TLS 1.3 protocol**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:175`

---

## CONFIG_MBEDTLS_SSL_RENEGOTIATION

**Support TLS renegotiation**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:877`

The two main uses of renegotiation are (1) refresh keys on long-lived
connections and (2) client authentication after the initial handshake.
If you don't need renegotiation, disabling it will save code size and
reduce the possibility of abuse/vulnerability.

---

## CONFIG_MBEDTLS_SSL_TLS1_3_COMPATIBILITY_MODE

**TLS 1.3 middlebox compatibility mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > Support TLS 1.3 protocol > TLS 1.3 related configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:185`

---

## CONFIG_MBEDTLS_SSL_TLS1_3_KEXM_EPHEMERAL

**TLS 1.3 ephemeral key exchange mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > Support TLS 1.3 protocol > TLS 1.3 related configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:193`

---

## CONFIG_MBEDTLS_SSL_TLS1_3_KEXM_PSK

**TLS 1.3 PSK key exchange mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > Support TLS 1.3 protocol > TLS 1.3 related configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:189`

---

## CONFIG_MBEDTLS_SSL_TLS1_3_KEXM_PSK_EPHEMERAL

**TLS 1.3 PSK ephemeral key exchange mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related > Support TLS 1.3 protocol > TLS 1.3 related configurations
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:197`

---

## CONFIG_MBEDTLS_SSL_VARIABLE_BUFFER_LENGTH

**Variable SSL buffer length**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:203`

This enables the SSL buffer to be resized automatically
based on the negotiated maximum fragment length in each direction.

---

## CONFIG_MBEDTLS_TEE_SEC_STG_ECDSA_SIGN

**Enable ECDSA signing using TEE secure storage**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:596`

---

## CONFIG_MBEDTLS_THREADING_ALT

**Enable threading alternate implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable the threading abstraction layer
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1222`

Enable threading alt to allow your own alternate threading implementation.

---

## CONFIG_MBEDTLS_THREADING_C

**Enable the threading abstraction layer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1215`

If you do intend to use contexts between threads, you will need to enable
this layer to prevent race conditions.

---

## CONFIG_MBEDTLS_THREADING_PTHREAD

**Enable threading pthread implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Enable the threading abstraction layer
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1229`

Enable the pthread wrapper layer for the threading layer.

---

## CONFIG_MBEDTLS_TLS_CLIENT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_MBEDTLS_TLS_CLIENT_ONLY

**Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Protocol Role
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:760`

---

## CONFIG_MBEDTLS_TLS_DISABLED

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Protocol Role
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:763`

---

## CONFIG_MBEDTLS_TLS_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_MBEDTLS_TLS_SERVER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_MBEDTLS_TLS_SERVER_AND_CLIENT

**Server & Client**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > TLS Protocol Role
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:753`

---

## CONFIG_MBEDTLS_TLS_SERVER_ONLY

**Server**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > TLS Protocol Role
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:757`

---

## CONFIG_MBEDTLS_USE_CRYPTO_ROM_IMPL

**Use ROM implementation of the crypto algorithm**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1255`

Enable this flag to use mbedtls crypto algorithm from ROM instead of ESP-IDF.

This configuration option saves flash footprint in the application binary.
Note that the version of mbedtls crypto algorithm library in ROM(ECO1~ECO3) is v2.16.12,
and the version of mbedtls crypto algorithm library in ROM(ECO4) is v3.6.0.
We have done the security analysis of the mbedtls revision in ROM (ECO1~ECO4)
and ensured that affected symbols have been patched (removed). If in the future
mbedtls revisions there are security issues that also affects the version in
ROM (ECO1~ECO4) then we shall patch the relevant symbols. This would increase
the flash footprint and hence care must be taken to keep some reserved space
for the application binary in flash layout.

---

## CONFIG_MBEDTLS_USE_CRYPTO_ROM_IMPL_BOOTLOADER

**Use ROM implementation of the crypto algorithm in the bootloader**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1244`

Enable this flag to use mbedtls crypto algorithm from ROM instead of ESP-IDF
in case of a bootloader build.
Similar to the MBEDTLS_USE_CRYPTO_ROM_IMPL config but enables usage of the
mbedtls crypto algorithm from ROM for the bootloader build.

---

## CONFIG_MBEDTLS_X509_CRL_PARSE_C

**X.509 CRL parsing**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificates
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1018`

Support for parsing X.509 Certificate Revocation Lists.

---

## CONFIG_MBEDTLS_X509_CSR_PARSE_C

**X.509 CSR parsing**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mbedTLS > Certificates
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:1024`

Support for parsing X.509 Certificate Signing Requests

---

## CONFIG_MBEDTLS_X509_TRUSTED_CERT_CALLBACK

**Enable trusted certificate callbacks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > mbedTLS v3.x related
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:219`

Enables users to configure the set of trusted certificates
through a callback instead of a linked list.

See mbedTLS documentation for required API and more details.

---

## CONFIG_MBEDTLS_XTEA_C

**XTEA block cipher**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mbedTLS > Symmetric Ciphers
- **Kconfig:** `/opt/esp/idf/components/mbedtls/Kconfig:958`

Enables the XTEA block cipher.

---

## CONFIG_MCPWM_CTRL_FUNC_IN_IRAM

**Place MCPWM control functions in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:MCPWM Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_mcpwm/Kconfig:20`

Place MCPWM control functions in IRAM, to reduce latency caused by cache miss.
If enabled, these functions can also be called when cache is disabled.

---

## CONFIG_MCPWM_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:MCPWM Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_mcpwm/Kconfig:35`

If enabled, MCPWM component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option with caution, as it will increase the binary size.

---

## CONFIG_MCPWM_ISR_CACHE_SAFE

**Allow MCPWM ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:MCPWM Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_mcpwm/Kconfig:11`

Enable this option to allow the MCPWM Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the MCPWM functionality is still required to operate correctly.

---

## CONFIG_MCPWM_ISR_HANDLER_IN_IRAM

**Place MCPWM ISR handler into IRAM to reduce latency**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:MCPWM Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_mcpwm/Kconfig:4`

Place MCPWM ISR handler(s) in IRAM to reduce latency caused by cache miss.

---

## CONFIG_MCPWM_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `y`

This will ensure the MCPWM object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_MCPWM_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy MCPWM Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:98`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_MCPWM_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy MCPWM Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:90`

Whether to suppress the deprecation warnings when using legacy MCPWM driver (driver/mcpwm.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_MDNS_ACTION_QUEUE_LEN

**Maximum actions pending to the server**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:28`

Allows setting the length of mDNS action queue.

---

## CONFIG_MDNS_DEBUG_BUFFER_SIZE

**Size of temporary buffer for debug prints**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > mDNS > Enable debug prints of mDNS packets > Route debug prints to ESP_LOG
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:152`

This buffer is used to output mDNS packets in debug prints.

---

## CONFIG_MDNS_DEBUG_USE_ESP_LOG

**Route debug prints to ESP_LOG**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > Enable debug prints of mDNS packets
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:145`

Enable this option to route debug prints to ESP_LOG, which allows more flexibility.

---

## CONFIG_MDNS_ENABLE_CONSOLE_CLI

**Enable Command Line Interface on device console**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:159`

Enable for the console cli to be available on the device.

---

## CONFIG_MDNS_ENABLE_DEBUG_PRINTS

**Enable debug prints of mDNS packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:139`

Enable for the library to log received and sent mDNS packets to stdout.

---

## CONFIG_MDNS_MAX_INTERFACES

**Max number of interfaces**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:3`

Number of network interfaces to be served by the mDNS library.
Lowering this number helps to reduce some static RAM usage.

---

## CONFIG_MDNS_MAX_SERVICES

**Max number of services**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:11`

Services take up a certain amount of memory, and allowing fewer
services to be open at the same time conserves memory. Specify
the maximum amount of services here.

---

## CONFIG_MDNS_MEMORY_ALLOC_INTERNAL

**Allocate mDNS memory from internal RAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > MDNS Memory Configuration > Select mDNS memory allocation type
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:91`

---

## CONFIG_MDNS_MEMORY_ALLOC_SPIRAM

**Allocate mDNS memory from SPIRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > MDNS Memory Configuration > Select mDNS memory allocation type
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:87`

---

## CONFIG_MDNS_MEMORY_CUSTOM_IMPL

**Implement custom memory functions**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > MDNS Memory Configuration
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:96`

Enable to implement custom memory functions for mDNS library.
This option is useful when the application wants to use custom
memory allocation functions for mDNS library.

---

## CONFIG_MDNS_MULTIPLE_INSTANCE

**Multiple instances under the same service type**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:172`

Enables adding multiple service instances under the same service type.

---

## CONFIG_MDNS_NETWORKING_SOCKET

**Use BSD sockets for mDNS networking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:122`

Enables optional mDNS networking implementation using BSD sockets
in UDP multicast mode.
This option creates a new thread to serve receiving packets (TODO).
This option uses additional N sockets, where N is number of interfaces.

---

## CONFIG_MDNS_PREDEF_NETIF_AP

**Use predefined interface for WiFi Access Point**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > MDNS Predefined interfaces
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:187`

Set up mDNS for the default WiFi Access Point.
Disable this option if you do not need mDNS on default WiFi AP.

---

## CONFIG_MDNS_PREDEF_NETIF_ETH

**Use predefined interface for Ethernet**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > MDNS Predefined interfaces
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:194`

Set up mDNS for the default Ethernet interface.
Disable this option if you do not need mDNS on default Ethernet.

---

## CONFIG_MDNS_PREDEF_NETIF_STA

**Use predefined interface for WiFi Station**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > MDNS Predefined interfaces
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:180`

Set up mDNS for the default WiFi station.
Disable this option if you do not need mDNS on default WiFi STA.

---

## CONFIG_MDNS_RESPOND_REVERSE_QUERIES

**Enable responding to IPv4 reverse queries**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:165`

Enables support for IPv4 reverse lookup. If enabled, the mDNS library
response to PTR queries of "A.B.C.D.in-addr.arpa" type.

---

## CONFIG_MDNS_SERVICE_ADD_TIMEOUT_MS

**mDNS adding service timeout (ms)**

- **Type:** `unknown`
- **Default:** `2000`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:106`

Configures timeout for adding a new mDNS service. Adding a service
fails if could not be completed within this time.

---

## CONFIG_MDNS_SKIP_SUPPRESSING_OWN_QUERIES

**Skip suppressing our own packets**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:131`

Enable only if the querier and the responder share the same IP address.
This usually happens in test mode, where we may run multiple instances of
responders/queriers on the same interface.

---

## CONFIG_MDNS_TASK_AFFINITY

- **Type:** `unknown`
- **Default:** `0x0`

---

## CONFIG_MDNS_TASK_AFFINITY_CPU0

**CPU0**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > mDNS task affinity
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:50`

---

## CONFIG_MDNS_TASK_AFFINITY_CPU1

**CPU1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > mDNS task affinity
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:52`

---

## CONFIG_MDNS_TASK_AFFINITY_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > mDNS task affinity
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:48`

---

## CONFIG_MDNS_TASK_CREATE_FROM_INTERNAL

**mDNS task creates on the internal RAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > mDNS > MDNS Memory Configuration > Select mDNS task create on which type of memory
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:78`

---

## CONFIG_MDNS_TASK_CREATE_FROM_SPIRAM

**mDNS task creates on the SPIRAM (READ HELP)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > mDNS > MDNS Memory Configuration > Select mDNS task create on which type of memory
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:69`

mDNS task creates on the SPIRAM.
This option requires FreeRTOS component to allow creating
tasks on the external memory.
Please read the documentation about FREERTOS_TASK_CREATE_ALLOW_EXT_MEM

---

## CONFIG_MDNS_TASK_PRIORITY

**mDNS task priority**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:19`

Allows setting mDNS task priority. Please do not set the task priority
higher than priorities of system tasks. Compile time warning/error
would be emitted if the chosen task priority were too high.

---

## CONFIG_MDNS_TASK_STACK_SIZE

**mDNS task stack size**

- **Type:** `unknown`
- **Default:** `4096`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:35`

Allows setting mDNS task stacksize.

---

## CONFIG_MDNS_TIMER_PERIOD_MS

**mDNS timer period (ms)**

- **Type:** `unknown`
- **Default:** `100`
- **Menu:** Component config > mDNS
- **Kconfig:** `managed_components/espressif__mdns/Kconfig:114`

Configures period of mDNS timer, which periodically transmits packets
and schedules mDNS searches.

---

## CONFIG_MERUS_NCLIP_PIN

**Clip indication pin**

- **Type:** `unknown`
- **Default:** `22`
- **Menu:** Audio Board > Custom Audio Board > Merus MA120 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:283`

GPIO number low if clip observed

---

## CONFIG_MERUS_NERR_PIN

**NERR monitor pin**

- **Type:** `unknown`
- **Default:** `21`
- **Menu:** Audio Board > Custom Audio Board > Merus MA120 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:277`

GPIO number to monitor NERROR.

---

## CONFIG_MMU_PAGE_MODE

- **Type:** `unknown`
- **Default:** `64KB`

---

## CONFIG_MMU_PAGE_SIZE

- **Type:** `unknown`
- **Default:** `0x10000`

---

## CONFIG_MMU_PAGE_SIZE_16KB

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_MMU_PAGE_SIZE_32KB

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_MMU_PAGE_SIZE_64KB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_MMU_PAGE_SIZE_8KB

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_MQTT_BUFFER_SIZE

**Default MQTT Buffer Size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:98`

This buffer size using for both transmit and receive

---

## CONFIG_MQTT_CUSTOM_OUTBOX

**Enable custom outbox implementation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:162`

Set to true if a specific implementation of message outbox is needed (e.g. persistent outbox in NVM or
similar).
Note: Implementation of the custom outbox must be added to the mqtt component. These CMake commands
could be used to append the custom implementation to lib-mqtt sources:
idf_component_get_property(mqtt mqtt COMPONENT_LIB)
set_property(TARGET ${mqtt} PROPERTY SOURCES ${PROJECT_DIR}/custom_outbox.c APPEND)

---

## CONFIG_MQTT_DISABLE_API_LOCKS

**Disable API locks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:112`

Default config employs API locks to protect internal structures. It is possible to disable
these locks if the user code doesn't access MQTT API from multiple concurrent tasks

---

## CONFIG_MQTT_EVENT_QUEUE_SIZE

**Number of queued events.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:134`

A value higher than 1 enables multiple queued events.

---

## CONFIG_MQTT_MSG_ID_INCREMENTAL

**Use Incremental Message Id**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:36`

Set this to true for the message id (2.3.1 Packet Identifier) to be generated
as an incremental number rather then a random value (used by default)

---

## CONFIG_MQTT_OUTBOX_DATA_ON_EXTERNAL_MEMORY

**Use external memory for outbox data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:155`

Set to true to use external memory for outbox data.

---

## CONFIG_MQTT_OUTBOX_EXPIRED_TIMEOUT_MS

**Outbox message expired timeout[ms]**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:173`

Messages which stays in the outbox longer than this value before being published will be discarded.

---

## CONFIG_MQTT_POLL_READ_TIMEOUT_MS

**MQTT transport poll read timeut**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:127`

Timeout when polling underlying transport for read.

---

## CONFIG_MQTT_PROTOCOL_311

**Enable MQTT protocol 3.1.1**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:3`

If not, this library will use MQTT protocol 3.1

---

## CONFIG_MQTT_PROTOCOL_5

**Enable MQTT protocol 5.0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:9`

If not, this library will not support MQTT 5.0

---

## CONFIG_MQTT_REPORT_DELETED_MESSAGES

**Report deleted messages**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:53`

Set this to true to post events for all messages which were deleted from the outbox
before being correctly sent and confirmed.

---

## CONFIG_MQTT_SKIP_PUBLISH_IF_DISCONNECTED

**Skip publish if disconnected**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:43`

Set this to true to avoid publishing (enqueueing messages) if the client is disconnected.
The MQTT client tries to publish all messages by default, even in the disconnected state
(where the qos1 and qos2 packets are stored in the internal outbox to be published later)
The MQTT_SKIP_PUBLISH_IF_DISCONNECTED option allows applications to override this behaviour
and not enqueue publish packets in the disconnected state.

---

## CONFIG_MQTT_SSL_DEFAULT_PORT

**Default MQTT over SSL port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:73`

Default MQTT over SSL port

---

## CONFIG_MQTT_TASK_CORE_SELECTION_ENABLED

**Enable MQTT task core selection**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:141`

This will enable core selection

---

## CONFIG_MQTT_TASK_PRIORITY

**MQTT task priority**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:120`

MQTT task priority. Higher number denotes higher priority.

---

## CONFIG_MQTT_TASK_STACK_SIZE

**MQTT task stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:105`

MQTT task stack size

---

## CONFIG_MQTT_TCP_DEFAULT_PORT

**Default MQTT over TCP port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:66`

Default MQTT over TCP port

---

## CONFIG_MQTT_TOPIC_PRESENT_ALL_DATA_EVENTS

**Enable publish topic in all data events**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:180`

Set to true to have publish topic in all data events. This changes the behaviour
when  the message is bigger than the receive buffer size. The first event of the sequence
always have the topic.
Note: This will allocate memory to store the topic only in case of messge bigger than the buffer size.

---

## CONFIG_MQTT_TRANSPORT_SSL

**Enable MQTT over SSL**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:15`

Enable MQTT transport over SSL with mbedtls

---

## CONFIG_MQTT_TRANSPORT_WEBSOCKET

**Enable MQTT over Websocket**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:21`

Enable MQTT transport over Websocket.

---

## CONFIG_MQTT_TRANSPORT_WEBSOCKET_SECURE

**Enable MQTT over Websocket Secure**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-MQTT Configurations > Enable MQTT over Websocket
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:28`

Enable MQTT transport over Websocket Secure.

---

## CONFIG_MQTT_USE_CORE_0

**Core 0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations > Enable MQTT task core selection > Core to use ?
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:149`

---

## CONFIG_MQTT_USE_CORE_1

**Core 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations > Enable MQTT task core selection > Core to use ?
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:151`

---

## CONFIG_MQTT_USE_CUSTOM_CONFIG

**MQTT Using custom configurations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-MQTT Configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:60`

Custom MQTT configurations.

---

## CONFIG_MQTT_WSS_DEFAULT_PORT

**Default MQTT over Websocket Secure port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:89`

Default MQTT over Websocket Secure port

---

## CONFIG_MQTT_WS_DEFAULT_PORT

**Default MQTT over Websocket port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP-MQTT Configurations > MQTT Using custom configurations
- **Kconfig:** `/opt/esp/idf/components/mqtt/esp-mqtt/Kconfig:81`

Default MQTT over Websocket port

---

## CONFIG_NVS_ALLOCATE_CACHE_IN_SPIRAM

**Prefers allocation of in-memory cache structures in SPI connected PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS
- **Kconfig:** `/opt/esp/idf/components/nvs_flash/Kconfig:39`

Enabling this option lets NVS library try to allocate page cache and key hash list in SPIRAM
instead of internal RAM. It can help applications using large nvs partitions or large number
of keys to save heap space in internal RAM. SPIRAM heap allocation negatively impacts speed
of NVS operations as the CPU accesses NVS cache via SPI instead of direct access to the internal RAM.

---

## CONFIG_NVS_ASSERT_ERROR_CHECK

**Use assertions for error checking**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS
- **Kconfig:** `/opt/esp/idf/components/nvs_flash/Kconfig:23`

This option switches error checking type between assertions (y) or return codes (n).

---

## CONFIG_NVS_COMPATIBLE_PRE_V4_3_ENCRYPTION_FLAG

**NVS partition encrypted flag compatible with ESP-IDF before v4.3**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS
- **Kconfig:** `/opt/esp/idf/components/nvs_flash/Kconfig:13`

Enabling this will ignore "encrypted" flag for NVS partitions. NVS encryption
scheme is different than hardware flash encryption and hence it is not recommended
to have "encrypted" flag for NVS partitions. This was not being checked in pre v4.3
IDF. Hence, if you have any devices where this flag is kept enabled in partition
table then enabling this config will allow to have same behavior as pre v4.3 IDF.

---

## CONFIG_NVS_ENCRYPTION

**Enable NVS encryption**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS
- **Kconfig:** `/opt/esp/idf/components/nvs_flash/Kconfig:3`

This option enables encryption for NVS. When enabled, XTS-AES is used to encrypt
the complete NVS data, except the page headers. It requires XTS encryption keys
to be stored in an encrypted partition (enabling flash encryption is mandatory here)
or to be derived from an HMAC key burnt in eFuse.

---

## CONFIG_NVS_LEGACY_DUP_KEYS_COMPATIBILITY

**Enable legacy nvs_set function behavior when same key is reused with different data types**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS
- **Kconfig:** `/opt/esp/idf/components/nvs_flash/Kconfig:29`

Enabling this option will switch the nvs_set() family of functions to the legacy mode:
when called repeatedly with the same key but different data type, the existing value
in the NVS remains active and the new value is just stored, actually not accessible through
corresponding nvs_get() call for the key given. Use this option only when your application
relies on such NVS API behaviour.

---

## CONFIG_NVS_SEC_HMAC_EFUSE_KEY_ID

**eFuse key ID storing the HMAC key**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > NVS Security Provider
- **Kconfig:** `/opt/esp/idf/components/nvs_sec_provider/Kconfig:32`

eFuse block key ID storing the HMAC key for deriving the NVS encryption keys

Note: The eFuse block key ID required by the HMAC scheme
(CONFIG_NVS_SEC_KEY_PROTECT_USING_HMAC) is set using this config when the default
NVS partition is initialized with nvs_flash_init(). The eFuse block key ID can
also be set at runtime by passing the appropriate value to the NVS security scheme
registration APIs.

---

## CONFIG_NVS_SEC_KEY_PROTECT_USING_FLASH_ENC

**Using Flash Encryption**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS Security Provider > NVS Encryption: Key Protection Scheme
- **Kconfig:** `/opt/esp/idf/components/nvs_sec_provider/Kconfig:14`

Protect the NVS Encryption Keys using Flash Encryption
Requires a separate 'nvs_keys' partition (which will be encrypted by flash encryption)
for storing the NVS encryption keys

---

## CONFIG_NVS_SEC_KEY_PROTECT_USING_HMAC

**Using HMAC peripheral**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > NVS Security Provider > NVS Encryption: Key Protection Scheme
- **Kconfig:** `/opt/esp/idf/components/nvs_sec_provider/Kconfig:22`

Derive and protect the NVS Encryption Keys using the HMAC peripheral
Requires the specified eFuse block (NVS_SEC_HMAC_EFUSE_KEY_ID or the v2 API argument)
to be empty or pre-written with a key with the purpose ESP_EFUSE_KEY_PURPOSE_HMAC_UP

---

## CONFIG_OPENTHREAD_ADDRESS_QUERY_MAX_RETRY_DELAY

**Maximum retry delay for address query (in seconds).**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters > Thread Address Query Config
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:413`

---

## CONFIG_OPENTHREAD_ADDRESS_QUERY_RETRY_DELAY

**Initial retry delay for address query (in seconds).**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters > Thread Address Query Config
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:408`

---

## CONFIG_OPENTHREAD_ADDRESS_QUERY_TIMEOUT

**Timeout (in seconds) for a address notification response after sending an address query.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters > Thread Address Query Config
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:403`

---

## CONFIG_OPENTHREAD_BORDER_AGENT_ENABLE

**Enable border agent feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:287`

Select this option to enable border agent feature

---

## CONFIG_OPENTHREAD_BORDER_ROUTER

**Enable Border Router**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:214`

Select this option to enable border router features in OpenThread.

---

## CONFIG_OPENTHREAD_BUS_LATENCY

**The bus latency between host and radio chip**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:437`

The device's bus latency, in us.

---

## CONFIG_OPENTHREAD_CLI

**Enable Openthread Command-Line Interface**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Console
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:42`

Select this option to enable Command-Line Interface in OpenThread.

---

## CONFIG_OPENTHREAD_COMMISSIONER

**Enable Commissioner**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:221`

Select this option to enable commissioner in OpenThread. This will enable the device to act as a
commissioner in the Thread network. A commissioner checks the pre-shared key from a joining device
with the Thread commissioning protocol and shares the network parameter with the joining device
upon success.

---

## CONFIG_OPENTHREAD_COMM_MAX_JOINER_ENTRIES

**The size of max commissioning joiner entries**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Enable Commissioner > Commissioner Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:231`

---

## CONFIG_OPENTHREAD_CONSOLE_COMMAND_PREFIX

**The prefix of the openthread CLI command registered on the esp console**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Console
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:48`

A prefix string used before a Thread CLI command, allowing the ESP console to identify
it and delegate the remaining command to the OpenThread callback for processing.

---

## CONFIG_OPENTHREAD_CONSOLE_TYPE_UART

**OpenThread console type UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Console > OpenThread console type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:34`

---

## CONFIG_OPENTHREAD_CONSOLE_TYPE_USB_SERIAL_JTAG

**OpenThread console type USB Serial/JTAG Controller**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Console > OpenThread console type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:37`

---

## CONFIG_OPENTHREAD_CSL_ACCURACY

**The current CSL rx/tx scheduling drift, in units of ± ppm**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > CSL Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:307`

The current accuracy of the clock used for scheduling CSL operations

---

## CONFIG_OPENTHREAD_CSL_DEBUG_ENABLE

**Enable CSL debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > CSL Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:320`

Select this option to set rx on when sleep in CSL feature, only for debug

---

## CONFIG_OPENTHREAD_CSL_ENABLE

**Enable CSL feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:300`

Select this option to enable CSL feature

---

## CONFIG_OPENTHREAD_CSL_UNCERTAIN

**The CSL Uncertainty in units of 10 us.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > CSL Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:314`

The fixed uncertainty of the Device for scheduling CSL Transmissions in units of 10 microseconds.

---

## CONFIG_OPENTHREAD_CUSTOM_HEADER_FILE_NAME

**Name of custom header file**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Extensioned Features > Use a header file defined by customer > OpenThread Custom Header Config
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:532`

Name of custom header file.

---

## CONFIG_OPENTHREAD_CUSTOM_HEADER_PATH

**Path of custom header file**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Extensioned Features > Use a header file defined by customer > OpenThread Custom Header Config
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:525`

Please use relative paths with respect to the project folder.

---

## CONFIG_OPENTHREAD_DIAG

**Enable diag**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:350`

Select this option to enable Diag in OpenThread. This will enable diag mode and a series of diag
commands in the OpenThread command line. These commands allow users to manipulate low-level
features of the storage and 15.4 radio.

---

## CONFIG_OPENTHREAD_DNS64_CLIENT

**Enable DNS64 Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:264`

Select this option to acquire NAT64 address from dns servers.

---

## CONFIG_OPENTHREAD_DNS_CLIENT

**Enable DNS Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:258`

Select this option to enable DNS Client in OpenThread.

---

## CONFIG_OPENTHREAD_DNS_SERVER_ADDR

**DNS server address (IPv4)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Enable DNS64 Client > DNS64 Client Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:273`

Set the DNS server IPv4 address.

---

## CONFIG_OPENTHREAD_ENABLED

**OpenThread**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:3`

Select this option to enable OpenThread and show the submenu with OpenThread configuration choices.

---

## CONFIG_OPENTHREAD_FTD

**Full Thread Device**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread device type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:106`

Select this to enable Full Thread Device which can act as router and leader in a Thread network.

---

## CONFIG_OPENTHREAD_HEADER_CUSTOM

**Use a header file defined by customer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Extensioned Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:509`

This option allows users to tailor the values of openthread macros according to their requirements.
The openthread submodule contains numerous macros, each with a default value set. In the Kconfig of
ESP openthread, users can set specific openthread parameters, which will be applied for certain
openthread macros in the openthread-core-esp32x-xxx-config.h file. During compilation, the values
specified in openthread-core-esp32x-xxx-config.h will replace the default settings in the openthread
submodule. However, Kconfig does not cover all openthread macros, particularly those typically
using default values. For such macros, users can enable the OPENTHREAD_HEADER_CUSTOM option in the
Kconfig and provide a custom header file. Macros defined in the custom header file will have the
highest priority.

---

## CONFIG_OPENTHREAD_JOINER

**Enable Joiner**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:236`

Select this option to enable Joiner in OpenThread. This allows a device to join the
Thread network with a pre-shared key using the Thread commissioning protocol.

---

## CONFIG_OPENTHREAD_LINK_METRICS

**Enable link metrics feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:280`

Select this option to enable link metrics feature

---

## CONFIG_OPENTHREAD_LOG_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_OPENTHREAD_LOG_LEVEL_CRIT

**Error logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:484`

---

## CONFIG_OPENTHREAD_LOG_LEVEL_DEBG

**Debug logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:492`

---

## CONFIG_OPENTHREAD_LOG_LEVEL_DYNAMIC

**Enable dynamic log level control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:469`

Select this option to enable dynamic log level control for OpenThread

---

## CONFIG_OPENTHREAD_LOG_LEVEL_INFO

**Info logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:490`

---

## CONFIG_OPENTHREAD_LOG_LEVEL_NONE

**No logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:482`

---

## CONFIG_OPENTHREAD_LOG_LEVEL_NOTE

**Notice logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:488`

---

## CONFIG_OPENTHREAD_LOG_LEVEL_WARN

**Warning logs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Log > OpenThread log verbosity
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:486`

---

## CONFIG_OPENTHREAD_MACFILTER_ENABLE

**Enable mac filter feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:294`

Select this option to enable mac filter feature

---

## CONFIG_OPENTHREAD_MAC_MAX_CSMA_BACKOFFS_DIRECT

**Maximum backoffs times before declaring a channel access failure.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:457`

The maximum number of backoffs the CSMA-CA algorithm will attempt before declaring a channel access
failure.

---

## CONFIG_OPENTHREAD_MESH_LOCAL_PREFIX

**OpenThread mesh local prefix, format <address>/<plen>**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:64`

A string in the format "<address>/<plen>", where `<address>` is an IPv6
address and `<plen>` is a prefix length. For example "fd00:db8:a0:0::/64"

---

## CONFIG_OPENTHREAD_MLE_MAX_CHILDREN

**The size of max MLE children entries**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:443`

---

## CONFIG_OPENTHREAD_MTD

**Minimal Thread Device**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread device type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:111`

Select this to enable Minimal Thread Device which can only act as end device in a Thread network.
This will reduce the code size of the OpenThread stack.

---

## CONFIG_OPENTHREAD_NCP_VENDOR_HOOK

**Enable vendor command for RCP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Radio Co-Processor Feature
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:207`

Select this to enable OpenThread NCP vendor commands.

---

## CONFIG_OPENTHREAD_NETWORK_CHANNEL

**OpenThread network channel**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:71`

---

## CONFIG_OPENTHREAD_NETWORK_EXTPANID

**OpenThread extended pan id**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:81`

The OpenThread network extended pan id in hex string format

---

## CONFIG_OPENTHREAD_NETWORK_MASTERKEY

**OpenThread network key**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:87`

The OpenThread network network key in hex string format

---

## CONFIG_OPENTHREAD_NETWORK_NAME

**OpenThread network name**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:60`

---

## CONFIG_OPENTHREAD_NETWORK_PANID

**OpenThread network pan id**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:76`

---

## CONFIG_OPENTHREAD_NETWORK_PSKC

**OpenThread pre-shared commissioner key**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Operational Dataset
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:93`

The OpenThread pre-shared commissioner key in hex string format

---

## CONFIG_OPENTHREAD_NUM_MESSAGE_BUFFERS

**The number of openthread message buffers**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:427`

---

## CONFIG_OPENTHREAD_PACKAGE_NAME

**OpenThread package name**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Version Message
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:12`

The OpenThread package name.

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_BACKOFF_INTERVAL_MINS

**The backoff interval in minutes for a child to not perform a parent search after triggering one**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Parent Search Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:371`

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_CHECK_INTERVAL_MINS

**The interval in minutes for a child to check the trigger condition to perform a parent search**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Parent Search Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:368`

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_MTD

**Enable Parent Search**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:358`

Select this option to enable "Periodic Parent Search" function for MTD. This checks the average
RSS to its current parent every periodically and starts a parent search process if the average
RSS is below OPENTHREAD_PARENT_SEARCH_RSS_THRESHOLD. This feature is always enabled for FTDs.

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_RESELECT_TIMEOUT_MINS

**The parent reselect timeout duration in minutes used on FTD child devices**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Parent Search Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:377`

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_RSS_MARGIN

**The RSS margin over the current parent RSS used on FTD child devices**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Parent Search Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:380`

---

## CONFIG_OPENTHREAD_PARENT_SEARCH_RSS_THRESHOLD

**The RSS threshold used to trigger a parent search**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Parent Search Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:374`

---

## CONFIG_OPENTHREAD_PLATFORM_INFO

**platform information**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Version Message
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:18`

The OpenThread platform information.

---

## CONFIG_OPENTHREAD_PLATFORM_MALLOC_CAP_SPIRAM

**Allocate memory from PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Memory Allocation
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:387`

Select this option to allocate buffer from PSRAM for Thread

---

## CONFIG_OPENTHREAD_PLATFORM_MSGPOOL_MANAGEMENT

**Allocate message pool buffer from PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Memory Allocation
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:393`

If enabled, the message pool is managed by platform defined logic.

---

## CONFIG_OPENTHREAD_PREFERRED_CHANNEL_MASK

**Preferred channel mask**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:419`

---

## CONFIG_OPENTHREAD_RADIO

**Radio Only Device**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread device type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:117`

Select this to enable Radio Only Device which can only forward 15.4 packets to the host.
The OpenThread stack will be run on the host and OpenThread will have minimal footprint on the
radio only device.

---

## CONFIG_OPENTHREAD_RADIO_154_NONE

**Disable the Thread radio based on 15.4 link**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread 15.4 Radio Link > Config the Thread radio type with 15.4 link
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:174`

Select this to disable the Thread radio based on 15.4 link.

---

## CONFIG_OPENTHREAD_RADIO_NATIVE

**Native 15.4 radio**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread 15.4 Radio Link > Config the Thread radio type with 15.4 link
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:158`

Select this to use the native 15.4 radio.

---

## CONFIG_OPENTHREAD_RADIO_SPINEL_SPI

**Connect via SPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread 15.4 Radio Link > Config the Thread radio type with 15.4 link
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:169`

Select this to connect to a Radio Co-Processor via SPI.

---

## CONFIG_OPENTHREAD_RADIO_SPINEL_UART

**Connect via UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread 15.4 Radio Link > Config the Thread radio type with 15.4 link
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:163`

Select this to connect to a Radio Co-Processor via UART.

---

## CONFIG_OPENTHREAD_RADIO_STATS_ENABLE

**Enable Radio Statistics feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:334`

Select this option to enable the radio statistics feature, you can use radio
command to print some radio Statistics information.

---

## CONFIG_OPENTHREAD_RADIO_TREL

**Enable Thread Radio Encapsulation Link (TREL)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Trel Radio Link
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:128`

Select this option to enable Thread Radio Encapsulation Link.

---

## CONFIG_OPENTHREAD_RCP_SPI

**SPI RCP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Radio Co-Processor Feature > The RCP transport type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:194`

Select this to enable SPI connection to host.

---

## CONFIG_OPENTHREAD_RCP_UART

**UART RCP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Radio Co-Processor Feature > The RCP transport type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:188`

Select this to enable UART connection to host.

---

## CONFIG_OPENTHREAD_RCP_USB_SERIAL_JTAG

**USB RCP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Radio Co-Processor Feature > The RCP transport type
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:200`

Select this to enable connection to host over USB JTAG serial.

---

## CONFIG_OPENTHREAD_RX_ON_WHEN_IDLE

**Enable OpenThread radio capability rx on when idle**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:342`

Select this option to enable OpenThread radio capability rx on when idle.
Do not support this feature when SW coexistence is enabled.

---

## CONFIG_OPENTHREAD_SPINEL_MAC_MAX_CSMA_BACKOFFS_DIRECT

**Maximum backoffs times before declaring a channel access failure.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread Spinel
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:554`

The maximum number of backoffs the CSMA-CA algorithm will attempt before declaring a channel access
failure.

---

## CONFIG_OPENTHREAD_SPINEL_ONLY

**Enable OpenThread External Radio Spinel feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread Spinel
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:542`

Select this option to enable the OpenThread Radio Spinel for external protocol stack, such as Zigbee.

---

## CONFIG_OPENTHREAD_SPINEL_RX_FRAME_BUFFER_SIZE

**The size of openthread spinel rx frame buffer**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread Spinel
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:548`

---

## CONFIG_OPENTHREAD_SRP_CLIENT

**Enable SRP Client**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:243`

Select this option to enable SRP Client in OpenThread.
This allows a device to register SRP services to SRP Server.

---

## CONFIG_OPENTHREAD_SRP_CLIENT_MAX_SERVICES

**Specifies number of service entries in the SRP client service pool**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Enable SRP Client > SRP Client Configurations
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:251`

Set the max buffer size of service entries in the SRP client service pool.

---

## CONFIG_OPENTHREAD_SUPPORTED_CHANNEL_MASK

**Supported channel mask**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:423`

---

## CONFIG_OPENTHREAD_TIME_SYNC

**Enable the time synchronization service feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:327`

Select this option to enable time synchronization feature, the devices in the same Thread network could
sync to the same network time.

---

## CONFIG_OPENTHREAD_TMF_ADDR_CACHE_ENTRIES

**The size of max TMF address cache entries**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:447`

---

## CONFIG_OPENTHREAD_TREL_BUFFER_SIZE

**The receive buffer size of openthread trel**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Trel Radio Link > Enable Thread Radio Encapsulation Link (TREL)
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:141`

Configure the receive buffer size of TREL service.

---

## CONFIG_OPENTHREAD_TREL_PORT

**The port of openthread trel service**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > Thread Trel Radio Link > Enable Thread Radio Encapsulation Link (TREL)
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:134`

Configure the port number of TREL service.

---

## CONFIG_OPENTHREAD_UART_BUFFER_SIZE

**The uart received buffer size of openthread**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:451`

Set the OpenThread UART buffer size.

---

## CONFIG_OPENTHREAD_XTAL_ACCURACY

**The accuracy of the XTAL**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > OpenThread > OpenThread > Thread Core Features > OpenThread Stack Parameters
- **Kconfig:** `/opt/esp/idf/components/openthread/Kconfig:431`

The device's XTAL accuracy, in ppm.

---

## CONFIG_PARLIO_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:48`

If enabled, Parallel IO driver component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_PARLIO_ISR_IRAM_SAFE

**Parallel IO ISR IRAM-Safe (Deprecated)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:58`

Ensure the Parallel IO interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_PARLIO_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the driver object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_PARLIO_RX_ISR_CACHE_SAFE

**Allow Parallel IO RX ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:31`

Enable this option to allow the Parallel IO RX Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the Parallel IO RX functionality is still required to operate correctly.

---

## CONFIG_PARLIO_RX_ISR_HANDLER_IN_IRAM

**Place Parallel IO RX ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:13`

Place Parallel IO RX ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_PARLIO_TX_ISR_CACHE_SAFE

**Allow Parallel IO TX ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:21`

Enable this option to allow the Parallel IO TX Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the Parallel IO TX functionality is still required to operate correctly.

---

## CONFIG_PARLIO_TX_ISR_HANDLER_IN_IRAM

**Place Parallel IO TX ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Parallel IO Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_parlio/Kconfig:4`

Place Parallel IO TX ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_PARTITION_TABLE_CUSTOM

**Custom partition table CSV**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:61`

Specify the path to the partition table CSV to use for your project.

Consult the Partition Table section in the ESP-IDF Programmers Guide
for more information.

---

## CONFIG_PARTITION_TABLE_CUSTOM_FILENAME

**Custom partition CSV file**

- **Type:** `unknown`
- **Default:** `partitions.csv`
- **Menu:** Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:129`

Name of the custom partition CSV filename.
This path is evaluated relative to the project root directory by default.
However, if the absolute path for the CSV file is provided, then the absolute path is configured.

---

## CONFIG_PARTITION_TABLE_FILENAME

- **Type:** `unknown`
- **Default:** `partitions_singleapp.csv`

---

## CONFIG_PARTITION_TABLE_MD5

**Generate an MD5 checksum for the partition table**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:167`

Generate an MD5 checksum for the partition table for protecting the
integrity of the table. The generation should be turned off for legacy
bootloaders which cannot recognize the MD5 checksum in the partition
table.

---

## CONFIG_PARTITION_TABLE_OFFSET

**Offset of partition table**

- **Type:** `unknown`
- **Default:** `0x8000`
- **Menu:** Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:153`

The address of partition table (by default 0x8000).
Allows you to move the partition table, it gives more space for the bootloader.
Note that the bootloader and app will both need to be compiled with the same PARTITION_TABLE_OFFSET value.

This number should be a multiple of 0x1000.

Note that partition offsets in the partition table CSV file may need to be changed if this value is set to
a higher value. To have each partition offset adapt to the configured partition table offset, leave all
partition offsets blank in the CSV file.

---

## CONFIG_PARTITION_TABLE_SINGLE_APP

**Single factory app, no OTA**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:17`

This is the default partition table, designed to fit into a 2MB or
larger flash with a single 1MB app partition.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_singleapp.csv

This partition table is not suitable for an app that needs OTA
(over the air update) capability.

---

## CONFIG_PARTITION_TABLE_SINGLE_APP_ENCRYPTED_NVS

**Single factory app, no OTA, encrypted NVS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:69`

This is a variation of the default "Single factory app, no OTA" partition table
that supports encrypted NVS when using flash encryption. See the Flash Encryption section
in the ESP-IDF Programmers Guide for more information.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_singleapp_encr_nvs.csv

---

## CONFIG_PARTITION_TABLE_SINGLE_APP_LARGE

**Single factory app (large), no OTA**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:29`

This is a variation of the default partition table, that expands
the 1MB app partition size to 1.5MB to fit more code.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_singleapp_large.csv

This partition table is not suitable for an app that needs OTA
(over the air update) capability.

---

## CONFIG_PARTITION_TABLE_SINGLE_APP_LARGE_ENC_NVS

**Single factory app (large), no OTA, encrypted NVS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:81`

This is a variation of the "Single factory app (large), no OTA" partition table
that supports encrypted NVS when using flash encryption. See the Flash Encryption section
in the ESP-IDF Programmers Guide for more information.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_singleapp_large_encr_nvs.csv

---

## CONFIG_PARTITION_TABLE_SINGLE_APP_TEE

**Single factory app, no OTA, TEE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:103`

This is a variation of the default "Single factory app, no OTA" partition table
that supports the ESP-TEE framework. See the Trusted Execution Environment (TEE) section
in the ESP-IDF Programmers Guide for more information.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_singleapp_tee.csv

---

## CONFIG_PARTITION_TABLE_TWO_OTA

**Factory app, two OTA definitions**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:41`

This is a basic OTA-enabled partition table with a factory app
partition plus two OTA app partitions. All are 1MB, so this
partition table requires 4MB or larger flash size.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_two_ota.csv

---

## CONFIG_PARTITION_TABLE_TWO_OTA_ENCRYPTED_NVS

**Factory app, two OTA definitions, encrypted NVS**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:92`

This is a variation of the "Factory app, two OTA definitions" partition table
that supports encrypted NVS when using flash encryption. See the Flash Encryption section
in the ESP-IDF Programmers Guide for more information.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_two_ota_encr_nvs.csv

---

## CONFIG_PARTITION_TABLE_TWO_OTA_LARGE

**Two large size OTA partitions**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:51`

This is a basic OTA-enabled partition table with
two OTA app partitions. Both app partition sizes are 1700K,
so this partition table requires 4MB or larger flash size.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_two_ota_large.csv

---

## CONFIG_PARTITION_TABLE_TWO_OTA_TEE

**Two OTA definitions, TEE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Partition Table > Partition Table
- **Kconfig:** `/opt/esp/idf/components/partition_table/Kconfig.projbuild:114`

This is a basic OTA-enabled partition table with two OTA app partitions each
for the TEE and the user (REE) application. The user app partition sizes are 1536K,
so this partition table requires 4MB or larger flash size. See the
Trusted Execution Environment (TEE) section in the ESP-IDF Programmers Guide
for more information.

The corresponding CSV file in the IDF directory is
components/partition_table/partitions_two_ota_tee.csv

---

## CONFIG_PCM5102A_MUTE_PIN

**Master mute/unmute for PCM5102A**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > TI PCM5102A interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:293`

GPIO number to control mute/unmute.
This is labeled "X(S)MT" on chip/boards

---

## CONFIG_PCM51XX_MUTE_PIN

**Master mute/unmute GPIO for PCM51XX**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > TI PCM51XX interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:304`

GPIO number to control mute/unmute pin (hardware mute).
Set to 255 to disable hardware mute pin control.
When enabled, this pin will be set HIGH on unmute and LOW on mute.
This is in addition to the I2C register-based mute control.

---

## CONFIG_PCNT_CTRL_FUNC_IN_IRAM

**Place PCNT control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:PCNT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_pcnt/Kconfig:3`

Place PCNT control functions (like start/stop) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.
Enabling this option can improve driver performance as well.

---

## CONFIG_PCNT_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:PCNT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_pcnt/Kconfig:18`

whether to enable the debug log message for PCNT driver.
Note that, this option only controls the PCNT driver log, won't affect other drivers.

---

## CONFIG_PCNT_ISR_IRAM_SAFE

**PCNT ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:PCNT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_pcnt/Kconfig:11`

Ensure the PCNT interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_PCNT_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy PCNT Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:175`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_PCNT_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy PCNT Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:167`

whether to suppress the deprecation warnings when using legacy PCNT driver (driver/pcnt.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_PIN_DAC_PWDN

**Power down pin**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > TAS5805M interface configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:188`

GPIO number to power down the DAC

---

## CONFIG_PM_CHECK_SLEEP_RETENTION_FRAME

- **Type:** `string`
- **Default:** `n`

This option is invisible to users, and it is only used for ci testing,
enabling it in the application will increase the sleep and wake-up time overhead

---

## CONFIG_PM_DFS_INIT_AUTO

**Enable dynamic frequency scaling (DFS) at startup**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management > Support for power management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:24`

If enabled, startup code configures dynamic frequency scaling.
Max CPU frequency is set to DEFAULT_CPU_FREQ_MHZ setting,
min frequency is set to XTAL frequency.
If disabled, DFS will not be active until the application
configures it using esp_pm_configure function.

---

## CONFIG_PM_ENABLE

**Support for power management**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:12`

If enabled, application is compiled with support for power management.
This option has run-time overhead (increased interrupt latency,
longer time to enter idle state), and it also reduces accuracy of
RTOS ticks and timers used for timekeeping.
Enable this option if application uses power management APIs.

---

## CONFIG_PM_LIGHTSLEEP_RTC_OSC_CAL_INTERVAL

**Calibrate the RTC_FAST/SLOW clock every N times of light sleep**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:106`

The value of this option determines the calibration interval of the RTC_FAST/SLOW clock during sleep when
power management is enabled. When it is configured as N, the RTC_FAST/SLOW clock will be calibrated
every N times of lightsleep.
Decreasing this value will increase the time the chip is in the active state, thereby increasing the
average power consumption of the chip.
Increasing this value can reduce the average power consumption, but when the external environment changes
drastically and the chip RTC_FAST/SLOW oscillator frequency drifts, it may cause system instability.

---

## CONFIG_PM_LIGHT_SLEEP_CALLBACKS

**Enable registration of pm light sleep callbacks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:197`

If enabled, it allows user to register entry and exit callbacks which are called before and after
entering auto light sleep.

NOTE: These callbacks are executed from the IDLE task context hence you cannot have any blocking calls
in your callbacks.

NOTE: Enabling these callbacks may change sleep duration calculations based on time spent in callback and
hence it is highly recommended to keep them as short as possible

---

## CONFIG_PM_POWER_DOWN_CPU_IN_LIGHT_SLEEP

**Power down CPU in light sleep**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:120`

If enabled, the CPU will be powered down in light sleep,
ESP chips supports saving and restoring CPU's running context before and after light sleep,
the feature provides applications with seamless CPU powerdowned lightsleep without user awareness.
But this will takes up some internal memory.
On esp32c3 soc, enabling this option will consume 1.68 KB of internal RAM
and will reduce sleep current consumption by about 100 uA.
On esp32s3 soc, enabling this option will consume 8.58 KB of internal RAM
and will reduce sleep current consumption by about 650 uA.

---

## CONFIG_PM_POWER_DOWN_PERIPHERAL_IN_LIGHT_SLEEP

**Power down Digital Peripheral in light sleep (EXPERIMENTAL)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:154`

If enabled, digital peripherals will try to powered down in light sleep, then all related peripherals will
not be available during sleep, including wake-up sources from the peripherals (For detailed availability
information, see the note of the corresponding wakeup source enable function).
The chip will automatically save/restore register context during sleep/wakeup to make the upper layer
user unaware of the peripheral powerdown during sleep. Enabling this option will increase static RAM and
heap usage but will also significantly reduce power.
consumption during lightsleep, the actual memory cost depends on the peripherals you have initialized,
for specific power consumption data in this mode, please refer to Electrical Characteristics section
in the chip datasheet.
(In order to save/restore the context of the necessary hardware for FreeRTOS to run, it will need
at least 4.55 KB free heap at sleep time. Otherwise sleep will not power down the peripherals.)

Note1: Please use this option with caution, the current IDF does not support the retention of
all peripherals. When the digital peripherals are powered off and a sleep and wake-up is completed,
the peripherals that have not saved the running context are equivalent to performing a reset.
!!! Please confirm the peripherals used in your application and their sleep retention support status
before enabling this option, peripherals sleep retention driver support status is tracked in
power_management.rst

Note2: When this option is enabled simultaneously with FREERTOS_USE_TICKLESS_IDLE, since the UART will
be powered down, the uart FIFO will be flushed before sleep to avoid data loss, however, this has the
potential to block the sleep process and cause the wakeup time to be skipped, which will cause the tick
of freertos to not be compensated correctly when returning from sleep and cause the system to crash.
To avoid this, you can increase FREERTOS_IDLE_TIME_BEFORE_SLEEP threshold in menuconfig.

Note3: Enabling this option does not necessarily mean that the peripheral power domain will be
turned down during sleep. The control priority of `esp_sleep_pd_config` is higher than this option,
user code can still prevent the peripheral power domain from powering down during sleep by
`esp_sleep_pd_config(ESP_PD_DOMAIN_TOP, ESP_PD_OPTION_ON)`. In addition, whether the peripheral power
domain is powered down during sleep also depends on the sleep working strategy selected by the driver.
If any module belonging to the peripheral power domain chooses not to be powered down during sleep,
then the peripheral power domain will not be powered off either.

---

## CONFIG_PM_PROFILING

**Enable profiling counters for PM locks**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management > Support for power management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:35`

If enabled, esp_pm_* functions will keep track of the amount of time
each of the power management locks has been held, and esp_pm_dump_locks
function will print this information.
This feature can be used to analyze which locks are preventing the chip
from going into a lower power state, and see what time the chip spends
in each power saving mode. This feature does incur some run-time
overhead, so should typically be disabled in production builds.

---

## CONFIG_PM_RESTORE_CACHE_TAGMEM_AFTER_LIGHT_SLEEP

**Restore I/D-cache tag memory after power down CPU light sleep**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Power Management > Power down CPU in light sleep
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:135`

Cache tag memory and CPU both belong to the CPU power domain.
ESP chips supports saving and restoring Cache tag memory before and after sleep,
this feature supports accesses to the external memory that was cached before sleep still
be cached when the CPU wakes up from a powerdowned CPU lightsleep.
This option controls the restore method for Cache tag memory in lightsleep.
If this option is enabled, the I/D-cache tag memory will be backuped to the internal RAM
before sleep and restored upon wakeup.
Depending on the the cache configuration, if this option is enabled,
it will consume up to 9 KB of internal RAM.
If this option is disabled, all cached data won't be kept after sleep,
the DCache will be writeback before sleep and invalid all cached data after sleep,
all accesses to external memory(Flash/PSRAM) will be cache missed after waking up,
resulting in performance degradation due to increased memory accesses latency.

---

## CONFIG_PM_RTOS_IDLE_OPT

**Put RTOS IDLE related codes in internal RAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:70`

If enabled, about 180Bytes of RTOS_IDLE related source code would be in IRAM and chip would sleep
longer for 20us at 160MHz CPU frequency most each time.
This feature is intended to be used when lower power consumption is needed
while there is enough place in IRAM to place source code.

---

## CONFIG_PM_SLEEP_FUNC_IN_IRAM

**Place Power Management module functions in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:3`

---

## CONFIG_PM_SLP_DEFAULT_PARAMS_OPT

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_PM_SLP_DISABLE_GPIO

**Disable all GPIO when chip at sleep**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:79`

This feature is intended to disable all GPIO pins at automantic sleep to get a lower power mode.
If enabled, chips will disable all GPIO pins at automantic sleep to reduce about 200~300 uA current.
If you want to specifically use some pins normally as chip wakes when chip sleeps,
you can call 'gpio_sleep_sel_dis' to disable this feature on those pins.
You can also keep this feature on and call 'gpio_sleep_set_direction' and 'gpio_sleep_set_pull_mode'
to have a different GPIO configuration at sleep.
Warning: If you want to enable this option on ESP32, you should enable `GPIO_ESP32_SUPPORT_SWITCH_SLP_PULL`
at first, otherwise you will not be able to switch pullup/pulldown mode.

---

## CONFIG_PM_SLP_IRAM_OPT

**Put lightsleep related codes in internal RAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Power Management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:60`

If enabled, about 2.1KB of lightsleep related source code would be in IRAM and chip would sleep
longer for 310us at 160MHz CPU frequency most each time.
This feature is intended to be used when lower power consumption is needed
while there is enough place in IRAM to place source code.

---

## CONFIG_PM_TRACE

**Enable debug tracing of PM using GPIOs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Power Management > Support for power management
- **Kconfig:** `/opt/esp/idf/components/esp_pm/Kconfig:48`

If enabled, some GPIOs will be used to signal events such as RTOS ticks,
frequency switching, entry/exit from idle state. Refer to pm_trace.c
file for the list of GPIOs.
This feature is intended to be used when analyzing/debugging behavior
of power management implementation, and should be kept disabled in
applications.

---

## CONFIG_PM_UPDATE_CCOMPARE_HLI_WORKAROUND

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_PT8211_MUTE_ACTIVE_LOW

**Mute active LOW**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Audio Board > Custom Audio Board > PT8211 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:340`

Output LOW instead of HIGH on mute/unmute pin when in mute state.

---

## CONFIG_PT8211_MUTE_PIN

**Master mute/unmute**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Audio Board > Custom Audio Board > PT8211 interface Configuration
- **Kconfig:** `components/audio_board/Kconfig.projbuild:333`

GPIO number to control mute/unmute.
                Note that PT8211 does not have a mute control pin. This output is intended to control external circuit (e.g., amplifier).

---

## CONFIG_PTHREAD_DEFAULT_CORE_0

**Core 0**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PThreads > Default pthread core affinity
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:31`

---

## CONFIG_PTHREAD_DEFAULT_CORE_1

**Core 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > PThreads > Default pthread core affinity
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:33`

---

## CONFIG_PTHREAD_DEFAULT_CORE_NO_AFFINITY

**No affinity**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > PThreads > Default pthread core affinity
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:29`

---

## CONFIG_PTHREAD_STACK_MIN

**Minimum allowed pthread stack size**

- **Type:** `unknown`
- **Default:** `768`
- **Menu:** Component config > PThreads
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:16`

Minimum allowed pthread stack size set in attributes passed to pthread_create

---

## CONFIG_PTHREAD_TASK_CORE_DEFAULT

- **Type:** `unknown`
- **Default:** `-1`

---

## CONFIG_PTHREAD_TASK_NAME_DEFAULT

**Default name of pthreads**

- **Type:** `unknown`
- **Default:** `pthread`
- **Menu:** Component config > PThreads
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:43`

The default name of pthreads.

---

## CONFIG_PTHREAD_TASK_PRIO_DEFAULT

**Default task priority**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > PThreads
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:3`

Priority used to create new tasks with default pthread parameters.

---

## CONFIG_PTHREAD_TASK_STACK_SIZE_DEFAULT

**Default task stack size**

- **Type:** `unknown`
- **Default:** `3072`
- **Menu:** Component config > PThreads
- **Kconfig:** `/opt/esp/idf/components/pthread/Kconfig:10`

Stack size used to create new tasks with default pthread parameters.

---

## CONFIG_RINGBUF_PLACE_FUNCTIONS_INTO_FLASH

**Place non-ISR ringbuf functions into flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Ringbuf
- **Kconfig:** `/opt/esp/idf/components/esp_ringbuf/Kconfig:4`

Place non-ISR ringbuf functions (like xRingbufferCreate/xRingbufferSend) into flash.
This frees up IRAM, but the functions can no longer be called when the cache is disabled.

---

## CONFIG_RINGBUF_PLACE_ISR_FUNCTIONS_INTO_FLASH

**Place ISR ringbuf functions into flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP Ringbuf > Place non-ISR ringbuf functions into flash
- **Kconfig:** `/opt/esp/idf/components/esp_ringbuf/Kconfig:12`

Place ISR ringbuf functions (like xRingbufferSendFromISR/xRingbufferReceiveFromISR) into flash.
This frees up IRAM, but the functions can no longer be called when the cache is disabled
or from an IRAM interrupt context.

This option is not compatible with ESP-IDF drivers which are configured to
run the ISR from an IRAM context, e.g. CONFIG_UART_ISR_IN_IRAM.

---

## CONFIG_RMT_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:57`

If enabled, RMT driver component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_RMT_ENCODER_FUNC_IN_IRAM

**Place RMT encoder function in IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:4`

Place RMT encoder function into IRAM for better performance and fewer cache misses.

---

## CONFIG_RMT_ISR_IRAM_SAFE

**RMT ISR IRAM-Safe (Deprecated)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:67`

Ensure the RMT interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_RMT_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `y`

This will ensure the RMT object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_RMT_RECV_FUNC_IN_IRAM

**Place RMT receive function in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:24`

Place RMT receive function into IRAM for better performance and fewer cache misses.

---

## CONFIG_RMT_RX_ISR_CACHE_SAFE

**Allow RMT RX ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:41`

Enable this option to allow the RMT RX Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the RMT RX functionality is still required to operate correctly.

---

## CONFIG_RMT_RX_ISR_HANDLER_IN_IRAM

**Place RMT RX ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:17`

Place RMT RX ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_RMT_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy RMT Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:132`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_RMT_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy RMT Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:124`

Whether to suppress the deprecation warnings when using legacy rmt driver (driver/rmt.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_RMT_TX_ISR_CACHE_SAFE

**Allow RMT TX ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:31`

Enable this option to allow the RMT TX Interrupt Service Routine (ISR)
to execute even when the cache is disabled. This can be useful in scenarios where the cache
might be turned off, but the RMT TX functionality is still required to operate correctly.

---

## CONFIG_RMT_TX_ISR_HANDLER_IN_IRAM

**Place RMT TX ISR handler in IRAM to reduce latency**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:RMT Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_rmt/Kconfig:10`

Place RMT TX ISR handler in IRAM to reduce latency caused by cache miss.

---

## CONFIG_RTC_CLK_CAL_CYCLES

**Number of cycles for RTC_SLOW_CLK calibration**

- **Type:** `unknown`
- **Default:** `1024`
- **Menu:** Component config > Hardware Settings > RTC Clock Config
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.rtc:19`

When the startup code initializes RTC_SLOW_CLK, it can perform
calibration by comparing the RTC_SLOW_CLK frequency with main XTAL
frequency. This option sets the number of RTC_SLOW_CLK cycles measured
by the calibration routine. Higher numbers increase calibration
precision, which may be important for applications which spend a lot of
time in deep sleep. Lower numbers reduce startup time.

When this option is set to 0, clock calibration will not be performed at
startup, and approximate clock frequencies will be assumed:

- 150000 Hz if internal RC oscillator is used as clock source. For this use value 1024.
- 32768 Hz if the 32k crystal oscillator is used. For this use value 3000 or more.
    In case more value will help improve the definition of the launch of the crystal.
    If the crystal could not start, it will be switched to internal RC.

---

## CONFIG_RTC_CLK_SRC_EXT_CRYS

**External 32 kHz crystal**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > RTC Clock Config > RTC clock source
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.rtc:9`

---

## CONFIG_RTC_CLK_SRC_EXT_OSC

**External 32 kHz oscillator at 32K_XP pin**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > RTC Clock Config > RTC clock source
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.rtc:12`

---

## CONFIG_RTC_CLK_SRC_INT_8MD256

**Internal 17.5 MHz oscillator, divided by 256**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Hardware Settings > RTC Clock Config > RTC clock source
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.rtc:15`

---

## CONFIG_RTC_CLK_SRC_INT_RC

**Internal 136 kHz RC oscillator**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > RTC Clock Config > RTC clock source
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.rtc:7`

---

## CONFIG_SDM_CTRL_FUNC_IN_IRAM

**Place SDM control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Sigma Delta Modulator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_sdm/Kconfig:3`

Place SDM control functions (like set_duty) into IRAM,
so that these functions can be IRAM-safe and able to be called in the other IRAM interrupt context.
Enabling this option can improve driver performance as well.

---

## CONFIG_SDM_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Sigma Delta Modulator Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_sdm/Kconfig:11`

whether to enable the debug log message for SDM driver.
Note that, this option only controls the SDM driver log, won't affect other drivers.

---

## CONFIG_SDM_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy SDM Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:192`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_SDM_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy SDM Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:184`

whether to suppress the deprecation warnings when using legacy SDM driver (driver/sigmadelta.h).
If you want to continue using the legacy driver, and don't want to see related deprecation warnings,
you can enable this option.

---

## CONFIG_SECURE_BOOT

**Enable hardware Secure Boot in bootloader (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:606`

Build a bootloader which enables Secure Boot on first boot.

Once enabled, Secure Boot will not boot a modified bootloader. The bootloader will only load a partition
table or boot an app if the data has a verified digital signature. There are implications for reflashing
updated apps once secure boot is enabled.

When enabling secure boot, JTAG and ROM BASIC Interpreter are permanently disabled by default.

---

## CONFIG_SECURE_BOOTLOADER_KEY_ENCODING_192BIT

**3/4 encoding (192 bit key)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Hardware Key Encoding
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:805`

---

## CONFIG_SECURE_BOOTLOADER_KEY_ENCODING_256BIT

**No encoding (256 bit key)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Hardware Key Encoding
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:802`

---

## CONFIG_SECURE_BOOTLOADER_ONE_TIME_FLASH

**One-time flash**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Secure bootloader mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:659`

On first boot, the bootloader will generate a key which is not readable externally or by software. A
digest is generated from the bootloader image itself. This digest will be verified on each subsequent
boot.

Enabling this option means that the bootloader cannot be changed after the first time it is booted.

---

## CONFIG_SECURE_BOOTLOADER_REFLASHABLE

**Reflashable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Secure bootloader mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:668`

Generate a reusable secure bootloader key, derived (via SHA-256) from the secure boot signing key.

This allows the secure bootloader to be re-flashed by anyone with access to the secure boot signing
key.

This option is less secure than one-time flash, because a leak of the digest key from one device
allows reflashing of any device that uses it.

---

## CONFIG_SECURE_BOOT_ALLOW_JTAG

**Allow JTAG Debugging**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:922`

If not set (default), the bootloader will permanently disable JTAG (across entire chip) on first boot
when either secure boot or flash encryption is enabled.

Setting this option leaves JTAG on for debugging, which negates all protections of flash encryption
and some of the protections of secure boot.

Only set this option in testing environments.

---

## CONFIG_SECURE_BOOT_ALLOW_ROM_BASIC

**Leave ROM BASIC Interpreter available on reset**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:908`

By default, the BASIC ROM Console starts on reset if no valid bootloader is
read from the flash.

When either flash encryption or secure boot are enabled, the default is to
disable this BASIC fallback mode permanently via eFuse.

If this option is set, this eFuse is not burned and the BASIC ROM Console may
remain accessible.  Only set this option in testing environments.

---

## CONFIG_SECURE_BOOT_ALLOW_SHORT_APP_PARTITION

**Allow app partition length not 64KB aligned**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:936`

If not set (default), app partition size must be a multiple of 64KB. App images are padded to 64KB
length, and the bootloader checks any trailing bytes after the signature (before the next 64KB
boundary) have not been written. This is because flash cache maps entire 64KB pages into the address
space. This prevents an attacker from appending unverified data after the app image in the flash,
causing it to be mapped into the address space.

Setting this option allows the app partition length to be unaligned, and disables padding of the app
image to this length. It is generally not recommended to set this option, unless you have a legacy
partitioning scheme which doesn't support 64KB aligned partition lengths.

---

## CONFIG_SECURE_BOOT_ALLOW_UNUSED_DIGEST_SLOTS

**Leave unused digest slots available (not revoke)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:950`

If not set (default), during startup in the app all unused digest slots will be revoked.
To revoke unused slot will be called esp_efuse_set_digest_revoke(num_digest) for each digest.
Revoking unused digest slots makes ensures that no trusted keys can be added later by an attacker.
If set, it means that you have a plan to use unused digests slots later.

Note that if you plan to enable secure boot during the first boot up, the bootloader will intentionally
revoke the unused digest slots while enabling secure boot, even if the above config is enabled because
keeping the unused key slots un-revoked would a security hazard.
In case for any development workflow if you need to avoid this revocation, you should enable
secure boot externally (host based mechanism) rather than enabling it during the boot up,
so that the bootloader would not need to enable secure boot and thus you could avoid its revocation
strategy.

---

## CONFIG_SECURE_BOOT_BUILD_SIGNED_BINARIES

**Sign binaries during build**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:681`

Once secure boot or signed app requirement is enabled, app images are required to be signed.

If enabled (default), these binary files are signed as part of the build process. The file named in
"Secure boot private signing key" will be used to sign the image.

If disabled, unsigned app/partition data will be built. They must be signed manually using espsecure.py.
Version 1 to enable ECDSA Based Secure Boot and Version 2 to enable RSA based Secure Boot.
(for example, on a remote signing server.)

---

## CONFIG_SECURE_BOOT_ECDSA_KEY_LEN_192_BITS

**Using ECC curve NISTP192**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > ECDSA key size
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:566`

---

## CONFIG_SECURE_BOOT_ECDSA_KEY_LEN_256_BITS

**Using ECC curve NISTP256 (Recommended)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > ECDSA key size
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:570`

---

## CONFIG_SECURE_BOOT_ECDSA_KEY_LEN_384_BITS

**Using ECC curve NISTP384 (Recommended)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > ECDSA key size
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:574`

---

## CONFIG_SECURE_BOOT_ENABLE_AGGRESSIVE_KEY_REVOKE

**Enable Aggressive key revoke strategy**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:727`

If this option is set, ROM bootloader will revoke the public key digest burned in efuse block
if it fails to verify the signature of software bootloader with it.
Revocation of keys does not happen when enabling secure boot. Once secure boot is enabled,
key revocation checks will be done on subsequent boot-up, while verifying the software bootloader

This feature provides a strong resistance against physical attacks on the device.

NOTE: Once a digest slot is revoked, it can never be used again to verify an image
This can lead to permanent bricking of the device, in case all keys are revoked
because of signature verification failure.

---

## CONFIG_SECURE_BOOT_FLASH_BOOTLOADER_DEFAULT

**Flash bootloader along with other artifacts when using the default flash command**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:770`

When Secure Boot V2 is enabled, by default the bootloader is not flashed along with other artifacts
like the application and the partition table images, i.e. bootloader has to be separately flashed
using the command `idf.py bootloader flash`, whereas, the application and partition table can be flashed
using the command `idf.py flash` itself.
Enabling this option allows flashing the bootloader along with the other artifacts
by invocation of the command `idf.py flash`.

If this option is enabled make sure that even the bootloader is signed using the correct secure boot key,
otherwise the bootloader signature verification would fail, as hash of the public key which is present in
the bootloader signature would not match with the digest stored into the efuses
and thus the device will not be able to boot up.

---

## CONFIG_SECURE_BOOT_FLASH_ENC_KEYS_BURN_TOGETHER

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_BOOT_INSECURE

**Allow potentially insecure options**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:810`

You can disable some of the default protections offered by secure boot, in order to enable testing or a
custom combination of security features.

Only enable these options if you are very sure.

Refer to the Secure Boot section of the ESP-IDF Programmer's Guide for this version before enabling.

---

## CONFIG_SECURE_BOOT_SIGNING_KEY

**Secure boot private signing key**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Security features > Sign binaries during build
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:695`

Path to the key file used to sign app images.

Key file is an ECDSA private key (NIST256p curve) in PEM format for Secure Boot V1.
Key file is an RSA private key in PEM format for Secure Boot V2.

Path is evaluated relative to the project directory.

You can generate a new signing key by running the following command:
espsecure.py generate_signing_key secure_boot_signing_key.pem

See the Secure Boot section of the ESP-IDF Programmer's Guide for this version for details.

---

## CONFIG_SECURE_BOOT_V1_ENABLED

**Enable Secure Boot version 1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable hardware Secure Boot in bootloader (READ DOCS FIRST) > Select secure boot version
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:638`

Build a bootloader which enables secure boot version 1 on first boot.
Refer to the Secure Boot section of the ESP-IDF Programmer's Guide for this version before enabling.

---

## CONFIG_SECURE_BOOT_V1_SUPPORTED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_BOOT_V2_ALLOW_EFUSE_RD_DIS

**Do not disable the ability to further read protect eFuses**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:743`

If not set (default, recommended), on first boot the bootloader will burn the WR_DIS_RD_DIS
efuse when Secure Boot is enabled. This prevents any more efuses from being read protected.

If this option is set, it will remain possible to write the EFUSE_RD_DIS efuse field after Secure
Boot is enabled. This may allow an attacker to read-protect the BLK2 efuse (for ESP32) and
BLOCK4-BLOCK10 (i.e. BLOCK_KEY0-BLOCK_KEY5)(for other chips) holding the secure boot public key digest,
causing an immediate denial of service and possibly allowing an additional fault injection attack to
bypass the signature protection.

The option must be set when you need to program any read-protected key type into the efuses,
e.g., HMAC, ECDSA etc. after secure boot has already been enabled on the device.
Please refer to secure boot V2 documentation guide for more details.

NOTE: Once a BLOCK is read-protected, the application will read all zeros from that block

NOTE: If "UART ROM download mode (Permanently disabled (recommended))" or
"UART ROM download mode (Permanently switch to Secure mode (recommended))" is set,
then it is __NOT__ possible to read/write efuses using espefuse.py utility.
However, efuse can be read/written from the application

Please refer to the Secure Boot V2 documentation guide for more information.

---

## CONFIG_SECURE_BOOT_V2_ECC_SUPPORTED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_BOOT_V2_ECDSA_ENABLED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_BOOT_V2_ENABLED

**Enable Secure Boot version 2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable hardware Secure Boot in bootloader (READ DOCS FIRST) > Select secure boot version
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:645`

Build a bootloader which enables Secure Boot version 2 on first boot.
Refer to Secure Boot V2 section of the ESP-IDF Programmer's Guide for this version before enabling.

---

## CONFIG_SECURE_BOOT_V2_PREFERRED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SECURE_BOOT_V2_RSA_ENABLED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_BOOT_V2_RSA_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SECURE_BOOT_VERIFICATION_KEY

**Secure boot public signature verification key**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:712`

Path to a public key file used to verify signed images.
Secure Boot V1: This ECDSA public key is compiled into the bootloader and/or
app, to verify app images.

Key file is in raw binary format, and can be extracted from a
PEM formatted private key using the espsecure.py
extract_public_key command.

Refer to the Secure Boot section of the ESP-IDF Programmer's Guide for this version before enabling.

---

## CONFIG_SECURE_DISABLE_ROM_DL_MODE

**UART ROM download mode (Permanently disabled (recommended))**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > UART ROM download mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1130`

If set, during startup the app will burn an eFuse bit to permanently disable the UART ROM
Download Mode. This prevents any future use of esptool.py, espefuse.py and similar tools.

Once disabled, if the SoC is booted with strapping pins set for ROM Download Mode
then an error is printed instead.

It is recommended to enable this option in any production application where Flash
Encryption and/or Secure Boot is enabled and access to Download Mode is not required.

It is also possible to permanently disable Download Mode by calling
esp_efuse_disable_rom_download_mode() at runtime.

---

## CONFIG_SECURE_ENABLE_SECURE_ROM_DL_MODE

**UART ROM download mode (Permanently switch to Secure mode (recommended))**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > UART ROM download mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1145`

If set, during startup the app will burn an eFuse bit to permanently switch the UART ROM
Download Mode into a separate Secure Download mode. This option can only work if
Download Mode is not already disabled by eFuse.

Secure Download mode limits the use of Download Mode functions to update SPI config,
changing baud rate, basic flash write and a command to return a summary of currently
enabled security features (`get_security_info`).

Secure Download mode is not compatible with the esptool.py flasher stub feature,
espefuse.py, read/writing memory or registers, encrypted download, or any other
features that interact with unsupported Download Mode commands.

Secure Download mode should be enabled in any application where Flash Encryption
and/or Secure Boot is enabled. Disabling this option does not immediately cancel
the benefits of the security features, but it increases the potential "attack
surface" for an attacker to try and bypass them with a successful physical attack.

It is also possible to enable secure download mode at runtime by calling
esp_efuse_enable_rom_secure_download_mode()

Note: Secure Download mode is not available for ESP32.

---

## CONFIG_SECURE_ENABLE_TEE

**Enable the ESP-TEE framework**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment)
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:4`

This configuration enables the Trusted Execution Environment (TEE) feature.

---

## CONFIG_SECURE_FLASH_CHECK_ENC_EN_IN_APP

**Check Flash Encryption enabled on app startup**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1069`

If set (default), in an app during startup code,
there is a check of the flash encryption eFuse bit is on
(as the bootloader should already have set it).
The app requires this bit is on to continue work otherwise abort.

If not set, the app does not care if the flash encryption eFuse bit is set or not.

---

## CONFIG_SECURE_FLASH_ENCRYPTION_AES128

**AES-128 (256-bit key)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable flash encryption on boot (READ DOCS FIRST) > Size of generated XTS-AES key
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:854`

---

## CONFIG_SECURE_FLASH_ENCRYPTION_AES128_DERIVED

**AES-128 key derived from 128 bits (SHA256(128 bits))**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable flash encryption on boot (READ DOCS FIRST) > Size of generated XTS-AES key
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:850`

---

## CONFIG_SECURE_FLASH_ENCRYPTION_AES256

**AES-256 (512-bit key)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable flash encryption on boot (READ DOCS FIRST) > Size of generated XTS-AES key
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:858`

---

## CONFIG_SECURE_FLASH_ENCRYPTION_MODE_DEVELOPMENT

**Development (NOT SECURE)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable flash encryption on boot (READ DOCS FIRST) > Enable usage mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:882`

---

## CONFIG_SECURE_FLASH_ENCRYPTION_MODE_RELEASE

**Release**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Enable flash encryption on boot (READ DOCS FIRST) > Enable usage mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:886`

---

## CONFIG_SECURE_FLASH_ENCRYPT_ONLY_IMAGE_LEN_IN_APP_PART

**Encrypt only the app image that is present in the partition of type app**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1053`

If set (default), optimise encryption time for the partition of type APP,
by only encrypting the app image that is present in the partition,
instead of the whole partition.
The image length used for encryption is derived from the image metadata, which
includes the size of the app image, checksum, hash and also the signature sector
when secure boot is enabled.

If not set, the whole partition of type APP would be encrypted,
which increases the encryption time but might be useful if there
is any custom data appended to the firmware image.

---

## CONFIG_SECURE_FLASH_ENC_ENABLED

**Enable flash encryption on boot (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:822`

If this option is set, flash contents will be encrypted by the bootloader on first boot.

Note: After first boot, the system will be permanently encrypted. Re-flashing an encrypted
system is complicated and not always possible.

Read https://docs.espressif.com/projects/esp-idf/en/latest/security/flash-encryption.html
before enabling.

---

## CONFIG_SECURE_FLASH_HAS_WRITE_PROTECTION_CACHE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_FLASH_PSEUDO_ROUND_FUNC

**Permanently enable XTS-AES's pseudo rounds function**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1081`

If set (default), the bootloader will permanently enable the XTS-AES peripheral's pseudo rounds function.
Note: Enabling this config would burn an efuse.

---

## CONFIG_SECURE_FLASH_PSEUDO_ROUND_FUNC_STRENGTH

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_SECURE_FLASH_PSEUDO_ROUND_FUNC_STRENGTH_HIGH

**High**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Permanently enable XTS-AES's pseudo rounds function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1109`

---

## CONFIG_SECURE_FLASH_PSEUDO_ROUND_FUNC_STRENGTH_LOW

**Low**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Permanently enable XTS-AES's pseudo rounds function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1103`

---

## CONFIG_SECURE_FLASH_PSEUDO_ROUND_FUNC_STRENGTH_MEDIUM

**Medium**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Permanently enable XTS-AES's pseudo rounds function > Strength of the pseudo rounds function
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1106`

---

## CONFIG_SECURE_FLASH_REQUIRE_ALREADY_ENABLED

**Require flash encryption to be already enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1002`

If not set (default), and flash encryption is not yet enabled in eFuses, the 2nd stage bootloader
will enable flash encryption: generate the flash encryption key and program eFuses.
If this option is set, and flash encryption is not yet enabled, the bootloader will error out and
reboot.
If flash encryption is enabled in eFuses, this option does not change the bootloader behavior.

Only use this option in testing environments, to avoid accidentally enabling flash encryption on
the wrong device. The device needs to have flash encryption already enabled using espefuse.py.

---

## CONFIG_SECURE_FLASH_SKIP_WRITE_PROTECTION_CACHE

**Skip write-protection of DIS_CACHE (DIS_ICACHE, DIS_DCACHE)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1016`

If not set (default, recommended), on the first boot the bootloader will burn the write-protection of
DIS_CACHE(for ESP32) or DIS_ICACHE/DIS_DCACHE(for other chips) eFuse when Flash Encryption is enabled.
Write protection for cache disable efuse prevents the chip from being blocked if it is set by accident.
App and bootloader use cache so disabling it makes the chip useless for IDF.
Due to other eFuses are linked with the same write protection bit (see the list below) then
write-protection will not be done if these SECURE_FLASH_UART_BOOTLOADER_ALLOW_ENC,
SECURE_BOOT_ALLOW_JTAG or SECURE_FLASH_UART_BOOTLOADER_ALLOW_CACHE options are selected
to give a chance to turn on the chip into the release mode later.

List of eFuses with the same write protection bit:
ESP32: MAC, MAC_CRC, DISABLE_APP_CPU, DISABLE_BT, DIS_CACHE, VOL_LEVEL_HP_INV.

ESP32-C3: DIS_ICACHE, DIS_USB_JTAG, DIS_DOWNLOAD_ICACHE, DIS_USB_SERIAL_JTAG,
DIS_FORCE_DOWNLOAD, DIS_TWAI, JTAG_SEL_ENABLE, DIS_PAD_JTAG, DIS_DOWNLOAD_MANUAL_ENCRYPT.

ESP32-C6: SWAP_UART_SDIO_EN, DIS_ICACHE, DIS_USB_JTAG, DIS_DOWNLOAD_ICACHE,
DIS_USB_SERIAL_JTAG, DIS_FORCE_DOWNLOAD, DIS_TWAI, JTAG_SEL_ENABLE,
DIS_PAD_JTAG, DIS_DOWNLOAD_MANUAL_ENCRYPT.

ESP32-H2 & ESP32H21: DIS_ICACHE, DIS_ICACHE, DIS_USB_JTAG, POWERGLITCH_EN, DIS_FORCE_DOWNLOAD,
SPI_DOWNLOAD_MSPI_DIS, DIS_TWAI, JTAG_SEL_ENABLE, DIS_PAD_JTAG, DIS_DOWNLOAD_MANUAL_ENCRYPT,
DIS_USB_SERIAL_JTAG

ESP32-S2: DIS_ICACHE, DIS_DCACHE, DIS_DOWNLOAD_ICACHE, DIS_DOWNLOAD_DCACHE,
DIS_FORCE_DOWNLOAD, DIS_USB, DIS_TWAI, DIS_BOOT_REMAP, SOFT_DIS_JTAG,
HARD_DIS_JTAG, DIS_DOWNLOAD_MANUAL_ENCRYPT.

ESP32-S3: DIS_ICACHE, DIS_DCACHE, DIS_DOWNLOAD_ICACHE, DIS_DOWNLOAD_DCACHE,
DIS_FORCE_DOWNLOAD, DIS_USB_OTG, DIS_TWAI, DIS_APP_CPU, DIS_PAD_JTAG,
DIS_DOWNLOAD_MANUAL_ENCRYPT, DIS_USB_JTAG, DIS_USB_SERIAL_JTAG, STRAP_JTAG_SEL, USB_PHY_SEL.

---

## CONFIG_SECURE_FLASH_UART_BOOTLOADER_ALLOW_CACHE

**Leave UART bootloader flash cache enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:990`

If not set (default), the bootloader will permanently disable UART bootloader flash cache access on
first boot. If set, the UART bootloader will still be able to access the flash cache.

Only set this option in testing environments.

---

## CONFIG_SECURE_FLASH_UART_BOOTLOADER_ALLOW_DEC

**Leave UART bootloader decryption enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:979`

If not set (default), the bootloader will permanently disable UART bootloader decryption access on
first boot. If set, the UART bootloader will still be able to access hardware decryption.

Only set this option in testing environments. Setting this option allows complete bypass of flash
encryption.

---

## CONFIG_SECURE_FLASH_UART_BOOTLOADER_ALLOW_ENC

**Leave UART bootloader encryption enabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > Potentially insecure options
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:968`

If not set (default), the bootloader will permanently disable UART bootloader encryption access on
first boot. If set, the UART bootloader will still be able to access hardware encryption.

It is recommended to only set this option in testing environments.

---

## CONFIG_SECURE_INSECURE_ALLOW_DL_MODE

**UART ROM download mode (Enabled (not recommended))**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > UART ROM download mode
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:1172`

This is a potentially insecure option.
Enabling this option will allow the full UART download mode to stay enabled.
This option SHOULD NOT BE ENABLED for production use cases.

---

## CONFIG_SECURE_ROM_DL_MODE_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SECURE_SIGNED_APPS

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_SIGNED_APPS_ECDSA_SCHEME

**ECDSA**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > App Signing Scheme
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:527`

Embeds the ECDSA public key in the bootloader and signs the application with an ECDSA key.
Refer to the documentation before enabling.

---

## CONFIG_SECURE_SIGNED_APPS_ECDSA_V2_SCHEME

**ECDSA (V2)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > App Signing Scheme
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:542`

For Secure boot V2 (e.g., ESP32-C2 SoC), appends ECDSA based signature block to the application.
Refer to documentation before enabling.

---

## CONFIG_SECURE_SIGNED_APPS_NO_SECURE_BOOT

**Require signed app images**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:497`

Require apps to be signed to verify their integrity.

This option uses the same app signature scheme as hardware secure boot, but unlike hardware secure boot it
does not prevent the bootloader from being physically updated. This means that the device can be secured
against remote network access, but not physical access. Compared to using hardware Secure Boot this option
is much simpler to implement.

---

## CONFIG_SECURE_SIGNED_APPS_RSA_SCHEME

**RSA**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features > App Signing Scheme
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:534`

Appends the RSA-3072 based Signature block to the application.
Refer to <Secure Boot Version 2 documentation link> before enabling.

---

## CONFIG_SECURE_SIGNED_ON_BOOT

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_SIGNED_ON_BOOT_NO_SECURE_BOOT

**Bootloader verifies app signatures**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:580`

If this option is set, the bootloader will be compiled with code to verify that an app is signed before
booting it.

If hardware secure boot is enabled, this option is always enabled and cannot be disabled.
If hardware secure boot is not enabled, this option doesn't add significant security by itself so most
users will want to leave it disabled.

---

## CONFIG_SECURE_SIGNED_ON_UPDATE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SECURE_SIGNED_ON_UPDATE_NO_SECURE_BOOT

**Verify app signature on update**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Security features
- **Kconfig:** `/opt/esp/idf/components/bootloader/Kconfig.projbuild:592`

If this option is set, any OTA updated apps will have the signature verified before being considered valid.

When enabled, the signature is automatically checked whenever the esp_ota_ops.h APIs are used for OTA
updates, or esp_image_format.h APIs are used to verify apps.

If hardware secure boot is enabled, this option is always enabled and cannot be disabled.
If hardware secure boot is not enabled, this option still adds significant security against network-based
attackers by preventing spoofing of OTA updates.

---

## CONFIG_SECURE_TEE_ATTESTATION

**Enable Attestation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:98`

This configuration enables the support for the Attestation service.

---

## CONFIG_SECURE_TEE_ATT_KEY_STR_ID

**Attestation: Secure Storage key ID for EAT signing**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services > Enable Attestation
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:105`

This configuration sets the key ID from the TEE secure storage
storing the ECDSA keypair for executing sign/verify operations
from the TEE side for attestation.

---

## CONFIG_SECURE_TEE_DEBUG_MODE

**Enable Debug Mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:145`

This configuration enables the logging from the TEE module.

---

## CONFIG_SECURE_TEE_DRAM_SIZE

**DRAM region size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Memory Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:21`

This configuration sets the DRAM size for the TEE module.
This should be 256-byte (0x100) aligned.

---

## CONFIG_SECURE_TEE_DROM_SIZE

- **Type:** `unknown`
- **Default:** ``

This should be a multiple of MMU_PAGE_SIZE.

---

## CONFIG_SECURE_TEE_EXT_FLASH_MEMPROT_SPI1

**Memprot: Isolate TEE flash regions over SPI1**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:116`

This configuration restricts access to TEE-reserved regions in external flash
by making them inaccessible to the REE via the SPI1 interface (physical addresses).

With this enabled, all SPI flash read, write, or erase operations over SPI1 will
be routed through service calls to the TEE, introducing additional performance
overhead.

When Flash Encryption (SECURE_FLASH_ENC_ENABLED) is enabled, the REE can still
access TEE-related flash partitions over SPI1, but read operations will return
encrypted data contents. This prevents attackers from inferring the TEE contents
with direct reads.

Additionally, with Secure Boot enabled (SECURE_BOOT_V2_ENABLED), any unauthorized
modifications to the TEE firmware will be detected during boot, causing signature
verification to fail. Thus, these options provide a level of protection suitable for
most applications. However, while the TEE firmware integrity is protected, other TEE
partitions (Secure Storage, TEE OTA data) can be manipulated through direct writes.

Enable this option only when complete isolation of all TEE flash regions is required,
even with the associated performance tradeoffs.

Note: All accesses to the TEE partitions over SPI0 (i.e. the MMU) are blocked
unconditionally.

---

## CONFIG_SECURE_TEE_INTR_STACK_SIZE

**Interrupt Stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Memory Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:38`

This configuration sets the interrupt stack size for the TEE module.
The TEE interrupt stack will be allocated from the TEE DRAM region.
This should be 16-byte (0x10) aligned.

---

## CONFIG_SECURE_TEE_IRAM_SIZE

**IRAM region size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Memory Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:13`

This configuration sets the IRAM size for the TEE module.
This should be 256-byte (0x100) aligned.

---

## CONFIG_SECURE_TEE_IROM_SIZE

- **Type:** `unknown`
- **Default:** ``

This should be a multiple of MMU_PAGE_SIZE.

---

## CONFIG_SECURE_TEE_LOG_LEVEL

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_SECURE_TEE_LOG_LEVEL_DEBUG

**Debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:167`

---

## CONFIG_SECURE_TEE_LOG_LEVEL_ERROR

**Error**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:161`

---

## CONFIG_SECURE_TEE_LOG_LEVEL_INFO

**Info**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:165`

---

## CONFIG_SECURE_TEE_LOG_LEVEL_NONE

**No output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:159`

---

## CONFIG_SECURE_TEE_LOG_LEVEL_VERBOSE

**Verbose**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:169`

---

## CONFIG_SECURE_TEE_LOG_LEVEL_WARN

**Warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Enable Debug Mode > Log verbosity
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:163`

---

## CONFIG_SECURE_TEE_SEC_STG_EFUSE_HMAC_KEY_ID

**Secure Storage: eFuse HMAC key ID**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:84`

eFuse block key ID storing the HMAC key for deriving the TEE secure storage encryption keys

---

## CONFIG_SECURE_TEE_SEC_STG_MODE_DEVELOPMENT

**Development**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services > Secure Storage: Mode
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:71`

Secure storage will be encrypted by a constant key embedded in the TEE firmware

---

## CONFIG_SECURE_TEE_SEC_STG_MODE_RELEASE

**Release**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services > Secure Storage: Mode
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:76`

Secure storage will be encrypted by the data stored in eFuse block
configured through the SECURE_TEE_SEC_STG_EFUSE_HMAC_KEY_ID option

---

## CONFIG_SECURE_TEE_SEC_STG_SUPPORT_SECP192R1_SIGN

**Secure Storage: Support signing with the ECDSA SECP192R1 curve**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Secure Services
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:92`

Enable ECDSA signing with the SECP192R1 curve using the TEE secure storage.

---

## CONFIG_SECURE_TEE_STACK_SIZE

**Stack size**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP-TEE (Trusted Execution Environment) > Enable the ESP-TEE framework > Memory Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:29`

This configuration sets the stack size for the TEE module.
The TEE stack will be allocated from the TEE DRAM region.
This should be 16-byte (0x10) aligned.

---

## CONFIG_SECURE_TEE_TEST_MODE

**Enable Test Mode**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP-TEE (Trusted Execution Environment)
- **Kconfig:** `/opt/esp/idf/components/esp_tee/Kconfig.projbuild:182`

This configuration sets up the TEE framework as required for executing the test suite.

---

## CONFIG_SNAPCLIENT_CONNECT_IPV6

**use IPv6**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:14`

Use IPv6 to connect to snapserver

---

## CONFIG_SNAPCLIENT_ETH_MDC_GPIO

**SMI MDC GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC
- **Kconfig:** `components/network_interface/Kconfig.projbuild:61`

Set the GPIO number used by SMI MDC.

---

## CONFIG_SNAPCLIENT_ETH_MDIO_GPIO

**SMI MDIO GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC
- **Kconfig:** `components/network_interface/Kconfig.projbuild:68`

Set the GPIO number used by SMI MDIO.

---

## CONFIG_SNAPCLIENT_ETH_PHY_ADDR

**PHY Address**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC
- **Kconfig:** `components/network_interface/Kconfig.projbuild:83`

Set PHY address according your board schematic.

---

## CONFIG_SNAPCLIENT_ETH_PHY_DP83848

**DP83848**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC > Ethernet PHY Device
- **Kconfig:** `components/network_interface/Kconfig.projbuild:45`

DP83848 is a single port 10/100Mb/s Ethernet Physical Layer Transceiver.
Goto http://www.ti.com/product/DP83848J for more information about it.

---

## CONFIG_SNAPCLIENT_ETH_PHY_IP101

**IP101**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC > Ethernet PHY Device
- **Kconfig:** `components/network_interface/Kconfig.projbuild:20`

IP101 is a single port 10/100 MII/RMII/TP/Fiber Fast Ethernet Transceiver.
Goto http://www.icplus.com.tw/pp-IP101G.html for more information about it.

---

## CONFIG_SNAPCLIENT_ETH_PHY_KSZ80XX

**KSZ80xx**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC > Ethernet PHY Device
- **Kconfig:** `components/network_interface/Kconfig.projbuild:51`

With the KSZ80xx series, Microchip offers single-chip 10BASE-T/100BASE-TX
Ethernet Physical Layer Tranceivers (PHY).
The following chips are supported: KSZ8001, KSZ8021, KSZ8031, KSZ8041,
    KSZ8051, KSZ8061, KSZ8081, KSZ8091
Goto https://www.microchip.com for more information about them.

---

## CONFIG_SNAPCLIENT_ETH_PHY_LAN87XX

**LAN87xx**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC > Ethernet PHY Device
- **Kconfig:** `components/network_interface/Kconfig.projbuild:32`

Below chips are supported:
LAN8710A is a small footprint MII/RMII 10/100 Ethernet Transceiver with HP Auto-MDIX and
    flexPWR® Technology.
LAN8720A is a small footprint RMII 10/100 Ethernet Transceiver with HP Auto-MDIX Support.
LAN8740A/LAN8741A is a small footprint MII/RMII 10/100 Energy Efficient Ethernet Transceiver
    with HP Auto-MDIX and flexPWR® Technology.
LAN8742A is a small footprint RMII 10/100 Ethernet Transceiver with HP Auto-MDIX and
    flexPWR® Technology.
Goto https://www.microchip.com for more information about them.

---

## CONFIG_SNAPCLIENT_ETH_PHY_RST_GPIO

**PHY Reset GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC
- **Kconfig:** `components/network_interface/Kconfig.projbuild:75`

Set the GPIO number used to reset PHY chip.
Set to -1 to disable PHY chip hardware reset.

---

## CONFIG_SNAPCLIENT_ETH_PHY_RTL8201

**RTL8201/SR8201**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > Internal EMAC > Ethernet PHY Device
- **Kconfig:** `components/network_interface/Kconfig.projbuild:26`

RTL8201F/SR8201F is a single port 10/100Mb Ethernet Transceiver with auto MDIX.
Goto http://www.corechip-sz.com/productsview.asp?id=22 for more information about it.

---

## CONFIG_SNAPCLIENT_ETH_SPI_CLOCK_MHZ

**SPI clock speed (MHz)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:169`

Set the clock speed (MHz) of SPI interface.

---

## CONFIG_SNAPCLIENT_ETH_SPI_CS0_GPIO

**SPI CS0 GPIO number for SPI Ethernet module #1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:177`

Set the GPIO number used by SPI CS0, i.e. Chip Select associated with the first SPI Eth module).

---

## CONFIG_SNAPCLIENT_ETH_SPI_CS1_GPIO

**SPI CS1 GPIO number for SPI Ethernet module #2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:186`

Set the GPIO number used by SPI CS1, i.e. Chip Select associated with the second SPI Eth module.

---

## CONFIG_SNAPCLIENT_ETH_SPI_HOST

**SPI Host Number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:132`

Set the SPI host used to communicate with the SPI Ethernet Controller.

---

## CONFIG_SNAPCLIENT_ETH_SPI_INT0_GPIO

**Interrupt GPIO number SPI Ethernet module #1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:198`

Set the GPIO number used by the first SPI Ethernet module interrupt line.

---

## CONFIG_SNAPCLIENT_ETH_SPI_INT1_GPIO

**Interrupt GPIO number SPI Ethernet module #2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:207`

Set the GPIO number used by the second SPI Ethernet module interrupt line.

---

## CONFIG_SNAPCLIENT_ETH_SPI_MISO_GPIO

**SPI MISO GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:159`

Set the GPIO number used by SPI MISO.

---

## CONFIG_SNAPCLIENT_ETH_SPI_MOSI_GPIO

**SPI MOSI GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:149`

Set the GPIO number used by SPI MOSI.

---

## CONFIG_SNAPCLIENT_ETH_SPI_PHY_ADDR0

**PHY Address of SPI Ethernet Module #1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:234`

Set the first SPI Ethernet module PHY address according your board schematic.

---

## CONFIG_SNAPCLIENT_ETH_SPI_PHY_ADDR1

**PHY Address of SPI Ethernet Module #2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:241`

Set the second SPI Ethernet module PHY address according your board schematic.

---

## CONFIG_SNAPCLIENT_ETH_SPI_PHY_RST0_GPIO

**PHY Reset GPIO number of SPI Ethernet Module #1**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:217`

Set the GPIO number used to reset PHY chip on the first SPI Ethernet module.
Set to -1 to disable PHY chip hardware reset.

---

## CONFIG_SNAPCLIENT_ETH_SPI_PHY_RST1_GPIO

**PHY Reset GPIO number of SPI Ethernet Module #2**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:225`

Set the GPIO number used to reset PHY chip on the second SPI Ethernet module.
Set to -1 to disable PHY chip hardware reset.

---

## CONFIG_SNAPCLIENT_ETH_SPI_SCLK_GPIO

**SPI SCLK GPIO number**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:139`

Set the GPIO number used by SPI SCLK.

---

## CONFIG_SNAPCLIENT_MIX_LR_TO_MONO

**mix stereo audio to mono**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP32 DSP processor config > enable signal processing on audio data
- **Kconfig:** `components/dsp_processor/Kconfig.projbuild:10`

mix stereo audio (left and right channel) to mono and play it on the left AND the right channel

---

## CONFIG_SNAPCLIENT_NAME

**snapclient name**

- **Type:** `unknown`
- **Default:** `ESP32-Caster`
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:34`

Name of the client to register the snapserver.

---

## CONFIG_SNAPCLIENT_SPI_ETHERNETS_NUM

**Number of SPI Ethernet modules to use at a time**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet
- **Kconfig:** `components/network_interface/Kconfig.projbuild:99`

Set the number of SPI Ethernet modules you want to use at a time. Multiple SPI modules can be connected
to one SPI interface and can be separately accessed based on state of associated Chip Select (CS).

---

## CONFIG_SNAPCLIENT_USE_DM9051

**DM9051 Module**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet > Ethernet SPI
- **Kconfig:** `components/network_interface/Kconfig.projbuild:113`

Select external SPI-Ethernet module (DM9051).

---

## CONFIG_SNAPCLIENT_USE_INTERNAL_ETHERNET

**Internal EMAC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:5`

Use internal Ethernet MAC controller.

---

## CONFIG_SNAPCLIENT_USE_KSZ8851SNL

**KSZ8851SNL Module**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet > Ethernet SPI
- **Kconfig:** `components/network_interface/Kconfig.projbuild:119`

Select external SPI-Ethernet module (KSZ8851SNL).

---

## CONFIG_SNAPCLIENT_USE_SOFT_VOL

**Use software volume**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP32 DSP processor config > enable signal processing on audio data
- **Kconfig:** `components/dsp_processor/Kconfig.projbuild:17`

Use software volume mixer instead of hardware mixer (when your DAC does not support hardware volume control).

---

## CONFIG_SNAPCLIENT_USE_SPI_ETHERNET

**SPI Ethernet**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:91`

Use external SPI-Ethernet module(s).

---

## CONFIG_SNAPCLIENT_USE_TIMEFILTER

**Use Kalman Timefilter**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:2`

Use Kalman Timefilter for latency to server estimation. The non default is a median filter.

---

## CONFIG_SNAPCLIENT_USE_W5500

**W5500 Module**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Snapclient Ethernet Configuration > SPI Ethernet > Ethernet SPI
- **Kconfig:** `components/network_interface/Kconfig.projbuild:125`

Select external SPI-Ethernet module (W5500).

---

## CONFIG_SNAPCLIENT_VOLUME_CURVE_DB_RANGE

**Volume curve range in dB (0=linear, 60=standard, 90=max)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** ESP32 DSP processor config > enable signal processing on audio data
- **Kconfig:** `components/dsp_processor/Kconfig.projbuild:24`

Controls the shape of the software volume curve.
0 = linear passthrough (y = x, no shaping).
>0 = exponential curve y = a * exp(b * x) where
a = 10^(-dB/20), mapping slider [0,1] to amplitude
[10^(-dB/20), 1.0] for perceptually linear loudness.
60 dB is standard for consumer audio.

---

## CONFIG_SNAPCLIENT_WEB_OTA

**Enable OTA firmware upload via web interface**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Snapclient Configuration > HTTP Server Setting
- **Kconfig:** `main/Kconfig.projbuild:47`

Expose a /api/ota/upload endpoint and an OTA Update tab in the web UI.
Disable to reduce firmware size or restrict update access.

---

## CONFIG_SNAPCLIENT_WIFI_TX_POWER_CONTROL

**Enable WiFi TX power control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:309`

Expose a configurable WiFi transmit power setting (8.5–20 dBm).
The value is persisted to NVS and applied at boot. On some boards,
WiFi TX interferes with RX, so reducing TX power can improve
reception.

---

## CONFIG_SNAPSERVER_HOST

**Snapserver host**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:20`

Host (ip or name) of snapserver to connect to if mDNS is disabled or the mDNS resolution fails.

---

## CONFIG_SNAPSERVER_PORT

**Snapserver port**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:27`

Port of the snapserver to connect to.

---

## CONFIG_SNAPSERVER_USE_MDNS

**Use mDNS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:8`

Use mDNS to look for the snapserver to connect to on the local network.

---

## CONFIG_SNTP_SERVER

**SNTP server**

- **Type:** `unknown`
- **Default:** `pool.ntp.org`
- **Menu:** SNTP Configuration
- **Kconfig:** `components/net_functions/Kconfig.projbuild:9`

SNTP server to use to synchronize time from.

---

## CONFIG_SNTP_TIMEZONE

**SNTP timezone**

- **Type:** `unknown`
- **Default:** `UTC`
- **Menu:** SNTP Configuration
- **Kconfig:** `components/net_functions/Kconfig.projbuild:3`

Timezone used for the SNTP configuration.

---

## CONFIG_SOC_ADC_ARBITER_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_ATTEN_NUM

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_ADC_CALIBRATION_V1_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_DIGI_CONTROLLER_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_ADC_DIGI_DATA_BYTES_PER_CONV

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_ADC_DIGI_IIR_FILTER_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_ADC_DIGI_MAX_BITWIDTH

- **Type:** `unknown`
- **Default:** `12`

---

## CONFIG_SOC_ADC_DIGI_MIN_BITWIDTH

- **Type:** `unknown`
- **Default:** `12`

---

## CONFIG_SOC_ADC_DIGI_MONITOR_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_ADC_DIGI_RESULT_BYTES

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_ADC_DIG_CTRL_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_DIG_IIR_FILTER_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_DMA_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_MAX_CHANNEL_NUM

- **Type:** `unknown`
- **Default:** `10`

---

## CONFIG_SOC_ADC_MONITOR_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_PATT_LEN_MAX

- **Type:** `unknown`
- **Default:** `24`

---

## CONFIG_SOC_ADC_PERIPH_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_ADC_RTC_CTRL_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_RTC_MAX_BITWIDTH

- **Type:** `unknown`
- **Default:** `12`

---

## CONFIG_SOC_ADC_RTC_MIN_BITWIDTH

- **Type:** `unknown`
- **Default:** `12`

---

## CONFIG_SOC_ADC_SAMPLE_FREQ_THRES_HIGH

- **Type:** `unknown`
- **Default:** `83333`

---

## CONFIG_SOC_ADC_SAMPLE_FREQ_THRES_LOW

- **Type:** `unknown`
- **Default:** `611`

---

## CONFIG_SOC_ADC_SELF_HW_CALI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_SHARED_POWER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ADC_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AES_GDMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AES_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AES_SUPPORT_AES_128

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AES_SUPPORT_AES_256

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AES_SUPPORT_DMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AHB_GDMA_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AHB_GDMA_SUPPORT_PSRAM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_AHB_GDMA_VERSION

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_APB_BACKUP_DMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_APPCPU_HAS_CLOCK_GATING_BUG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ASYNC_MEMCPY_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BLE_50_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BLE_DEVICE_PRIVACY_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BLE_MESH_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BLE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BLUFI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BOD_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BROWNOUT_RESET_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_BT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CACHE_ACS_INVALID_STATE_ON_PANIC

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CACHE_FREEZE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CACHE_SUPPORT_WRAP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CACHE_WRITEBACK_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CCOMP_TIMER_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CLK_LP_FAST_SUPPORT_XTAL_D2

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CLK_RC_FAST_D256_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CLK_RC_FAST_SUPPORT_CALIBRATION

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CLK_TREE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CLK_XTAL32K_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_COEX_HW_PTI

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CONFIGURABLE_VDDSDIO_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CPU_BREAKPOINTS_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_CPU_CORES_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_CPU_HAS_FPU

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_CPU_INTR_NUM

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_SOC_CPU_WATCHPOINTS_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_CPU_WATCHPOINT_MAX_REGION_SIZE

- **Type:** `unknown`
- **Default:** `0x40`

---

## CONFIG_SOC_DEDICATED_GPIO_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_DEDIC_GPIO_IN_CHANNELS_NUM

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_DEDIC_GPIO_OUT_AUTO_ENABLE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_DEDIC_GPIO_OUT_CHANNELS_NUM

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_DEEP_SLEEP_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_DIG_SIGN_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_DS_KEY_CHECK_MAX_WAIT_US

- **Type:** `unknown`
- **Default:** `1100`

---

## CONFIG_SOC_DS_KEY_PARAM_MD_IV_LENGTH

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_DS_SIGNATURE_MAX_BIT_LEN

- **Type:** `unknown`
- **Default:** `4096`

---

## CONFIG_SOC_EFUSE_BLOCK9_KEY_PURPOSE_QUIRK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_DIS_DIRECT_BOOT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_DIS_DOWNLOAD_DCACHE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_DIS_DOWNLOAD_ICACHE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_DIS_ICACHE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_DIS_USB_JTAG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_HARD_DIS_JTAG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_KEY_PURPOSE_FIELD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_REVOKE_BOOT_KEY_DIGESTS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_SECURE_BOOT_KEY_DIGESTS

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_EFUSE_SOFT_DIS_JTAG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EFUSE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_EXTERNAL_COEX_ADVANCE

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SOC_EXTERNAL_COEX_LEADER_TX_LINE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_FLASH_ENCRYPTED_XTS_AES_BLOCK_MAX

- **Type:** `unknown`
- **Default:** `64`

---

## CONFIG_SOC_FLASH_ENCRYPTION_XTS_AES

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_FLASH_ENCRYPTION_XTS_AES_128

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_FLASH_ENCRYPTION_XTS_AES_256

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_FLASH_ENCRYPTION_XTS_AES_OPTIONS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_FLASH_ENC_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GDMA_NUM_GROUPS_MAX

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_GDMA_PAIRS_PER_GROUP

- **Type:** `unknown`
- **Default:** `5`

---

## CONFIG_SOC_GDMA_PAIRS_PER_GROUP_MAX

- **Type:** `unknown`
- **Default:** `5`

---

## CONFIG_SOC_GDMA_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_CLOCKOUT_BY_IO_MUX

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_CLOCKOUT_CHANNEL_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_GPIO_FILTER_CLK_SUPPORT_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_IN_RANGE_MAX

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_SOC_GPIO_OUT_RANGE_MAX

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_SOC_GPIO_PIN_COUNT

- **Type:** `unknown`
- **Default:** `49`

---

## CONFIG_SOC_GPIO_PORT

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_GPIO_SUPPORT_FORCE_HOLD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_SUPPORT_HOLD_IO_IN_DSLP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_SUPPORT_PIN_GLITCH_FILTER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_SUPPORT_RTC_INDEPENDENT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPIO_VALID_DIGITAL_IO_PAD_MASK

- **Type:** `unknown`
- **Default:** `0x0001FFFFFC000000`

---

## CONFIG_SOC_GPIO_VALID_GPIO_MASK

- **Type:** `unknown`
- **Default:** `0x1FFFFFFFFFFFF`

---

## CONFIG_SOC_GPSPI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_GPTIMER_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_HMAC_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_HP_CPU_HAS_MULTIPLE_CORES

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_HP_I2C_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_I2C_CMD_REG_NUM

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_I2C_FIFO_LEN

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_SOC_I2C_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_I2C_SLAVE_CAN_GET_STRETCH_CAUSE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SLAVE_SUPPORT_BROADCAST

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SLAVE_SUPPORT_I2CRAM_ACCESS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORT_10BIT_ADDR

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORT_HW_CLR_BUS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORT_RTC

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORT_SLAVE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2C_SUPPORT_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_HW_VERSION_2

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_I2S_PDM_MAX_RX_LINES

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_I2S_PDM_MAX_TX_LINES

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_I2S_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PCM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PCM2PDM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PDM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PDM2PCM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PDM_RX

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PDM_TX

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_PLL_F160M

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_TDM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_I2S_SUPPORTS_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCDCAM_CAM_DATA_WIDTH_MAX

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LCDCAM_CAM_PERIPH_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_LCDCAM_CAM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCDCAM_CAM_SUPPORT_RGB_YUV_CONV

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCDCAM_I80_BUS_WIDTH

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LCDCAM_I80_LCD_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCDCAM_I80_NUM_BUSES

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_LCDCAM_RGB_DATA_WIDTH

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LCDCAM_RGB_LCD_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCDCAM_RGB_NUM_PANELS

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_LCDCAM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCD_I80_BUSES

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_LCD_I80_BUS_WIDTH

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LCD_I80_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCD_RGB_DATA_WIDTH

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LCD_RGB_PANELS

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_LCD_RGB_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LCD_SUPPORT_RGB_YUV_CONV

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LEDC_CHANNEL_NUM

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_LEDC_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LEDC_SUPPORT_APB_CLOCK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LEDC_SUPPORT_FADE_STOP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LEDC_SUPPORT_XTAL_CLOCK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LEDC_TIMER_BIT_WIDTH

- **Type:** `unknown`
- **Default:** `14`

---

## CONFIG_SOC_LEDC_TIMER_NUM

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_LIGHT_SLEEP_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LP_IO_CLOCK_IS_INDEPENDENT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LP_PERIPH_SHARE_INTERRUPT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_LP_TIMER_BIT_WIDTH_HI

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_LP_TIMER_BIT_WIDTH_LO

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_SOC_MAC_BB_PD_MEM_SIZE

- **Type:** `unknown`
- **Default:** `192`

---

## CONFIG_SOC_MCPWM_CAPTURE_CHANNELS_PER_TIMER

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MCPWM_CAPTURE_TIMERS_PER_GROUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MCPWM_COMPARATORS_PER_OPERATOR

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_MCPWM_GENERATORS_PER_OPERATOR

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_MCPWM_GPIO_FAULTS_PER_GROUP

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MCPWM_GPIO_SYNCHROS_PER_GROUP

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MCPWM_GROUPS

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_MCPWM_OPERATORS_PER_GROUP

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MCPWM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MCPWM_SWSYNC_CAN_PROPAGATE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MCPWM_TIMERS_PER_GROUP

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MCPWM_TRIGGERS_PER_OPERATOR

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_MEMPROT_CPU_PREFETCH_PAD_SIZE

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_MEMPROT_MEM_ALIGN_SIZE

- **Type:** `unknown`
- **Default:** `256`

---

## CONFIG_SOC_MEMPROT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_CORE_CLK_SHARED_WITH_PSRAM

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_IS_INDEPENDENT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_SRC_FREQ_120M_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_SRC_FREQ_20M_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_SRC_FREQ_40M_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_SRC_FREQ_80M_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MEMSPI_TIMING_TUNING_BY_MSPI_DELAY

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MMU_LINEAR_ADDRESS_REGION_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_MMU_PERIPH_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_MPI_MEM_BLOCKS_NUM

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_MPI_OPERATIONS_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_MPI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_MPU_CONFIGURABLE_REGIONS_SUPPORTED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SOC_MPU_MIN_REGION_SIZE

- **Type:** `unknown`
- **Default:** `0x20000000`

---

## CONFIG_SOC_MPU_REGIONS_MAX_NUM

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_MPU_REGION_RO_SUPPORTED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SOC_MPU_REGION_WO_SUPPORTED

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_SOC_MPU_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PCNT_CHANNELS_PER_UNIT

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_PCNT_GROUPS

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_PCNT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PCNT_THRES_POINT_PER_UNIT

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_PCNT_UNITS_PER_GROUP

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_PHY_COMBO_MODULE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PHY_DIG_REGS_MEM_SIZE

- **Type:** `unknown`
- **Default:** `21`

---

## CONFIG_SOC_PHY_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_CPU_RETENTION_BY_RTCCNTL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_MODEM_PD_BY_SW

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_MODEM_RETENTION_BY_BACKUPDMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_BT_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_CPU_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_DEEPSLEEP_CHECK_STUB_ONLY

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_EXT0_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_EXT1_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_EXT_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_MAC_BB_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_MODEM_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_RC_FAST_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_RTC_PERIPH_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_TAGMEM_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_TOUCH_SENSOR_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_VDDSDIO_PD

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PM_SUPPORT_WIFI_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_PSRAM_DMA_CAPABLE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RISCV_COPROC_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_CHANNELS_PER_GROUP

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_RMT_GROUPS

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_RMT_MEM_WORDS_PER_CHANNEL

- **Type:** `unknown`
- **Default:** `48`

---

## CONFIG_SOC_RMT_RX_CANDIDATES_PER_GROUP

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_RMT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_DMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_RC_FAST

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_RX_DEMODULATION

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_RX_PINGPONG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_TX_ASYNC_STOP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_TX_CARRIER_DATA_ONLY

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_TX_LOOP_AUTO_STOP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_TX_LOOP_COUNT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_TX_SYNCHRO

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_SUPPORT_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RMT_TX_CANDIDATES_PER_GROUP

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_RNG_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RSA_MAX_BIT_LEN

- **Type:** `unknown`
- **Default:** `4096`

---

## CONFIG_SOC_RTCIO_HOLD_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTCIO_INPUT_OUTPUT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTCIO_PIN_COUNT

- **Type:** `unknown`
- **Default:** `22`

---

## CONFIG_SOC_RTCIO_WAKE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTC_CNTL_CPU_PD_DMA_BUS_WIDTH

- **Type:** `unknown`
- **Default:** `128`

---

## CONFIG_SOC_RTC_CNTL_CPU_PD_REG_FILE_NUM

- **Type:** `unknown`
- **Default:** `549`

---

## CONFIG_SOC_RTC_CNTL_TAGMEM_PD_DMA_BUS_WIDTH

- **Type:** `unknown`
- **Default:** `128`

---

## CONFIG_SOC_RTC_FAST_MEM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTC_MEM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTC_SLOW_CLK_SUPPORT_RC_FAST_D256

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_RTC_SLOW_MEM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SDMMC_DELAY_PHASE_NUM

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_SDMMC_HOST_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SDMMC_NUM_SLOTS

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_SDMMC_SUPPORT_XTAL_CLOCK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SDMMC_USE_GPIO_MATRIX

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SDM_CHANNELS_PER_GROUP

- **Type:** `unknown`
- **Default:** `8`

---

## CONFIG_SOC_SDM_CLK_SUPPORT_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SDM_GROUPS

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_SDM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SECURE_BOOT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SECURE_BOOT_V2_RSA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_DMA_MAX_BUFFER_SIZE

- **Type:** `unknown`
- **Default:** `3968`

---

## CONFIG_SOC_SHA_GDMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_DMA

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_RESUME

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA1

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA224

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA256

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA384

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA512

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA512_224

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA512_256

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SHA_SUPPORT_SHA512_T

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SIMD_INSTRUCTION_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SIMD_PREFERRED_DATA_ALIGNMENT

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_SPIRAM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPIRAM_XIP_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_FLASH_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MAXIMUM_BUFFER_SIZE

- **Type:** `unknown`
- **Default:** `64`

---

## CONFIG_SOC_SPI_MAX_CS_NUM

- **Type:** `unknown`
- **Default:** `6`

---

## CONFIG_SOC_SPI_MAX_PRE_DIVIDER

- **Type:** `unknown`
- **Default:** `16`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_AUTO_RESUME

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_AUTO_SUSPEND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_AUTO_WAIT_IDLE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_CACHE_32BIT_ADDR_MAP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_CONFIG_GPIO_BY_EFUSE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_FLASH_OPI_MODE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_SW_SUSPEND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_TIMING_TUNING

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_MEM_SUPPORT_WRAP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_PERIPH_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_SPI_PERIPH_SUPPORT_CONTROL_DUMMY_OUT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SCT_BUFFER_NUM_MAX

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SCT_CONF_BITLEN_MAX

- **Type:** `unknown`
- **Default:** `0x3FFFA`

---

## CONFIG_SOC_SPI_SCT_REG_NUM

- **Type:** `unknown`
- **Default:** `14`

---

## CONFIG_SOC_SPI_SCT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SLAVE_SUPPORT_SEG_TRANS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_CD_SIG

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_CLK_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_CLK_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_CONTINUOUS_TRANS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_DDRCLK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_OCT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SPI_SUPPORT_SLAVE_HD_VER2

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SUPPORTS_SECURE_DL_MODE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SUPPORT_COEXISTENCE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SUPPORT_SECURE_BOOT_REVOKE_KEY

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SYSTIMER_ALARM_MISS_COMPENSATE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SYSTIMER_ALARM_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_SYSTIMER_BIT_WIDTH_HI

- **Type:** `unknown`
- **Default:** `20`

---

## CONFIG_SOC_SYSTIMER_BIT_WIDTH_LO

- **Type:** `unknown`
- **Default:** `32`

---

## CONFIG_SOC_SYSTIMER_COUNTER_NUM

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_SYSTIMER_FIXED_DIVIDER

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SYSTIMER_INT_LEVEL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_SYSTIMER_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TEMPERATURE_SENSOR_SUPPORT_FAST_RC

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TEMP_SENSOR_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TIMER_GROUPS

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_TIMER_GROUP_COUNTER_BIT_WIDTH

- **Type:** `unknown`
- **Default:** `54`

---

## CONFIG_SOC_TIMER_GROUP_SUPPORT_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TIMER_GROUP_SUPPORT_XTAL

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TIMER_GROUP_TIMERS_PER_GROUP

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_TIMER_GROUP_TOTAL_TIMERS

- **Type:** `unknown`
- **Default:** `4`

---

## CONFIG_SOC_TOUCH_MAX_CHAN_ID

- **Type:** `unknown`
- **Default:** `14`

---

## CONFIG_SOC_TOUCH_MIN_CHAN_ID

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_TOUCH_PROXIMITY_CHANNEL_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_TOUCH_PROXIMITY_MEAS_DONE_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SAMPLE_CFG_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_TOUCH_SENSOR_NUM

- **Type:** `unknown`
- **Default:** `15`

---

## CONFIG_SOC_TOUCH_SENSOR_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SENSOR_VERSION

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_TOUCH_SUPPORT_BENCHMARK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SUPPORT_DENOISE_CHAN

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SUPPORT_PROX_SENSING

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SUPPORT_SLEEP_WAKEUP

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TOUCH_SUPPORT_WATERPROOF

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TWAI_BRP_MAX

- **Type:** `unknown`
- **Default:** `16384`

---

## CONFIG_SOC_TWAI_BRP_MIN

- **Type:** `unknown`
- **Default:** `2`

---

## CONFIG_SOC_TWAI_CLK_SUPPORT_APB

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TWAI_CONTROLLER_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_TWAI_MASK_FILTER_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_TWAI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_TWAI_SUPPORTS_RX_STATUS

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_BITRATE_MAX

- **Type:** `unknown`
- **Default:** `5000000`

---

## CONFIG_SOC_UART_FIFO_LEN

- **Type:** `unknown`
- **Default:** `128`

---

## CONFIG_SOC_UART_HP_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_UART_NUM

- **Type:** `unknown`
- **Default:** `3`

---

## CONFIG_SOC_UART_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_SUPPORT_APB_CLK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_SUPPORT_FSM_TX_WAIT_SEND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_SUPPORT_RTC_CLK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_SUPPORT_WAKEUP_INT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_SUPPORT_XTAL_CLK

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UART_WAKEUP_SUPPORT_ACTIVE_THRESH_MODE

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_UHCI_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_UHCI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ULP_FSM_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ULP_HAS_ADC

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_ULP_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_USB_OTG_PERIPH_NUM

- **Type:** `unknown`
- **Default:** `1`

---

## CONFIG_SOC_USB_OTG_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_USB_SERIAL_JTAG_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WDT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_CSI_SUPPORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_FTM_SUPPORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_GCMP_SUPPORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_HW_TSF

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_LIGHT_SLEEP_CLK_WIDTH

- **Type:** `unknown`
- **Default:** `12`

---

## CONFIG_SOC_WIFI_MESH_SUPPORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_PHY_NEEDS_USB_WORKAROUND

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_SUPPORT_VARIABLE_BEACON_WINDOW

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_WIFI_WAPI_SUPPORT

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_XTAL_SUPPORT_40M

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SOC_XT_WDT_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPIFFS_API_DBG

**Enable SPIFFS API debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:147`

Enabling this option will print API debug messages to the console.

---

## CONFIG_SPIFFS_CACHE

**Enable SPIFFS Cache**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration > SPIFFS Cache Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:11`

Enables/disable memory read caching of nucleus file system
operations.

---

## CONFIG_SPIFFS_CACHE_DBG

**Enable SPIFFS Cache debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:159`

Enabling this option will print cache debug messages to the console.

---

## CONFIG_SPIFFS_CACHE_STATS

**Enable SPIFFS Cache Statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > SPIFFS Cache Configuration > Enable SPIFFS Cache
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:25`

Enable/disable statistics on caching. Debug/test purpose only.

---

## CONFIG_SPIFFS_CACHE_WR

**Enable SPIFFS Write Caching**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration > SPIFFS Cache Configuration > Enable SPIFFS Cache
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:18`

Enables memory write caching for file descriptors in hydrogen.

---

## CONFIG_SPIFFS_CHECK_DBG

**Enable SPIFFS Filesystem Check debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:166`

Enabling this option will print Filesystem Check debug messages
to the console.

---

## CONFIG_SPIFFS_DBG

**Enable general SPIFFS debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:141`

Enabling this option will print general debug messages to the console.

---

## CONFIG_SPIFFS_FOLLOW_SYMLINKS

**Enable symbolic links for image creation**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:79`

If this option is enabled, symbolic links are taken into account
during partition image creation.

---

## CONFIG_SPIFFS_GC_DBG

**Enable SPIFFS Garbage Cleaner debug**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:153`

Enabling this option will print GC debug messages to the console.

---

## CONFIG_SPIFFS_GC_MAX_RUNS

**Set Maximum GC Runs**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:42`

Define maximum number of GC runs to perform to reach desired free pages.

---

## CONFIG_SPIFFS_GC_STATS

**Enable SPIFFS GC Statistics**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:49`

Enable/disable statistics on gc. Debug/test purpose only.

---

## CONFIG_SPIFFS_MAX_PARTITIONS

**Maximum Number of Partitions**

- **Type:** `unknown`
- **Default:** `3`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:3`

Define maximum number of partitions that can be mounted.

---

## CONFIG_SPIFFS_META_LENGTH

**Size of per-file metadata field**

- **Type:** `unknown`
- **Default:** `4`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:104`

This option sets the number of extra bytes stored in the file header.
These bytes can be used in an application-specific manner.
Set this to at least 4 bytes to enable support for saving file
modification time.

SPIFFS_OBJ_NAME_LEN + SPIFFS_META_LENGTH should not exceed
SPIFFS_PAGE_SIZE - 64.

---

## CONFIG_SPIFFS_MTIME_WIDE_64_BITS

**The time field occupies 64 bits in the image instead of 32 bits**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:126`

If this option is not set, the time field is 32 bits (up to 2106 year),
otherwise it is 64 bits and make sure it matches SPIFFS_META_LENGTH.
If the chip already has the spiffs image with the time field = 32 bits
then this option cannot be applied in this case.
Erase it first before using this option.
To resolve the Y2K38 problem for the spiffs, use a toolchain with
64-bit time_t support.

---

## CONFIG_SPIFFS_OBJ_NAME_LEN

**Set SPIFFS Maximum Name Length**

- **Type:** `unknown`
- **Default:** `32`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:67`

Object name maximum length. Note that this length include the
zero-termination character, meaning maximum string of characters
can at most be SPIFFS_OBJ_NAME_LEN - 1.

SPIFFS_OBJ_NAME_LEN + SPIFFS_META_LENGTH should not exceed
SPIFFS_PAGE_SIZE - 64.

---

## CONFIG_SPIFFS_PAGE_CHECK

**Enable SPIFFS Page Check**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:34`

Always check header of each accessed page to ensure consistent state.
If enabled it will increase number of reads from flash, especially
if cache is disabled.

---

## CONFIG_SPIFFS_PAGE_SIZE

**SPIFFS logical page size**

- **Type:** `unknown`
- **Default:** `256`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:55`

Logical page size of SPIFFS partition, in bytes. Must be multiple
of flash page size (which is usually 256 bytes).
Larger page sizes reduce overhead when storing large files, and
improve filesystem performance when reading large files.
Smaller page sizes reduce overhead when storing small (< page size)
files.

---

## CONFIG_SPIFFS_TEST_VISUALISATION

**Enable SPIFFS Filesystem Visualization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPIFFS Configuration > Debug Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:173`

Enable this option to enable SPIFFS_vis function in the API.

---

## CONFIG_SPIFFS_USE_MAGIC

**Enable SPIFFS Filesystem Magic**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:86`

Enable this to have an identifiable spiffs filesystem.
This will look for a magic in all sectors to determine if this
is a valid spiffs system or not at mount time.

---

## CONFIG_SPIFFS_USE_MAGIC_LENGTH

**Enable SPIFFS Filesystem Length Magic**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration > Enable SPIFFS Filesystem Magic
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:94`

If this option is enabled, the magic will also be dependent
on the length of the filesystem. For example, a filesystem
configured and formatted for 4 megabytes will not be accepted
for mounting with a configuration defining the filesystem as 2 megabytes.

---

## CONFIG_SPIFFS_USE_MTIME

**Save file modification time**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPIFFS Configuration
- **Kconfig:** `/opt/esp/idf/components/spiffs/Kconfig:116`

If enabled, then the first 4 bytes of per-file metadata will be used
to store file modification time (mtime), accessible through
stat/fstat functions.
Modification time is updated when the file is opened.

---

## CONFIG_SPIRAM

**Support for external, SPI-connected RAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:1`

This enables support for an external SPI RAM chip, connected in parallel with the
main SPI flash chip.

---

## CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY

**Allow .bss segment placed in external memory**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:129`

If enabled, variables with EXT_RAM_BSS_ATTR attribute will be placed in SPIRAM instead of internal DRAM.
BSS section of `lwip`, `net80211`, `pp`, `bt` libraries will be automatically placed
in SPIRAM. BSS sections from other object files and libraries can also be placed in SPIRAM through
linker fragment scheme `extram_bss`.

Note that the variables placed in SPIRAM using EXT_RAM_BSS_ATTR will be zero initialized.

---

## CONFIG_SPIRAM_ALLOW_NOINIT_SEG_EXTERNAL_MEMORY

**Allow .noinit segment placed in external memory**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:142`

If enabled, noinit variables can be placed in PSRAM using EXT_RAM_NOINIT_ATTR.

Note the values placed into this section will not be initialized at startup and should keep its value
after software restart.

---

## CONFIG_SPIRAM_BOOT_HW_INIT

**Initialise the PSRAM related hardware**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:5`

If this is enabled, the PSRAM hardware will be initialized during startup.
Enabling this config does not ensure make sure the availability of the PSRAM for usage,
but just initialises the PSRAM hardware.
This is necessary to configure PSRAM memory protection during the boot up.

---

## CONFIG_SPIRAM_BOOT_INIT

**Initialize PSRAM during startup, including the hardware and memory related configurations**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Initialise the PSRAM related hardware
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:15`

If this is enabled, the PSRAM will be enabled during initial boot. Unless you
have specific requirements, you'll want to leave this enabled so memory allocated
during boot-up can also be placed in PSRAM.

---

## CONFIG_SPIRAM_CLK_IO

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_SPIRAM_CS_IO

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_SPIRAM_ECC_ENABLE

**Enable SPI RAM ECC**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:135`

Enable MSPI Error-Correcting Code function when accessing SPIRAM.

If enabled, 1/16 of the SPI RAM total size will be reserved for error-correcting code.

---

## CONFIG_SPIRAM_FETCH_INSTRUCTIONS

**Move Instructions in Flash to PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:57`

If enabled, instructions in flash will be moved into PSRAM on startup.
If SPIRAM_RODATA is also enabled, code that requires execution during an MSPI1 Flash operation
can forgo being placed in IRAM. Therefore codes that need to be executing during Flash
operation can continue working normally.

This feature is useful for high throughput peripheral involved applications to improve
the performance during MSPI1 flash operations. PSRAM access speed is faster than Flash access.
So the performance is better. (see External RAM documentation for more details).

---

## CONFIG_SPIRAM_IGNORE_NOTFOUND

**Ignore PSRAM when not found**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:48`

Normally, if psram initialization is enabled during compile time but not found at runtime, it
is seen as an error making the CPU panic. If this is enabled, booting will complete
but no PSRAM will be available. In particular, it is important to note that when SPIRAM_IGNORE_NOTFOUND
is enabled, some WIFI related configs will be set to the default value used when SPIRAM is disabled.
It can be assumed that by enabling this config, WIFI and LWIP will assume that the current chip does not
have SPIRAM. SPIRAM_TRY_ALLOCATE_WIFI_LWIP will not work, buffers in WIFI and LWIP will be set to smaller
ranges and default values. WIFI_CACHE_TX_BUFFER_NUM and ESP_WIFI_AMSDU_TX_ENABLED will be disabled,
ESP_WIFI_TX_BUFFER will be set to ESP_WIFI_DYNAMIC_TX_BUFFER.

---

## CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL

**Maximum malloc() size, in bytes, to always put in internal memory**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:89`

If malloc() is capable of also allocating SPI-connected ram, its allocation strategy will prefer to
allocate chunks less than this size in internal memory, while allocations larger than this will be
done from external RAM. If allocation from the preferred region fails, an attempt is made to allocate
from the non-preferred region instead, so malloc() will not suddenly fail when either internal or
external memory is full.

---

## CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL

**Reserve this amount of bytes for data that specifically needs to be in DMA or internal memory**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:109`

Because the external/internal RAM allocation strategy is not always perfect, it sometimes may happen
that the internal memory is entirely filled up. This causes allocations that are specifically done in
internal memory, for example the stack for new tasks or memory to service DMA or have memory that's
also available when SPI cache is down, to fail. This option reserves a pool specifically for requests
like that; the memory in this pool is not given out when a normal malloc() is called.

Set this to 0 to disable this feature.

Note that because FreeRTOS stacks are forced to internal memory, they will also use this memory pool;
be sure to keep this in mind when adjusting this value.

Note also that the DMA reserved pool may not be one single contiguous memory region, depending on the
configured size and the static memory usage of the app.

---

## CONFIG_SPIRAM_MEMTEST

**Run memory test on SPI RAM initialization**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:81`

Runs a rudimentary memory test on initialization. Aborts when memory test fails. Disable this for
slightly faster startup.

---

## CONFIG_SPIRAM_MODE_OCT

**Octal Mode PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Mode (QUAD/OCT) of SPI RAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:18`

---

## CONFIG_SPIRAM_MODE_QUAD

**Quad Mode PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Mode (QUAD/OCT) of SPI RAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:15`

---

## CONFIG_SPIRAM_PRE_CONFIGURE_MEMORY_PROTECTION

**Pre-configure memory protection for PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:24`

If this is enabled, the PSRAM will be pre-configured for memory protection during initial boot.
This configuration takes into consideration the PSRAM memory configurations that are performed
by ESP-IDF's default PSRAM initialization function, esp_psram_init().
Thus, the config is enabled by default when SPIRAM_BOOT_INIT is enabled,
because the function esp_psram_init() would be called in the startup code.

In case you wish to disable SPIRAM_BOOT_INIT just for delaying the PSRAM initialization and plan
to use the ESP-IDF's default PSRAM initialization function, esp_psram_init() in the application code,
you should still enable this config to enable memory protection for the PSRAM.

Note that enabling this config also considers that the rest of the PSRAM memory that is left after
the memory configurations are performed by esp_psram_init(), can be allocated to the heap using the function
esp_psram_extram_add_to_heap_allocator(), thus configures this region with heap memory protection (RW).

As an advanced usage, if you plan to initialize the PSRAM memory regions manually by yourself without
using the function esp_psram_init(), you should disable this config to avoid any memory protection and
usage conflicts.

---

## CONFIG_SPIRAM_RODATA

**Move Read-Only Data in Flash to PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:70`

If enabled, rodata in flash will be moved into PSRAM on startup.
If SPIRAM_FETCH_INSTRUCTIONS is also enabled, code that requires execution during an MSPI1 Flash operation
is not necessary to be placed in IRAM. Therefore codes that need to be executing during Flash
operation can continue working normally.

This feature is useful for high throughput peripheral involved applications to improve
the performance during MSPI1 flash operations. PSRAM access speed is faster than Flash access.
So the performance is better. (see External RAM documentation for more details).

---

## CONFIG_SPIRAM_SPEED

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_SPIRAM_SPEED_120M

**120MHz clock speed (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Set RAM clock speed
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:89`

- Quad PSRAM 120 MHz is stable.

- Octal PSRAM 120 MHz is an experimental feature, it works when
  the temperature is stable.

    Risks:
        If your chip powers on at a certain temperature, then after the temperature
        increases or decreases by approximately 20 Celsius degrees (depending on the
        chip), the accesses to / from PSRAM will crash randomly.

---

## CONFIG_SPIRAM_SPEED_40M

**40Mhz clock speed**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Set RAM clock speed
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:106`

---

## CONFIG_SPIRAM_SPEED_80M

**80MHz clock speed**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Set RAM clock speed
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:104`

---

## CONFIG_SPIRAM_TIMING_MEASURE_TEMPERATURE_INTERVAL_SECOND

**PSRAM timing tuning measure temperature interval (unit: second)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Adjust PSRAM timing tuning point via on-chip temperature sensor in real-time
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:120`

Due to adjusting timing point automatically when temperature change, the task will measure current
temperature value in some specific interval. This configuration option would help you to set the
proper interval. The interval unit is second.

---

## CONFIG_SPIRAM_TIMING_TUNING_POINT_VIA_TEMPERATURE_SENSOR

**Adjust PSRAM timing tuning point via on-chip temperature sensor in real-time**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:110`

Temperature would influence the source clock frequency so that the timing tuning point set in start-up
might not be always proper when temperature varies in high range. This configuration would help checking
the temperature in real-time, and adjust timing point automatically when temperature change.
(see External RAM documentation for more details)

---

## CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP

**Try to allocate memories of WiFi and LWIP in SPIRAM firstly. If failed, allocate internal memory**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:101`

Try to allocate memories of WiFi and LWIP in SPIRAM firstly. If failed, try to allocate internal
memory then.

---

## CONFIG_SPIRAM_TYPE_AUTO

**Auto-detect**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Type of SPIRAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:26`

---

## CONFIG_SPIRAM_TYPE_ESPPSRAM16

**ESP-PSRAM16 or APS1604**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Type of SPIRAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:29`

---

## CONFIG_SPIRAM_TYPE_ESPPSRAM32

**ESP-PSRAM32**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Type of SPIRAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:33`

---

## CONFIG_SPIRAM_TYPE_ESPPSRAM64

**ESP-PSRAM64 , LY68L6400 or APS6408**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > Type of SPIRAM chip in use
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:37`

---

## CONFIG_SPIRAM_USE_CAPS_ALLOC

**Make RAM allocatable using heap_caps_malloc(..., MALLOC_CAP_SPIRAM)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > SPI RAM access method
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:74`

---

## CONFIG_SPIRAM_USE_MALLOC

**Make RAM allocatable using malloc() as well**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > SPI RAM access method
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:76`

---

## CONFIG_SPIRAM_USE_MEMMAP

**Integrate RAM into memory map**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config > SPI RAM access method
- **Kconfig:** `/opt/esp/idf/components/esp_psram/Kconfig.spiram.common:71`

---

## CONFIG_SPIRAM_XIP_FROM_PSRAM

**Enable Executable in place from (XiP) from PSRAM feature**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP PSRAM > Support for external, SPI-connected RAM > SPI RAM config
- **Kconfig:** `/opt/esp/idf/components/esp_psram/./esp32s3/Kconfig.spiram:49`

Helper for selecting both `SPIRAM_FETCH_INSTRUCTIONS` and `SPIRAM_RODATA`

---

## CONFIG_SPI_FLASH_AUTO_CHECK_SUSPEND_STATUS

**Check flash status automatically after flash suspend**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:169`

Majority flash supports to use flash register to judge if flash suspend status is
done or not. So enable this config, the behavior would use flash register WIP bit to judge
whether suspend is valid instead of waiting for a specific long time, which can save a
lot of time and benefit for performance improvement.

---

## CONFIG_SPI_FLASH_AUTO_SUSPEND

**Auto suspend long erase/write operations (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:86`

This option is disabled by default because it is supported only
for specific flash chips and for specific Espressif chips.
To evaluate if you can use this feature refer to
`Optional Features for Flash` > `Auto Suspend & Resume` of the `ESP-IDF Programming Guide`.

CAUTION: If you want to OTA to an app with this feature turned on, please make
sure the bootloader has the support for it. (later than IDF v4.3)

If you are using an official Espressif module, please contact Espressif Business support
to check if the module has the flash that support this feature installed.
Also refer to `Concurrency Constraints for Flash on SPI1` > `Flash Auto Suspend Feature`
before enabling this option.

---

## CONFIG_SPI_FLASH_BROWNOUT_RESET

- **Type:** `string`
- **Default:** `y`

When brownout happens during flash erase/write operations,
send reset command to stop the flash operations to improve stability.

---

## CONFIG_SPI_FLASH_BROWNOUT_RESET_XMC

**Enable sending reset when brownout for XMC flash chips**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Main Flash configuration > SPI Flash behavior when brownout
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:6`

When this option is selected, the patch will be enabled for XMC.
Follow the recommended flow by XMC for better stability.

DO NOT DISABLE UNLESS YOU KNOW WHAT YOU ARE DOING.

---

## CONFIG_SPI_FLASH_BYPASS_BLOCK_ERASE

**Bypass a block erase and always do sector erase**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:311`

Some flash chips can have very high "max" erase times, especially for block erase (32KB or 64KB).
This option allows to bypass "block erase" and always do sector erase commands.
This will be much slower overall in most cases, but improves latency for other code to run.

---

## CONFIG_SPI_FLASH_CHECK_ERASE_TIMEOUT_DISABLED

**Flash timeout checkout disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:364`

This option is helpful if you are using a flash chip whose timeout is quite large or unpredictable.

---

## CONFIG_SPI_FLASH_DANGEROUS_WRITE_ABORTS

**Aborts**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Writing to dangerous flash regions
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:289`

---

## CONFIG_SPI_FLASH_DANGEROUS_WRITE_ALLOWED

**Allowed**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver > Writing to dangerous flash regions
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:293`

---

## CONFIG_SPI_FLASH_DANGEROUS_WRITE_FAILS

**Fails**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver > Writing to dangerous flash regions
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:291`

---

## CONFIG_SPI_FLASH_DISABLE_SCHEDULER_IN_SUSPEND

**Disable task scheduler when suspend is enabled when SPI1 operation is ongoing**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:157`

Disable freertos task scheduler when CONFIG_SPI_FLASH_AUTO_SUSPEND is enabled.
Thus only interrupt can trigger a suspend. When SPI_FLASH_AUTO_SUSPEND is enabled,
default behavior is not disable the task scheduler, so both interrupt and high priority
task can suspend the erase/program operation. When this option is enabled, task
scheduler is disabled, only interrupt can suspend erase/program operation.

---

## CONFIG_SPI_FLASH_ENABLE_COUNTERS

**Enable operation counters**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:231`

This option enables the following APIs:

- esp_flash_reset_counters
- esp_flash_dump_counters
- esp_flash_get_counters

These APIs may be used to collect performance data for spi_flash APIs
and to help understand behaviour of libraries which use SPI flash.

---

## CONFIG_SPI_FLASH_ENABLE_ENCRYPTED_READ_WRITE

**Enable encrypted partition read/write operations**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:459`

This option enables flash read/write operations to encrypted partition/s. This option
is kept enabled irrespective of state of flash encryption feature. However, in case
application is not using flash encryption feature and is in need of some additional
memory from IRAM region (~1KB) then this config can be disabled.

---

## CONFIG_SPI_FLASH_ERASE_YIELD_DURATION_MS

**Duration of erasing to yield CPUs (ms)**

- **Type:** `unknown`
- **Default:** `20`
- **Menu:** Component config > SPI Flash driver > Enables yield operation during flash erase
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:331`

If a duration of one erase command is large
then it will yield CPUs after finishing a current command.

---

## CONFIG_SPI_FLASH_ERASE_YIELD_TICKS

**CPU release time (tick) for an erase operation**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > SPI Flash driver > Enables yield operation during flash erase
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:339`

Defines how many ticks will be before returning to continue a erasing.

---

## CONFIG_SPI_FLASH_FORCE_ENABLE_C6_H2_SUSPEND

**Enable chip suspend feature on c6 or h2 anyway (DO NOT ENABLE FOR NEW USERS OR APPLICATIONS)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:135`

Flash suspend has a defect on ESP32C6 until v0.2 and ESP32H2 until v1.2. If you already use suspend
feature for mass production, you can enable this for bypassing check after knowing the risk.
But if you are new users, or developing new applications, or producing a new batch,
please DO NOT enable this config option.

For more information, please refer to errata or connect to Espressif business support team.

---

## CONFIG_SPI_FLASH_FORCE_ENABLE_XMC_C_SUSPEND

**Enable XMC-C series flash chip suspend feature anyway**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:124`

XMC-C series is regarded as not qualified for the Suspend feature, since its specification
has a tRS >= 1ms restriction. We strongly do not suggest using it for the Suspend feature.
However, if your product in field has enabled this feature, you may still enable this
config option to keep the legacy behavior.

For new users, DO NOT enable this config.

---

## CONFIG_SPI_FLASH_HPM_AUTO

**Auto (Not recommended)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST) > High Performance Mode (READ DOCS FIRST, > 80MHz)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:50`

---

## CONFIG_SPI_FLASH_HPM_DC_AUTO

**Auto (Enable when bootloader support enabled (BOOTLOADER_FLASH_DC_AWARE))**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST) > Support HPM using DC (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:73`

---

## CONFIG_SPI_FLASH_HPM_DC_DISABLE

**Disable (READ DOCS FIRST)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST) > Support HPM using DC (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:75`

---

## CONFIG_SPI_FLASH_HPM_DC_ON

- **Type:** `string`
- **Default:** `n`

This is a helper config for HPM. Whether HPM-DC is enabled is also determined by bootloader.
Invisible for users.

---

## CONFIG_SPI_FLASH_HPM_DIS

**Disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST) > High Performance Mode (READ DOCS FIRST, > 80MHz)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:52`

---

## CONFIG_SPI_FLASH_HPM_ENA

**Enable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST) > High Performance Mode (READ DOCS FIRST, > 80MHz)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:46`

---

## CONFIG_SPI_FLASH_HPM_ON

- **Type:** `string`
- **Default:** `y`

This option is invisible, and will be selected automatically
when ``ESPTOOLPY_FLASHFREQ_120M`` is selected.

---

## CONFIG_SPI_FLASH_LOG_FAILED_WRITE

**Log errors if verification fails**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver > Verify SPI flash writes
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:208`

If this option is enabled, if SPI flash write verification fails then a log error line
will be written with the address, expected & actual values. This can be useful when
debugging hardware SPI flash problems.

---

## CONFIG_SPI_FLASH_OVERRIDE_CHIP_DRIVER_LIST

**Override default chip driver list**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:370`

This option allows the chip driver list to be customized, instead of using the default list provided by
ESP-IDF.

When this option is enabled, the default list is no longer compiled or linked. Instead, the
`default_registered_chips` structure must be provided by the user.

See example: custom_chip_driver under examples/storage for more details.

---

## CONFIG_SPI_FLASH_PLACE_FUNCTIONS_IN_IRAM

**Place spi_flash operation functions into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:179`

When disabled, certain functions in `spi_flash` component will be placed into Flash memory
instead of IRAM. Disabling this option will save almost 10KB of IRAM depending on which
functions are used.

When enabled, these functions will be placed in internal RAM, with better performance.

For more information please refer to programming guide.

---

## CONFIG_SPI_FLASH_ROM_DRIVER_PATCH

**Enable SPI flash ROM driver patched functions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:244`

Enable this flag to use patched versions of SPI flash ROM driver functions.
This option should be enabled, if any one of the following is true: (1) need to write
to flash on ESP32-D2WD; (2) main SPI flash is connected to non-default pins; (3) main
SPI flash chip is manufactured by ISSI.

---

## CONFIG_SPI_FLASH_ROM_IMPL

**Use esp_flash implementation in ROM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:253`

Enable this flag to use new SPI flash driver functions from ROM instead of ESP-IDF.

If keeping this as "n" in your project, you will have less free IRAM.
But you can use all of our flash features.

If making this as "y" in your project, you will increase free IRAM.
But you may miss out on some flash features and support for new flash chips.

Currently the ROM cannot support the following features:

- SPI_FLASH_AUTO_SUSPEND (C3, S3)

---

## CONFIG_SPI_FLASH_SHARE_SPI1_BUS

**Support other devices attached to SPI1 bus**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:297`

Each SPI bus needs a lock for arbitration among devices. This allows multiple
devices on a same bus, but may reduce the speed of esp_flash driver access to the
main flash chip.

If you only need to use esp_flash driver to access the main flash chip, disable
this option, and the lock will be bypassed on SPI1 bus. Otherwise if extra devices
are needed to attach to SPI1 bus, enable this option.

---

## CONFIG_SPI_FLASH_SIZE_OVERRIDE

**Override flash size in bootloader header by ESPTOOLPY_FLASHSIZE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:356`

SPI Flash driver uses the flash size configured in bootloader header by default.
Enable this option to override flash size with latest ESPTOOLPY_FLASHSIZE value from
the app header if the size in the bootloader header is incorrect.

---

## CONFIG_SPI_FLASH_SOFTWARE_RESUME

**Resume flash program/erase form suspend state by software control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:146`

Enable this config will disable auto-resume from hardware. Thus the software will resume the chip
after any higher priority task/interrupt which suspend the chip. The benefit is that the suspend-resume
will not disturb the higher priority task and interrupt.

This currently is only valid on single core chip.

---

## CONFIG_SPI_FLASH_SUPPORT_BOYA_CHIP

**BOYA**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:428`

Enable this to support auto detection of BOYA chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUPPORT_GD_CHIP

**GigaDevice**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:405`

Enable this to support auto detection of GD (GigaDevice) chips if chip vendor not
directly given by ``chip_drv`` member of the chip struct. If you are using Wrover
modules, please don't disable this, otherwise your flash may not work in 4-bit
mode.

This adds support for variant chips, however will extend detecting time and image
size. Note that the default chip driver supports the GD chips with product ID
60H.

---

## CONFIG_SPI_FLASH_SUPPORT_ISSI_CHIP

**ISSI**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:387`

Enable this to support auto detection of ISSI chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUPPORT_MXIC_CHIP

**MXIC**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:396`

Enable this to support auto detection of MXIC chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUPPORT_MXIC_OPI_CHIP

**mxic (opi)**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:448`

Enable this to support auto detection of Octal MXIC chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUPPORT_TH_CHIP

**TH**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:438`

Enable this to support auto detection of TH chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUPPORT_WINBOND_CHIP

**Winbond**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver > Auto-detect flash chips
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:419`

Enable this to support auto detection of Winbond chips if chip vendor not directly
given by ``chip_drv`` member of the chip struct. This adds support for variant
chips, however will extend detecting time.

---

## CONFIG_SPI_FLASH_SUSPEND_TRS_VAL_US

**SPI flash tRS value (refer to chapter AC CHARACTERISTICS)**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:112`

This config is used for setting Trs parameter. Trs means CS Latency Between Resume And Next Suspend.
You can refer to the chapter of AC CHARACTERISTICS of flash datasheet.
For high-performance scenarios, some flash chips allow this set value to be smaller than the
given value in the datasheet without causing errors in the flash state machine.
When you have any related needs, please contact espressif business team.

---

## CONFIG_SPI_FLASH_SUSPEND_TSUS_VAL_US

**SPI flash tSUS value (refer to chapter AC CHARACTERISTICS)**

- **Type:** `unknown`
- **Default:** `50`
- **Menu:** Component config > Main Flash configuration > Optional and Experimental Features (READ DOCS FIRST)
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:104`

This config is used for setting Tsus parameter. Tsus means CS# high to next command after
suspend. You can refer to the chapter of AC CHARACTERISTICS of flash datasheet.

---

## CONFIG_SPI_FLASH_UNDER_HIGH_FREQ

- **Type:** `string`
- **Default:** `n`

This is a helper config for HPM. Invisible for users.

---

## CONFIG_SPI_FLASH_VENDOR_BOYA_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_GD_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_ISSI_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_MXIC_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_TH_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_WINBOND_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VENDOR_XMC_SUPPORT_ENABLED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_SPI_FLASH_VERIFY_WRITE

**Verify SPI flash writes**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:197`

If this option is enabled, any time SPI flash is written then the data will be read
back and verified. This can catch hardware problems with SPI flash, or flash which
was not erased before verification.

This will slightly influence the write performance.

---

## CONFIG_SPI_FLASH_WARN_SETTING_ZERO_TO_ONE

**Log warning if writing zero bits to ones**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > SPI Flash driver > Verify SPI flash writes
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:217`

If this option is enabled, any SPI flash write which tries to set zero bits in the flash to
ones will log a warning. Such writes will not result in the requested data appearing identically
in flash once written, as SPI NOR flash can only set bits to one when an entire sector is erased.
After erasing, individual bits can only be written from one to zero.

Note that some software (such as SPIFFS) which is aware of SPI NOR flash may write one bits as an
optimisation, relying on the data in flash becoming a bitwise AND of the new data and any existing data.
Such software will log spurious warnings if this option is enabled.

---

## CONFIG_SPI_FLASH_WRITE_CHUNK_SIZE

**Flash write chunk size**

- **Type:** `unknown`
- **Default:** `8192`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:346`

Flash write is broken down in terms of multiple (smaller) write operations.
This configuration options helps to set individual write chunk size, smaller
value here ensures that cache (and non-IRAM resident interrupts) remains
disabled for shorter duration.

---

## CONFIG_SPI_FLASH_YIELD_DURING_ERASE

**Enables yield operation during flash erase**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > SPI Flash driver
- **Kconfig:** `/opt/esp/idf/components/spi_flash/Kconfig:319`

This allows to yield the CPUs between erase commands.
Prevents starvation of other tasks.
Please use this configuration together with ``SPI_FLASH_ERASE_YIELD_DURATION_MS`` and
``SPI_FLASH_ERASE_YIELD_TICKS`` after carefully checking flash datasheet to avoid a
watchdog timeout.
For more information, please check `SPI Flash API` reference documentation
under section `OS Function`.

---

## CONFIG_SPI_MASTER_IN_IRAM

**Place transmitting functions of SPI master into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:SPI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_spi/Kconfig:3`

Normally only the ISR of SPI master is placed in the IRAM, so that it
can work without the flash when interrupt is triggered.
For other functions, there's some possibility that the flash cache
miss when running inside and out of SPI functions, which may increase
the interval of SPI transactions.
Enable this to put ``queue_trans``, ``get_trans_result`` and
``transmit`` functions into the IRAM to avoid possible cache miss.

This configuration won't be available if `CONFIG_FREERTOS_PLACE_FUNCTIONS_INTO_FLASH` is enabled.

During unit test, this is enabled to measure the ideal case of api.

---

## CONFIG_SPI_MASTER_ISR_IN_IRAM

**Place SPI master ISR function into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:SPI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_spi/Kconfig:22`

Place the SPI master ISR in to IRAM to avoid possible cache miss.

Enabling this configuration is possible only when HEAP_PLACE_FUNCTION_INTO_FLASH
is disabled since the spi master uses can allocate transactions buffers into DMA
memory section using the heap component API that ipso facto has to be placed in IRAM.

Also you can forbid the ISR being disabled during flash writing
access, by add ESP_INTR_FLAG_IRAM when initializing the driver.

---

## CONFIG_SPI_SLAVE_IN_IRAM

**Place transmitting functions of SPI slave into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:SPI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_spi/Kconfig:40`

Normally only the ISR of SPI slave is placed in the IRAM, so that it
can work without the flash when interrupt is triggered.
For other functions, there's some possibility that the flash cache
miss when running inside and out of SPI functions, which may increase
the interval of SPI transactions.
Enable this to put ``queue_trans``, ``get_trans_result`` and
``transmit`` functions into the IRAM to avoid possible cache miss.

---

## CONFIG_SPI_SLAVE_ISR_IN_IRAM

**Place SPI slave ISR function into IRAM**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:SPI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_spi/Kconfig:53`

Place the SPI slave ISR in to IRAM to avoid possible cache miss.

Also you can forbid the ISR being disabled during flash writing
access, by add ESP_INTR_FLAG_IRAM when initializing the driver.

---

## CONFIG_STDATOMIC_S32C1I_SPIRAM_WORKAROUND

- **Type:** `string`
- **Default:** `n`

---

## CONFIG_TEMP_SENSOR_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Temperature Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_tsens/Kconfig:4`

whether to enable the debug log message for temperature sensor driver.
Note that, this option only controls the temperature sensor driver log, won't affect other drivers.

---

## CONFIG_TEMP_SENSOR_ISR_IRAM_SAFE

**Temperature sensor ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Temperature Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_tsens/Kconfig:11`

Ensure the Temperature Sensor interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_TEMP_SENSOR_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Temperature Sensor Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:209`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_TEMP_SENSOR_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Temperature Sensor Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:201`

whether to suppress the deprecation warnings when using legacy temperature sensor driver
(driver/temp_sensor.h). If you want to continue using the legacy driver,
and don't want to see related deprecation warnings, you can enable this option.

---

## CONFIG_TOUCH_CTRL_FUNC_IN_IRAM

**Place touch sensor control functions into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Touch Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_touch_sens/Kconfig:3`

Place touch sensor oneshot scanning and continuous scanning functions into IRAM,
so that these function can be IRAM-safe and able to be called when the flash cache is disabled.
Enabling this option can improve driver performance as well.

---

## CONFIG_TOUCH_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Touch Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_touch_sens/Kconfig:18`

Whether to enable the debug log message for touch driver.
Note that, this option only controls the touch driver log, won't affect other drivers.

---

## CONFIG_TOUCH_ISR_IRAM_SAFE

**Touch sensor ISR IRAM-Safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Touch Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_touch_sens/Kconfig:11`

Ensure the touch sensor interrupt is IRAM-Safe by allowing the interrupt handler to be
executable when the cache is disabled (e.g. SPI Flash write).

---

## CONFIG_TOUCH_SKIP_FSM_CHECK

**Skip the FSM check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:Touch Sensor Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_touch_sens/Kconfig:25`

If skipped the FSM check, the driver will allow to re-configure the touch sensor during the runtime.
It is mainly used to tune the parameters in the runtime to find a proper set of touch parameters.
CAUTION: Not suggest to enable this option in the final product, it might cause false triggering
which is not safe in the actual environment.

---

## CONFIG_TOUCH_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Touch Sensor Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:226`

This configuration option allows the user to bypass the conflict check mechanism with legacy code.

---

## CONFIG_TOUCH_SUPPRESS_DEPRECATE_WARN

**Suppress legacy driver deprecated warning**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy Touch Sensor Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/Kconfig:218`

whether to suppress the deprecation warnings when using legacy touch sensor driver
(driver/touch_sensor.h). If you want to continue using the legacy driver,
and don't want to see related deprecation warnings, you can enable this option.

---

## CONFIG_TWAI_ENABLE_DEBUG_LOG

**Force enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:TWAI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_twai/Kconfig:27`

If enabled, TWAI driver component will:
1. ignore the global logging settings
2. compile all log messages into the binary
3. set the runtime log level to VERBOSE
Please enable this option by caution, as it will increase the binary size.

---

## CONFIG_TWAI_ERRATA_FIX_BUS_OFF_REC

**Add SW workaround for REC change during bus-off**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:10`

When the bus-off condition is reached, the REC should be reset to 0 and frozen (via LOM) by the
driver's ISR. However on the ESP32, there is an edge case where the REC will increase before the
driver's ISR can respond in time (e.g., due to the rapid occurrence of bus errors), thus causing the
REC to be non-zero after bus-off. A non-zero REC can prevent bus-off recovery as the bus-off recovery
condition is that both TEC and REC become 0. Enabling this option will add a workaround in the driver
to forcibly reset REC to zero on reaching bus-off.

---

## CONFIG_TWAI_ERRATA_FIX_LISTEN_ONLY_DOM

**Add SW workaround for listen only transmits dominant bit errata**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:52`

When in the listen only mode, the TWAI controller must not influence the TWAI bus (i.e., must not send
any dominant bits). However, while in listen only mode on the ESP32/ESP32-S2/ESP32-S3/ESP32-C3, the
TWAI controller will still transmit dominant bits when it detects an error (i.e., as part of an active
error frame). Enabling this option will add a workaround that forces the TWAI controller into an error
passive state on initialization, thus preventing any dominant bits from being sent.

---

## CONFIG_TWAI_ERRATA_FIX_RX_FIFO_CORRUPT

**Add SW workaround for RX FIFO corruption errata**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:42`

On the ESP32, when the RX FIFO overruns and the RX message counter maxes out at 64 messages, the entire
RX FIFO is no longer recoverable. Enabling this option will add a workaround that resets the peripheral
on detection of this errata condition. Note that if a frame is being sent on the bus during the reset
bus during the reset, the message will be lost.

---

## CONFIG_TWAI_ERRATA_FIX_RX_FRAME_INVALID

**Add SW workaround for invalid RX frame errata**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:31`

On the ESP32, when receiving a data or remote frame, if a bus error occurs in the data or CRC field,
the data of the next received frame could be invalid. Enabling this option will add a workaround that
will reset the peripheral on detection of this errata condition. Note that if a frame is transmitted on
the bus whilst the reset is ongoing, the message will not be receive by the peripheral sent on the bus
during the reset, the message will be lost.

---

## CONFIG_TWAI_ERRATA_FIX_TX_INTR_LOST

**Add SW workaround for TX interrupt lost errata**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:22`

On the ESP32, when a transmit interrupt occurs, and interrupt register is read on the same APB clock
cycle, the transmit interrupt could be lost. Enabling this option will add a workaround that checks the
transmit buffer status bit to recover any lost transmit interrupt.

---

## CONFIG_TWAI_ISR_CACHE_SAFE

**Allow TWAI ISR execute when cache disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:TWAI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_twai/Kconfig:11`

Allow TWAI works under Cache disabled (such as when writing to SPI Flash),
to enabled this config, all callbacks and user_ctx should also place in IRAM

---

## CONFIG_TWAI_ISR_IN_IRAM

**Place TWAI ISR function in IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:TWAI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_twai/Kconfig:4`

Place the TWAI ISR in to IRAM to reduce latency and increase performance

---

## CONFIG_TWAI_OBJ_CACHE_SAFE

- **Type:** `string`
- **Default:** `n`

This will ensure the TWAI driver object will not be allocated from a memory region
where its cache can be disabled.

---

## CONFIG_TWAI_SKIP_LEGACY_CONFLICT_CHECK

**Skip legacy driver conflict check**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Driver Configurations > Legacy TWAI Driver Configurations
- **Kconfig:** `/opt/esp/idf/components/driver/./twai/Kconfig.twai:4`

This configuration option used to bypass the conflict check mechanism with legacy code.

---

## CONFIG_UART_BAUDRATE_115200

**115200**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart Hci Baud Rate
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1268`

---

## CONFIG_UART_BAUDRATE_230400

**230400**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart Hci Baud Rate
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1270`

---

## CONFIG_UART_BAUDRATE_460800

**460800**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart Hci Baud Rate
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1272`

---

## CONFIG_UART_BAUDRATE_921600

**921600**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart Hci Baud Rate
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1274`

---

## CONFIG_UART_HW_FLOWCTRL_CTS_RTS

**Enable hardware flow control**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Uart Flow Control
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1341`

---

## CONFIG_UART_HW_FLOWCTRL_DISABLE

**Disable**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Uart Flow Control
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1339`

---

## CONFIG_UART_ISR_IN_IRAM

**Place UART ISR function into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:UART Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_uart/Kconfig:3`

If this option is not selected, UART interrupt will be disabled for a long time and
may cause data lost when doing spi flash operation.

---

## CONFIG_UART_PARITY_EVEN

**Even**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart PARITY
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1297`

---

## CONFIG_UART_PARITY_NONE

**None**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart PARITY
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1293`

---

## CONFIG_UART_PARITY_ODD

**Odd**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Bluetooth > NimBLE Options > Host-controller Transport > Enable Uart Transport > Uart PARITY
- **Kconfig:** `/opt/esp/idf/components/bt/host/nimble/Kconfig.in:1295`

---

## CONFIG_UHCI_ENABLE_DEBUG_LOG

**Enable debug log**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:UHCI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_uart/Kconfig:34`

whether to enable the debug log message for UHCI driver.
Note that, this option only controls the UHCI driver log, won't affect other drivers.

---

## CONFIG_UHCI_ISR_CACHE_SAFE

**Allow UHCI ISR to execute when cache is disabled**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:UHCI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_uart/Kconfig:24`

Enable this option to allow the ISR for UHCI to execute even when the cache is disabled.
This can be useful in scenarios where the cache might be turned off, but the UHCI
functionality is still required to operate correctly.

---

## CONFIG_UHCI_ISR_HANDLER_IN_IRAM

**Place UHCI ISR function into IRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:UHCI Configurations
- **Kconfig:** `/opt/esp/idf/components/esp_driver_uart/Kconfig:16`

If this option is not selected, UHCI interrupt will be disabled for a long time and
may cause data lost when doing spi flash operation.

---

## CONFIG_ULP_COPROC_ENABLED

**Enable Ultra Low Power (ULP) Co-processor**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:4`

Enable this feature if you plan to use the ULP Co-processor.
Once this option is enabled, further ULP co-processor configuration will appear in the menu.

---

## CONFIG_ULP_COPROC_RESERVE_MEM

**RTC slow memory reserved for coprocessor**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > Enable Ultra Low Power (ULP) Co-processor
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:29`

Bytes of memory to reserve for ULP Co-processor firmware & data.
Data is reserved at the beginning of RTC slow memory.

---

## CONFIG_ULP_COPROC_TYPE_FSM

**ULP FSM (Finite State Machine)**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > Enable Ultra Low Power (ULP) Co-processor > ULP Co-processor type
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:18`

---

## CONFIG_ULP_COPROC_TYPE_LP_CORE

**LP core RISC-V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > Enable Ultra Low Power (ULP) Co-processor > ULP Co-processor type
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:24`

---

## CONFIG_ULP_COPROC_TYPE_RISCV

**ULP RISC-V**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > Enable Ultra Low Power (ULP) Co-processor > ULP Co-processor type
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:21`

---

## CONFIG_ULP_HP_UART_CONSOLE_PRINT

**Route lp_core_printf to the console HP-UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP Debugging Options
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:109`

Set this option to route lp_core_printf to the console HP-UART.
This allows you to easily view print outputs from the LP core, without
having to connect to the LP-UART. This option comes with the following
limitations:

1. There is no mutual exclusion between the HP-Core and the LP-Core accessing
the HP-UART, which means that if both cores are logging heavily the output
strings might get mangled together.
2. The HP-UART can only work while the HP-Core is running, which means that
if the HP-Core is in deep sleep, the LP-Core will not be able to print to the
console HP-UART.

Due to these limitations it is only recommended to use this option for easy debugging.
For more serious use-cases you should use the LP-UART.

---

## CONFIG_ULP_NORESET_UNDER_DEBUG

**Avoid resetting LP core when debugger is attached**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP Debugging Options
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:129`

Enable this feature to avoid resetting LP core in sleep mode when debugger is attached,
otherwise configured HW breakpoints and dcsr.ebreak* bits will be missed.
This is a workaround until it will be fixed in HW.

---

## CONFIG_ULP_PANIC_OUTPUT_ENABLE

**Enable panic handler which outputs over LP UART**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP Debugging Options
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:96`

Set this option to enable panic handler functionality. If this option is
enabled then the LP Core will output a panic dump over LP UART,
similar to what the main core does. Output depends on LP UART already being
initialized and configured.
Disabling this option will reduce the LP core binary size by not
linking in panic handler functionality.

---

## CONFIG_ULP_RISCV_I2C_RW_TIMEOUT

**Set timeout for ULP RISC-V I2C transaction timeout in ticks.**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP RISC-V Settings
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:61`

Set the ULP RISC-V I2C read/write timeout. Set this value to -1
if the ULP RISC-V I2C read and write APIs should wait forever.
Please note that the tick rate of the ULP co-processor would be
different than the OS tick rate of the main core and therefore
can have different timeout value depending on which core the API
is invoked on.

---

## CONFIG_ULP_RISCV_INTERRUPT_ENABLE

**Enable ULP RISC-V interrupts**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP RISC-V Settings
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:46`

Turn on this setting to enabled interrupts on the ULP RISC-V core.

---

## CONFIG_ULP_RISCV_UART_BAUDRATE

**Baudrate used by the bitbanged ULP RISC-V UART driver**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor > ULP RISC-V Settings
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:53`

The accuracy of the bitbanged UART driver is limited, it is not
recommend to increase the value above 19200.

---

## CONFIG_ULP_ROM_PRINT_ENABLE

**Enable print utilities from LP ROM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Ultra Low Power (ULP) Co-processor
- **Kconfig:** `/opt/esp/idf/components/ulp/Kconfig:83`

Set this option to enable printf functionality from LP ROM. This option
can help reduce the LP core binary size by not linking printf functionality
from RAM code.
Note: For LP ROM prints to work properly, make sure that the LP core boots
from the LP ROM.

---

## CONFIG_ULP_SHARED_MEM

- **Type:** `unknown`
- **Default:** ``

Size of the shared memory defined in ulp_lp_core_memory_shared.c.
Size should be kept in-sync with the size of the struct defined there.

---

## CONFIG_UNITY_ENABLE_64BIT

**Support for 64-bit integer types**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:16`

If not set, assertions on 64-bit integer types will always fail.
If this feature is enabled, take care not to pass pointers (which are 32 bit)
to UNITY_ASSERT_EQUAL, as that will cause pointer-to-int-cast warnings.

---

## CONFIG_UNITY_ENABLE_BACKTRACE_ON_FAIL

**Print a backtrace when a unit test fails**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:53`

If set, the unity framework will print the backtrace information before
jumping back to the test menu. The jumping is usually occurs in assert
functions such as TEST_ASSERT, TEST_FAIL etc.

---

## CONFIG_UNITY_ENABLE_COLOR

**Colorize test output**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:24`

If set, Unity will colorize test results using console escape sequences.

---

## CONFIG_UNITY_ENABLE_DOUBLE

**Support for double type**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:10`

If not set, assertions on double arguments will not be available.

---

## CONFIG_UNITY_ENABLE_FIXTURE

**Include Unity test fixture**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:45`

If set, unity_fixture.h header file and associated source files are part of
the build. These provide an optional set of macros and functions to
implement test groups.

---

## CONFIG_UNITY_ENABLE_FLOAT

**Support for float type**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:3`

If not set, assertions on float arguments will not be available.

---

## CONFIG_UNITY_ENABLE_IDF_TEST_RUNNER

**Include ESP-IDF test registration/running helpers**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:31`

If set, then the following features will be available:

- TEST_CASE macro which performs automatic registration of test functions
- Functions to run registered test functions: unity_run_all_tests,
  unity_run_tests_with_filter, unity_run_single_test_by_name.
- Interactive menu which lists test cases and allows choosing the tests to
  be run, available via unity_run_menu function.

Disable if a different test registration mechanism is used.

---

## CONFIG_UNITY_TEST_ORDER_BY_FILE_PATH_AND_LINE

**Order unit tests by file path and line number**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Unity unit testing library
- **Kconfig:** `/opt/esp/idf/components/unity/Kconfig:61`

If enabled, the Unity test framework will automatically insert test cases
in a sorted order at registration time (during constructor execution),
based on their source file path and line number.

This ensures consistent execution order across platforms (e.g., Linux vs. on-chip),
preserving the logical order in which tests are written in the source files.

Note, the file path used for sorting follows the full absolute path format.
/IDF/examples/system/unit_test/components/testable/test/test_mean.c

---

## CONFIG_USB_HOST_CONTROL_TRANSFER_MAX_SIZE

**Largest size (in bytes) of transfers to/from default endpoints**

- **Type:** `unknown`
- **Default:** `256`
- **Menu:** Component config > USB-OTG
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:4`

Each USB device attached is allocated a dedicated buffer for its OUT/IN transfers to/from the device's
control endpoint. The maximum size of that buffer is determined by this option. The limited size of the
transfer buffer have the following implications:
- The maximum length of control transfers is limited
- Device's with configuration descriptors larger than this limit cannot be supported

---

## CONFIG_USB_HOST_DEBOUNCE_DELAY_MS

**Debounce delay in ms**

- **Type:** `unknown`
- **Default:** `250`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Root Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:53`

On connection of a USB device, the USB 2.0 specification requires
a "debounce interval with a minimum duration of 100ms" to allow the connection to stabilize
(see USB 2.0 chapter 7.1.7.3 for more details).
During the debounce interval, no new connection/disconnection events are registered.

The default value is set to 250 ms to be safe.

---

## CONFIG_USB_HOST_DWC_DMA_CAP_MEMORY_IN_PSRAM

**Allocate USB_DWC DMA capable memory in PSRAM**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:178`

In the ESP32P4, the USB-DWC internal DMA can access external RAM. Enabling this configuration can save
internal RAM by allocating the memory buffers used by the USB-DWC peripheral's DMA to external RAM.
However, it introduces minor performance degradation due to the overhead of accessing external RAM.

---

## CONFIG_USB_HOST_ENABLE_ENUM_FILTER_CALLBACK

**Enable enumeration filter callback**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:167`

The enumeration filter callback is called before enumeration of each newly attached device.
This callback allows users to control whether a device should be enumerated, and what configuration
number to use when enumerating a device.

If enabled, the enumeration filter callback can be set via 'usb_host_config_t' when calling
'usb_host_install()'.

---

## CONFIG_USB_HOST_EXT_PORT_CUSTOM_POWER_ON_DELAY_ENABLE

**Custom bPwrOn2PwrGood value**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Support Hubs > Downstream Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:141`

Enables the possibility to configure custom time for the power-on sequence on a port
until power is good on that port.

When enabled, applies the custom PwrOn2PwrGood delay.
When disabled, applies the PwrOn2PwrGood value from the Hub Descriptor.

---

## CONFIG_USB_HOST_EXT_PORT_CUSTOM_POWER_ON_DELAY_MS

**PwrOn2PwrGood delay in ms**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Support Hubs > Downstream Port configuration > Custom bPwrOn2PwrGood value
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:151`

Custom value of delay from the time the power-on sequence begins on a port
until power is good on that port.
Value 0 is used for a hub with no power switches.

The default value is 100 ms.

---

## CONFIG_USB_HOST_EXT_PORT_RESET_ATTEMPTS

- **Type:** `unknown`
- **Default:** ``

Amount of attempts to reset the device.

The default value is 1.

---

## CONFIG_USB_HOST_EXT_PORT_RESET_RECOVERY_DELAY_MS

**Reset recovery delay in ms**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Support Hubs > Downstream Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:129`

After a port stops driving the reset signal, the USB 2.0 specification requires that
the "USB System Software guarantees a minimum of 10 ms for reset recovery" before the
attached device is expected to respond to data transfers (see USB 2.0 chapter 7.1.7.3 for
more details).
The device may ignore any data transfers during the recovery interval.

The default value is set to 30 ms to be safe.

---

## CONFIG_USB_HOST_HUBS_SUPPORTED

**Support Hubs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:102`

Enables support of external Hubs.

---

## CONFIG_USB_HOST_HUB_MULTI_LEVEL

**Support multiple Hubs**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Support Hubs
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:108`

Enables support for connecting multiple Hubs simultaneously.

---

## CONFIG_USB_HOST_HW_BUFFER_BIAS_BALANCED

**Balanced**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > USB-OTG > Hardware FIFO size biasing
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:41`

---

## CONFIG_USB_HOST_HW_BUFFER_BIAS_IN

**Bias IN**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG > Hardware FIFO size biasing
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:43`

---

## CONFIG_USB_HOST_HW_BUFFER_BIAS_PERIODIC_OUT

**Periodic OUT**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > USB-OTG > Hardware FIFO size biasing
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:45`

---

## CONFIG_USB_HOST_RESET_HOLD_MS

**Reset hold in ms**

- **Type:** `unknown`
- **Default:** `30`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Root Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:64`

The reset signaling can be generated on any Hub or Host Controller port by request from
the USB System Software. The USB 2.0 specification requires that "the reset signaling must
be driven for a minimum of 10ms" (see USB 2.0 chapter 7.1.7.5 for more details).
After the reset, the hub port will transition to the Enabled state (refer to Section 11.5).

The default value is set to 30 ms to be safe.

---

## CONFIG_USB_HOST_RESET_RECOVERY_MS

**Reset recovery delay in ms**

- **Type:** `unknown`
- **Default:** `30`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Root Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:75`

After a port stops driving the reset signal, the USB 2.0 specification requires that
the "USB System Software guarantees a minimum of 10 ms for reset recovery" before the
attached device is expected to respond to data transfers (see USB 2.0 chapter 7.1.7.3 for
more details).
The device may ignore any data transfers during the recovery interval.

The default value is set to 30 ms to be safe.

---

## CONFIG_USB_HOST_SET_ADDR_RECOVERY_MS

**SetAddress() recovery time in ms**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > USB-OTG > Hub Driver Configuration > Root Port configuration
- **Kconfig:** `/opt/esp/idf/components/usb/Kconfig:88`

"After successful completion of the Status stage, the device is allowed a SetAddress()
recovery interval of 2 ms. At the end of this interval, the device must be able to accept
Setup packets addressed to the new address. Also, at the end of the recovery interval, the
device must not respond to tokens sent to the old address (unless, of course, the old and new
address is the same)." See USB 2.0 chapter 9.2.6.3 for more details.

The default value is set to 10 ms to be safe.

---

## CONFIG_USB_OTG_SUPPORTED

- **Type:** `string`
- **Default:** `y`

---

## CONFIG_USE_DSP_PROCESSOR

**enable signal processing on audio data**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP32 DSP processor config
- **Kconfig:** `components/dsp_processor/Kconfig.projbuild:4`

enable audio filtering before queueing it to player component

---

## CONFIG_USE_DSP_SOFT_CLIP

**Use cubic soft clipping**

- **Type:** `string`
- **Default:** `n`
- **Menu:** ESP32 DSP processor config > enable signal processing on audio data
- **Kconfig:** `components/dsp_processor/Kconfig.projbuild:37`

Apply cubic soft clipping to prevent hard clipping distortion when signal exceeds the ±1.0 range.
This provides a smoother transition when the signal approaches clipping, reducing harsh distortion.

---

## CONFIG_USE_SAMPLE_INSERTION

**Use sample insertion**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Snapclient Configuration
- **Kconfig:** `main/Kconfig.projbuild:55`

There are two syncronization implementations.

1. APLL tuning (ONLY use with DACs having MCLK input)
2. sample insertion (duplicates a sample now and then to keep up sync)

Both approaches have similar performance keeping clients in sync <= 500µs

---

## CONFIG_USJ_ENABLE_USB_SERIAL_JTAG

**Enable USB-Serial-JTAG Module**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > ESP-Driver:USB Serial/JTAG Configuration
- **Kconfig:** `/opt/esp/idf/components/esp_driver_usb_serial_jtag/Kconfig:4`

The USB-Serial-JTAG module on ESP chips is turned on by default after power-on.
If your application does not need it and not rely on it to be used as system
console or use the built-in JTAG for debugging, you can disable this option,
then the clock of this module will be disabled at startup, which will save
some power consumption.

---

## CONFIG_USJ_NO_AUTO_LS_ON_CONNECTION

**Don't enter the automatic light sleep when USB Serial/JTAG port is connected**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > ESP-Driver:USB Serial/JTAG Configuration > Enable USB-Serial-JTAG Module
- **Kconfig:** `/opt/esp/idf/components/esp_driver_usb_serial_jtag/Kconfig:14`

If enabled, the chip will constantly monitor the connection status of the USB Serial/JTAG port. As long
as the USB Serial/JTAG is connected, a ESP_PM_NO_LIGHT_SLEEP power management lock will be acquired to
prevent the system from entering light sleep.
This option can be useful if serial monitoring is needed via USB Serial/JTAG while power management is
enabled, as the USB Serial/JTAG cannot work under light sleep and after waking up from light sleep.
Note. This option can only control the automatic Light-Sleep behavior. If esp_light_sleep_start() is
called manually from the program, enabling this option will not prevent light sleep entry even if the
USB Serial/JTAG is in use.

---

## CONFIG_VFS_INITIALIZE_DEV_NULL

**Initialize /dev/null VFS**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:100`

If enabled, /dev/null VFS will be automatically initialized at startup.

---

## CONFIG_VFS_MAX_COUNT

**Maximum Number of Virtual Filesystems**

- **Type:** `unknown`
- **Default:** `8`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:81`

Define maximum number of virtual filesystems that can be registered.

---

## CONFIG_VFS_SELECT_IN_RAM

**Make VFS driver select() callbacks IRAM-safe**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions > Provide select function
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:66`

If enabled, VFS driver select() callback function will be placed in IRAM.

---

## CONFIG_VFS_SEMIHOSTFS_MAX_MOUNT_POINTS

**Host FS: Maximum number of the host filesystem mount points**

- **Type:** `unknown`
- **Default:** `1`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions > Host File System I/O (Semihosting)
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:93`

Define maximum number of host filesystem mount points.

---

## CONFIG_VFS_SUPPORT_DIR

**Provide directory related functions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:22`

If enabled, the following functions are provided by the VFS component.

stat, link, unlink, rename, utime, access, truncate, rmdir, mkdir,
opendir, closedir, readdir, readdir_r, seekdir, telldir, rewinddir

Filesystem drivers can then be registered to handle these functions
for specific paths.

Disabling this option can save memory when the support for these functions
is not required.

---

## CONFIG_VFS_SUPPORT_IO

**Provide basic I/O functions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:3`

If enabled, the following functions are provided by the VFS component.

open, close, read, write, pread, pwrite, lseek, fstat, fsync, ioctl, fcntl

Filesystem drivers can then be registered to handle these functions
for specific paths.

Disabling this option can save memory when the support for these functions
is not required.

Note that the following functions can still be used with socket file descriptors
when this option is disabled:

close, read, write, ioctl, fcntl.

---

## CONFIG_VFS_SUPPORT_SELECT

**Provide select function**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:41`

If enabled, select function is provided by the VFS component, and can be used
on peripheral file descriptors (such as UART) and sockets at the same time.

If disabled, the default select implementation will be provided by LWIP for
sockets only.

Disabling this option can reduce code size if support for "select" on UART file
descriptors is not required.

---

## CONFIG_VFS_SUPPORT_TERMIOS

**Provide termios.h functions**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:73`

Disabling this option can save memory when the support for termios.h is not required.

---

## CONFIG_VFS_SUPPRESS_SELECT_DEBUG_OUTPUT

**Suppress select() related debug outputs**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Virtual file system > Provide basic I/O functions > Provide select function
- **Kconfig:** `/opt/esp/idf/components/vfs/Kconfig:56`

Select() related functions might produce an inconveniently lot of
debug outputs when one sets the default log level to DEBUG or higher.
It is possible to suppress these debug outputs by enabling this
option.

---

## CONFIG_WEBSOCKET_SERVER_MAX_CLIENTS

**Max clients**

- **Type:** `unknown`
- **Default:** `20`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:4`

Maximum number of clients that the WebSocket
server can handle at a time.

---

## CONFIG_WEBSOCKET_SERVER_PINNED

**Server pinned to core**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:45`

Pin the WebSocket server task to a specific core.
The task handles reads.

---

## CONFIG_WEBSOCKET_SERVER_PINNED_CORE

**Pinned core**

- **Type:** `unknown`
- **Default:** ``
- **Menu:** Component config > WebSocket Server > Server pinned to core
- **Kconfig:** `components/websocket/Kconfig:52`

Core that the WebSocket server is pinned to.
The task handles reads.

---

## CONFIG_WEBSOCKET_SERVER_QUEUE_SIZE

**Queue read size**

- **Type:** `unknown`
- **Default:** `10`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:12`

Size of the queue to deal with incoming
WebSocket messages. The queue holds the
connection, not the actual message.

---

## CONFIG_WEBSOCKET_SERVER_QUEUE_TIMEOUT

**Queue timeout**

- **Type:** `unknown`
- **Default:** `30`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:21`

Timeout for adding new connections to the
read queue.

---

## CONFIG_WEBSOCKET_SERVER_TASK_PRIORITY

**Priority**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:37`

Priority for the WebSocket server. The task
handles reads.

---

## CONFIG_WEBSOCKET_SERVER_TASK_STACK_DEPTH

**Stack depth**

- **Type:** `unknown`
- **Default:** `6000`
- **Menu:** Component config > WebSocket Server
- **Kconfig:** `components/websocket/Kconfig:29`

Stack depth for the WebSocket server. The task
handles reads.

---

## CONFIG_WEB_PORT

**User interface HTTP Server Port**

- **Type:** `unknown`
- **Default:** `8000`
- **Menu:** Snapclient Configuration > HTTP Server Setting
- **Kconfig:** `main/Kconfig.projbuild:41`

HTTP server port to use.

---

## CONFIG_WIFI_AUTH_ENTERPRISE

**ENTERPRISE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:288`

---

## CONFIG_WIFI_AUTH_OWE

**OWE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:298`

---

## CONFIG_WIFI_AUTH_WAPI_PSK

**WAPI**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:296`

---

## CONFIG_WIFI_AUTH_WEP

**WEP**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:278`

---

## CONFIG_WIFI_AUTH_WPA2_ENTERPRISE

**WPA2 ENTERPRISE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:290`

---

## CONFIG_WIFI_AUTH_WPA2_PSK

**WPA2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:282`

---

## CONFIG_WIFI_AUTH_WPA2_WPA3_PSK

**WPA2/WPA3**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:286`

---

## CONFIG_WIFI_AUTH_WPA3_ENT_192

**WPA3 ENT 192**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:294`

---

## CONFIG_WIFI_AUTH_WPA3_PSK

**WPA3 ENTERPRISE**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:292`

---

## CONFIG_WIFI_AUTH_WPA_PSK

**WPA**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:280`

---

## CONFIG_WIFI_AUTH_WPA_WPA2_PSK

**WPA/WPA2**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Wifi Configuration > Authentication mode
- **Kconfig:** `components/network_interface/Kconfig.projbuild:284`

---

## CONFIG_WIFI_MAXIMUM_RETRY

**Maximum connection retry**

- **Type:** `unknown`
- **Default:** `5`
- **Menu:** Wifi Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:302`

Set the Maximum retry to avoid station reconnecting to the AP unlimited when the AP is really inexistent.
Set to 0 if unlimited reconnections are preferred.

---

## CONFIG_WIFI_PASSWORD

**WiFi Password**

- **Type:** `unknown`
- **Default:** `mypassword`
- **Menu:** Wifi Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:266`

WiFi password (WPA or WPA2) for the example to use.

---

## CONFIG_WIFI_PROV_AUTOSTOP_TIMEOUT

**Provisioning auto-stop timeout**

- **Type:** `unknown`
- **Default:** `30`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:10`

Time (in seconds) after which the Wi-Fi provisioning manager will auto-stop after connecting to
a Wi-Fi network successfully.

---

## CONFIG_WIFI_PROV_BLE_BONDING

**Enable BLE bonding**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:18`

This option is applicable only when provisioning transport is BLE.

---

## CONFIG_WIFI_PROV_BLE_FORCE_ENCRYPTION

**Force Link Encryption during characteristic Read / Write**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:33`

Used to enforce link encryption when attempting to read / write characteristic

---

## CONFIG_WIFI_PROV_BLE_NOTIFY

**Add support for Notification for provisioning BLE descriptors**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:40`

Used to enable support Notification in BLE descriptors of prov* characteristics

---

## CONFIG_WIFI_PROV_BLE_SEC_CONN

**Enable BLE Secure connection flag**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:25`

Used to enable Secure connection support when provisioning transport is BLE.

---

## CONFIG_WIFI_PROV_DISCONNECT_AFTER_PROV

**Terminate connection after provisioning is done**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager > Keep BT on after provisioning is done
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:52`

---

## CONFIG_WIFI_PROV_KEEP_BLE_ON_AFTER_PROV

**Keep BT on after provisioning is done**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:47`

---

## CONFIG_WIFI_PROV_SCAN_MAX_ENTRIES

**Max Wi-Fi Scan Result Entries**

- **Type:** `unknown`
- **Default:** `16`
- **Menu:** Component config > Wi-Fi Provisioning Manager
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:3`

This sets the maximum number of entries of Wi-Fi scan results that will be kept by the provisioning manager

---

## CONFIG_WIFI_PROV_STA_ALL_CHANNEL_SCAN

**All Channel Scan**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wi-Fi Provisioning Manager > Wifi Provisioning Scan Method
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:61`

Scan will end after scanning the entire channel. This option is useful in Mesh WiFi Systems.

---

## CONFIG_WIFI_PROV_STA_FAST_SCAN

**Fast Scan**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wi-Fi Provisioning Manager > Wifi Provisioning Scan Method
- **Kconfig:** `/opt/esp/idf/components/wifi_provisioning/Kconfig:65`

Scan will end after an AP matching with the SSID has been detected.

---

## CONFIG_WIFI_SSID

**WiFi SSID**

- **Type:** `unknown`
- **Default:** `myssid`
- **Menu:** Wifi Configuration
- **Kconfig:** `components/network_interface/Kconfig.projbuild:259`

SSID (network name) for the example to connect to.

---

## CONFIG_WL_SECTOR_MODE

- **Type:** `unknown`
- **Default:** ``

---

## CONFIG_WL_SECTOR_MODE_PERF

**Performance**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wear Levelling > Sector store mode
- **Kconfig:** `/opt/esp/idf/components/wear_levelling/Kconfig:50`

---

## CONFIG_WL_SECTOR_MODE_SAFE

**Safety**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wear Levelling > Sector store mode
- **Kconfig:** `/opt/esp/idf/components/wear_levelling/Kconfig:53`

---

## CONFIG_WL_SECTOR_SIZE

- **Type:** `unknown`
- **Default:** `4096`

---

## CONFIG_WL_SECTOR_SIZE_4096

**4096**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Wear Levelling > Wear Levelling library sector size
- **Kconfig:** `/opt/esp/idf/components/wear_levelling/Kconfig:23`

---

## CONFIG_WL_SECTOR_SIZE_512

**512**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > Wear Levelling > Wear Levelling library sector size
- **Kconfig:** `/opt/esp/idf/components/wear_levelling/Kconfig:21`

---

## CONFIG_WS_BUFFER_SIZE

**Websocket transport buffer size**

- **Type:** `unknown`
- **Default:** `1024`
- **Menu:** Component config > TCP Transport > Websocket > Enable Websocket Transport
- **Kconfig:** `/opt/esp/idf/components/tcp_transport/Kconfig:10`

Size of the buffer used for constructing the HTTP Upgrade request during connect

---

## CONFIG_WS_DYNAMIC_BUFFER

**Using dynamic websocket transport buffer**

- **Type:** `string`
- **Default:** `n`
- **Menu:** Component config > TCP Transport > Websocket > Enable Websocket Transport
- **Kconfig:** `/opt/esp/idf/components/tcp_transport/Kconfig:17`

If enable this option, websocket transport buffer will be freed after connection
succeed to save more heap.

---

## CONFIG_WS_TRANSPORT

**Enable Websocket Transport**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > TCP Transport > Websocket
- **Kconfig:** `/opt/esp/idf/components/tcp_transport/Kconfig:4`

Enable support for creating websocket transport.

---

## CONFIG_XTAL_FREQ

- **Type:** `unknown`
- **Default:** `40`

---

## CONFIG_XTAL_FREQ_40

**40 MHz**

- **Type:** `string`
- **Default:** `y`
- **Menu:** Component config > Hardware Settings > Main XTAL Config > Main XTAL frequency
- **Kconfig:** `/opt/esp/idf/components/esp_hw_support/./port/esp32s3/Kconfig.xtal:8`

---

